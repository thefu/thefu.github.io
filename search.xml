<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ADB(android调试桥)</title>
    <url>/2022/10/15/ADB-android%E8%B0%83%E8%AF%95%E6%A1%A5/</url>
    <content><![CDATA[<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>android调试桥（adb）是一种功能多样的命令行工具，可让您与设备进行通信。adb命令可用于执行各种设备操作（例如安装和调试应用）,并提供对Unix shell（可用来在设备商运行各种命令）的访问权限。它是一种客户端-服务器程序，包括以下三个组件：<br>客户端：用于发送命令。客户端在开发机器上运行。您可以通过发出adb命令从命令行终端调用客户端。<br>守护程序（adbd）：用于在设备商运行命令。守护程序在每个设备上作为后台进程运行。<br>服务器：用于管理客户端与守护程序之间的通信。服务器在开发机器上作为后台进程运行。</p>
<span id="more"></span>
<h2 id="常用的命令："><a href="#常用的命令：" class="headerlink" title="常用的命令："></a>常用的命令：</h2><h3 id="查询设备"><a href="#查询设备" class="headerlink" title="查询设备"></a>查询设备</h3><p>在发出adb命令之前，了解哪些设备实例已经了解到adb服务器会很有帮助<br>adb devices -l</p>
<h3 id="将命令发送至特定设备"><a href="#将命令发送至特定设备" class="headerlink" title="将命令发送至特定设备"></a>将命令发送至特定设备</h3><p>如果有多个命令正在运行，发出adb命令时必须指定目标设备。为此，请使用devices命令获取目标设备的序列号。获得序列号时，请结合-s选项和adb命令来指定序列号。<br>adb -s xxxxxxxxx installl helloworld.apk</p>
<h3 id="安装应用"><a href="#安装应用" class="headerlink" title="安装应用"></a>安装应用</h3><p>使用adb的install命令在模拟器或链接的设备上安装APK<br>adb install path_to_apk</p>
<h3 id="设置端口转发"><a href="#设置端口转发" class="headerlink" title="设置端口转发"></a>设置端口转发</h3><p>可以使用forward命令设置任意端口转发，将特定主机端口上的请求转发到设备上其他端口。以下实例设置了主机端口6100到设备端口7100的转发：<br>adb forward tcp:6100 tcp:7100</p>
<h3 id="将文件复制到设备-x2F-从设备复制文件"><a href="#将文件复制到设备-x2F-从设备复制文件" class="headerlink" title="将文件复制到设备&#x2F;从设备复制文件"></a>将文件复制到设备&#x2F;从设备复制文件</h3><p>可以使用pull和push命令将文件复制到设备或从设备复制文件。与install命令（仅将apk文件复制到特定位置）不同，使用pull和push命令可将任意目录和文件复制到设备中任何位置。<br>从设备中复制某个文件或目录（及其子目录）,使用以下命令：<br>Adb pull remote local<br>如需将某个文件或目录复制到设备，使用以下命令：<br>Adb push local remote<br>将local和remote替换为开发机器（本地）和设备（远程）上的目标文件&#x2F;目录的路径：<br>Adb push foo.txt &#x2F;sdcard&#x2F;foo.txt</p>
<h3 id="停止adb服务器"><a href="#停止adb服务器" class="headerlink" title="停止adb服务器"></a>停止adb服务器</h3><p>在某些情况下，您可能需要终止adb服务器进程，然后重启以解决问题（例如，如果adb不响应命令）。<br>如需停止adb服务器，请使用adb kill-server命令。然后，您可以通过发出其他任何adb命令来重启服务器。</p>
<h3 id="发出adb命令"><a href="#发出adb命令" class="headerlink" title="发出adb命令"></a>发出adb命令</h3><p>可以从开发机器上的命令行发出adb命令，也可以通过脚本发出，用法如下：<br>adb [-d | -e | -s  serial_number] command<br>如果只有一个模拟器在运行或者只连接了一个设备，系统会默认将adb命令发送到该设备，如果有多个模拟器在运行并且&#x2F;或者连接了多个设备，您需要使用-d、-e、-s选项指定应向其发送命令的目标设备，您可以使用以下命令来查看所有支持的adb命令的详细列表：<br>adb –help</p>
<h3 id="发出shell命令"><a href="#发出shell命令" class="headerlink" title="发出shell命令"></a>发出shell命令</h3><p>可以使用shell命令通过adb发出设备命令，也可以启交互式shell。如需发出单个命令，请使用shell命令，如下所示：<br>adb shell shell_command<br>要在设备上启动交互式shell，请使用shell命令，如下：<br>adb shell<br>要退出交互式shell，按初crtl+D或者输入exit</p>
<h3 id="从手机上导出应用软件apk文件"><a href="#从手机上导出应用软件apk文件" class="headerlink" title="从手机上导出应用软件apk文件"></a>从手机上导出应用软件apk文件</h3><p>第一步：查看app包名<br>adb shell ammonitor<br>这个时候点击运行手机上要看的包名的app，就可以看到启动的包名了<br>第二步：查看包存放的路径<br>adb shell pm path com.eda.abcd<br>这个时候就可以看到该应用程序的包存放路径<br>第三步：使用adb pull + 包存放路径<br>将apk导出到电脑</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>Android developer 必备的几个资源</title>
    <url>/2023/02/22/Android-developer-%E5%BF%85%E5%A4%87%E7%9A%84%E5%87%A0%E4%B8%AA%E8%B5%84%E6%BA%90/</url>
    <content><![CDATA[<p>作为一名合格的 Android Coder，除了认真工作，写出优秀代码外，我们还需要在日新月异的技术栈中找到有用的资源，来装备自己，开阔视野。希望以下的资源对大家有用。</p>
<span id="more"></span>

<h2 id="1-官方资源"><a href="#1-官方资源" class="headerlink" title="1.官方资源"></a>1.官方资源</h2><p>一手、最新、最全的资料。<br><a href="https://imgse.com/i/p9ZShM8"><img src="https://s1.ax1x.com/2023/04/22/p9ZShM8.png" alt="p9ZShM8.png"></a><br>包含 Android Platform、Android Studio、Jetpack、Kotlin 等一切跟 Android 开发相关的指导、例子Sample 等。大部分人肯定都知道。<br>建议每隔一段时间上去看看，经常更新，比如最近增加了模块化的指导。除此之外，其他架构相关的内容，不管是初学者还是初学者，<br>都有很多指导、学习的意义。</p>
<p>其他官方资源，比如：</p>
<ul>
<li>Android Developers on Medium <a href="https://medium.com/androiddevelopers">https://medium.com/androiddevelopers</a></li>
<li>Android Developers on YouTube <a href="https://www.youtube.com/@AndroidDevelopers">https://www.youtube.com/@AndroidDevelopers</a></li>
<li>Google Codelabs <a href="https://codelabs.developers.google.com/?product=android">https://codelabs.developers.google.com/?product=android</a></li>
</ul>
<p>网址：<a href="https://developer.android.com/docs">https://developer.android.com/docs</a></p>
<h2 id="2-Android-Weekly"><a href="#2-Android-Weekly" class="headerlink" title="2.Android Weekly"></a>2.Android Weekly</h2><p><a href="https://imgse.com/i/p9ZprlV"><img src="https://s1.ax1x.com/2023/04/22/p9ZprlV.png" alt="p9ZprlV.png"></a><br>聚合了最近、最新的 Android 相关的文章、开源项目、视频&amp;播客、新闻等，每周一更新，不容错过。</p>
<p>网址：<a href="https://androidweekly.net/">https://androidweekly.net/</a></p>
<h2 id="3-kotlin-Weekly"><a href="#3-kotlin-Weekly" class="headerlink" title="3.kotlin Weekly"></a>3.kotlin Weekly</h2><p><a href="https://imgse.com/i/p9Zpgw4"><img src="https://s1.ax1x.com/2023/04/22/p9Zpgw4.png" alt="p9Zpgw4.png"></a><br>类似于上面的 Android Weekly，主要提供一些 kotlin 相关技术栈更新、文章、新特性等，也包括了一些 Android 相关的内容。</p>
<p>网址：<a href="http://www.kotlinweekly.net/">http://www.kotlinweekly.net/</a></p>
<h2 id="4-ProAndroidDev"><a href="#4-ProAndroidDev" class="headerlink" title="4.ProAndroidDev"></a>4.ProAndroidDev</h2><p><a href="https://imgse.com/i/p9Zphf1"><img src="https://s1.ax1x.com/2023/04/22/p9Zphf1.png" alt="p9Zphf1.png"></a><br>Medium（国外的简书）上的一个组织，都是 Android专家和大牛（Google Developer Experts）写的一些易懂、简洁的文章，质量不错。而且，如果你要在该专栏发布文章的话，需要专家 review 才能发出来。</p>
<p>网址：<a href="https://proandroiddev.com/">https://proandroiddev.com/</a></p>
<h2 id="5-Github"><a href="#5-Github" class="headerlink" title="5.Github"></a>5.Github</h2><p>这个应该都知道吧，最大的基友交流社区，全世界的代码仓库集中地。<br>比如 Android 官方的仓库，<a href="https://github.com/android%EF%BC%8C%E5%8C%85%E5%90%AB%E5%BE%88%E5%A4%9A%E6%9C%80%E4%BD%B3%E5%AE%9E%E8%B7%B5%EF%BC%8C%E6%AF%94%E5%A6%82">https://github.com/android，包含很多最佳实践，比如</a> Now In Android, architecture-samples,  sunflower 等。<br>也可以通过 GitHub Trending page 来查看最近热门的仓库，类似微博热搜🔥 （筛选 kotlin 语言，前几个就是 android 相关的项目）<br>顺便推荐一个 android-developer-roadmap 项目，包含了一些 Android 开发的资源：<br><a href="https://imgse.com/i/p9Zpo6K"><img src="https://s1.ax1x.com/2023/04/22/p9Zpo6K.png" alt="p9Zpo6K.png"></a></p>
<h2 id="6-Google-Developer-Library"><a href="#6-Google-Developer-Library" class="headerlink" title="6.Google Developer Library"></a>6.Google Developer Library</h2><p>又是一个宝藏资源库！<br>谷歌官方的资源库，包含了 Android、Flutter、Firebase 和 Machine Learning 等资源，光 Android 方面就有一大堆，打开慢慢看吧<br><a href="https://imgse.com/i/p9ZpOkd"><img src="https://s1.ax1x.com/2023/04/22/p9ZpOkd.png" alt="p9ZpOkd.png"></a></p>
<h2 id="7-Android-Arsenal"><a href="#7-Android-Arsenal" class="headerlink" title="7.Android Arsenal"></a>7.Android Arsenal</h2><p>安卓军火库，聚集了大量的开源库，工具等，支持分类查找。<br>也可以提交自己的开源库，通过审核就可以展示出来，供别人使用。<br>关注它的推特，及时获取最新的开源项目。</p>
<p>网址：<a href="https://android-arsenal.com/">https://android-arsenal.com/</a></p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>APP性能优化的一些建议</title>
    <url>/2022/10/15/APP%E6%80%A7%E8%83%BD%E4%BC%98%E5%8C%96%E7%9A%84%E4%B8%80%E4%BA%9B%E5%BB%BA%E8%AE%AE/</url>
    <content><![CDATA[<p>除了利用性能测试工具分析性能应用，寻找优化控件，开发者在开发过程中，就应该注意一些性能优化技巧，养成良好的开发习惯，减轻性能优化的负担。下面是一些优化建议：</p>
<span id="more"></span>
<p>1.在LinearLayout和RelativeLayout都可以完成布局的情况下优先选择LinearLayout，可以减少View的层级，但是注意相同组件可能RelativeLayout绘制时间长<br>2.onDraw中不要创建新的局部对象，onDraw方法中不要做耗时的任务<br>3.不要在主线程进行网络访问&#x2F;大文件的IO操作<br>4.绘制UI时，尽量减少绘制UI层次；减少不必要的view嵌套<br>5.当我们的布局是用的FrameLayout的时候，我们可以把它改成merge，可以避免自己的帧布局和系统的ContentFrameLayout帧布局重叠造成重复计算（measure和layout）<br>6.在view层级相同的情况下，尽量使用LinearLayout而不是RelativeLayout；因为RelativeLayout在测量的时候会测量两次，而LinearLayout测量一次<br>7.删除控件中无用的属性<br>8.布局复用，比如listview布局复用<br>9.尽量避免过度绘制（overdraw）比如：背景经常容易造成过度绘制，由于我们布局设置了背景，溶蚀用到MaterialDesign的主题会默认给一个背景。这时应该把主题添加的背景去掉；还有移除XML中非必须的背景<br>10.自定义view优化，使用canvas.clipRect()来帮助系统识别那些可见的区域，只有在这个区域内才会被绘制。也是避免过度绘制。<br>11.启动优化，启动速度的监控，发现影响启动速度的问题所在，优化启动逻辑，提高应用的启动速度。比如闪屏页面，合理优化布局，加载逻辑优化，数据准备。<br>12.合理的刷新机制，尽量减少刷新次数，尽量避免后台有高的CPU线程运行，缩小刷新区域。</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>Android工程项目结构解析</title>
    <url>/2022/11/10/Android%E5%B7%A5%E7%A8%8B%E9%A1%B9%E7%9B%AE%E7%BB%93%E6%9E%84%E8%A7%A3%E6%9E%90/</url>
    <content><![CDATA[<p>android工程的重点目录解析</p>
<span id="more"></span>

<p><img src="https://s3.bmp.ovh/imgs/2022/11/10/7b843f31f0a99719.png"></p>
<p>接下来对关键部分进行讲解：<br>Java，我们写Java代码的地方，业务功能都在这里实现<br>res，我们存放各种资源文件的地方，有图片，字符串，动画，音频，还有各种形式的xml文件</p>
<h1 id="一、res资源文件夹介绍："><a href="#一、res资源文件夹介绍：" class="headerlink" title="一、res资源文件夹介绍："></a>一、res资源文件夹介绍：</h1><p>注：说道这个res目录，另外还有一个assets目录，虽然这里没有，但是我们可以自己创建，两者的区别在于是否前者下所有的资源文件都会在R.java文件下生成对应的资源id，而后者并不会；前者我们可以通过资源id访问到对应的资源；而后者则需要我们通过AssetManager以二进制的形式来读取了。这个R文件可以理解为字典，res下每个资源都会在这里生成一个唯一的id。<br>分辨率不同的系统会根据屏幕的分辨率来选择hdpi,mdpi,xmdpi,xxhdpi下的对应图片，所以你解压别人的apk可以看到上述目录统一名称的图片，在四个文件夹下都有，只是大小和像素不一样而已，当然，这也不是绝对的，比如我们把所有图片都丢到hdpi下的话，本该加载ldpi文件夹下的图片资源，但是ldpi下没有，那么加载的还会使hdpi下的图片，另外还有一种情况，比如hdpi，mdpi目录下有ldpi下没有，那么会加载mdpi中的资源！原则是使用最接近密度级别，另外如果你想禁止android不跟随屏幕密度加载不同文件夹的资源，只需在AndroidManifest.xml文件中添加android:anyDensity&#x3D;”false”字段即可。</p>
<h2 id="1-图片资源"><a href="#1-图片资源" class="headerlink" title="1.图片资源"></a>1.图片资源</h2><ul>
<li>drawable：存放各种位图文件，(.png，.jpg，.9png，.gif等)除此之外可能是一些其他的drawable类型的XML文件</li>
<li>mipmap-hdpi：高分辨率，一般我们把图片丢这里</li>
<li>mipmap-mdpi：中等分辨率，很少，除非兼容的的手机很旧</li>
<li>mipmap-xhdpi：超高分辨率，手机屏幕材质越来越好，以后估计会慢慢往这里过渡</li>
<li>mipmap-xxhdpi：超超高分辨率，这个在高端机上有所体现</li>
</ul>
<h2 id="2-布局资源"><a href="#2-布局资源" class="headerlink" title="2.布局资源"></a>2.布局资源</h2><ul>
<li>layout：该目录下存放的就是我们的布局文件，另外在一些特定的机型上，我们做屏幕适配，比如480*320这样的手机，我们会另外创建一套布局，就行：layout-480x320这样的文件夹！</li>
</ul>
<h2 id="3-菜单资源"><a href="#3-菜单资源" class="headerlink" title="3.菜单资源"></a>3.菜单资源</h2><ul>
<li>menu：在以前有物理菜单按钮，即menu键的手机上，用的较多，现在用的并不多，菜单项相关的资源xml可在这里编写，不知道谷歌会不会出新的东西来替代菜单了~</li>
</ul>
<h2 id="4-values目录"><a href="#4-values目录" class="headerlink" title="4.values目录"></a>4.values目录</h2><ul>
<li>demens.xml：定义尺寸资源</li>
<li>string.xml：定义字符串资源</li>
<li>styles.xml：定义样式资源</li>
<li>colors.xml：定义颜色资源</li>
<li>arrays.xml：定义数组资源</li>
<li>attrs.xml：自定义控件时用的较多，自定义控件的属性！</li>
<li>theme主题文件，和styles很相似，但是会对整个应用中的Actvitiy或指定Activity起作用，一般是改变窗口外观的！可在Java代码中通过setTheme使用，或者在Androidmanifest.xml中为&lt;application…&gt;添加theme的属性！ PS:你可能看到过这样的values目录：values-w820dp，values-v11等，前者w代表平板设备，820dp代表屏幕宽度；而v11这样代表在API(11)，即android 3.0后才会用到的！</li>
</ul>
<h2 id="5-raw目录"><a href="#5-raw目录" class="headerlink" title="5.raw目录"></a>5.raw目录</h2><p>用于存放各种原生资源（音频，视频，一些XML文件等），我们可以通过openRawResource(int id)来获得资源的二进制流，其实和Assets差不多，不过这里面的资源会在R文件里生成一个资源id而已<br>6.最后还有动画的，动画有两种：属性动画和补间动画</p>
<ul>
<li>animator：存放属性动画的XML文件</li>
<li>anim：存放补间动画的XML文件</li>
</ul>
<h1 id="二、深入了解三个文件"><a href="#二、深入了解三个文件" class="headerlink" title="二、深入了解三个文件"></a>二、深入了解三个文件</h1><p>工程中三个比较重要的文件：MainActivity.java， 布局文件：activity_main.xml和配置文件AndroidMainfest.xml<br>代码分析：</p>
<p>MainAcitivity.java<br><img src="https://s3.bmp.ovh/imgs/2022/11/10/48c14edb5816ca14.png"><br>activity_main.xml：<br><img src="https://s3.bmp.ovh/imgs/2022/11/10/e8bfda21011a2f54.png"><br>AndroidMainfest.xml配置文件：<br><img src="https://s3.bmp.ovh/imgs/2022/11/10/d4a0c330f888bfbe.png"><br>①如果app包含其他组件的话,都要使用类型说明语法在该文件中进行声明 Server:<br>②权限的声明: 在该文件中显式地声明程序需要的权限,防止app错误地使用服务, 不恰当地访问 资源,最终提高android app的健壮性 android.permission.SEND_SMS 有这句话表示app需要使用发送信息的权限,安装的时候就会提示用户, 相关权限可以在sdk参考手册查找！</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>Android文件管理</title>
    <url>/2022/12/07/Android%E6%96%87%E4%BB%B6%E7%AE%A1%E7%90%86/</url>
    <content><![CDATA[<h2 id="1-Android文件的操作模式"><a href="#1-Android文件的操作模式" class="headerlink" title="1.Android文件的操作模式"></a>1.Android文件的操作模式</h2><p>Android和其他的操作系统不一样，它是基于Linux的，在读写文件的时候，还需要加上文件的操作模式，Android中的操作模式如下：</p>
<span id="more"></span>
<p>1）MODE_PRIVATE ——&gt; 默认操作模式，代表该文件是私有数据，只能够被本应用本身访问，写入的内容会覆盖原文件的内容<br>2）MODE_APPEND ——&gt; 会检验文件是否存在，存在的话往文件中追加内容，否则建立新文件<br>3）MODE_WORLD_READABLE ——&gt; 当前文件可以被其他应用读取<br>4）MODE_WORLD_WRITEABLE ——&gt; 当前文件可以被其他应用写入<br>后面两个适用于控制其他应用是否有读写文件得权限得<br>如果文件既可以写，又可以读的话，可以写成下面的形式：<br>openFileOutput(“jay123.txt”, Content.MODE_WORLD_READABLE + Content.MODE_WORLD_WRITEABLE);</p>
<h2 id="2-文件操作的相关方法"><a href="#2-文件操作的相关方法" class="headerlink" title="2.文件操作的相关方法"></a>2.文件操作的相关方法</h2><p><a href="https://imgse.com/i/zh3Gz4"><img src="https://s1.ax1x.com/2022/12/11/zh3Gz4.png" alt="zh3Gz4.png"></a></p>
<h2 id="3-示例"><a href="#3-示例" class="headerlink" title="3.示例"></a>3.示例</h2><p>首先是布局文件：main_activity.xml</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt;</span><br><span class="line">&lt;LinearLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;</span><br><span class="line">    xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot;</span><br><span class="line">    xmlns:tools=&quot;http://schemas.android.com/tools&quot;</span><br><span class="line">    android:layout_width=&quot;match_parent&quot;</span><br><span class="line">    android:layout_height=&quot;match_parent&quot;</span><br><span class="line">    tools:context=&quot;.FileActivity&quot;</span><br><span class="line">    android:orientation=&quot;horizontal&quot;&gt;</span><br><span class="line"></span><br><span class="line">    &lt;TextView</span><br><span class="line">        android:layout_width=&quot;wrap_content&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">        android:text=&quot;@string/nametitle&quot; /&gt;</span><br><span class="line"></span><br><span class="line">    &lt;EditText</span><br><span class="line">        android:id=&quot;@+id/editname&quot;</span><br><span class="line">        android:layout_width=&quot;match_parent&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot; /&gt;</span><br><span class="line"></span><br><span class="line">    &lt;TextView</span><br><span class="line">        android:layout_width=&quot;wrap_content&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">        android:text=&quot;@string/detailtitle&quot; /&gt;</span><br><span class="line"></span><br><span class="line">    &lt;EditText</span><br><span class="line">        android:id=&quot;@+id/editdetail&quot;</span><br><span class="line">        android:layout_width=&quot;match_parent&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">        android:minLines=&quot;2&quot; /&gt;</span><br><span class="line"></span><br><span class="line">    &lt;LinearLayout</span><br><span class="line">        android:layout_width=&quot;fill_parent&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">        android:orientation=&quot;horizontal&quot;&gt;</span><br><span class="line"></span><br><span class="line">        &lt;Button</span><br><span class="line">            android:id=&quot;@+id/btnsave&quot;</span><br><span class="line">            android:layout_width=&quot;wrap_content&quot;</span><br><span class="line">            android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">            android:text=&quot;@string/btnwrite&quot; /&gt;</span><br><span class="line"></span><br><span class="line">        &lt;Button</span><br><span class="line">            android:id=&quot;@+id/btnclean&quot;</span><br><span class="line">            android:layout_width=&quot;wrap_content&quot;</span><br><span class="line">            android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">            android:text=&quot;@string/btnclean&quot; /&gt;</span><br><span class="line">    &lt;/LinearLayout&gt;</span><br><span class="line"></span><br><span class="line">    &lt;Button</span><br><span class="line">        android:id=&quot;@+id/btnread&quot;</span><br><span class="line">        android:layout_width=&quot;wrap_content&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">        android:text=&quot;@string/btnread&quot; /&gt;</span><br><span class="line"></span><br><span class="line">&lt;/LinearLayout&gt;</span><br></pre></td></tr></table></figure>

<p>然后再写一个文件协助类：FileHelper.java</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">package com.thefu.androidstudyproject;</span><br><span class="line"></span><br><span class="line">import android.content.Context;</span><br><span class="line"></span><br><span class="line">import java.io.FileInputStream;</span><br><span class="line">import java.io.FileOutputStream;</span><br><span class="line">import java.io.IOError;</span><br><span class="line">import java.io.IOException;</span><br><span class="line"></span><br><span class="line">public class FileHelper &#123;</span><br><span class="line"></span><br><span class="line">    private Context context;</span><br><span class="line"></span><br><span class="line">    public FileHelper() &#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public FileHelper(Context context) &#123;</span><br><span class="line">        super();</span><br><span class="line">        this.context = context;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 这里定义的是一个文件保存的方法，写入到文件中，所以是输出流</span><br><span class="line">     * @param fileName</span><br><span class="line">     * @param fileContent</span><br><span class="line">     * @throws Exception</span><br><span class="line">     */</span><br><span class="line">    public void save(String fileName, String fileContent) throws Exception &#123;</span><br><span class="line">        //这里使用私有模式，创建出来的文件只能被本应用访问，还会覆盖原文件</span><br><span class="line">        FileOutputStream output = context.openFileOutput(fileName, Context.MODE_PRIVATE);</span><br><span class="line">        output.write(fileContent.getBytes()); //将String字符串以字节的形式写入到输出流中</span><br><span class="line">        output.close();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 这里定义的是文件读取的方法</span><br><span class="line">     * @param fileName</span><br><span class="line">     * @return</span><br><span class="line">     * @throws IOException</span><br><span class="line">     */</span><br><span class="line">    public String read(String fileName) throws IOException &#123;</span><br><span class="line">        //打开文件输入流</span><br><span class="line">        FileInputStream input = context.openFileInput(fileName);</span><br><span class="line">        byte[] temp = new byte[1024];</span><br><span class="line">        StringBuilder sb = new StringBuilder(&quot;&quot;);</span><br><span class="line">        int len = 0;</span><br><span class="line">        //读取文件内容</span><br><span class="line">        while ((len = input.read(temp)) &gt; 0) &#123;</span><br><span class="line">            sb.append(new String(temp, 0, len));</span><br><span class="line">        &#125;</span><br><span class="line">        //关闭输入流</span><br><span class="line">        input.close();</span><br><span class="line">        return sb.toString();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后是Activity，在这里我们完成相关操作：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">package com.thefu.androidstudyproject;</span><br><span class="line"></span><br><span class="line">import androidx.appcompat.app.AppCompatActivity;</span><br><span class="line"></span><br><span class="line">import android.content.Context;</span><br><span class="line">import android.os.Bundle;</span><br><span class="line">import android.view.View;</span><br><span class="line">import android.widget.Button;</span><br><span class="line">import android.widget.EditText;</span><br><span class="line">import android.widget.Toast;</span><br><span class="line"></span><br><span class="line">import java.io.IOException;</span><br><span class="line"></span><br><span class="line">public class FileActivity extends AppCompatActivity implements View.OnClickListener&#123;</span><br><span class="line"></span><br><span class="line">    private EditText editname;</span><br><span class="line">    private EditText editdetail;</span><br><span class="line">    private Button btnsave;</span><br><span class="line">    private Button btnclean;</span><br><span class="line">    private Button btnread;</span><br><span class="line">    private Context mContext;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate(Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_file);</span><br><span class="line">        mContext = getApplicationContext();</span><br><span class="line">        bindView();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    private void bindView() &#123;</span><br><span class="line">        editdetail = (EditText) findViewById(R.id.editdetail);</span><br><span class="line">        editname = (EditText) findViewById(R.id.editname);</span><br><span class="line">        btnclean = (Button) findViewById(R.id.btnclean);</span><br><span class="line">        btnsave = (Button) findViewById(R.id.btnsave);</span><br><span class="line">        btnread = (Button) findViewById(R.id.btnread);</span><br><span class="line"></span><br><span class="line">        btnclean.setOnClickListener(this);</span><br><span class="line">        btnsave.setOnClickListener(this);</span><br><span class="line">        btnread.setOnClickListener(this);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void onClick(View view) &#123;</span><br><span class="line">        switch (view.getId()) &#123;</span><br><span class="line">            case R.id.btnclean:</span><br><span class="line">                editdetail.setText(&quot;&quot;);</span><br><span class="line">                editname.setText(&quot;&quot;);</span><br><span class="line">                break;</span><br><span class="line">            case R.id.btnsave:</span><br><span class="line">                FileHelper fHelper = new FileHelper(mContext);</span><br><span class="line">                String filename = editname.getText().toString();</span><br><span class="line">                String filedetail = editdetail.getText().toString();</span><br><span class="line">                try &#123;</span><br><span class="line">                    fHelper.save(filename, filedetail);</span><br><span class="line">                    Toast.makeText(getApplicationContext(), &quot;数据写入成功&quot;, Toast.LENGTH_SHORT).show();</span><br><span class="line">                &#125; catch (Exception e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                    Toast.makeText(getApplicationContext(), &quot;数据写入失败&quot;, Toast.LENGTH_SHORT).show();</span><br><span class="line">                &#125;</span><br><span class="line">                break;</span><br><span class="line">            case R.id.btnread:</span><br><span class="line">                String detail = &quot;&quot;;</span><br><span class="line">                FileHelper fHelper2 = new FileHelper(getApplicationContext());</span><br><span class="line">                try &#123;</span><br><span class="line">                    String fname = editname.getText().toString();</span><br><span class="line">                    detail = fHelper2.read(fname);</span><br><span class="line">                &#125; catch (IOException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                Toast.makeText(getApplicationContext(), detail, Toast.LENGTH_SHORT).show();</span><br><span class="line">                break;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-读取SD卡上的文件"><a href="#4-读取SD卡上的文件" class="headerlink" title="4.读取SD卡上的文件"></a>4.读取SD卡上的文件</h2><p><a href="https://imgse.com/i/zh3BFK"><img src="https://s1.ax1x.com/2022/12/11/zh3BFK.png" alt="zh3BFK.png"></a></p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>Android横竖屏切换以及对应布局加载</title>
    <url>/2022/11/10/Android%E6%A8%AA%E7%AB%96%E5%B1%8F%E5%88%87%E6%8D%A2%E4%BB%A5%E5%8F%8A%E5%AF%B9%E5%BA%94%E5%B8%83%E5%B1%80%E5%8A%A0%E8%BD%BD/</url>
    <content><![CDATA[<p>如果要让软件在横竖屏之间切换，由于横竖屏的宽高会发生转换，有可能会要求不同的布局。</p>
<span id="more"></span>
<h1 id="1-横竖屏切换连带横竖屏布局问题："><a href="#1-横竖屏切换连带横竖屏布局问题：" class="headerlink" title="1.横竖屏切换连带横竖屏布局问题："></a>1.横竖屏切换连带横竖屏布局问题：</h1><p>如果要让软件在横竖屏之间切换，由于横竖屏的宽高会发生转换，有可能会要求不同的布局。<br>可以通过一下两种方法切换布局：<br>1）在res目录下建立layout-land和layout-port目录，相应的layout文件名不变，比如：layout-land是横屏的，layout-port是竖屏的时候，其他的不用管，横竖屏切换时程序调用Activity的onCreate方法中的setOnContent(xxx)，并自动加载相应的布局。<br>2）假如布局资源不按照如上设置，则可以通过Java代码来判断当前是横屏还是竖屏然后来加载相应的xml布局文件。因为当屏幕变为横屏的时候，系统会重新加载当前Activity的onCreate方法（也就是说：该Activity的声明周期要重头开始），你可以把以下方法放在你的onCreate中来检查当前的方法，然后可以让你的setContentVIew来载入不同的layout</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Private int orientation;</span><br><span class="line">Orientation = getResources().getConfiguration().orientation;</span><br></pre></td></tr></table></figure>
<h1 id="2-强制设定屏幕的横竖屏方向"><a href="#2-强制设定屏幕的横竖屏方向" class="headerlink" title="2.强制设定屏幕的横竖屏方向"></a>2.强制设定屏幕的横竖屏方向</h1><p>android横竖屏切换在手机开发中比较常见，很多软件在开发过程中为了避免横竖屏切换时引发不必要的麻烦，通常要强制设置横竖屏的方向，通过在androidMainfest.xml中设置activity中的android:screenOrientation属性值来实现。<br>比如下列设置：<br>横屏显示设置，android:screenOrientation&#x3D;”landscape”<br>竖屏显示设置，android:screenOrientation&#x3D;”portrait”<br>当然上述修改也可以在Java代码中通过代码实现：（android屏幕的切换会重启Activity，所以在Activity销毁前保存当前活动的状态，并在Activity再次Create的时候载入配置）<br>setRequestedOrientation(ActivityInfo.SCREEN_ORIENTATION_LANDSCAPE)</p>
<h1 id="3-拦截横竖屏切换-需要的配置文件：onConfigurationChanged"><a href="#3-拦截横竖屏切换-需要的配置文件：onConfigurationChanged" class="headerlink" title="3.拦截横竖屏切换 需要的配置文件：onConfigurationChanged"></a>3.拦截横竖屏切换 需要的配置文件：onConfigurationChanged</h1><p>Activity每次横竖屏切换都会重新调用onPause-&gt;onStop-&gt;onDestory-&gt;onCreate-&gt;onStart-&gt;onResume（为此内容和数据要保存和读取，否则转屏之前内容将就会消失了）<br>很多时候这样的结果让程序繁琐，为此android提供了在mainfest中设置android:configChanges属性，从而让activity不延续上述的重建流程；<br>方式一：在android工程的Mainfest.xml中配置activity：android:configChanges&#x3D;”keyboardHidden|orientation”，横竖屏切换之后就不会去执行OnCreate函数了，而是会去调用onConfigurationChanged()这样就能控制横竖屏的切换了。<br>方式二：用户可以在Activity或View的：onConfigurationChanged(configuration config)，函数中获取当前横竖屏参数，至于其调用顺序跟touch时间的传递顺序相似，不过他没有消费事件的概念，会顺次调用到每一个onConfigurationChanged函数。<br>需要重写Activity的onConfigurationChanged方法。实现方式如下，不需要做太多的内容：<br>需要注意的是，onConfigurationChanged函数中只能获得横竖屏切换后的参数，在该函数中获取不到新的Layout和控件尺寸位置信息，如果要处理尺寸和位置信息，必须通过消息异步或者延时调用；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@Override</span><br><span class="line"></span><br><span class="line">       public void onConfigurationChanged(Configuration newConfig) &#123;</span><br><span class="line"></span><br><span class="line">               super.onConfigurationChanged(newConfig);</span><br><span class="line"></span><br><span class="line">               if (getResources().getConfiguration().orientation == Configuration.ORIENTATION_LANDSCAPE) &#123;</span><br><span class="line"></span><br><span class="line">                       // land do nothing is ok</span><br><span class="line"></span><br><span class="line">               &#125; else if (getResources().getConfiguration().orientation == Configuration.ORIENTATION_PORTRAIT) &#123;</span><br><span class="line"></span><br><span class="line">                       // port do nothing is ok</span><br><span class="line"></span><br><span class="line">               &#125;</span><br><span class="line"></span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>
<h1 id="4-自适应切换屏幕："><a href="#4-自适应切换屏幕：" class="headerlink" title="4.自适应切换屏幕："></a>4.自适应切换屏幕：</h1><p>如果想让它启动的时候是横屏的话就横屏表示，纵屏的话就纵屏表示，然后手机切换横竖屏就不能用了该怎么解决呢？<br>首先：在Mainfest.xml中追加android:screenOrientation&#x3D;”sensor”<br>android:configurationChanges&#x3D;”orientation|keyboardHidden”<br>然后：取得屏幕的长和宽，进行比较设置横竖屏的变量。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">Display display = getWindowManager().getDefaultDisplay();</span><br><span class="line">    int width = display.getWidth();</span><br><span class="line">    int height = display.getHeight();</span><br><span class="line">    if (width &gt; height) &#123;</span><br><span class="line">      orientation = ActivityInfo.SCREEN_ORIENTATION_LANDSCAPE; // 横屏</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      orientation = ActivityInfo.SCREEN_ORIENTATION_PORTRAIT; // 竖屏</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">然后：在onConfigurationChanged()函数中追加this.setRequestedOrientation(orientation)</span><br><span class="line">public void onConfigurationChanged(Configuration newConfig) &#123; </span><br><span class="line">         super.onConfigurationChanged(newConfig); </span><br><span class="line">         this.setRequestedOrientation(orientation); </span><br><span class="line">       &#125; </span><br></pre></td></tr></table></figure>
<p>但是这样的话你切到别的画面的时候再回到原画面，它就仍然是横的或者是纵的。怎么让它从别的屏幕回来后，又重新横竖屏布局呢？<br>只要在onResume()中再设定下就行了，但是这个仅仅只是支持横竖屏只有一个layout的；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">protected void onResume() &#123;</span><br><span class="line">    orientation = ActivityInfo.SCREEN_ORIENTATION_USER;</span><br><span class="line">    this.setRequestedOrientation(orientation);</span><br><span class="line">    Display display = getWindowManager().getDefaultDisplay();</span><br><span class="line">    int width = display.getWidth();</span><br><span class="line">    int height = display.getHeight();</span><br><span class="line">    if (width &gt; height) &#123;</span><br><span class="line">      orientation = ActivityInfo.SCREEN_ORIENTATION_LANDSCAPE;</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      orientation = ActivityInfo.SCREEN_ORIENTATION_PORTRAIT;</span><br><span class="line">    &#125;</span><br><span class="line">    super.onResume();</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>
<p>有三点注意：<br>1.不设置activity的android:confiChanges时，切屏会重新调用各个生命周期，切横屏时会执行一次，切竖屏时会执行两次<br>2.设置Activity的android:configChanges&#x3D;”orientation”时，切屏还是会重新调用各个生命周期，切横竖屏时只会执行一次<br>3.设置activity的android:configChanges&#x3D;”orientation|keyboardHidden”时，切屏不会重新调用各个生命周期，只会执行onConfigurationChanged方法</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>Android之碎片Fragment</title>
    <url>/2022/12/11/Android%E4%B9%8B%E7%A2%8E%E7%89%87Fragment/</url>
    <content><![CDATA[<p>安卓自3.0开始引入Fragment的概念，主要是为了能在不同分辨率的屏幕上进行更为动态和灵活的UI设计，让程序更加合理和充分利用大屏幕空间。</p>
<span id="more"></span>
<p>本篇将学习Fragment以下几个知识点：<br>Fragment概要<br>Fragment生命周期<br>加载Fragment方法</p>
<pre><code>静态加载
动态加载
</code></pre>
<p>Fragment与Activity之间通信</p>
<h3 id="1-Fragment概要"><a href="#1-Fragment概要" class="headerlink" title="1.Fragment概要"></a>1.Fragment概要</h3><p>学习Fragment的时候可以联系之前学习过的Activity，因为它们有很多相似点：都可包含布局，有自己的生命周期，Fragment可看似迷你活动。正如Fragment的名字一样–碎片，它的出现是为了解决Android碎片化，它可以作为Activity界面的组成部分，可在Activity运行中实现动态地加入、移除和交换。一个Activity中可同时出现多个Fragment，一个Fragment也可在多个Activity中使用。活动和碎片像极了夫妻，虽然紧密联系但是又有独立空间，在一起让彼此变得更好。<br><a href="https://imgse.com/i/zhthRK"><img src="https://s1.ax1x.com/2022/12/11/zhthRK.jpg" alt="zhthRK.jpg"></a></p>
<h3 id="2-Fragment生命周期"><a href="#2-Fragment生命周期" class="headerlink" title="2.Fragment生命周期"></a>2.Fragment生命周期</h3><p><a href="https://imgse.com/i/zhtose"><img src="https://s1.ax1x.com/2022/12/11/zhtose.jpg" alt="zhtose.jpg"></a><br>是不是发现Fragment和Activity的生命周期太相似了，现在只需要再介绍几个Activity中没讲过的几个新方法：<br>onAttach()：当Fragment和Activity建立关联时调用<br>onCreateView()：当Fragment创建视图的时候调用<br>onActivityCreated()：当与Fragment相关联的Activity完成onCreate()之后调用<br>onDestoryView()：当Fragment中的布局被移除的时候调用<br>onDetach()：当Fragment和Activity解除关联时调用</p>
<p>在上图中画了几条线，可以看到Fragment周期中的状态几乎都是成对出现的，所以不难理解下图几种变化下Fragment生命周期方法的调用顺序了。<br><a href="https://imgse.com/i/zhNion"><img src="https://s1.ax1x.com/2022/12/11/zhNion.jpg" alt="zhNion.jpg"></a></p>
<p>加载到Activity中Fragment在各种变化下方法的调用顺序更值得注意。需要提一句的是，Activity的FragmentManager负责调用队列中Fragment的生命周期方法，只要Fragment的状态与Activity的状态保持了同步，托管Activity的Fragment便会继续调用其他生命周期方法以继续保持Fragment与Activity的状态一致<br><a href="https://imgse.com/i/zhjBs1"><img src="https://s1.ax1x.com/2022/12/12/zhjBs1.jpg" alt="zhjBs1.jpg"></a><br>Fragment生命周期与Activity生命周期的一个关键就在于，Fragment的生命周期方法是由托管Activity而不是操作系统调用的。Activity中生命周期都是protected，而Fragment都是public，也能印证这一点，因为Activity需要调用Fragment那些方法并管理它</p>
<h3 id="3-加载Fragment方法"><a href="#3-加载Fragment方法" class="headerlink" title="3.加载Fragment方法"></a>3.加载Fragment方法</h3><p>现在来学习如何在Activity中加载Fragment<br>静态加载Fragment大致过程如图，分成四步：<br><a href="https://imgse.com/i/zhvASJ"><img src="https://s1.ax1x.com/2022/12/12/zhvASJ.jpg" alt="zhvASJ.jpg"></a><br>下面通过一个简单的例子感受Fragment静态加载到Activity的过程。<br>第一步：新建frag_layout.xml，为Fragment指定一个布局，这里简单的放一个Textview和一个Button。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;LinearLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;</span><br><span class="line">    xmlns:tools=&quot;http://schemas.android.com/tools&quot;</span><br><span class="line">    android:layout_width=&quot;match_parent&quot;</span><br><span class="line">    android:layout_height=&quot;match_parent&quot;</span><br><span class="line">    tools:context=&quot;.MyFragment&quot;</span><br><span class="line">    android:orientation=&quot;vertical&quot;&gt;</span><br><span class="line"></span><br><span class="line">    &lt;TextView</span><br><span class="line">        android:id=&quot;@+id/text&quot;</span><br><span class="line">        android:layout_width=&quot;match_parent&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">        android:text=&quot;这是一个Fragment&quot;</span><br><span class="line">        android:textSize=&quot;20sp&quot;</span><br><span class="line">        android:textColor=&quot;#000000&quot;</span><br><span class="line">        android:gravity=&quot;center&quot; /&gt;</span><br><span class="line"></span><br><span class="line">    &lt;Button</span><br><span class="line">        android:id=&quot;@+id/frg_btn&quot;</span><br><span class="line">        android:layout_width=&quot;match_parent&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">        android:text=&quot;点击&quot;</span><br><span class="line">        android:textSize=&quot;20sp&quot; /&gt;</span><br><span class="line">&lt;/LinearLayout&gt;</span><br></pre></td></tr></table></figure>

<p>第二步：新建一个MyFragment类并继承Fragment，这里引用Android.app包下的就可以，另一个包下主要用于兼容低版本的安卓系统。然后重写onCreateView()方法，这个方法里通过LayoutInflater的inflate()方法将刚刚定义的frag_layout布局加载进来并得到一个View，再return这个View。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class MyFragment extends Fragment &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public View onCreateView(LayoutInflater inflater, ViewGroup container,</span><br><span class="line">                             Bundle savedInstanceState) &#123;</span><br><span class="line">        /**</span><br><span class="line">         * resource：fragment需要加载的布局支持</span><br><span class="line">         * root：加载layout的父ViewGroup</span><br><span class="line">         * attachGroup：false表示不反悔父ViewGroup</span><br><span class="line">         */</span><br><span class="line">        return inflater.inflate(R.layout.fragment_my, container, false);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第三步：新建main.xml，作为Activity的布局，使用&lt;fragment&gt;标签添加碎片，并且一定要有android:name属性且值为被加载的Fragment类的包名.类名完整形式。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;LinearLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;</span><br><span class="line">    xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot;</span><br><span class="line">    xmlns:tools=&quot;http://schemas.android.com/tools&quot;</span><br><span class="line">    android:layout_width=&quot;match_parent&quot;</span><br><span class="line">    android:layout_height=&quot;match_parent&quot;</span><br><span class="line">    tools:context=&quot;.FragmentActivity&quot;</span><br><span class="line">    android:orientation=&quot;vertical&quot;&gt;</span><br><span class="line"></span><br><span class="line">    &lt;LinearLayout</span><br><span class="line">        android:id=&quot;@+id/layout&quot;</span><br><span class="line">        android:layout_width=&quot;match_parent&quot;</span><br><span class="line">        android:layout_height=&quot;match_parent&quot;</span><br><span class="line">        android:orientation=&quot;horizontal&quot;&gt;</span><br><span class="line"></span><br><span class="line">        &lt;!--这是静态调用的方式--&gt;</span><br><span class="line">        &lt;fragment</span><br><span class="line">            android:layout_width=&quot;match_parent&quot;</span><br><span class="line">            android:layout_height=&quot;match_parent&quot;</span><br><span class="line">            android:id=&quot;@+id/fragment&quot;</span><br><span class="line">            android:name=&quot;com.thefu.androidstudyproject.MyFragment&quot; /&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &lt;/LinearLayout&gt;</span><br><span class="line">&lt;/LinearLayout&gt;</span><br></pre></td></tr></table></figure>

<p>第四步：再MainActivity中加载main布局，现在MyFragment就完成了静态加载到MainActivity中，这时碎片里的控件自然也是活动的一个部分，可直接再活动中获取到Button的实例，来注册点击事件了。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">private Button frag_btn;</span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate(Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate(savedInstanceState);</span><br><span class="line">        //静态加载</span><br><span class="line">        setContentView(R.layout.activity_ftagment);</span><br><span class="line">        frag_btn = findViewById(R.id.frg_btn);</span><br><span class="line">        frag_btn.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line"></span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View view) &#123;</span><br><span class="line">                Toast.makeText(FragmentActivity.this, &quot;您点击了按钮&quot;, Toast.LENGTH_SHORT).show();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br></pre></td></tr></table></figure>
<h2 id="（2）动态加载：在托管Activity通过代码动态添加"><a href="#（2）动态加载：在托管Activity通过代码动态添加" class="headerlink" title="（2）动态加载：在托管Activity通过代码动态添加"></a>（2）动态加载：在托管Activity通过代码动态添加</h2><p>动态加载的代码也非常简单，直接看例子，修改main.xml，将整个&lt;fragment&gt;删掉。但害保留一个LinearLayout的空间并且还给了id。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;LinearLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;</span><br><span class="line">    xmlns:app=&quot;http://schemas.android.com/apk/res-auto&quot;</span><br><span class="line">    xmlns:tools=&quot;http://schemas.android.com/tools&quot;</span><br><span class="line">    android:layout_width=&quot;match_parent&quot;</span><br><span class="line">    android:layout_height=&quot;match_parent&quot;</span><br><span class="line">    tools:context=&quot;.FragmentActivity&quot;</span><br><span class="line">    android:orientation=&quot;vertical&quot;&gt;</span><br><span class="line"></span><br><span class="line">    &lt;LinearLayout</span><br><span class="line">        android:id=&quot;@+id/layout&quot;</span><br><span class="line">        android:layout_width=&quot;match_parent&quot;</span><br><span class="line">        android:layout_height=&quot;match_parent&quot;</span><br><span class="line">        android:orientation=&quot;horizontal&quot;&gt;</span><br><span class="line"></span><br><span class="line">        &lt;!--这是静态调用的方式--&gt;</span><br><span class="line">&lt;!--        &lt;fragment--&gt;</span><br><span class="line">&lt;!--            android:layout_width=&quot;match_parent&quot;--&gt;</span><br><span class="line">&lt;!--            android:layout_height=&quot;match_parent&quot;--&gt;</span><br><span class="line">&lt;!--            android:id=&quot;@+id/fragment&quot;--&gt;</span><br><span class="line">&lt;!--            android:name=&quot;com.thefu.androidstudyproject.MyFragment&quot; /&gt;--&gt;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &lt;/LinearLayout&gt;</span><br><span class="line"></span><br><span class="line">&lt;/LinearLayout&gt;</span><br></pre></td></tr></table></figure>
<p>接下来在MainActivity中添加代码：<br>public class FragmentActivity extends AppCompatActivity {</p>
<pre><code>private Button frag_btn;
private LinearLayout linearLayout;
@Override
protected void onCreate(Bundle savedInstanceState) &#123;
    super.onCreate(savedInstanceState);
    //静态加载
    setContentView(R.layout.activity_ftagment);
</code></pre>
<p>&#x2F;&#x2F;        frag_btn &#x3D; findViewById(R.id.frg_btn);<br>&#x2F;&#x2F;        frag_btn.setOnClickListener(new View.OnClickListener() {<br>&#x2F;&#x2F;<br>&#x2F;&#x2F;            @Override<br>&#x2F;&#x2F;            public void onClick(View view) {<br>&#x2F;&#x2F;                Toast.makeText(FragmentActivity.this, “您点击了按钮”, Toast.LENGTH_SHORT).show();<br>&#x2F;&#x2F;            }<br>&#x2F;&#x2F;        });<br>        linearLayout &#x3D; findViewById(R.id.layout);</p>
<pre><code>    //动态加载
    MyFragment fragment = new MyFragment();
    FragmentManager fragmentManager = getSupportFragmentManager();
    FragmentTransaction beginTransacction = fragmentManager.beginTransaction();
    beginTransacction.add(R.id.layout, fragment); //把碎片放到布局的layout容器里面
    beginTransacction.addToBackStack(null); //允许用户通过按键返回前一个Fragment状态
    beginTransacction.commit();

&#125;
</code></pre>
<p>}</p>
<p>可将整个过程大致分为三个步骤：<br>第一步，先用getSupportFragmentManager()方法获取一个FragmentManager对象，再通过它的beginTransacction()获取一个FragmentTransaction的实例。</p>
<p>第二步，用beginTransaction.add()方法将MyFragemnt实例添加到main布局里LinearLayout里，终于知道之前铺垫的Id是怎么回事了。一定要注意，add()方法里的第一个参数是容器视图资源Id，而不是layout。容器视图资源Id有两个作用：告知FragmentManager，碎片视图应该出现在活动视图的什么地方；它也是FragmentManager队列中碎片的唯一标识符。而静态加载时碎片的唯一标识符正是在活动布局里&lt; fragment&gt;下的id。</p>
<p>第三步，调用beginTransaction.commit()提交。另外，如果允许用户通过按下返回按键返回到前一个Fragment状态，在调用commit()之前先调用addToBackStack(true)方法。</p>
<p>这里注意到动态加载进来的Fragment里的空间并不能直接在活动中findViewById得到，那么如何实现点击效果呢？</p>
<h3 id="4-Fragment与Activity之间通信"><a href="#4-Fragment与Activity之间通信" class="headerlink" title="4.Fragment与Activity之间通信"></a>4.Fragment与Activity之间通信</h3><p>在活动中可以调用FragmentManager的findFragmentById()方法来得到相应碎片的实例，继而可以调用碎片里的方法，以上面的demo为例，如果想得到静态加载碎片的实例，可在MainActivity添加代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MyFragment myFragment = (MyFragment)getFragmentManager(). findFragmentById(R.id.fragment);</span><br></pre></td></tr></table></figure>
<p>如果想得到动态加载碎片的实例，代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MyFragment myFragment = (MyFragment)fragmentManager(). findFragmentById(R.id.layout);</span><br></pre></td></tr></table></figure>

<p>在碎片中也可以调用getActivity()方法来得到和当前碎片相关联的活动实例，这样调用活动里的方法就变得轻而易举了，比如想在MyFragment得到MainActivity的实例：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">MainActivity activity=(MainActivity)getActivity();</span><br></pre></td></tr></table></figure>
<p>于是碎片和活动可以很方便的进行通信了，再想一下碎片和碎片之间如何进行通信？先在要给碎片中可以得到与它相关的活动，然后再通过这个活动去获取另一个碎片实例，这样实现了不同碎片之间的通信了。</p>
<p>在实际开发中，如果某一板块每次用到都需要相同的功能，就完全可以在碎片中实现需求，而不必在活动中重复代码，这样可以提高代码的复用性。</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
  </entry>
  <entry>
    <title>Android中view的理解</title>
    <url>/2022/09/25/Android%E4%B8%ADview%E7%9A%84%E7%90%86%E8%A7%A3/</url>
    <content><![CDATA[<h3 id="View是什么？"><a href="#View是什么？" class="headerlink" title="View是什么？"></a>View是什么？</h3><p>View是屏幕上的一块矩形区域，它负责用来显示一个区域，并且响应这个区域内的事件。可以说，手机屏幕上的任意一部分看得见的地方都是view，它很常见，比如TextView、imageview、button、以及linearLayout、relativelayout都是继承于view的。<br>对于activity来说，我们通过setContentView添加的布局到activity上，实际上都是添加到了activity内部的decorview上面，这个DecorView，其实就是一个FrameLayout，因此实际上，我们的布局是添加到了FrameLayout里面。</p>
<h2 id="View绘制的三个流程"><a href="#View绘制的三个流程" class="headerlink" title="View绘制的三个流程"></a>View绘制的三个流程</h2><p>我们知道，在自定义view的时候一般需要重写父类的onMeasure()、onLayout()、onDraw()三个方法，来完成视图的展示过程。当然，这三个暴露给开发者重写的方法只不过是整个绘制流程的冰山一角，更多复杂的幕后工作，都让系统给代劳了，一个完整的绘制流程包括measure、layout、draw三个步骤，其中：<br>measure：测量。系统会现根据xml布局文件和代码中对控件属性的设置，来获取或者计算出每个view和viewgroup的尺寸，并将这些尺寸保存下来。<br>layout：布局。根据测量出的结果以及对应的参数，来确定每一个控件应该显示的位置。<br>draw：绘制。确定好位置后，就将这些控件绘制到屏幕上。</p>
<h2 id="Android视图层次结果简介"><a href="#Android视图层次结果简介" class="headerlink" title="Android视图层次结果简介"></a>Android视图层次结果简介</h2><p>在介绍view绘制流程之间，咱们先接单介绍一下Android视图层次结果以及DecorView因为view的绘制流程的入口和DecorView有着密切的联系。</p>
<p>咱们平时看到的视图，其实存在如上的嵌套关系。上图是针对比较老的Android系统版本中制作的，新的版本中会略有出入，还有一个状态栏，但整体上没变。我们平时在activity中setContentView(…)中对应layout内容，对应的是上图中viewgrop的树状结构，实际上添加到系统中时，会再裹上一层FrameLayout，就是上图中最里面的浅蓝色部分。<br>这里咱们再通过一个实例来继续查看。Androidstudio工具中提供了一个布局观察期工具，通过Tools &gt; android &gt; layout Inspector可以查看具体某个activity的布局情况。下图中，左边树状结果对应了右边的可视图，可见DecorView就是整个界面的根视图，对应右边的红色框，是整个屏幕的大小。黄色边框为状态栏部分；那个绿色边框中有两个部分，一个是白框中的actionBar，对应了上图中紫色部分的TitleActionBar部分，即标题栏，平时咱们可以在activity中将其隐藏掉；另外一个蓝色边框部分，对应上图中最里面的蓝色部分，即contentview部分，下图中左边有两个蓝色框，上面那个中有个布局”content_layout”，咱们能通过layout布局文件直接完全操作的也就是这一块，当其被add到视图系统中时，会被过上contentFramelayout（显然是FrameLayout的子类），这也就是为什么添加到layout.xml视图的方法叫setContentView()而不叫setView()的原因了。</p>
<h2 id="故事开始的地方"><a href="#故事开始的地方" class="headerlink" title="故事开始的地方"></a>故事开始的地方</h2><p>如果对activity的启动流程有一定的了解的话，应该知道这个启动会在activityThread.java类中完成，在启动activity的过程中，会调用handleResumeActivity()方法，关于视图的绘制过程最初就是从这个方法开始的。</p>
<h3 id="1-view绘制起源UML时序图"><a href="#1-view绘制起源UML时序图" class="headerlink" title="1.view绘制起源UML时序图"></a>1.view绘制起源UML时序图</h3><p>整个调用链如下图所示，知道ViewRootImpl类中的performTraversals()中，才正式开始绘制流程了，所以一般都是以该方法作为正式绘制的源头。</p>
<h3 id="2-handResumeActivity-方法"><a href="#2-handResumeActivity-方法" class="headerlink" title="2.handResumeActivity()方法"></a>2.handResumeActivity()方法</h3><p>在这先大致看一下ActivityThread类中的handleResumeActivity方法，咱们这里只贴出关键代码：<br><code>//===========ActivityThread.java========== final void handleResumeActivity(...) &#123;     ......     //跟踪代码后发现其初始赋值为mWindow = new PhoneWindow(this, window, activityConfigCallback);     r.window = r.activity.getWindow();        //从PhoneWindow实例中获取DecorView     View decor = r.window.getDecorView();     ......     //跟踪代码后发现，vm值为上述PhoneWindow实例中获取的WindowManager。     ViewManager wm = a.getWindowManager();     ......     //当前window的属性，从代码跟踪来看是PhoneWindow窗口的属性     WindowManager.LayoutParams l = r.window.getAttributes();     ......     wm.addView(decor, l);     ...... &#125;</code><br>上述代码第八行中，ViewManager是一个接口，addView是其中定义一个空方法，WindowManager是其子类，WindowManagerImpl是WindowManager的实现类，在第四行代码中的r.window的值可以根据Activity.java的如下代码得知，其值为PhoneWindow实例。<br>&#96;&#x2F;&#x2F;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;Activity.java&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;<br>private Window mWindow;<br>public Window getWindow() {<br>   return mWindow;<br>}</p>
<p>final void attach(…){<br>   ……<br>   mWindow &#x3D; new PhoneWindow(this, window, activityConfigCallback);<br>   ……<br>}&#96;</p>
<h3 id="3-两个重要参数分析"><a href="#3-两个重要参数分析" class="headerlink" title="3.两个重要参数分析"></a>3.两个重要参数分析</h3><p>之所以要在这里特意分析handleResumeActivity()方法，除了因为它是整个绘制流程的最初源头外，还有就是addView的两个参数比较重要，它们经过一层一层传递后进入到ViewRootImpl中，在后面分析绘制中要用到。这里再看看这两个参数的相关信息：<br>（1）参数dector<br>&#96;&#x2F;&#x2F;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;Activity.java&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;<br>private Window mWindow;<br>public Window getWindow() {<br>   return mWindow;<br>}</p>
<p>final void attach(…){<br>   ……<br>   mWindow &#x3D; new PhoneWindow(this, window, activityConfigCallback);<br>   ……<br>}<code>可见decor参数表示的是DecorView实例，注释中也有说明：这是window的顶级视图，包含了window的decor （2）参数l</code>&#x2F;&#x2F;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;Window.java&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;<br>&#x2F;&#x2F;The current window attributes.<br>    private final WindowManager.LayoutParams mWindowAttributes &#x3D;<br>        new WindowManager.LayoutParams();<br>……<br>public final WindowManager.LayoutParams getAttributes() {<br>        return mWindowAttributes;<br>    }<br>……</p>
<p>&#x2F;&#x2F;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;WindowManager.java的内部类LayoutParams extends ViewGroup.LayoutParams&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;<br>public LayoutParams() {<br>            super(LayoutParams.MATCH_PARENT, LayoutParams.MATCH_PARENT);<br>            ……<br>        }</p>
<p>&#x2F;&#x2F;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;ViewGroup.java内部类LayoutParams&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;&#x3D;<br>public LayoutParams(int width, int height) {<br>            this.width &#x3D; width;<br>            this.height &#x3D; height;<br>        }&#96;<br>该参数表示的是PhoneWindow的LayoutParams属性，其width和height值均为LayoutParmas.MATCH_PARENT。<br>在源码中，WindowPhone和DecorView通过组合方法联系在一起的，而DecorView是整个View体系的根View。在前面handleResumeActivity()方法代码片段中，当Activity启动后，就通过第14行的addView方法，来间接调用ViewRootImpl类中的performTraversals()，从而实现视图的绘制。</p>
]]></content>
  </entry>
  <entry>
    <title>Android 开发调用C++库</title>
    <url>/2023/01/15/Android-%E5%BC%80%E5%8F%91%E8%B0%83%E7%94%A8C++%E5%BA%93/</url>
    <content><![CDATA[<p>背景：实际项目中的算法库很多都是C++代码编写的，而Android开发是使用kotlin或者java语言实现的，如果遇到一个APP需要调用算法库，<br>不同语言有不同的书写规范，直接引用当然不可能编译成功，因此我们需要通过中间层（JNI编程）来搭建java语言和C++语言沟通的桥梁。</p>
<span id="more"></span>

<h2 id="一、新建使用JNI编程的工程"><a href="#一、新建使用JNI编程的工程" class="headerlink" title="一、新建使用JNI编程的工程"></a>一、新建使用JNI编程的工程</h2><p>在android studio中，新建一个native C++的工程<br><a href="https://imgse.com/i/p9VvgP0"><img src="https://s1.ax1x.com/2023/04/22/p9VvgP0.png" alt="p9VvgP0.png"></a><br>设置项目和包的名称（包的名称需要和你自己实际使用的项目包名一致，否则就只能通过模块的方式去调用）<br><a href="https://imgse.com/i/p9VvR2T"><img src="https://s1.ax1x.com/2023/04/22/p9VvR2T.png" alt="p9VvR2T.png"></a></p>
<h2 id="二、下载JNI开发所需的NKD，LLDB，CMake工具包"><a href="#二、下载JNI开发所需的NKD，LLDB，CMake工具包" class="headerlink" title="二、下载JNI开发所需的NKD，LLDB，CMake工具包"></a>二、下载JNI开发所需的NKD，LLDB，CMake工具包</h2><p>打开Androidstudio -&gt; Preferences -&gt; Android SDK<br><a href="https://imgse.com/i/p9VvhMF"><img src="https://s1.ax1x.com/2023/04/22/p9VvhMF.png" alt="p9VvhMF.png"></a><br>JNI：Java Native Interface（java本地编程接口），一套编程规范，它提供了若干的API，实现了Java和其他语言的通信（主要是C&#x2F;C++）<br>NDK：Native Development Kit（本地开发工具），一系列工具的集合，这套工具允许你在Android开发中使用C和C++代码<br>CMake：跨平台编译工具<br>LLDB：一种调试程序，Android Studio使用它来调试原生代码</p>
<h2 id="三、在cpp文件夹下防止需被打包的C-x2F-C-源码，CMakeLists里添加所有C-x2F-C-代码和native-lib-cpp：如图，inc里放置头文件，src里放置-c-x2F-cpp文件（注意修改引用路径）"><a href="#三、在cpp文件夹下防止需被打包的C-x2F-C-源码，CMakeLists里添加所有C-x2F-C-代码和native-lib-cpp：如图，inc里放置头文件，src里放置-c-x2F-cpp文件（注意修改引用路径）" class="headerlink" title="三、在cpp文件夹下防止需被打包的C&#x2F;C++源码，CMakeLists里添加所有C&#x2F;C++代码和native-lib.cpp：如图，inc里放置头文件，src里放置.c&#x2F;.cpp文件（注意修改引用路径）"></a>三、在cpp文件夹下防止需被打包的C&#x2F;C++源码，CMakeLists里添加所有C&#x2F;C++代码和native-lib.cpp：如图，inc里放置头文件，src里放置.c&#x2F;.cpp文件（注意修改引用路径）</h2><p><a href="https://imgse.com/i/p9VvoZ9"><img src="https://s1.ax1x.com/2023/04/22/p9VvoZ9.png" alt="p9VvoZ9.png"></a></p>
<h2 id="四、新建一个工具类，在类中声明需要调用的方法。（这一步设计类型转换C-gt-JNI-gt-JAVA）"><a href="#四、新建一个工具类，在类中声明需要调用的方法。（这一步设计类型转换C-gt-JNI-gt-JAVA）" class="headerlink" title="四、新建一个工具类，在类中声明需要调用的方法。（这一步设计类型转换C++ -&gt; JNI -&gt; JAVA）"></a>四、新建一个工具类，在类中声明需要调用的方法。（这一步设计类型转换C++ -&gt; JNI -&gt; JAVA）</h2><p><a href="https://imgse.com/i/p9Vv7I1"><img src="https://s1.ax1x.com/2023/04/22/p9Vv7I1.png" alt="p9Vv7I1.png"></a></p>
<h2 id="五、在native-lib-cpp里实现方法从C-到JNI的转换"><a href="#五、在native-lib-cpp里实现方法从C-到JNI的转换" class="headerlink" title="五、在native-lib.cpp里实现方法从C++到JNI的转换"></a>五、在native-lib.cpp里实现方法从C++到JNI的转换</h2><p><a href="https://imgse.com/i/p9VxFRf"><img src="https://s1.ax1x.com/2023/04/22/p9VxFRf.png" alt="p9VxFRf.png"></a></p>
<h2 id="六、执行Build-gt-Make-Project，可得到-so文件"><a href="#六、执行Build-gt-Make-Project，可得到-so文件" class="headerlink" title="六、执行Build -&gt; Make Project，可得到.so文件"></a>六、执行Build -&gt; Make Project，可得到.so文件</h2><p><a href="https://imgse.com/i/p9Vxkz8"><img src="https://s1.ax1x.com/2023/04/22/p9Vxkz8.png" alt="p9Vxkz8.png"></a></p>
<h2 id="七、把-so库放入需要算法库的工程下"><a href="#七、把-so库放入需要算法库的工程下" class="headerlink" title="七、把.so库放入需要算法库的工程下"></a>七、把.so库放入需要算法库的工程下</h2><p><a href="https://imgse.com/i/p9VxmZj"><img src="https://s1.ax1x.com/2023/04/22/p9VxmZj.png" alt="p9VxmZj.png"></a></p>
<h2 id="八、在新工程中，同一报名目录下新建名字相同的类，复写加载和声明函数"><a href="#八、在新工程中，同一报名目录下新建名字相同的类，复写加载和声明函数" class="headerlink" title="八、在新工程中，同一报名目录下新建名字相同的类，复写加载和声明函数"></a>八、在新工程中，同一报名目录下新建名字相同的类，复写加载和声明函数</h2><p><a href="https://imgse.com/i/p9Vxo6S"><img src="https://s1.ax1x.com/2023/04/22/p9Vxo6S.png" alt="p9Vxo6S.png"></a></p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>EditText:android:focusable和android:focusableInTouchMode的区别</title>
    <url>/2022/10/15/EditText-android-focusable%E5%92%8Candroid-focusableInTouchMode%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
    <content><![CDATA[<p>本文向大家介绍EditText:android:focusable和android:focusableInTouchMode的区别，主要包括EditText:android:focusable和android:focusableInTouchMode的区别使用实例，应用技巧，基本知识点总结和需要注意事项，具有一定的参考价值。<br>android:focusable</p>
<span id="more"></span>
<p>之所以有这个属性主要是因为android系统不仅仅是针对手机的，有可能在电视，手表等等的非触摸输入设备上（比如android TV）这些设备只有物理上下键不具备触摸功能，<br>那么当把这个属性值设置为true的时候，比如你按了一下向下键，屏幕上的内容就会对应选中一个向下的控件（如果这个控件设置android:focusable&#x3D;true的话），否则就会选中下一个具备focusable能力的控件。<br>android:focusableInTouchMode<br>与上面的那种情况相反，如果是针对手机开发的话，那么大部分手机都具备触摸功能，那么这个属性有什么作用呢？<br>从交互的角度来讲，焦点的作用其实就是为了提示用户当前控件已经被选中了，可以进行下一步的操作。比如在android5.0上新退出的Material Design效果上面，如果一个EditText被选中了，那么它的输入框就会更换颜色以示区别，但是在手机开发中并不是所有的控件都需要有选中的这种状态，通常在点击后可能只想达到某种效果（如点击按钮就是想触发点击事件，不需要有选中效果），那么这个时候，如果把android:focusableInTouchMode设置为true，此时执行的就是更换焦点操作，相应的事件就无法得到及时相应，这样的体验就会很不好。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&lt;LinearLayout xmlns:android=&quot;http://schemas.android.com/apk/res/android&quot;</span><br><span class="line">    android:layout_width=&quot;match_parent&quot;</span><br><span class="line">    android:layout_height=&quot;match_parent&quot;</span><br><span class="line">    android:background=&quot;#ddffff&quot;</span><br><span class="line">    android:gravity=&quot;center&quot;</span><br><span class="line">    android:orientation=&quot;vertical&quot;</span><br><span class="line">    android:focusableInTouchMode=&quot;true&quot;</span><br><span class="line">    &gt;</span><br><span class="line">    </span><br><span class="line">    &lt;View</span><br><span class="line">        android:id=&quot;@+id/view_test&quot;</span><br><span class="line">        android:layout_width=&quot;100dip&quot;</span><br><span class="line">        android:layout_height=&quot;100dip&quot;</span><br><span class="line">        android:focusableInTouchMode=&quot;true&quot;</span><br><span class="line">        android:background=&quot;#ff0000&quot;</span><br><span class="line">        /&gt;</span><br><span class="line">    </span><br><span class="line">    &lt;Button</span><br><span class="line">        android:id=&quot;@+id/bt_test&quot;</span><br><span class="line">        android:layout_width=&quot;wrap_content&quot;</span><br><span class="line">        android:layout_height=&quot;wrap_content&quot;</span><br><span class="line">        android:layout_marginBottom=&quot;20dp&quot;</span><br><span class="line">        android:text=&quot;click me&quot;</span><br><span class="line">        android:focusableInTouchMode=&quot;true&quot;</span><br><span class="line">        /&gt;</span><br><span class="line">&lt;/LinearLayout&gt;</span><br></pre></td></tr></table></figure>
<p>我们分别为LinearLayout，View，Button都增加一个触摸时能够获取到焦点的属性</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">package com.yuneec.testdemo;</span><br><span class="line">import android.os.Bundle;import android.support.v7.app.AppCompatActivity;import android.util.Log;import android.view.View;import android.widget.Button;</span><br><span class="line">public class MainActivity extends AppCompatActivity &#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    private View view_test;</span><br><span class="line">    private Button bt_test;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected void onCreate(Bundle savedInstanceState) &#123;</span><br><span class="line">        super.onCreate(savedInstanceState);</span><br><span class="line">        setContentView(R.layout.activity_main);</span><br><span class="line"></span><br><span class="line">        view_test = findViewById(R.id.view_test);</span><br><span class="line">        bt_test = (Button) findViewById(R.id.bt_test);</span><br><span class="line"></span><br><span class="line">        view_test.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View v) &#123;</span><br><span class="line">                Log.i(&quot;xp.chen&quot;, &quot;I am view, click................&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        view_test.setOnFocusChangeListener(new View.OnFocusChangeListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onFocusChange(View v, boolean hasFocus) &#123;</span><br><span class="line">                Log.i(&quot;xp.chen&quot;, &quot;==========================================&quot;);</span><br><span class="line">                Log.i(&quot;xp.chen&quot;, &quot;I am view, focus: &quot;+view_test.isFocused());</span><br><span class="line">                Log.i(&quot;xp.chen&quot;, &quot;I am button, focus: &quot;+bt_test.isFocused());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        bt_test.setOnClickListener(new View.OnClickListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onClick(View v) &#123;</span><br><span class="line">                Log.i(&quot;xp.chen&quot;, &quot;I am button, click................&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        bt_test.setOnFocusChangeListener(new View.OnFocusChangeListener() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void onFocusChange(View v, boolean hasFocus) &#123;</span><br><span class="line">                Log.i(&quot;xp.chen&quot;, &quot;==========================================&quot;);</span><br><span class="line">                Log.i(&quot;xp.chen&quot;, &quot;I am button, focus: &quot;+bt_test.isFocused());</span><br><span class="line">                Log.i(&quot;xp.chen&quot;, &quot;I am view, focus: &quot;+view_test.isFocused());</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>我们先点击View，发现View的onClick事件未打印，再点击view，发现view的onclick事件可以正常打印了。<br>然后点击button，发现button的onclick事件也未打印，再点击一次button，发现button的onclick事件可以正常打印了。<br>这说明第一次点击view或者button，知识单纯将焦点转换到了view和button上，此时会调用onFocusChangeListener方法，只有当再次点击的时候才会调用onClick方法。<br>因此，这种操作对Button来说是多余的，而button的android:focusableInTouchMode属性默认也就是false。<br>但对于EditText这种控件来说就不一样了，比方说一个界面上有很多EditText，在你点击了其中一个的时候，你必须先告知用户他点击的是哪一个，这样用户才不会输错，事实上在android源代码里面，EditText的focusableInTouchMode属性默认也就是True。</p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
  <entry>
    <title>GitHub+Hexo搭建个人博客网站</title>
    <url>/2022/09/18/GitHub-Hexo%E6%90%AD%E5%BB%BA%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E7%BD%91%E7%AB%99/</url>
    <content><![CDATA[<h2 id="什么是hexo？"><a href="#什么是hexo？" class="headerlink" title="什么是hexo？"></a>什么是hexo？</h2><p>Hexo是一款基于Node.js的静态博客框架，特点是快速，简介并且高效的博客框架，Hexo使用Markdown解析文章，在几秒内，即可利用靓丽的主题生成静态网页。它可以方便的生成静态网页托管在GitHub和其他托管平台上，是搭建博客的首选框架。</p>
<span id="more"></span>
<h2 id="什么是github？"><a href="#什么是github？" class="headerlink" title="什么是github？"></a>什么是github？</h2><p>GitHub是一个在线软件源代码托管服务平台，使用Git作为版本控制软件，是世界上最大的代码托管网站和开源社区。我们使用的是github上的一个服务，GitHub Pages，它是由GitHub提供的一个网页寄存服务，可以用于存放静态网页，包括博客，项目文档甚至整本书。一般GitHub Pages使用github.io的子域名，但是用户也可以使用第三方域名。</p>
<h2 id="搭建步骤："><a href="#搭建步骤：" class="headerlink" title="搭建步骤："></a>搭建步骤：</h2><h3 id="1-GitHub创建个人仓库"><a href="#1-GitHub创建个人仓库" class="headerlink" title="1.GitHub创建个人仓库"></a>1.GitHub创建个人仓库</h3><p>登录到GitHub，如果没有GitHub账号，使用你的邮箱注册GitHub账号，点击GitHub中的New repository创建新仓库，仓库名应该为：“用户名.github.io”这个用户名使用你的github账号名称代替，这是一个相对固定的写法。</p>
<h3 id="2-安装git"><a href="#2-安装git" class="headerlink" title="2.安装git"></a>2.安装git</h3><p>什么是git？简单来说Git是开源的分布式版本控制系统，用于敏捷高效的处理项目。我们网站在本地搭建好了，需要使用Git同步到GitHub上，推荐一个巨好的<a href="http://git.oschina.net/progit/index.html">git教程</a>。<br>安装成功后，将你的Git与GitHub账号绑定，鼠标右击打开Git Bash，设置user.name和user.eamil配置信息：<br>git config –global user.name “你的GitHub用户名”<br>git config –global user.email “你的GitHub注册邮箱”<br>生成ssh密钥文件：<br>ssh-keygen -t rsa -C “你的GitHub注册邮箱”<br>然后直接三个回车即可，默认不需要设置密码<br>然后找到生成的.ssh文件夹中的id_rsa.pub密钥，将内容全部复制<br>打开GitHub_Settings_keys页面，新建new SSH Key<br>Title为标题，任意填写即可，将刚刚复制的id_rsa.pub内容粘贴进去，最后点击Add SSH Key。<br>在git bash中检测GitHub公钥是否成功，输入ssh <a href="mailto:&#x67;&#x69;&#116;&#64;&#x67;&#105;&#116;&#104;&#117;&#x62;&#46;&#99;&#111;&#109;">&#x67;&#x69;&#116;&#64;&#x67;&#105;&#116;&#104;&#117;&#x62;&#46;&#99;&#111;&#109;</a><br>这里之所以设置GitHub密钥的原因是，通过非对称加密的公钥与私钥来完成加密，公钥放置在github上，私钥放置在自己的电脑里。github要求每次推送代码都是合法用户，所以每次推送都要输入账号密码验证推送用户是否合法用户，为了省去每次输入密码的步骤，采用ssh，当你推送的时候，git就会匹配你的私钥跟github上面的公钥是否是配对的，若是匹配就认为你是合法用户，则允许推送。</p>
<h3 id="3-安装Node-js"><a href="#3-安装Node-js" class="headerlink" title="3.安装Node.js"></a>3.安装Node.js</h3><p>Hexo基于Node.js，<a href="https://nodejs.org/en/download/">Node.js下载地址</a>，注意安装node.js会包含环境变量及npm的安装，安装后，检测node.js是否安装成功，在命令行输入node -v，检测npm是否安装成功，npm -v<br>到这儿，安装Hexo的环境已经全部搭建完成。</p>
<h3 id="4-安装Hexo"><a href="#4-安装Hexo" class="headerlink" title="4.安装Hexo"></a>4.安装Hexo</h3><p>hexo就是我们的个人博客网站的框架，这里需要自己在电脑里创建一个文件夹，可以命名为Blog，Hexo框架与以后你自己发布的网页都在这个文件夹中，创建好后，进入文件夹中，打开命令行窗口。<br>使用npm命令安装Hexo，输入：<br>npm install -g hexo-cli<br>这个安装时间较长耐心等待，安装完成后，初始化我们的博客，输入：<br>hexo init blog<br>注意：这里的命令都是作用在刚刚创建的blog文件夹中。<br>在命令行中输入启动命令：<br>hexo server<br>打开浏览器哦网址可以看到网站的样式</p>
<h3 id="5-推送网站"><a href="#5-推送网站" class="headerlink" title="5.推送网站"></a>5.推送网站</h3><p>上面只是在本地预览，接下来要做的就是推送网站，也就是发布网站，让我们的网站可以被更多人访问到。在设置之前，需要解释一个概念，在blog根目录里的_config.yml文件称为站点配置文件<br>进入themes文件夹，里面也有个_config.yml文件，这个称为主题配置文件<br>现在需要做的是将我们的Hexo和GitHub关联起来，打开站点的配置文件_config.yml，翻到最后修改为：<br>deploy:<br>type: git<br>repo: 这里填入你之前在GitHub上创建仓库的完整历经，记得加上.git<br>branch: master<br>保存站点配置文件。<br>其实就是给hexo d这个命令做相应的配置，让hexo知道你要把blog部署在哪个位置，很显然，我们部署在我们GitHub的仓库里，最后安装git部署插件，输入命令：<br>npm install hexo-deployer-git –save<br>这时，我们分别输入三条命令：<br>hexo clean<br>hexo g<br>hexo d<br>其实第三条hexo d就是部署网站的命令，d是deploy的缩写，完成后，打开浏览器，在地址栏输入你的放置个人网站的仓库路径，你就可以发现你的博客已经上线了，可以在网络上被访问到。</p>
<h2 id="常用hexo命令及解释"><a href="#常用hexo命令及解释" class="headerlink" title="常用hexo命令及解释"></a>常用hexo命令及解释</h2><p>hexo init [folder]<br>新建一个网站。如果没有设置folder，Hexo默认在目前的文件夹建立网站。<br>本命令相当于执行了以下几步：<br>1.Git clone <a href="https://github.com/hexojs/hexo-starter">hexo-starter</a>和<a href="https://github.com/hexojs/hexo-theme-landscape">hexo-theme-landscape</a>主题到当前目录或者指定目录<br>2.使用<a href="https://classic.yarnpkg.com/lang/en/">Yarn 1</a>、<a href="https://pnpm.js.org/">pnpm</a>或者<a href="https://docs.npmjs.com/cli/install">npm</a>包管理器下载依赖（如有已安装多个，则列在前面的优先）。npm默认随<a href="https://hexo.io/docs/#Install-Node-js">Node.js</a>安装。</p>
<p>hexo new [layout] &lt;title&gt;<br>新建一篇文章。如果没有设置layout的话，默认使用_config.yml中的default_layout参数代替，如果标题包含空格的话，请使用引号括起来。<br>默认情况下，Hexo会使用文章的标题来决定文章文件的路径。对于独立页面来说，Hexo会创建一个以标题为名字的目录，并在目录中放置一个index.md文件。你可以使用–path参数覆盖上述行为、自行决定文件的目录：<br>hexo new page –path about&#x2F;me “About me”<br>以上命令会创建一个source&#x2F;about&#x2F;me.md，同时me.md的Front Matter中的title为“page”。这是因为在上述命令中，hexo-cli将page视为指定文章的标题，并采用默认的layout</p>
<p>hexo generate<br>生成静态文件。</p>
<p>hexo publish [layout] &lt;filename&gt;<br>发表草稿</p>
<p>hexo server<br>启动服务器，默认情况下，访问地址为：<br><a href="http://localhost:4000/%E3%80%82">http://localhost:4000/。</a></p>
<p>hexo deploy<br>部署网站。</p>
<p>hexo clean<br>清楚缓存文件（db.json）和已生成的静态文件（public)。在某些情况下（尤其是更换主题后）,如果发现您对站点的更改无论如何也不生效，您可能需要运行该命令。</p>
<p>hexo list &lt;type&gt;<br>列出网站资料</p>
<h3 id="选项"><a href="#选项" class="headerlink" title="选项"></a>选项</h3><p>hexo –safe<br>在安全模式下，不会载入插件和脚本。当您在安装新插件遭遇问题时，可以尝试以安全模式重新执行。</p>
<p>hexo –debug<br>在终端中显示调试信息并记录到debug.log。当您碰到问题时，可以尝试用调试模式重新执行一次，并<a href="https://github.com/hexojs/hexo/issues/new">提交调式信息到GitHub</a>.</p>
<p>hexo –silent<br>隐藏终端信息。</p>
<p>更多详情可以查看<a href="https://zhuanlan.zhihu.com/p/26625249">此篇文档</a></p>
]]></content>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中字符串与格式化</title>
    <url>/2023/11/24/Rust%E4%B8%AD%E5%AD%97%E7%AC%A6%E4%B8%B2%E4%B8%8E%E6%A0%BC%E5%BC%8F%E5%8C%96/</url>
    <content><![CDATA[<p>Rust 提供了多种字符串类型和强大的格式化工具。</p>
<span id="more"></span>

<h2 id="字符串类型"><a href="#字符串类型" class="headerlink" title="字符串类型"></a>字符串类型</h2><p>Rust 中主要有两种字符串类型：<code>&amp;str</code>（字符串切片）和 <code>String</code>（动态字符串），二者均基于 <strong>UTF-8 编码</strong>。</p>
<h3 id="String-动态字符串"><a href="#String-动态字符串" class="headerlink" title="String - 动态字符串"></a>String - 动态字符串</h3><p><code>String</code> 是标准库提供的拥有所有权的动态字符串类型，存储在堆上：</p>
<ul>
<li><strong>可变</strong>：可通过方法修改内容（如添加、删除字符）。</li>
<li><strong>动态分配</strong>：内存会根据内容自动扩容。</li>
<li><strong>所有权</strong>：<code>String</code> 拥有其数据，赋值或传递时会触发所有权转移。</li>
</ul>
<h4 id="创建方式"><a href="#创建方式" class="headerlink" title="创建方式"></a>创建方式</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">new</span>();                   <span class="comment">// 空字符串</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">s</span> = <span class="string">&quot;initial&quot;</span>.<span class="title function_ invoke__">to_string</span>();               <span class="comment">// 从字面量转换</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;initial&quot;</span>);             <span class="comment">// 同上</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">s</span> = <span class="built_in">format!</span>(<span class="string">&quot;Hello &#123;&#125;&quot;</span>, <span class="string">&quot;world&quot;</span>);        <span class="comment">// 通过格式化宏创建</span></span><br></pre></td></tr></table></figure>

<h3 id="amp-str-字符串切片"><a href="#amp-str-字符串切片" class="headerlink" title="&amp;str - 字符串切片"></a>&amp;str - 字符串切片</h3><p><code>&amp;str</code> 是对一段 UTF-8 编码字符串的不可变引用，它不拥有数据所有权，仅作为”视图”存在：</p>
<ul>
<li><strong>不可变</strong>：无法直接修改 <code>&amp;str</code> 指向的内容。</li>
<li><strong>轻量</strong>：由两部分组成（指针 + 长度），不分配堆内存，传递时无需复制数据。</li>
<li><strong>字面量默认类型</strong>：代码中的字符串字面量（如 <code>&quot;hello&quot;</code>）本质上是 <code>&amp;&#39;static str</code>，生命周期为整个程序运行期。</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">s</span>: &amp;<span class="type">str</span> = <span class="string">&quot;hello world&quot;</span>;  <span class="comment">// 字符串字面量，类型为 &amp;&#x27;static str</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">slice</span>: &amp;<span class="type">str</span> = &amp;s[<span class="number">0</span>..<span class="number">5</span>];   <span class="comment">// 从 s 中截取切片，结果为 &quot;hello&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="amp-str-与-String-间转换"><a href="#amp-str-与-String-间转换" class="headerlink" title="&amp;str 与 String 间转换"></a>&amp;str 与 String 间转换</h3><h4 id="String-→-amp-str"><a href="#String-→-amp-str" class="headerlink" title="String → &amp;str"></a>String → &amp;str</h4><p>通过 <code>as_str()</code> 方法或直接引用（<code>&amp;s</code>），因为 <code>String</code> 实现了 <code>Deref&lt;Target=str&gt;</code>，会自动解引用为 <code>&amp;str</code>。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;test&quot;</span>);</span><br><span class="line"><span class="keyword">let</span> <span class="variable">s_slice</span>: &amp;<span class="type">str</span> = s.<span class="title function_ invoke__">as_str</span>();  <span class="comment">// 显式转换</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">s_slice2</span>: &amp;<span class="type">str</span> = &amp;s;         <span class="comment">// 自动解引用，更常用</span></span><br></pre></td></tr></table></figure>

<h4 id="amp-str-→-String"><a href="#amp-str-→-String" class="headerlink" title="&amp;str → String"></a>&amp;str → String</h4><p>通过 <code>String::from</code> 或 <code>to_string()</code>。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">slice</span> = <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">from</span>(slice);  <span class="comment">// 转换为 String</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">s2</span> = slice.<span class="title function_ invoke__">to_string</span>();   <span class="comment">// 等价方式</span></span><br></pre></td></tr></table></figure>

<h3 id="常用操作"><a href="#常用操作" class="headerlink" title="常用操作"></a>常用操作</h3><table>
<thead>
<tr>
<th>分类</th>
<th>方法</th>
<th>返回类型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>创建</strong></td>
<td><code>String::new()</code></td>
<td><code>String</code></td>
<td>创建空字符串</td>
</tr>
<tr>
<td></td>
<td><code>String::from(&quot;abc&quot;)</code></td>
<td><code>String</code></td>
<td>从字面量创建</td>
</tr>
<tr>
<td></td>
<td><code>&quot;abc&quot;.to_string()</code></td>
<td><code>String</code></td>
<td>从 <code>&amp;str</code> 创建 String</td>
</tr>
<tr>
<td></td>
<td><code>String::with_capacity(n)</code></td>
<td><code>String</code></td>
<td>预分配容量，减少重分配</td>
</tr>
<tr>
<td><strong>转换</strong></td>
<td><code>s.as_str()</code></td>
<td><code>&amp;str</code></td>
<td>转换为 <code>&amp;str</code></td>
</tr>
<tr>
<td></td>
<td><code>s.into_bytes()</code></td>
<td><code>Vec&amp;lt;u8&amp;gt;</code></td>
<td>转换为 Vec&lt;u8&gt;</td>
</tr>
<tr>
<td></td>
<td><code>String::from_utf8(v)</code></td>
<td><code>String</code></td>
<td>从字节向量还原字符串</td>
</tr>
<tr>
<td></td>
<td><code>s.to_string()</code></td>
<td><code>String</code></td>
<td>创建新 String</td>
</tr>
<tr>
<td><strong>长度</strong></td>
<td><code>s.len()</code></td>
<td><code>usize</code></td>
<td>返回字节长度（不是字符数）</td>
</tr>
<tr>
<td></td>
<td><code>s.chars().count()</code></td>
<td><code>usize</code></td>
<td>返回字符数（Unicode scalar）</td>
</tr>
<tr>
<td><strong>判断</strong></td>
<td><code>s.is_empty()</code></td>
<td><code>bool</code></td>
<td>是否为空字符串</td>
</tr>
<tr>
<td></td>
<td><code>s.contains(&quot;ab&quot;)</code></td>
<td><code>bool</code></td>
<td>是否包含子串</td>
</tr>
<tr>
<td></td>
<td><code>s.starts_with(&quot;ab&quot;)</code></td>
<td><code>bool</code></td>
<td>是否以指定子串开头</td>
</tr>
<tr>
<td></td>
<td><code>s.ends_with(&quot;ab&quot;)</code></td>
<td><code>bool</code></td>
<td>是否以指定子串结尾</td>
</tr>
<tr>
<td><strong>拼接</strong></td>
<td><code>s.push(&#39;a&#39;)</code></td>
<td><code>()</code></td>
<td>追加单个字符</td>
</tr>
<tr>
<td></td>
<td><code>s.push_str(&quot;abc&quot;)</code></td>
<td><code>()</code></td>
<td>追加字符串切片</td>
</tr>
<tr>
<td></td>
<td><code>s1 + &amp;s2</code></td>
<td><code>String</code></td>
<td>拼接（移动左侧）</td>
</tr>
<tr>
<td></td>
<td><code>format!(&quot;&#123;&#125;&#123;&#125;&quot;, a, b)</code></td>
<td><code>String</code></td>
<td>格式化拼接，不移动</td>
</tr>
<tr>
<td><strong>截取&#x2F;分割</strong></td>
<td><code>&amp;s[a..b]</code></td>
<td><code>&amp;str</code></td>
<td>按字节切片（需在字符边界）</td>
</tr>
<tr>
<td></td>
<td><code>s.split(&#39;,&#39;)</code></td>
<td>迭代器</td>
<td>按分隔符分割为迭代器</td>
</tr>
<tr>
<td></td>
<td><code>s.split_whitespace()</code></td>
<td>迭代器</td>
<td>按空白分割</td>
</tr>
<tr>
<td></td>
<td><code>s.lines()</code></td>
<td>迭代器</td>
<td>按行分割</td>
</tr>
<tr>
<td><strong>修剪</strong></td>
<td><code>s.trim()</code></td>
<td><code>&amp;str</code></td>
<td>去除首尾空白</td>
</tr>
<tr>
<td></td>
<td><code>s.trim_start() / trim_end()</code></td>
<td><code>&amp;str</code></td>
<td>去除首或尾空白</td>
</tr>
<tr>
<td><strong>替换</strong></td>
<td><code>s.replace(&quot;a&quot;,&quot;b&quot;)</code></td>
<td><code>String</code></td>
<td>全部替换</td>
</tr>
<tr>
<td></td>
<td><code>s.replacen(&quot;a&quot;,&quot;b&quot;,n)</code></td>
<td><code>String</code></td>
<td>替换前 n 次</td>
</tr>
<tr>
<td><strong>大小写</strong></td>
<td><code>s.to_lowercase()</code></td>
<td><code>String</code></td>
<td>转小写</td>
</tr>
<tr>
<td></td>
<td><code>s.to_uppercase()</code></td>
<td><code>String</code></td>
<td>转大写</td>
</tr>
<tr>
<td><strong>查找</strong></td>
<td><code>s.find(&quot;ab&quot;)</code></td>
<td><code>Option&lt;usize&gt;</code></td>
<td>返回首个匹配位置</td>
</tr>
<tr>
<td></td>
<td><code>s.rfind(&quot;ab&quot;)</code></td>
<td><code>Option&lt;usize&gt;</code></td>
<td>从后往前找</td>
</tr>
<tr>
<td><strong>迭代</strong></td>
<td><code>s.chars()</code></td>
<td>迭代器</td>
<td>按字符迭代</td>
</tr>
<tr>
<td></td>
<td><code>s.bytes()</code></td>
<td>迭代器</td>
<td>按字节迭代</td>
</tr>
<tr>
<td><strong>比较</strong></td>
<td><code>s1 == s2</code></td>
<td><code>bool</code></td>
<td>内容比较（UTF-8 安全）</td>
</tr>
<tr>
<td><strong>拷贝&#x2F;克隆</strong></td>
<td><code>s.clone()</code></td>
<td><code>String</code></td>
<td>深拷贝（堆上内容复制）</td>
</tr>
<tr>
<td><strong>清空&#x2F;容量</strong></td>
<td><code>s.clear()</code></td>
<td><code>()</code></td>
<td>清空内容</td>
</tr>
<tr>
<td></td>
<td><code>s.capacity()</code></td>
<td><code>usize</code></td>
<td>当前容量</td>
</tr>
<tr>
<td></td>
<td><code>s.reserve(n)</code></td>
<td><code>()</code></td>
<td>预留额外容量</td>
</tr>
<tr>
<td><strong>字节访问</strong></td>
<td><code>s.as_bytes()</code></td>
<td><code>&amp;[u8]</code></td>
<td>获取字节切片</td>
</tr>
<tr>
<td><strong>连接 Vec</strong></td>
<td><code>vec.join(&quot;,&quot;)</code></td>
<td><code>String</code></td>
<td>用分隔符连接</td>
</tr>
<tr>
<td><strong>格式化输出</strong></td>
<td><code>format!()</code></td>
<td><code>String</code></td>
<td>返回格式化字符串</td>
</tr>
</tbody></table>
<h2 id="格式化"><a href="#格式化" class="headerlink" title="格式化"></a>格式化</h2><p>Rust 提供了一套强大的格式化宏，用于字符串拼接、输出等场景，核心是 <code>format!</code>（返回 <code>String</code>），以及衍生的 <code>print!</code>（打印到标准输出）、<code>println!</code>（带换行的打印）、<code>eprint!</code>（打印到标准错误）等。</p>
<h3 id="格式化语法"><a href="#格式化语法" class="headerlink" title="格式化语法"></a>格式化语法</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&#123;[argument][:[fill][align][width][.precision][type]]&#125;</span><br></pre></td></tr></table></figure>

<h4 id="完整示例"><a href="#完整示例" class="headerlink" title="完整示例"></a>完整示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 动态宽度 + 精度（后面需要 $）</span></span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;value:&gt;width$.prec$&#125;&quot;</span>, value=<span class="number">3.14159</span>, width=<span class="number">10</span>, prec=<span class="number">3</span>); <span class="comment">// &quot;     3.142&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 带前缀的十六进制</span></span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:#08x&#125;&quot;</span>, <span class="number">42</span>); <span class="comment">// &quot;0x00002a&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 命名参数 + 格式化</span></span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;name:*^10&#125;&quot;</span>, name=<span class="string">&quot;ALICE&quot;</span>); <span class="comment">// &quot;**ALICE***&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="参数指定"><a href="#参数指定" class="headerlink" title="参数指定"></a>参数指定</h3><p><code>&#123;argument&#125;</code> 用于指定要格式化的参数，默认省略（按顺序）：</p>
<table>
<thead>
<tr>
<th>方式</th>
<th>说明</th>
<th>示例</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td>省略</td>
<td>按参数顺序匹配（默认）</td>
<td><code>println!(&quot;&#123;&#125;, &#123;&#125;&quot;, &quot;a&quot;, &quot;b&quot;)</code></td>
<td><code>a, b</code></td>
</tr>
<tr>
<td>位置索引 <code>&#123;n&#125;</code></td>
<td>按索引 n（从 0 开始）匹配</td>
<td><code>println!(&quot;&#123;1&#125;, &#123;0&#125;&quot;, &quot;a&quot;, &quot;b&quot;)</code></td>
<td><code>b, a</code></td>
</tr>
<tr>
<td>命名 <code>&#123;name&#125;</code></td>
<td>按参数名匹配</td>
<td><code>println!(&quot;&#123;x&#125;, &#123;y&#125;&quot;, x=1, y=2)</code></td>
<td><code>1, 2</code></td>
</tr>
</tbody></table>
<h3 id="填充与对齐"><a href="#填充与对齐" class="headerlink" title="填充与对齐"></a>填充与对齐</h3><p>语法片段：<code>[fill][align][width]</code></p>
<ul>
<li>**<code>fill</code>**：任意单字符（默认空格）；使用 fill 时需要同时指定 <code>align+width</code>。</li>
<li>**<code>align</code>**：<code>&lt;</code>（左对齐）、<code>&gt;</code>（右对齐，默认）、<code>^</code>（居中）。</li>
<li>**<code>width</code>**：最小字段宽度（整数），不足时使用 <code>fill</code> 填充。</li>
</ul>
<h4 id="示例"><a href="#示例" class="headerlink" title="示例"></a>示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="built_in">format!</span>(<span class="string">&quot;&#123;:&gt;8&#125;&quot;</span>, <span class="string">&quot;hi&quot;</span>)       <span class="comment">// 右对齐 -&gt; &quot;      hi&quot;</span></span><br><span class="line"><span class="built_in">format!</span>(<span class="string">&quot;&#123;:&lt;8&#125;&quot;</span>, <span class="string">&quot;hi&quot;</span>)       <span class="comment">// 左对齐 -&gt; &quot;hi      &quot;</span></span><br><span class="line"><span class="built_in">format!</span>(<span class="string">&quot;&#123;:^8&#125;&quot;</span>, <span class="string">&quot;hi&quot;</span>)       <span class="comment">// 居中 -&gt; &quot;   hi   &quot;</span></span><br><span class="line"><span class="built_in">format!</span>(<span class="string">&quot;&#123;:0&gt;5&#125;&quot;</span>, <span class="number">42</span>)        <span class="comment">// 填充字符 &#x27;0&#x27;, 右对齐 -&gt; &quot;00042&quot;</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>说明</th>
<th>示例</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td>固定宽度 <code>&#123;:w&#125;</code></td>
<td><code>println!(&quot;width: [&#123;:*^5&#125;]&quot;, 123);</code></td>
<td><code>[*123*]</code></td>
</tr>
<tr>
<td>动态宽度 <code>&#123;:width$&#125;</code></td>
<td><code>println!(&quot;[&#123;:w$&#125;]&quot;, 123, w=5);</code></td>
<td><code>[ 123]</code></td>
</tr>
</tbody></table>
<h3 id="精度"><a href="#精度" class="headerlink" title="精度"></a>精度</h3><p>语法片段：<code>.precision</code>（写作 <code>.N</code> 或动态 <code>.*</code>）</p>
<table>
<thead>
<tr>
<th>类型</th>
<th>说明</th>
<th>示例</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td><strong>字符串</strong></td>
<td>最大长度（超过则截断）</td>
<td><code>println!(&quot;&#123;:.3&#125;&quot;, &quot;hello&quot;)</code></td>
<td><code>hel</code></td>
</tr>
<tr>
<td><strong>浮点数</strong></td>
<td>小数位数（四舍五入）</td>
<td><code>println!(&quot;&#123;:.2&#125;&quot;, 3.1415)</code></td>
<td><code>3.14</code></td>
</tr>
<tr>
<td><strong>整数</strong></td>
<td>最小位数（不足补 0，超过则原样输出）</td>
<td><code>println!(&quot;&#123;:.4&#125;&quot;, 12)</code></td>
<td><code>0012</code></td>
</tr>
<tr>
<td><strong>动态精度 <code>&#123;:.*&#125;</code></strong></td>
<td>精度由参数指定（前一个参数为精度值）</td>
<td><code>println!(&quot;&#123;&#125;-&#123;:.*&#125;&quot;,&quot;ab&quot;, 2, 3.1415)</code></td>
<td><code>ab-3.14</code></td>
</tr>
</tbody></table>
<h3 id="类型格式"><a href="#类型格式" class="headerlink" title="类型格式"></a>类型格式</h3><h4 id="整数类型"><a href="#整数类型" class="headerlink" title="整数类型"></a>整数类型</h4><p>整数类型（i8&#x2F;i16&#x2F;i32&#x2F;i64&#x2F;u8&#x2F;u16&#x2F;…&#x2F;usize&#x2F;isize）：</p>
<table>
<thead>
<tr>
<th>格式</th>
<th>说明</th>
<th>示例</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td><code>b</code></td>
<td>二进制</td>
<td><code>println!(&quot;&#123;:b&#125;&quot;, 10)</code></td>
<td><code>1010</code></td>
</tr>
<tr>
<td><code>o</code></td>
<td>八进制</td>
<td><code>println!(&quot;&#123;:o&#125;&quot;, 10)</code></td>
<td><code>12</code></td>
</tr>
<tr>
<td><code>d</code></td>
<td>十进制（默认）</td>
<td><code>println!(&quot;&#123;:d&#125;&quot;, 10)</code></td>
<td><code>10</code></td>
</tr>
<tr>
<td><code>x</code></td>
<td>小写十六进制</td>
<td><code>println!(&quot;&#123;:x&#125;&quot;, 255)</code></td>
<td><code>ff</code></td>
</tr>
<tr>
<td><code>X</code></td>
<td>大写十六进制</td>
<td><code>println!(&quot;&#123;:X&#125;&quot;, 255)</code></td>
<td><code>FF</code></td>
</tr>
</tbody></table>
<h4 id="浮点数类型"><a href="#浮点数类型" class="headerlink" title="浮点数类型"></a>浮点数类型</h4><p>浮点数类型（f32&#x2F;f64）：</p>
<table>
<thead>
<tr>
<th>格式</th>
<th>说明</th>
<th>示例</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td><code>f</code></td>
<td>小数形式（默认）</td>
<td><code>println!(&quot;&#123;:f&#125;&quot;, 123.456)</code></td>
<td><code>123.456000</code>（默认 6 位小数）</td>
</tr>
<tr>
<td><code>e</code></td>
<td>科学计数法（小写 e）</td>
<td><code>println!(&quot;&#123;:e&#125;&quot;, 123.456)</code></td>
<td><code>1.234560e+02</code></td>
</tr>
<tr>
<td><code>E</code></td>
<td>科学计数法（大写 E）</td>
<td><code>println!(&quot;&#123;:E&#125;&quot;, 123.456)</code></td>
<td><code>1.234560E+02</code></td>
</tr>
<tr>
<td><code>g</code></td>
<td>自动选择 f 或 e（取较短形式）</td>
<td><code>println!(&quot;&#123;:g&#125;&quot;, 123456789.0)</code></td>
<td><code>1.23457e+08</code></td>
</tr>
<tr>
<td><code>G</code></td>
<td>自动选择 f 或 E</td>
<td><code>println!(&quot;&#123;:G&#125;&quot;, 123456789.0)</code></td>
<td><code>1.23457E+08</code></td>
</tr>
</tbody></table>
<h4 id="通用与特殊类型"><a href="#通用与特殊类型" class="headerlink" title="通用与特殊类型"></a>通用与特殊类型</h4><table>
<thead>
<tr>
<th>格式</th>
<th>说明</th>
<th>适用类型</th>
<th>示例</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td><code>?</code></td>
<td>按 Debug trait 格式化（调试用）</td>
<td>所有实现 Debug 的类型</td>
<td><code>println!(&quot;&#123;:?&#125;&quot;, vec![1,2])</code></td>
<td><code>[1, 2]</code></td>
</tr>
<tr>
<td><code>#?</code></td>
<td>美化的 Debug 格式（换行 + 缩进）</td>
<td>复杂结构（如结构体、嵌套集合）</td>
<td><code>println!(&quot;&#123;:#?&#125;&quot;, vec![1,2])</code></td>
<td><code>[\n 1,\n 2\n]</code></td>
</tr>
<tr>
<td><code>p</code></td>
<td>指针地址</td>
<td>仅用于引用类型</td>
<td><code>println!(&quot;&#123;:p&#125;&quot;, &amp;10)</code></td>
<td><code>0x7ff72194e7b8</code></td>
</tr>
</tbody></table>
<h4 id="标志（Flags）"><a href="#标志（Flags）" class="headerlink" title="标志（Flags）"></a>标志（Flags）</h4><p>特殊修饰符，用于调整输出格式，可与其他说明符组合使用。</p>
<table>
<thead>
<tr>
<th>标志</th>
<th>说明</th>
<th>示例</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td><code>#</code></td>
<td>为数值添加前缀（二进制 0b、八进制 0o、十六进制 0x）</td>
<td><code>println!(&quot;&#123;:#x&#125;&quot;, 255)</code></td>
<td><code>0xff</code></td>
</tr>
<tr>
<td><code>+</code></td>
<td>强制显示正负号（正数显 +，负数显 -）</td>
<td><code>println!(&quot;&#123;:+&#125;&quot;, 10); println!(&quot;&#123;:+&#125;&quot;, -10)</code></td>
<td><code>+10 -10</code></td>
</tr>
<tr>
<td><code>0</code></td>
<td>用 0 填充（代替空格）</td>
<td><code>println!(&quot;&#123;:05&#125;&quot;, 123)</code></td>
<td><code>00123</code></td>
</tr>
<tr>
<td><code>_</code></td>
<td>为大数值添加千位分隔符（仅整数和浮点数）</td>
<td><code>println!(&quot;&#123;:_&#125;&quot;, 1000000)</code></td>
<td><code>1_000_000</code></td>
</tr>
</tbody></table>
<h4 id="特殊格式"><a href="#特殊格式" class="headerlink" title="特殊格式"></a>特殊格式</h4><table>
<thead>
<tr>
<th>说明</th>
<th>输出</th>
</tr>
</thead>
<tbody><tr>
<td><code>&#123;&#123;` | 输出左花括号 `&#123;` |
| `&#125;&#125;</code></td>
<td>输出右花括号 <code>&#125;</code></td>
</tr>
</tbody></table>
<h3 id="自定义-Display-获取参数"><a href="#自定义-Display-获取参数" class="headerlink" title="自定义 Display 获取参数"></a>自定义 Display 获取参数</h3><p>当在类型上实现 <code>fmt::Display</code> 时，可以在 <code>fmt(&amp;self, f: &amp;mut fmt::Formatter&lt;&#39;_&gt;)</code> 中读取用户给的 <code>width</code>、<code>precision</code>、<code>alternate</code> 等：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fmt;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Point</span> &#123; </span><br><span class="line">    x: <span class="type">f64</span>, </span><br><span class="line">    y: <span class="type">f64</span> </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">fmt</span>::Display <span class="keyword">for</span> <span class="title class_">Point</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">fmt</span>(&amp;<span class="keyword">self</span>, f: &amp;<span class="keyword">mut</span> fmt::Formatter&lt;<span class="symbol">&#x27;_</span>&gt;) <span class="punctuation">-&gt;</span> fmt::<span class="type">Result</span> &#123;</span><br><span class="line">        <span class="comment">// 获取 width / precision（Option&lt;usize&gt;）</span></span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>(prec) = f.<span class="title function_ invoke__">precision</span>() &#123;</span><br><span class="line">            <span class="built_in">write!</span>(f, <span class="string">&quot;(&#123;:.prec$&#125;, &#123;:.prec$&#125;)&quot;</span>, <span class="keyword">self</span>.x, <span class="keyword">self</span>.y)</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> f.<span class="title function_ invoke__">alternate</span>() &#123;</span><br><span class="line">            <span class="comment">// 如果用户写了 &quot;&#123;:#&#125;&quot;，可切换格式</span></span><br><span class="line">            <span class="built_in">write!</span>(f, <span class="string">&quot;Point &#123;&#123; x: &#123;&#125;, y: &#123;&#125; &#125;&#125;&quot;</span>, <span class="keyword">self</span>.x, <span class="keyword">self</span>.y)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">write!</span>(f, <span class="string">&quot;(&#123;&#125;, &#123;&#125;)&quot;</span>, <span class="keyword">self</span>.x, <span class="keyword">self</span>.y)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">p</span> = Point &#123; x: <span class="number">3.14159</span>, y: <span class="number">2.71828</span> &#125;;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, p);              <span class="comment">// &quot;(3.14159, 2.71828)&quot;</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;:.2&#125;&quot;</span>, p);           <span class="comment">// &quot;(3.14, 2.72)&quot;</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;:#&#125;&quot;</span>, p);            <span class="comment">// &quot;Point &#123; x: 3.14159, y: 2.71828 &#125;&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong>字符串类型</strong>：<code>String</code>（可变、拥有所有权、堆存储）vs <code>&amp;str</code>（不可变、引用、轻量）</li>
<li><strong>转换</strong>：<code>String</code> &lt;→ <code>&amp;str</code> 通过 <code>as_str()</code>、<code>to_string()</code>、自动解引用</li>
<li><strong>常用操作</strong>：创建、拼接、查找、替换、分割等</li>
<li><strong>格式化</strong>：强大的宏系统，支持对齐、精度、进制、自定义格式</li>
<li><strong>Format Spec</strong>：<code>&#123;[argument][:[fill][align][width][.precision][type]]&#125;</code></li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中所有权和作用域以及生周期</title>
    <url>/2024/07/12/Rust%E4%B8%AD%E6%89%80%E6%9C%89%E6%9D%83%E5%92%8C%E4%BD%9C%E7%94%A8%E5%9F%9F%E4%BB%A5%E5%8F%8A%E7%94%9F%E5%91%A8%E6%9C%9F/</url>
    <content><![CDATA[<p>在rust中，作用域（Scope）和所有权（Ownership）是保证内存安全的核心机制，它们共同决定了变量的生命周期和内存管理方式，无需垃圾回收即可避免悬垂指针、双重释放等内存问题。</p>
<span id="more"></span>

<table>
<thead>
<tr>
<th>概念</th>
<th>含义</th>
<th>是否自动触发销毁</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td>作用域（Scope</td>
<td>变量的可访问范围</td>
<td>是</td>
<td>{ let x &#x3D; 1; }</td>
</tr>
<tr>
<td>所有权（Ownership</td>
<td>资源的唯一持有者</td>
<td>是</td>
<td>let s &#x3D; String:from(“hi”);</td>
</tr>
<tr>
<td>生命周期（Lifetime）</td>
<td>引用的有效区间</td>
<td>否</td>
<td>&amp;&#96;a str</td>
</tr>
</tbody></table>
<h1 id="作用域"><a href="#作用域" class="headerlink" title="作用域"></a>作用域</h1><p>作用域是一个变量（或引用、资源）在程序中有效、可访问的代码范围，由一对花括号{}界定；</p>
<p>1.确定性：作用域由代码结构显示界定，编译期即可确定，不存在运行时动态变化；<br>2.自动清理：Rust会自动调用其drop方法(若实现了drop trait，则会调用drop函数)，释放内存或资源（如文件句柄、网络连接等）。<br>3.嵌套性：作用域可以嵌套（内部作用域包含在外部作用域中），内部作用域可以访问外部作用域的实体，但外部作用域无法访问内部作用域的实体。</p>
<table>
<thead>
<tr>
<th>事件</th>
<th>发生时机</th>
<th>结果</th>
</tr>
</thead>
<tbody><tr>
<td>变量绑定</td>
<td>语句执行时</td>
<td>内存分配（如需要）</td>
</tr>
<tr>
<td>变量可见</td>
<td>声明点到作用域结束</td>
<td>可在作用域内访问</td>
</tr>
<tr>
<td>变量销毁</td>
<td>离开作用域时</td>
<td>调用drop释放资源</td>
</tr>
<tr>
<td>变量的销毁顺序：</td>
<td></td>
<td></td>
</tr>
<tr>
<td>1.后进先出（LIFO）：与声明顺序相反</td>
<td></td>
<td></td>
</tr>
<tr>
<td>2.结构体字段：按声明顺序销毁</td>
<td></td>
<td></td>
</tr>
<tr>
<td>3.数组元素：从第一个到最后一个</td>
<td></td>
<td></td>
</tr>
</tbody></table>
<h1 id="所有权"><a href="#所有权" class="headerlink" title="所有权"></a>所有权</h1><p>rust不适用垃圾回收（GC），而是通过所有权系统在编译期保证内存安全（在编译时会进行一系列的规则检查，在运行时零开销）；</p>
<p>每个值（资源）在任意时刻只有一个所有者（owner）；<br>当所有者离开作用域时，值会被自动销毁；<br>赋值或传参会移动（move）所有权，除非实现了copy trait。</p>
<h2 id="引用与借用"><a href="#引用与借用" class="headerlink" title="引用与借用"></a>引用与借用</h2><p>为避免频繁的所有权转移，Rust提供了引用机制：<br>1.不可变引用（&amp;T）：只读访问，允许多个同时存在；<br>2.可变引用（&amp;mut T）:可修改数据，同一作用域内仅允许一个。</p>
<p>借用规则：<br>1.引用作用域不能超过被引用变量的作用域（防悬垂引用）；<br>2.同一数据不能同时存在可变和不可变引用。</p>
<h2 id="赋值"><a href="#赋值" class="headerlink" title="赋值"></a>赋值</h2><p>赋值会引起两种语义：<br>1.复制（COPY）：a仍然可以使用；<br>    隐式浅拷贝：若类型实现Copy trait（如整数、浮点、布尔、字符、元组中全是Copy类型），则赋值时为Copy语义，不会转移所有权；<br>    Clone（显式深拷贝）：会同时赋值堆数据；<br>2.所有权转移（Move）：a不再有效。</p>
<h2 id="Copy类型"><a href="#Copy类型" class="headerlink" title="Copy类型"></a>Copy类型</h2><table>
<thead>
<tr>
<th>类型类别</th>
<th>示例</th>
<th>特点</th>
</tr>
</thead>
<tbody><tr>
<td>标量类型（scalar）</td>
<td>i32, u64, f32, bool, char</td>
<td>小且固定大小，在栈上</td>
</tr>
<tr>
<td>全是Copy类型的元组</td>
<td>（i32, i32），（u8, f64, bool）</td>
<td>只要里面每个字段都是Copy</td>
</tr>
<tr>
<td>指针类（少见）</td>
<td>*const T, *mut T原生指针</td>
<td>不涉及所有权</td>
</tr>
<tr>
<td>函数指针</td>
<td>fn(i32) -&gt; i32</td>
<td>也是Copy</td>
</tr>
</tbody></table>
<h2 id="非Copy类型"><a href="#非Copy类型" class="headerlink" title="非Copy类型"></a>非Copy类型</h2><table>
<thead>
<tr>
<th>类型类别</th>
<th>示例</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>堆分配类型</td>
<td>String，Vec<T>, Box<T></T></T></td>
<td>包含堆内存指针</td>
</tr>
<tr>
<td>动态大小类型</td>
<td>&amp;str, [T], Rc<T></T></td>
<td>内部有引用计数或指针</td>
</tr>
<tr>
<td>用户定义结构体</td>
<td>若字段中含非Copy成员</td>
<td>整体就不是Copy</td>
</tr>
<tr>
<td>其他智能指针</td>
<td>Rc<T>, Arc<T>, RefCell<T></T></T></T></td>
<td>有运行时管理逻辑</td>
</tr>
</tbody></table>
<h1 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h1><p>生命周期（Lifetime）是编译器用来确保引用始终有效的核心机制。它们解决了”悬垂引用”问题——确保引用不会指向已释放的内存。生命周期是引用有效的作用域范围，由编译器在编译时进行静态分析，不产生运行时开销：**  **<br>1.引用在使用时式合法的；<br>2.不会悬空（dangling reference）<br>3.不会同时存在冲突的可变&#x2F;不可变引用。</p>
<p>生命周期参数用 ‘a, ‘b 等表示（撇号’声明生命周期参数），用法类似于泛型参数 （ 在大多数情况下，Rust 会自动推断生命周期）：<br>1.’a 并不改变运行逻辑；<br>2.只是告诉编译器：“这些引用之间的关系”</p>
<table>
<thead>
<tr>
<th>场景</th>
<th>必须明确标注的情况</th>
<th>可以省略的情况</th>
</tr>
</thead>
<tbody><tr>
<td>结构体</td>
<td>包含引用类型的字段（无论多少个）</td>
<td>不包含引用字段（无需生命周期参数）</td>
</tr>
<tr>
<td>普通函数</td>
<td>1.多个引用参数且返回引用</td>
<td>1.无返回引用；2.单个引用参数且返回引用</td>
</tr>
<tr>
<td>方法</td>
<td>1.返回引用来自外部参数（非self）；2.多个外部引用参数</td>
<td>1.返回引用来自self，2.无返回引用</td>
</tr>
<tr>
<td>Trait&#x2F;枚举</td>
<td>包含引用类型的关联项或方法参数 &#x2F; 返回值</td>
<td>不包含引用类型的关联项或方法</td>
</tr>
</tbody></table>
<h2 id="结构体"><a href="#结构体" class="headerlink" title="结构体"></a>结构体</h2><p>当结构体包含引用时，必须为这些引用标注生命周期，以确保结构体的生命周期不超过它所包含的引用的生命周期。</p>
<p>若结构体的多个引用来自不同生命周期，可用多个参数区分</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 两个字段的生命周期不同，分别用&#x27;a和&#x27;b标注</span><br><span class="line">struct Article&lt;&#x27;a, &#x27;b&gt; &#123;</span><br><span class="line">    content: &amp;&#x27;a str, // 内容的生命周期为&#x27;a    </span><br><span class="line">    source: &amp;&#x27;b str,  // 来源的生命周期为&#x27;b</span><br><span class="line">&#125;</span><br><span class="line">fn main() &#123;    </span><br><span class="line">    let content = String::from(&quot;Rust lifecycle intro&quot;);    </span><br><span class="line">    let source = &quot;Wikipedia&quot;; // 字符串字面量，生命周期为&#x27;static</span><br><span class="line">    let art = Article &#123;        </span><br><span class="line">        content: &amp;content,        </span><br><span class="line">        source: &amp;source,    </span><br><span class="line">    &#125;;    </span><br><span class="line">    // art的生命周期受限于&#x27;a（content的生命周期）和&#x27;b（source的生命周期）中较短的一个</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="函数"><a href="#函数" class="headerlink" title="函数"></a>函数</h2><p>函数的参数或返回值如果是引用类型，可能需要标注生命周期。<br>省略规则：编译器有3条生命周期推断规则，允许在常见场景中省略显式注解：<br>1.每个引用参数获取自己的声明周期参数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fn foo(x: &amp;i32)         // 隐式：fn foo&lt;&#x27;a&gt;(x: &amp;&#x27;a i32)</span><br><span class="line">fn foo(x: &amp;i32, y: &amp;i32) // 隐式：fn foo&lt;&#x27;a, &#x27;b&gt;(x: &amp;&#x27;a i32, y: &amp;&#x27;b i32)</span><br></pre></td></tr></table></figure>
<p>2.若只有一个输入生命周期参数，它被赋予所有输出声明周期参数；</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fn bar(x: &amp;str) -&gt; &amp;str // 隐式：fn bar&lt;&#x27;a&gt;(x: &amp;&#x27;a str) -&gt; &amp;&#x27;a str</span><br></pre></td></tr></table></figure>
<p>3.若有多个输入生命周期参数，但其中一个是&amp;self或 &amp;mut self，则self的生命周期将赋予所有输出周期参数</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">impl&lt;&#x27;a&gt; ImportantExcerpt&lt;&#x27;a&gt; &#123;    </span><br><span class="line">    fn announce(&amp;self, announcement: &amp;str) -&gt; &amp;str &#123;        </span><br><span class="line">        // 隐式：fn announce&lt;&#x27;b&gt;(&amp;&#x27;a self, announcement: &amp;&#x27;b str) -&gt; &amp;&#x27;a str        </span><br><span class="line">        println!(&quot;注意：&#123;&#125;&quot;, announcement);        </span><br><span class="line">        self.part    </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>必须明确标注的情况：当函数的返回引用可能来自多个输入引用，或编译器无法推断返回引用的来源时，必须手动标注生命周期：<br>多个输入引用参数，且返回引用</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 正确示例：标注生命周期&#x27;a，说明x、y和返回值共享同一生命周期</span><br><span class="line">// 注解&#x27;a表示：返回的引用必须来自x或y，且其生命周期不超过x和y中生命周期较短的那个。</span><br><span class="line">fn longest&lt;&#x27;a&gt;(x: &amp;&#x27;a str, y: &amp;&#x27;a str) -&gt; &amp;&#x27;a str &#123;    </span><br><span class="line">    if x.len() &gt; y.len() &#123; x &#125; else &#123; y &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="静态生命周期"><a href="#静态生命周期" class="headerlink" title="静态生命周期"></a>静态生命周期</h2><p>‘static是一个特殊的生命周期，表示引用可以存活到整个程序运行期间。常见：<br>1.字符串字面量：存储在程序的只读内存中，生命周期为’static。<br>2.显式声明为’static的引用（需确保引用的值确实能存活到程序结束，否则会报错）。</p>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Harness Engineering到底是什么</title>
    <url>/2026/05/28/Harness-Engineering%E5%88%B0%E5%BA%95%E6%98%AF%E4%BB%80%E4%B9%88/</url>
    <content><![CDATA[<p>最近 AI 工程圈里，一个新词开始频繁出现：Harness Engineering。它出现在 OpenAI 的工程实践文章里，也出现在 Anthropic 关于 long-running agents 的实践总结里，Martin Fowler 网站也专门写过第一印象。这个概念之所以引发讨论，是因为它试图回答一个很现实的问题：当大模型已经足够强之后，工程师到底应该继续优化 Prompt，还是应该把精力放在模型外部的系统设计上？</p>
<p>这期视频的核心观点可以概括为一句话：Harness Engineering 不是简单的新名词炒作，它是一种围绕 Agent 搭建运行系统的工程思维；但它也未必是最终形态，因为模型能力越强，很多外部约束和补救机制就越可能被模型自身吸收。</p>
<span id="more"></span>

<p>视频来源：<a href="https://www.youtube.com/watch?v=7nCzfgDjSo8">Harness Engineering 到底是什么？概念、实战与争议，一次全部讲清楚</a></p>
<h2 id="从-Prompt-到-Context，再到-Harness"><a href="#从-Prompt-到-Context，再到-Harness" class="headerlink" title="从 Prompt 到 Context，再到 Harness"></a>从 Prompt 到 Context，再到 Harness</h2><p>理解 Harness Engineering，最好先把它放到 Prompt Engineering 和 Context Engineering 的演进链条里看。</p>
<p>Prompt Engineering 关注的是“怎么问问题”。它研究如何组织输入，让模型更准确理解用户意图，比如明确任务目标、输出格式、限制条件和示例。</p>
<p>Context Engineering 往外走了一步，关注的是“怎么给信息”。它不只关心用户说了什么，还关心什么时候把什么材料放进上下文里，包括历史对话、工具列表、外部检索结果、压缩后的摘要、渐进式披露的文档等。</p>
<p>Harness Engineering 再往外扩一圈，关注的是“怎么搭系统”。它研究如何围绕模型构建一个完整可靠的 Agent，包括权限控制、工具管理、上下文组织、执行环境、验证反馈、任务拆解、质量评估和长期维护机制。（截图提示：展示 Prompt Engineering、Context Engineering、Harness Engineering 三层递进关系）</p>
<p>视频里给了一个非常直观的公式：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Agent = Model + Harness</span><br><span class="line">Harness = Agent - Model</span><br></pre></td></tr></table></figure>

<p>也就是说，一个完整 Agent 里，除了大模型本身之外，剩下的运行框架、工具、规则、调度、记忆、环境和验证机制，都可以看作 Harness。（截图提示：展示 Agent &#x3D; Model + Harness 的组成结构）</p>
<p>以 Claude Code 为例，Claude 模型本身不是 Harness；但 <code>CLAUDE.md</code> 里的规则、可调用的工具、执行沙箱、定时调度、文件读写权限、任务上下文管理，都属于 Harness 的范围。</p>
<h2 id="Harness-的核心：驯服模型，而不是替代模型"><a href="#Harness-的核心：驯服模型，而不是替代模型" class="headerlink" title="Harness 的核心：驯服模型，而不是替代模型"></a>Harness 的核心：驯服模型，而不是替代模型</h2><p>Harness 这个词原本有“马具”的意思。视频借这个类比说明：模型像一匹能力很强的马，但如果没有缰绳、鞍具和方向控制，它可能会发散、幻觉、误解目标，甚至在复杂任务中跑偏。</p>
<p>Harness 的作用不是让模型变聪明，而是给模型提供一个可控的工作环境。它让模型知道该看什么、不该碰什么、可以调用哪些工具、完成后如何验证、失败后如何修复。</p>
<p>这也是 Harness Engineering 和单纯提示词优化的区别：提示词优化只是在模型入口处做文章，而 Harness Engineering 是在模型周围搭一整套工程系统。</p>
<h2 id="OpenAI-的实践：让-Agent-在系统里写代码"><a href="#OpenAI-的实践：让-Agent-在系统里写代码" class="headerlink" title="OpenAI 的实践：让 Agent 在系统里写代码"></a>OpenAI 的实践：让 Agent 在系统里写代码</h2><p>视频中最重要的案例来自 OpenAI。OpenAI 做过一个实验：用 AI 从零开发一个真实运行的软件产品，业务逻辑、测试、CI 配置、文档、内部工具等都由 AI 生成，最终代码规模接近 100 万行，耗时约 5 个月，团队从 3 人扩展到 7 人。</p>
<p>这个实验一开始并不顺利。问题不是模型完全不会写代码，而是 Agent 经常走错方向、重复犯错、缺少项目背景、无法稳定验证结果。OpenAI 后来意识到，真正的关键不是继续追问“模型够不够强”，而是要把 Agent 周围的 Harness 搭好。</p>
<p>视频把 OpenAI 的 Harness Engineering 实践概括为三类：上下文管理、验证与反馈、技术债清理。（截图提示：展示 OpenAI Harness Engineering 的三类优化点）</p>
<h3 id="上下文管理：不要把所有信息都塞给模型"><a href="#上下文管理：不要把所有信息都塞给模型" class="headerlink" title="上下文管理：不要把所有信息都塞给模型"></a>上下文管理：不要把所有信息都塞给模型</h3><p>OpenAI 最初尝试把项目规范和相关信息都放进一个很大的 <code>AGENTS.md</code> 文件里，让 Agent 每次任务都能看到完整背景。但这个方案很快暴露出两个问题。</p>
<p>第一，信息太多会淹没重点。就像新员工第一天入职时拿到一本厚厚的员工手册，虽然信息很全，但不知道该从哪里看，也不知道哪些内容和当前任务真正相关。</p>
<p>第二，文档会腐化。项目不断演进，如果没人维护，大而全的文档很快会混入过时规则、废弃约定和历史包袱。Agent 无法判断哪些内容还有效，反而更容易被误导。</p>
<p>OpenAI 的改进方式是把 <code>AGENTS.md</code> 压缩成一个较短的入口目录，只保留导航和核心规则，详细内容分散到更细的文档里，按需提供给 Agent。（截图提示：展示精简版 AGENTS.md 作为目录入口，关联多个细分文档）</p>
<p>另一个重要做法是把关键决策和团队约定搬进代码仓库。很多项目知识原本散落在 Slack、Google Docs 或老员工脑子里，对 Agent 来说等同于不存在。要让 Agent 真正参与工程，就必须让仓库成为事实来源。</p>
<h3 id="验证与反馈：让-Agent-自己跑闭环"><a href="#验证与反馈：让-Agent-自己跑闭环" class="headerlink" title="验证与反馈：让 Agent 自己跑闭环"></a>验证与反馈：让 Agent 自己跑闭环</h3><p>仅仅让 Agent 写代码是不够的，关键是让它能验证自己写出来的东西是否正确。</p>
<p>OpenAI 给 Codex 配置了足够完善的工具和 Skill，让它能在任务中检查自己的输出。例如接入 Chrome DevTools 后，Codex 可以自己截图、查看 DOM、模拟用户操作，从而验证 UI 是否满足要求。（截图提示：展示 Codex 调用 Chrome DevTools 验证 UI 的闭环）</p>
<p>OpenAI 还给 Codex 接入可观测性工具栈，让它读取日志、指标和链路追踪，用于排查运行问题。每个任务运行在隔离环境中，拥有独立日志和指标，任务结束后环境可以自动销毁。</p>
<p>在代码规范层面，OpenAI 使用 linter 和测试形成自动反馈闭环：Agent 生成代码后，系统运行检查；如果失败，错误信息返回给 Agent；Agent 根据错误修复后再次运行检查，直到规则和测试通过。（截图提示：展示 Agent 生成代码、linter&#x2F;test 检查、反馈修复、再次检查的循环）</p>
<p>视频还提到一种架构约束：系统按层划分为 UI、Runtime、Service、Repo、Config、Types，每一层只能依赖更底层的模块，不能反向依赖。（截图提示：展示 UI、Runtime、Service、Repo、Config、Types 的分层依赖结构）</p>
<h3 id="技术债清理：让-Agent-维护自己造成的混乱"><a href="#技术债清理：让-Agent-维护自己造成的混乱" class="headerlink" title="技术债清理：让 Agent 维护自己造成的混乱"></a>技术债清理：让 Agent 维护自己造成的混乱</h3><p>大规模 AI 生成代码会带来重复代码、命名不一致、偏离架构规范、文档过时等技术债。OpenAI 的做法不是等人类工程师手动清理，而是设置后台 Codex 任务定期扫描代码库和文档库，发现问题后自动修复并提交。</p>
<p>这体现了 OpenAI 文章里的核心理念：Humans steer, Agents execute。人类负责掌舵、定方向、给上下文、制定规则和做关键判断；Agent 在 Harness 里负责执行大量重复、细碎、可验证的工程任务。</p>
<h2 id="Anthropic-的实践：Planner、Generator、Evaluator"><a href="#Anthropic-的实践：Planner、Generator、Evaluator" class="headerlink" title="Anthropic 的实践：Planner、Generator、Evaluator"></a>Anthropic 的实践：Planner、Generator、Evaluator</h2><p>Anthropic 的相关实践来自两篇文章：<code>Effective Harnesses for Long-Running Agents</code> 和 <code>Harness Design for Long-Running Application Development</code>。视频把它们的重点总结为两个方向：任务规划和质量评估。</p>
<h3 id="任务规划：先拆解，再执行"><a href="#任务规划：先拆解，再执行" class="headerlink" title="任务规划：先拆解，再执行"></a>任务规划：先拆解，再执行</h3><p>Anthropic 让 Agent 克隆 <code>claude.ai</code> 这样的复杂应用时发现，如果直接把大需求扔给一个 Agent，它往往会急着开工，试图一次做完所有功能。结果是上下文耗尽、功能半成品堆积、后续 Agent 接手时无法判断哪些做完了、哪些只是看起来做完了。</p>
<p>为了解决这个问题，Anthropic 引入了 Initializer Agent，用于初始化执行环境、拆解需求、写启动脚本、添加进度文件。后来他们把需求拆解这件事独立出来，形成 Planner Agent。</p>
<p>Planner 的职责是把模糊需求扩展成清晰的功能列表，让后续 Agent 不再靠猜，而是按功能点推进。（截图提示：展示用户需求经过 Planner 拆成可执行功能列表）</p>
<h3 id="质量评估：不要让-Agent-给自己打分"><a href="#质量评估：不要让-Agent-给自己打分" class="headerlink" title="质量评估：不要让 Agent 给自己打分"></a>质量评估：不要让 Agent 给自己打分</h3><p>质量评估也不能简单交给生成代码的同一个 Agent。视频里提到，Anthropic 发现 Agent 自评并不可靠，因为它会天然倾向于认可自己的输出，即使存在明显 bug，也可能给自己一个高分然后收工。</p>
<p>因此 Anthropic 把生成和评估拆开：Generator 负责实现，Evaluator 作为独立第三方负责评估。Evaluator 不替 Generator 护短，因此反馈更客观，也可以单独优化评估能力。</p>
<p>完整的 Full Harness 方案包含三个角色：Planner 拆解需求，Generator 实现功能，Evaluator 评估质量。Generator 和 Evaluator 会先讨论交付标准，再进入实现与评估的循环，直到 Evaluator 认为通过。（截图提示：展示 Planner、Generator、Evaluator 三 Agent 协作流程）</p>
<p>Anthropic 用一个游戏制作工具任务对比了 Solo 和 Full Harness。Solo 方案只靠一个 Agent，耗时约 20 分钟、花费约 9 美元，但结果问题很多；Full Harness 方案耗时约 6 小时、花费约 200 美元，但产品逻辑、布局和可用性明显更好。（截图提示：展示 Solo 与 Full Harness 在耗时、成本、质量上的对比表）</p>
<p>这说明 Harness 不是免费的。它提高质量和稳定性，但也带来更高的成本和更长的执行时间。</p>
<h2 id="争议：这是新技术，还是新瓶装旧酒？"><a href="#争议：这是新技术，还是新瓶装旧酒？" class="headerlink" title="争议：这是新技术，还是新瓶装旧酒？"></a>争议：这是新技术，还是新瓶装旧酒？</h2><p>视频最后讨论了 Harness Engineering 最大的争议：它到底是不是噱头？</p>
<p>从词源看，Harness 并不是新词。传统软件测试里有 Test Harness，AI 评估领域也有 <code>lm-evaluation-harness</code>。Anthropic 早期文章也用 Harness 描述支持 Agent 长时间运行的框架。</p>
<p>真正变热的是 Harness Engineering 这个组合。视频提到，较早的起点是 Mitchell Hashimoto 在 2026 年 2 月 5 日发表的 <code>My AI Adoption Journey</code>。他大意是说，自己暂时把这种“Agent 犯错后就改造系统，避免它再犯同样错误”的方法叫 Harness Engineering。</p>
<p>随后 OpenAI 在 2026 年 2 月 11 日发布相关文章，引爆了这个词；Martin Fowler 网站在 2 月 17 日发表 <code>Harness Engineering - First Thoughts</code>；LangChain 在 3 月 10 日发布 <code>The Anatomy of an Agent Harness</code>，明确提出 <code>Agent = Model + Harness</code>；Anthropic 在 3 月 24 日发布三 Agent 架构案例后，这个词进一步被技术圈接受。（截图提示：展示 Harness Engineering 概念传播时间线）</p>
<p>怀疑者的观点也有道理：Harness Engineering 里的 linter、测试、任务拆解、质量评估、权限隔离、文档维护并不是新技术。它更像是把已有工程实践重新组织到一个新的系统框架下面。</p>
<p>但工程进步并不总是来自单点新技术。很多时候，真正有价值的是把零散能力组织成一套可设计、可复用、可持续优化的方法。Harness Engineering 的意义就在这里。</p>
<h2 id="模型越强，Harness-会越少吗？"><a href="#模型越强，Harness-会越少吗？" class="headerlink" title="模型越强，Harness 会越少吗？"></a>模型越强，Harness 会越少吗？</h2><p>另一个更深的争议是：如果模型越来越强，今天这些复杂的 Harness 会不会被淘汰？</p>
<p>视频认为，这种趋势已经在 Anthropic 的实践里出现了。比如早期模型在长上下文中会出现“上下文焦虑”，容易急着结束任务、降低交付质量，因此需要用上下文重置等 Harness 技术来补救。但模型升级后，这类问题会减轻，相应的 Harness 设计也就不再那么必要。</p>
<p>再比如 Anthropic 一开始需要强制 Generator 每次只做一个功能点，避免它急于求成；但当更强的模型具备更好的全局统筹能力后，这种强制分步机制就可以简化。</p>
<p>所以，一个现实判断是：模型越强，某些 Harness 会被模型能力吸收；但这不意味着 Harness 会完全消失。它更可能从显式、复杂、强约束的系统，逐渐演化成更底层、更通用的环境接口和基础设施。</p>
<h2 id="我的理解"><a href="#我的理解" class="headerlink" title="我的理解"></a>我的理解</h2><p>Harness Engineering 不是噱头，因为它已经在 OpenAI 和 Anthropic 的实践中展示了明确的工程收益：更稳定的 Agent、更强的自动化能力、更可控的质量反馈，以及更高的生产力。</p>
<p>但它也不是终局。随着模型能力继续增强，今天很多用于约束、纠错和兜底的设计可能会被压缩、简化，甚至消失。</p>
<p>更准确地说，Harness Engineering 是当前阶段把 AI 能力转化为真实生产力的关键过渡技术。只要模型还会幻觉、会误解需求、会在复杂任务中偏航，谁能把 Harness 搭得更稳，谁就能更早把 Agent 用到真实工程里。</p>
]]></content>
      <tags>
        <tag>AI</tag>
        <tag>Agent</tag>
        <tag>Harness Engineering</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中模块定义与导入</title>
    <url>/2024/09/17/Rust%E4%B8%AD%E6%A8%A1%E5%9D%97%E5%AE%9A%E4%B9%89%E4%B8%8E%E5%AF%BC%E5%85%A5/</url>
    <content><![CDATA[<p>模块（mod）是Rust的核心代码组织工具，用于解决命名冲突、控制代码可见性（封装）、拆分大型项目。</p>
<span id="more"></span>

<h2 id="模块的作用"><a href="#模块的作用" class="headerlink" title="模块的作用"></a>模块的作用</h2><ol>
<li><strong>组织代码结构</strong>：把逻辑相关的函数、结构等放在一个模块中，保持代码清晰</li>
<li><strong>控制可见性</strong>：模块内的项（如函数、结构等）默认是私有的（<code>private</code>），根据需要设定其公开性</li>
<li><strong>命名空间管理（作用域隔离）</strong>：模块支持层级路径访问，可防止命名冲突</li>
</ol>
<h2 id="mod、use-的关系"><a href="#mod、use-的关系" class="headerlink" title="mod、use 的关系"></a>mod、use 的关系</h2><table>
<thead>
<tr>
<th>概念</th>
<th>说明</th>
<th>层面</th>
</tr>
</thead>
<tbody><tr>
<td>文件&#x2F;目录作为 mod 名</td>
<td>模块的「物理载体」（单文件 &#x3D; 单个 mod，目录 &#x3D; 模块容器）</td>
<td>物理层面组织代码</td>
</tr>
<tr>
<td>明确用 mod&lt;name&gt; 声明</td>
<td>模块的「编译器注册」（告诉编译器”这个模块存在，加入项目”）</td>
<td>逻辑层面注册</td>
</tr>
<tr>
<td>use 语句</td>
<td>模块元素的「作用域导入」</td>
<td>把已注册模块中的元素导入当前作用域</td>
</tr>
</tbody></table>
<h2 id="1-模块定义"><a href="#1-模块定义" class="headerlink" title="1. 模块定义"></a>1. 模块定义</h2><p>模块既可以内联定义（inline module），也可以放在独立文件&#x2F;目录中。</p>
<h3 id="Rust-项目的根模块"><a href="#Rust-项目的根模块" class="headerlink" title="Rust 项目的根模块"></a>Rust 项目的根模块</h3><p>Rust 项目的根模块由入口文件决定：</p>
<ul>
<li><strong>二进制项目</strong>：入口是 <code>src/main.rs</code>，<code>main.rs</code> 就是根模块（<code>crate</code>）</li>
<li><strong>库项目</strong>：入口是 <code>src/lib.rs</code>，<code>lib.rs</code> 是根模块（<code>crate</code>）</li>
</ul>
<p>所有模块（文件&#x2F;目录）都需在根模块中通过 <code>mod</code> 注册，才能被访问。</p>
<h3 id="内联模块"><a href="#内联模块" class="headerlink" title="内联模块"></a>内联模块</h3><p>在同一个文件内定义模块；用 <code>mod 模块名 &#123; ... &#125;</code> 声明模块。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义根模块下的模块 math</span></span><br><span class="line"><span class="keyword">mod</span> math &#123;</span><br><span class="line">    <span class="comment">// 模块内的私有函数（默认私有，仅模块内可访问）</span></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">square</span>(x: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">        x * x</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用 pub 暴露公共函数（外部可访问）</span></span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">add</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">        a + b</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 嵌套模块（模块可嵌套）</span></span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">mod</span> advanced &#123;</span><br><span class="line">        <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">multiply</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">            a * b</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">square</span>(x: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">            x * x</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">result</span> = math::<span class="title function_ invoke__">add</span>(<span class="number">5</span>, <span class="number">3</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, result); <span class="comment">// 8</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">let</span> <span class="variable">advanced_result</span> = math::advanced::<span class="title function_ invoke__">multiply</span>(<span class="number">5</span>, <span class="number">3</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, advanced_result); <span class="comment">// 15</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="文件或目录模块"><a href="#文件或目录模块" class="headerlink" title="文件或目录模块"></a>文件或目录模块</h3><p>将模块拆分为多个文件&#x2F;目录，核心规则：</p>
<ul>
<li>一个文件对应一个模块（如 <code>math.rs</code> 对应模块 <code>math</code>）</li>
<li>一个目录对应一个模块（如 <code>utils/</code> 对应模块 <code>utils</code>），目录名即为模块名</li>
</ul>
<h4 id="项目目录结构示例"><a href="#项目目录结构示例" class="headerlink" title="项目目录结构示例"></a>项目目录结构示例</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">src/</span><br><span class="line">├── main.rs                # 根模块</span><br><span class="line">├── math.rs                # 单文件模块：math</span><br><span class="line">└── utils/                 # 目录模块：utils</span><br><span class="line">    ├── mod.rs             # 声明子模块（Rust 1.32+ 可选）</span><br><span class="line">    ├── string.rs          # 子模块 utils::string</span><br><span class="line">    └── math.rs            # 子模块 utils::math</span><br></pre></td></tr></table></figure>

<h4 id="文件模块示例"><a href="#文件模块示例" class="headerlink" title="文件模块示例"></a>文件模块示例</h4><p><strong>src&#x2F;math.rs（定义 math 模块）：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">add</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    a + b</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">subtract</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    a - b</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;main.rs（根模块，注册 math）：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">mod</span> math; <span class="comment">// 注册 math 模块</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">result</span> = math::<span class="title function_ invoke__">add</span>(<span class="number">10</span>, <span class="number">5</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, result); <span class="comment">// 15</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="目录模块示例"><a href="#目录模块示例" class="headerlink" title="目录模块示例"></a>目录模块示例</h4><p><strong>src&#x2F;utils&#x2F;mod.rs（声明 utils 模块及其子模块）：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">mod</span> string;</span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">mod</span> math;</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;utils&#x2F;string.rs：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">trim_str</span>(s: &amp;<span class="type">str</span>) <span class="punctuation">-&gt;</span> &amp;<span class="type">str</span> &#123;</span><br><span class="line">    s.<span class="title function_ invoke__">trim</span>()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">to_uppercase</span>(s: &amp;<span class="type">str</span>) <span class="punctuation">-&gt;</span> <span class="type">String</span> &#123;</span><br><span class="line">    s.<span class="title function_ invoke__">to_uppercase</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;utils&#x2F;math.rs：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">multiply</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    a * b</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;main.rs（根模块）：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">mod</span> utils;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">trimmed</span> = utils::string::<span class="title function_ invoke__">trim_str</span>(<span class="string">&quot;  hello  &quot;</span>);</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">product</span> = utils::math::<span class="title function_ invoke__">multiply</span>(<span class="number">5</span>, <span class="number">3</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, trimmed);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, product);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="可见性修饰符"><a href="#可见性修饰符" class="headerlink" title="可见性修饰符"></a>可见性修饰符</h3><p>默认是模块私有（仅当前模块及子模块可访问），通过 <code>pub</code> 扩展可见性，支持细粒度控制：</p>
<table>
<thead>
<tr>
<th>修饰符</th>
<th>范围</th>
<th>用途</th>
</tr>
</thead>
<tbody><tr>
<td><code>pub</code></td>
<td>全局可见</td>
<td>暴露公共 API</td>
</tr>
<tr>
<td><code>pub(crate)</code></td>
<td>当前 crate 内所有地方可见</td>
<td>内部模块间共享，不对外暴露</td>
</tr>
<tr>
<td><code>pub(super)</code></td>
<td>当前模块的「父模块」及其后代模块中可见</td>
<td>子模块向父模块暴露</td>
</tr>
<tr>
<td><code>pub(in 路径)</code></td>
<td>在指定路径代表的模块及其所有后代中可见</td>
<td>跨多级模块的有限共享</td>
</tr>
<tr>
<td>默认（无修饰）</td>
<td>当前模块及其后代模块中可见</td>
<td>完全私有</td>
</tr>
</tbody></table>
<h4 id="可见性示例"><a href="#可见性示例" class="headerlink" title="可见性示例"></a>可见性示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">mod</span> outer &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">a</span>() &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;a: 全局可见&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">pub</span>(<span class="keyword">crate</span>) <span class="keyword">fn</span> <span class="title function_">b</span>() &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;b: 仅当前 crate 可见&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">pub</span>(<span class="keyword">super</span>) <span class="keyword">fn</span> <span class="title function_">c</span>() &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;c: 仅父模块（根模块）可见&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">pub</span>(<span class="keyword">in</span> crate::outer::inner) <span class="keyword">fn</span> <span class="title function_">d</span>() &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;d: 仅 inner 模块可见&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">mod</span> inner &#123;</span><br><span class="line">        <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">e</span>() &#123;</span><br><span class="line">            <span class="comment">// 子模块可访问父模块的所有元素（包括私有）</span></span><br><span class="line">            super::<span class="title function_ invoke__">a</span>();</span><br><span class="line">            super::<span class="title function_ invoke__">b</span>();</span><br><span class="line">            super::<span class="title function_ invoke__">c</span>();</span><br><span class="line">            super::<span class="title function_ invoke__">d</span>();</span><br><span class="line">            <span class="built_in">println!</span>(<span class="string">&quot;e: 私有函数，仅当前模块可见&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    outer::<span class="title function_ invoke__">a</span>(); <span class="comment">// ✓ 可以访问</span></span><br><span class="line">    outer::<span class="title function_ invoke__">c</span>(); <span class="comment">// ✓ 可以访问</span></span><br><span class="line">    <span class="comment">// outer::b(); // ✗ 编译错误：pub(crate) 只在 crate 内可访问</span></span><br><span class="line">    <span class="comment">// outer::d(); // ✗ 编译错误：pub(in ...) 只在指定路径可访问</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-模块的引用"><a href="#2-模块的引用" class="headerlink" title="2. 模块的引用"></a>2. 模块的引用</h2><p>引用模块内的元素时，需要通过路径。</p>
<h3 id="路径类型"><a href="#路径类型" class="headerlink" title="路径类型"></a>路径类型</h3><ul>
<li><strong>绝对路径</strong>：从根模块（<code>crate</code>）开始，以 <code>crate::</code> 开头（<code>crate</code> 代表当前项目）</li>
<li><strong>相对路径</strong>：从当前模块开始，用 <code>self</code>（当前模块，默认可省略）或 <code>super</code>（父模块）导航</li>
</ul>
<h4 id="路径示例"><a href="#路径示例" class="headerlink" title="路径示例"></a>路径示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 绝对路径</span></span><br><span class="line">crate::math::<span class="title function_ invoke__">add</span>(<span class="number">10</span>, <span class="number">5</span>);</span><br><span class="line">crate::utils::string::<span class="title function_ invoke__">trim_str</span>(<span class="string">&quot;  hello  &quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 相对路径（在同级模块中）</span></span><br><span class="line">self::math::<span class="title function_ invoke__">add</span>(<span class="number">10</span>, <span class="number">5</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 相对路径（访问父模块）</span></span><br><span class="line">super::math::<span class="title function_ invoke__">add</span>(<span class="number">10</span>, <span class="number">5</span>);</span><br></pre></td></tr></table></figure>

<h3 id="注册与导入"><a href="#注册与导入" class="headerlink" title="注册与导入"></a>注册与导入</h3><p>当前项目内的源代码文件和内部模块，首次在项目中引入（<code>use</code>）时，必须用 <code>mod</code> 声明注册（在根模块（<code>main.rs</code>&#x2F;<code>lib.rs</code>）写 <code>mod &lt;mod-name&gt;;</code>）。</p>
<p>对于外部依赖（如 <code>serde</code>、<code>prost</code>），通过 <code>Cargo.toml</code> 声明（已在其自身的 <code>lib.rs</code> 中注册过了，无需 <code>mod</code>），直接用 <code>use</code> 导入即可。</p>
<h4 id="注册规则"><a href="#注册规则" class="headerlink" title="注册规则"></a>注册规则</h4><ul>
<li>单文件 mod（如 <code>engine.rs</code>）：在根模块（<code>main.rs</code>&#x2F;<code>lib.rs</code>）写 <code>mod engine;</code>（注册）</li>
<li>目录 mod（如 <code>engine/</code>）：在根模块写 <code>mod engine &#123; pub mod core; &#125;</code>（注册）</li>
<li>同一文件内的显式 mod：直接写 <code>mod math &#123; ... &#125;</code>（声明即注册）</li>
</ul>
<h4 id="mod-注册查找规则"><a href="#mod-注册查找规则" class="headerlink" title="mod 注册查找规则"></a>mod 注册查找规则</h4><p>以 <code>mod game;</code> 为例：</p>
<ol>
<li>查找当前目录下的文件 <code>game.rs</code></li>
<li>查找当前目录下的文件夹 <code>game</code></li>
<li>一旦找到其中一个，就会停止继续查找</li>
</ol>
<h4 id="注册与导入完整示例"><a href="#注册与导入完整示例" class="headerlink" title="注册与导入完整示例"></a>注册与导入完整示例</h4><p><strong>目录结构：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">src/</span><br><span class="line">├── main.rs               # 根模块（注册所有自定义模块）</span><br><span class="line">├── utils.rs              # 单文件模块：通用工具函数</span><br><span class="line">└── calc/                 # 目录模块：计算核心</span><br><span class="line">    ├── mod.rs</span><br><span class="line">    ├── base.rs           # 子模块 calc::base：基础加减</span><br><span class="line">    └── advanced.rs       # 子模块 calc::advanced：高级乘除/平方</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;main.rs（根模块）：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="comment">// 第一步：mod 注册（告诉编译器&quot;这些模块存在，加入项目&quot;）</span></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="keyword">mod</span> utils;</span><br><span class="line"><span class="keyword">mod</span> calc;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="comment">// 演示1：不用 use，通过完整路径访问（不简化）</span></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">demo_without_use</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">trimmed</span> = crate::utils::<span class="title function_ invoke__">trim_str</span>(<span class="string">&quot;  hello world  &quot;</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Trimmed: &#123;&#125;&quot;</span>, trimmed);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">add_res</span> = crate::calc::base::<span class="title function_ invoke__">add</span>(<span class="number">10</span>, <span class="number">20</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Add result: &#123;&#125;&quot;</span>, add_res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="comment">// 演示2：用 use 导入后访问（简化路径，适合频繁访问）</span></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 场景1：导入单个元素（直接用 trim_str，无需前缀）</span></span><br><span class="line"><span class="keyword">use</span> utils::trim_str;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 场景2：导入模块（用&quot;模块名::元素&quot;访问，适合多个元素）</span></span><br><span class="line"><span class="keyword">use</span> calc::base;</span><br><span class="line"><span class="keyword">use</span> calc::advanced;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">demo_with_use</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">trimmed</span> = <span class="title function_ invoke__">trim_str</span>(<span class="string">&quot;  use 简化访问  &quot;</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Trimmed: &#123;&#125;&quot;</span>, trimmed);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">sub_res</span> = base::<span class="title function_ invoke__">subtract</span>(<span class="number">20</span>, <span class="number">10</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Subtract result: &#123;&#125;&quot;</span>, sub_res);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">mul_res</span> = advanced::<span class="title function_ invoke__">multiply</span>(<span class="number">5</span>, <span class="number">6</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Multiply result: &#123;&#125;&quot;</span>, mul_res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="comment">// 演示3：导入多个元素（大括号 &#123;&#125;）</span></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="keyword">use</span> calc::&#123;base, advanced&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">demo_import_multiple</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">add_res</span> = base::<span class="title function_ invoke__">add</span>(<span class="number">100</span>, <span class="number">200</span>);</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">mul_res</span> = advanced::<span class="title function_ invoke__">multiply</span>(<span class="number">10</span>, <span class="number">20</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Add: &#123;&#125;, Multiply: &#123;&#125;&quot;</span>, add_res, mul_res);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="comment">// 演示4：导入全部公共元素（通配符 *）</span></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="keyword">use</span> utils::*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">demo_wildcard</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">trimmed</span> = <span class="title function_ invoke__">trim_str</span>(<span class="string">&quot;  wildcard import  &quot;</span>);</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">upper</span> = <span class="title function_ invoke__">to_uppercase</span>(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Trimmed: &#123;&#125;, Upper: &#123;&#125;&quot;</span>, trimmed, upper);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="comment">// 演示5：别名 as（解决命名冲突）</span></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="keyword">use</span> calc::base <span class="keyword">as</span> base_calc;</span><br><span class="line"><span class="keyword">use</span> calc::advanced <span class="keyword">as</span> advanced_calc;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">demo_alias</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">res1</span> = base_calc::<span class="title function_ invoke__">add</span>(<span class="number">5</span>, <span class="number">10</span>);</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">res2</span> = advanced_calc::<span class="title function_ invoke__">square</span>(<span class="number">5</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Add: &#123;&#125;, Square: &#123;&#125;&quot;</span>, res1, res2);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="comment">// 演示6：重导出 pub use（暴露给外部）</span></span><br><span class="line"><span class="comment">// =====================================================================</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">use</span> utils::trim_str <span class="keyword">as</span> public_trim_str;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;=== Demo 1: Without use ===&quot;</span>);</span><br><span class="line">    <span class="title function_ invoke__">demo_without_use</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;\n=== Demo 2: With use ===&quot;</span>);</span><br><span class="line">    <span class="title function_ invoke__">demo_with_use</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;\n=== Demo 3: Import multiple ===&quot;</span>);</span><br><span class="line">    <span class="title function_ invoke__">demo_import_multiple</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;\n=== Demo 4: Wildcard ===&quot;</span>);</span><br><span class="line">    <span class="title function_ invoke__">demo_wildcard</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;\n=== Demo 5: Alias ===&quot;</span>);</span><br><span class="line">    <span class="title function_ invoke__">demo_alias</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;utils.rs：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">trim_str</span>(s: &amp;<span class="type">str</span>) <span class="punctuation">-&gt;</span> &amp;<span class="type">str</span> &#123;</span><br><span class="line">    s.<span class="title function_ invoke__">trim</span>()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">to_uppercase</span>(s: &amp;<span class="type">str</span>) <span class="punctuation">-&gt;</span> <span class="type">String</span> &#123;</span><br><span class="line">    s.<span class="title function_ invoke__">to_uppercase</span>()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">to_lowercase</span>(s: &amp;<span class="type">str</span>) <span class="punctuation">-&gt;</span> <span class="type">String</span> &#123;</span><br><span class="line">    s.<span class="title function_ invoke__">to_lowercase</span>()</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;calc&#x2F;mod.rs：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">mod</span> base;</span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">mod</span> advanced;</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;calc&#x2F;base.rs：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">add</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    a + b</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">subtract</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    a - b</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>src&#x2F;calc&#x2F;advanced.rs：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">multiply</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    a * b</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">divide</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> b == <span class="number">0</span> &#123;</span><br><span class="line">        <span class="built_in">panic!</span>(<span class="string">&quot;Division by zero&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    a / b</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">square</span>(x: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    x * x</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="use-关键字用法"><a href="#use-关键字用法" class="headerlink" title="use 关键字用法"></a>use 关键字用法</h3><h4 id="导入多个元素（大括号）"><a href="#导入多个元素（大括号）" class="headerlink" title="导入多个元素（大括号）"></a>导入多个元素（大括号）</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 导入多个相关元素</span></span><br><span class="line"><span class="keyword">use</span> std::collections::&#123;HashMap, HashSet, BTreeMap&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 导入某个元素和该模块自身</span></span><br><span class="line"><span class="keyword">use</span> std::io::&#123;<span class="keyword">self</span>, Write&#125;;</span><br></pre></td></tr></table></figure>

<h4 id="导入全部公共元素（通配符）"><a href="#导入全部公共元素（通配符）" class="headerlink" title="导入全部公共元素（通配符）"></a>导入全部公共元素（通配符）</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 导入 prelude 中的所有元素（通常用于标准库）</span></span><br><span class="line"><span class="keyword">use</span> std::prelude::*;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 导入自定义模块的所有元素</span></span><br><span class="line"><span class="keyword">use</span> my_module::*;</span><br></pre></td></tr></table></figure>

<h4 id="别名（as）"><a href="#别名（as）" class="headerlink" title="别名（as）"></a>别名（as）</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 解决名称冲突</span></span><br><span class="line"><span class="keyword">use</span> std::io::<span class="type">Result</span> <span class="keyword">as</span> IoResult;</span><br><span class="line"><span class="keyword">use</span> std::fmt::<span class="type">Result</span> <span class="keyword">as</span> FmtResult;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">io_operation</span>() <span class="punctuation">-&gt;</span> IoResult&lt;()&gt; &#123;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(())</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">fmt_operation</span>() <span class="punctuation">-&gt;</span> FmtResult &#123;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="重导出（pub-use）"><a href="#重导出（pub-use）" class="headerlink" title="重导出（pub use）"></a>重导出（pub use）</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 在 mod.rs 中重导出，简化 API 路径</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">use</span> some_module::deeply::nested::Item;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用者可以直接访问</span></span><br><span class="line"><span class="keyword">use</span> my_crate::Item; <span class="comment">// 而不是 my_crate::some_module::deeply::nested::Item</span></span><br></pre></td></tr></table></figure>

<h2 id="3-最佳实践"><a href="#3-最佳实践" class="headerlink" title="3. 最佳实践"></a>3. 最佳实践</h2><ol>
<li><p><strong>文件模块 vs 内联模块</strong></p>
<ul>
<li>简单的模块：使用内联模块（同一文件）</li>
<li>复杂的模块：拆分为单独文件或目录</li>
</ul>
</li>
<li><p><strong>可见性控制</strong></p>
<ul>
<li>默认私有，按需暴露公共 API</li>
<li>使用 <code>pub(crate)</code> 隐藏内部实现细节</li>
</ul>
</li>
<li><p><strong>模块组织</strong></p>
<ul>
<li>按功能分组，逻辑清晰</li>
<li>避免过深的模块嵌套（通常不超过3层）</li>
</ul>
</li>
<li><p><strong>导入方式</strong></p>
<ul>
<li>频繁使用：导入具体元素（<code>use module::function;</code>）</li>
<li>多个元素：导入模块（<code>use module;</code>）</li>
<li>解决冲突：使用 <code>as</code> 别名</li>
</ul>
</li>
</ol>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong>模块</strong>是 Rust 的核心代码组织工具</li>
<li><strong>mod 注册</strong>告诉编译器模块的存在</li>
<li><strong>use 导入</strong>在当前作用域简化访问</li>
<li><strong>可见性修饰符</strong>控制元素的访问范围</li>
<li><strong>绝对&#x2F;相对路径</strong>用于引用模块元素</li>
<li><strong>重导出（pub use）</strong>可以简化外部 API</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中的 Send 和 Sync trait 详解</title>
    <url>/2023/09/20/Rust%E4%B8%AD%E7%9A%84-Send-%E5%92%8C-Sync-trait-%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<p>Rust中的标记 trait（Marker Trait）<code>Send</code> 和 <code>Sync</code> 是并发安全的核心基石。用于标记类型的并发安全属性，让编译器在编译时检查线程间数据传递的合法性，从根源上避免数据竞争（Data Race）。</p>
<span id="more"></span>

<h2 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h2><ul>
<li>**<code>Send</code>**：类型的值能不能在线程之间”移动所有权”。</li>
<li>**<code>Sync</code>**：类型的值能不能在多个线程中”共享引用 &amp;T”。</li>
</ul>
<h3 id="重要原则"><a href="#重要原则" class="headerlink" title="重要原则"></a>重要原则</h3><ul>
<li>对同一个值进行<strong>并发访问（只读）</strong>是内存安全的。</li>
<li><strong>并发修改</strong>仍然要靠锁&#x2F;原子&#x2F;其他机制。</li>
</ul>
<h2 id="1-Send-所有权在线程间的转移"><a href="#1-Send-所有权在线程间的转移" class="headerlink" title="1. Send - 所有权在线程间的转移"></a>1. Send - 所有权在线程间的转移</h2><p>一个类型 <code>T</code> 是 <code>Send</code>，表示把 <code>T</code> 的一个值从一个线程移动到另一个线程，不会引发数据竞争或内存安全问题。</p>
<h3 id="Send-的特点"><a href="#Send-的特点" class="headerlink" title="Send 的特点"></a>Send 的特点</h3><ul>
<li>移动的是<strong>所有权</strong>，而不是引用。</li>
<li>移动之后，原线程不能再用这个值（所有权已经被 move）。</li>
</ul>
<h3 id="不是-Send-的类型（-Send）"><a href="#不是-Send-的类型（-Send）" class="headerlink" title="不是 Send 的类型（!Send）"></a>不是 Send 的类型（<code>!Send</code>）</h3><p>绝大多数类型自动实现 <code>Send</code>，以下一些例外：</p>
<ul>
<li>**<code>Rc&lt;T&gt;</code>**：引用计数是非原子操作，跨线程转移会导致计数竞争（数据竞争）。</li>
<li>**<code>RefCell&lt;T&gt;</code> &#x2F; <code>Cell&lt;T&gt;</code>**：内部可变性无同步机制，跨线程转移后，多个线程可能同时修改数据。</li>
<li>**裸指针 <code>*const T</code> &#x2F; <code>*mut T</code>**：无安全保证，直接跨线程转移可能导致悬垂指针或数据竞争。</li>
<li>**<code>UnsafeCell&lt;T&gt;</code>**：内部可变性的底层实现，本身 <code>!Send</code>（但基于它的线程安全类型如 <code>Mutex</code> 会手动实现 <code>Send</code>）。</li>
</ul>
<h3 id="Send-示例"><a href="#Send-示例" class="headerlink" title="Send 示例"></a>Send 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">v</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line">    <span class="comment">// Vec&lt;i32&gt; 是 Send，通过move把所有权移到线程中</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">handle</span> = thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, v);</span><br><span class="line">    &#125;);</span><br><span class="line">    handle.<span class="title function_ invoke__">join</span>().<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-Sync-引用在线程间的共享"><a href="#2-Sync-引用在线程间的共享" class="headerlink" title="2. Sync - 引用在线程间的共享"></a>2. Sync - 引用在线程间的共享</h2><p>一个类型 <code>T</code> 是 <code>Sync</code>，表示可以安全地被多个线程共享（即 <code>&amp;T</code> 是 <code>Send</code>）。即，如果一个类型的引用 <code>&amp;T</code> 能安全跨线程传递，那么这个类型就是 <code>Sync</code>（多个线程持有 <code>&amp;T</code> 不会导致数据竞争）。</p>
<h3 id="不是-Sync-的类型（-Sync）"><a href="#不是-Sync-的类型（-Sync）" class="headerlink" title="不是 Sync 的类型（!Sync）"></a>不是 Sync 的类型（<code>!Sync</code>）</h3><p>任何允许通过共享引用 <code>&amp;T</code> 获得可变访问且没有同步保护的类型，都不是 <code>Sync</code>：</p>
<ul>
<li>**<code>Cell&lt;T&gt;</code> &#x2F; <code>RefCell&lt;T&gt;</code>**：允许通过 <code>&amp;T</code> 改变内部数据（”内部可变性”），但不保证线程安全。</li>
<li>**<code>Rc&lt;T&gt;</code>**：通过共享引用可以克隆 <code>Rc</code> 得到更多引用，从而在多线程下导致竞争。</li>
<li><strong>非线程安全的 FFI 对象</strong>，如果通过 <code>&amp;</code> 能调用会修改内部状态的方法。</li>
</ul>
<h3 id="Sync-示例"><a href="#Sync-示例" class="headerlink" title="Sync 示例"></a>Sync 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::&#123;Arc, Mutex&#125;;</span><br><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">data</span> = Arc::<span class="title function_ invoke__">new</span>(Mutex::<span class="title function_ invoke__">new</span>(<span class="number">5</span>)); <span class="comment">// Arc&lt;Mutex&lt;i32&gt;&gt;</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">data_clone</span> = data.<span class="title function_ invoke__">clone</span>();</span><br><span class="line"></span><br><span class="line">    thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">guard</span> = data_clone.<span class="title function_ invoke__">lock</span>().<span class="title function_ invoke__">unwrap</span>(); <span class="comment">// 加锁，独占访问</span></span><br><span class="line">        *guard += <span class="number">1</span>;</span><br><span class="line">    &#125;).<span class="title function_ invoke__">join</span>().<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, data.<span class="title function_ invoke__">lock</span>().<span class="title function_ invoke__">unwrap</span>()); <span class="comment">// 输出 6</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-Auto-Trait-自动推导"><a href="#3-Auto-Trait-自动推导" class="headerlink" title="3. Auto Trait - 自动推导"></a>3. Auto Trait - 自动推导</h2><p><code>Send</code> 和 <code>Sync</code> 都是 auto trait，编译器会根据类型的组成部分自动推导实现。</p>
<h3 id="Send-和-Sync-的四种组合"><a href="#Send-和-Sync-的四种组合" class="headerlink" title="Send 和 Sync 的四种组合"></a>Send 和 Sync 的四种组合</h3><table>
<thead>
<tr>
<th>组合</th>
<th>说明</th>
<th>例子</th>
</tr>
</thead>
<tbody><tr>
<td><code>Send + Sync</code></td>
<td>可以在线程间移动，也可以多线程共享引用</td>
<td><code>i32</code>、<code>Mutex&lt;T&gt;</code>等</td>
</tr>
<tr>
<td><code>Send + !Sync</code></td>
<td>可以在线程间移动，但不能多线程共享引用</td>
<td>一些 <code>*mut T</code> 封装、<code>Sender&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>!Send + Sync</code></td>
<td>不能跨线程移动；不移动，只读共享是安全的</td>
<td>少数特殊 FFI 对象（较少见）</td>
</tr>
<tr>
<td><code>!Send + !Sync</code></td>
<td>既不能跨线程移动，也不能多线程共享引用</td>
<td><code>Rc&lt;RefCell&lt;T&gt;&gt;</code> 等</td>
</tr>
</tbody></table>
<h2 id="4-手动实现-Send-和-Sync"><a href="#4-手动实现-Send-和-Sync" class="headerlink" title="4. 手动实现 Send 和 Sync"></a>4. 手动实现 Send 和 Sync</h2><p>只有在特殊情况下（写底层库 &#x2F; FFI 封装 &#x2F; 自己的锁和原子数据结构时）才会需要手动实现 <code>Send</code> 和 <code>Sync</code>：</p>
<ul>
<li><code>unsafe impl Send</code> &#x2F; <code>unsafe impl Sync</code> 是非常严肃的承诺</li>
<li>要自己保证所有可能的并发访问路径都不会产生数据竞争、悬垂指针等。</li>
</ul>
<h3 id="手动实现示例：用-Mutex-保护裸指针"><a href="#手动实现示例：用-Mutex-保护裸指针" class="headerlink" title="手动实现示例：用 Mutex 保护裸指针"></a>手动实现示例：用 Mutex 保护裸指针</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::Mutex;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 自定义类型：用 Mutex 保护裸指针（裸指针本身 !Send/!Sync）</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">SafePtr</span>&lt;T&gt;(Mutex&lt;*<span class="keyword">mut</span> T&gt;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span>&lt;T&gt; SafePtr&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">new</span>(value: T) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">ptr</span> = <span class="type">Box</span>::<span class="title function_ invoke__">into_raw</span>(<span class="type">Box</span>::<span class="title function_ invoke__">new</span>(value)); <span class="comment">// 裸指针指向堆内存</span></span><br><span class="line">        <span class="title function_ invoke__">SafePtr</span>(Mutex::<span class="title function_ invoke__">new</span>(ptr))</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 安全访问：通过 Mutex 加锁，保证独占访问</span></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">get</span>(&amp;<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;&amp;T&gt; &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">guard</span> = <span class="keyword">self</span>.<span class="number">0</span>.<span class="title function_ invoke__">lock</span>().<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">        <span class="keyword">unsafe</span> &#123; <span class="title function_ invoke__">Some</span>(&amp;**guard) &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">set</span>(&amp;<span class="keyword">self</span>, new_value: T) &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">guard</span> = <span class="keyword">self</span>.<span class="number">0</span>.<span class="title function_ invoke__">lock</span>().<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">        <span class="keyword">unsafe</span> &#123;</span><br><span class="line">            std::ptr::<span class="title function_ invoke__">drop_in_place</span>(*guard);</span><br><span class="line">            *guard = <span class="type">Box</span>::<span class="title function_ invoke__">into_raw</span>(<span class="type">Box</span>::<span class="title function_ invoke__">new</span>(new_value));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 手动实现 Send 和 Sync，因为我们通过 Mutex 确保了线程安全</span></span><br><span class="line"><span class="keyword">unsafe</span> <span class="keyword">impl</span>&lt;T: <span class="built_in">Send</span>&gt; <span class="built_in">Send</span> <span class="keyword">for</span> <span class="title class_">SafePtr</span>&lt;T&gt; &#123;&#125;</span><br><span class="line"><span class="keyword">unsafe</span> <span class="keyword">impl</span>&lt;T: <span class="built_in">Send</span>&gt; <span class="built_in">Sync</span> <span class="keyword">for</span> <span class="title class_">SafePtr</span>&lt;T&gt; &#123;&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong><code>Send</code></strong> 保证所有权可以安全地在线程间转移</li>
<li><strong><code>Sync</code></strong> 保证引用可以安全地在多个线程间共享</li>
<li>大多数类型会自动实现这些 trait</li>
<li>只有在处理特殊情况（裸指针、FFI、自定义同步原语）时才需要手动实现</li>
<li>手动实现 <code>unsafe impl Send/Sync</code> 时要特别谨慎，确保确实满足并发安全条件</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中的Enum与Struct详解</title>
    <url>/2024/02/13/Rust%E4%B8%AD%E7%9A%84Enum%E4%B8%8EStruct%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<p>在 Rust 中，<code>struct</code>（结构体）和 <code>enum</code>（枚举）是两种核心的自定义类型，分别用于<strong>组合相关数据</strong>和<strong>表示互斥的可能性</strong>。</p>
<span id="more"></span>

<h2 id="结构体（struct）"><a href="#结构体（struct）" class="headerlink" title="结构体（struct）"></a>结构体（struct）</h2><p><code>struct</code> 用于将多个相关的值组合在一起，形成一个有意义的组。</p>
<h3 id="具名结构体（named-struct）"><a href="#具名结构体（named-struct）" class="headerlink" title="具名结构体（named struct）"></a>具名结构体（named struct）</h3><p>最常用的结构体形式，字段有明确的名称，可读性强，适合数据含义明确的场景。</p>
<ul>
<li>字段默认私有（仅当前模块内可见）</li>
<li>通过 <code>impl</code> 实现关联函数（方法）</li>
<li>通过 <code>..</code> 实现赋值（所有未指定的字段，全部替换为 <code>..</code> 对应变量中的）；若字段为移动语义，所有权被移走</li>
<li>通过 <code>trait</code> 实现多态</li>
</ul>
<h4 id="基本示例"><a href="#基本示例" class="headerlink" title="基本示例"></a>基本示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fmt;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">pub</span> name: <span class="type">String</span>,</span><br><span class="line">    age: <span class="type">u8</span>,</span><br><span class="line">    active: <span class="type">bool</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">fmt</span>::Display <span class="keyword">for</span> <span class="title class_">Person</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">fmt</span>(&amp;<span class="keyword">self</span>, f: &amp;<span class="keyword">mut</span> fmt::Formatter&lt;<span class="symbol">&#x27;_</span>&gt;) <span class="punctuation">-&gt;</span> fmt::<span class="type">Result</span> &#123;</span><br><span class="line">        <span class="built_in">write!</span>(f, <span class="string">&quot;Person &#123;&#123; name: &#123;&#125;, age: &#123;&#125;, active: &#123;&#125; &#125;&#125;&quot;</span>, </span><br><span class="line">            <span class="keyword">self</span>.name, <span class="keyword">self</span>.age, <span class="keyword">self</span>.active)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">alice</span> = Person &#123; </span><br><span class="line">        name: <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Alice&quot;</span>), </span><br><span class="line">        age: <span class="number">30</span>, </span><br><span class="line">        active: <span class="literal">true</span> </span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">mike</span> = Person &#123; </span><br><span class="line">        name: <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Mike&quot;</span>), </span><br><span class="line">        ..alice </span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;person: &#123;&#125;&quot;</span>, mike);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 解构与 ..</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">Person</span> &#123; name, .. &#125; = mike;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;name: &#123;&#125;&quot;</span>, name);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="可见性"><a href="#可见性" class="headerlink" title="可见性"></a>可见性</h4><p>默认为私有，可通过 <code>pub</code> 修饰符修改：</p>
<table>
<thead>
<tr>
<th>修饰符</th>
<th>作用域</th>
<th>典型场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>pub</code></td>
<td>全局可见（跨 crate）</td>
<td>完全公开</td>
</tr>
<tr>
<td><code>pub(crate)</code></td>
<td>整个当前 crate 内可见</td>
<td>库内部共享</td>
</tr>
<tr>
<td><code>pub(super)</code></td>
<td>父模块可见</td>
<td>子模块辅助函数</td>
</tr>
<tr>
<td><code>pub(in path)</code></td>
<td>指定模块路径内可见</td>
<td>最灵活的控制</td>
</tr>
<tr>
<td>（无修饰）</td>
<td>当前模块私有</td>
<td>默认私有</td>
</tr>
</tbody></table>
<h4 id="Trait（继承-x2F-多态）"><a href="#Trait（继承-x2F-多态）" class="headerlink" title="Trait（继承&#x2F;多态）"></a>Trait（继承&#x2F;多态）</h4><p>Rust 没有传统 OOP 的类继承（没有基类&#x2F;子类）。用 <code>trait</code> + <code>impl</code> 来实现接口&#x2F;多态。</p>
<p>Trait 定义方法签名集合，实现 trait 的类必须提供所有对应方法：</p>
<ul>
<li><strong>静态分发</strong>（<code>T: Trait</code>）在编译期确定具体类型（单态化，性能好）。</li>
<li><strong>动态分发</strong>（<code>&amp;dyn Trait</code> 或 <code>Box&lt;dyn Trait&gt;</code>）通过虚函数表 (vtable)，在运行时分发（适合异构集合&#x2F;插件式架构）。</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">trait</span> <span class="title class_">Drawable</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">draw</span>(&amp;<span class="keyword">self</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Circle</span> &#123; </span><br><span class="line">    radius: <span class="type">f64</span> </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Square</span> &#123; </span><br><span class="line">    side: <span class="type">f64</span> </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Drawable</span> <span class="keyword">for</span> <span class="title class_">Circle</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">draw</span>(&amp;<span class="keyword">self</span>) &#123; </span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;circle r=&#123;&#125;&quot;</span>, <span class="keyword">self</span>.radius); </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Drawable</span> <span class="keyword">for</span> <span class="title class_">Square</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">draw</span>(&amp;<span class="keyword">self</span>) &#123; </span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;square s=&#123;&#125;&quot;</span>, <span class="keyword">self</span>.side); </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 静态分发（泛型）</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">draw_all</span>&lt;T: Drawable&gt;(items: &amp;[T]) &#123;</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">item</span> <span class="keyword">in</span> items &#123;</span><br><span class="line">        item.<span class="title function_ invoke__">draw</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 动态分发（虚函数表）</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">draw_dyn</span>(items: &amp;[<span class="type">Box</span>&lt;<span class="keyword">dyn</span> Drawable&gt;]) &#123;</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">item</span> <span class="keyword">in</span> items &#123;</span><br><span class="line">        item.<span class="title function_ invoke__">draw</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="泛型与生命周期"><a href="#泛型与生命周期" class="headerlink" title="泛型与生命周期"></a>泛型与生命周期</h4><p>结构体可以是泛型的，也可以带生命周期参数。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Holder</span>&lt;<span class="symbol">&#x27;a</span>, T&gt; &#123; </span><br><span class="line">    r: &amp;<span class="symbol">&#x27;a</span> T, </span><br><span class="line">    val: T </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Pair</span>&lt;T, U&gt;(T, U);</span><br></pre></td></tr></table></figure>

<p>泛型说明：</p>
<table>
<thead>
<tr>
<th>语法</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>struct Point&lt;T&gt;</code></td>
<td>定义泛型结构体</td>
</tr>
<tr>
<td><code>impl&lt;T&gt; Point&lt;T&gt;</code></td>
<td>为所有类型实现方法</td>
</tr>
<tr>
<td><code>impl Point&lt;i32&gt;</code></td>
<td>为特定类型实现方法</td>
</tr>
<tr>
<td><code>T: Display</code></td>
<td>泛型约束，要求类型实现 Display trait</td>
</tr>
<tr>
<td><code>where T: Clone, U: Debug</code></td>
<td>泛型约束，更清晰的约束语法</td>
</tr>
</tbody></table>
<h3 id="元组结构体（tuple-struct）"><a href="#元组结构体（tuple-struct）" class="headerlink" title="元组结构体（tuple struct）"></a>元组结构体（tuple struct）</h3><p>字段没有名称，仅通过位置区分，适合数据结构简单、字段含义可通过位置推断的场景。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 元组结构体：通过位置区分（x, y）</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">Point</span>(<span class="type">i32</span>, <span class="type">i32</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建实例</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">p1</span> = <span class="title function_ invoke__">Point</span>(<span class="number">3</span>, <span class="number">4</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 访问字段（通过索引）</span></span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;x坐标：&#123;&#125;&quot;</span>, p1.<span class="number">0</span>); <span class="comment">// 输出：x坐标：3</span></span><br></pre></td></tr></table></figure>

<h3 id="单元结构体（unit-struct）"><a href="#单元结构体（unit-struct）" class="headerlink" title="单元结构体（unit struct）"></a>单元结构体（unit struct）</h3><p>一种不包含任何字段的结构体。它的定义形式类似于一个”空”的结构体：</p>
<ul>
<li>在内存中不占用任何空间（<code>size_of::&lt;UnitStruct&gt;() == 0</code>）</li>
<li>主要用于标记类型（表示”存在性”而非”数据”）</li>
</ul>
<h4 id="状态标记示例"><a href="#状态标记示例" class="headerlink" title="状态标记示例"></a>状态标记示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义状态</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">Locked</span>;</span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">Unlocked</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 门的结构，使用泛型参数表示状态</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">Door</span>&lt;TState&gt; &#123;</span><br><span class="line">    id: <span class="type">u32</span>,</span><br><span class="line">    _state: std::marker::PhantomData&lt;TState&gt;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 只有 Locked 状态才能解锁</span></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Door</span>&lt;Locked&gt; &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">unlock</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> Door&lt;Unlocked&gt; &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;门 &#123;&#125; 已解锁&quot;</span>, <span class="keyword">self</span>.id);</span><br><span class="line">        Door &#123;</span><br><span class="line">            id: <span class="keyword">self</span>.id,</span><br><span class="line">            _state: std::marker::PhantomData,</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 只有 Unlocked 状态才能打开</span></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Door</span>&lt;Unlocked&gt; &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">open</span>(&amp;<span class="keyword">self</span>) &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;门 &#123;&#125; 已打开&quot;</span>, <span class="keyword">self</span>.id);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译器保证操作顺序：必须先解锁再打开</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">door</span> = Door::&lt;Locked&gt; &#123;</span><br><span class="line">    id: <span class="number">1</span>,</span><br><span class="line">    _state: std::marker::PhantomData,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">unlocked_door</span> = door.<span class="title function_ invoke__">unlock</span>();</span><br><span class="line">unlocked_door.<span class="title function_ invoke__">open</span>();</span><br></pre></td></tr></table></figure>

<h2 id="枚举（enum）"><a href="#枚举（enum）" class="headerlink" title="枚举（enum）"></a>枚举（enum）</h2><p>枚举的核心作用是定义一个类型，其值是有限的、互斥的几种可能变体（variants）之一。每个变体可以关联不同类型和数量的数据。</p>
<p>枚举通过 <code>enum</code> 关键字定义，每个变体可以是”空”的，也可以携带数据（类似元组或结构体）。</p>
<h3 id="基本定义"><a href="#基本定义" class="headerlink" title="基本定义"></a>基本定义</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 定义枚举：消息类型</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">Message</span> &#123;</span><br><span class="line">    Quit,                          <span class="comment">// 无数据：退出消息</span></span><br><span class="line">    Move &#123; x: <span class="type">i32</span>, y: <span class="type">i32</span> &#125;,       <span class="comment">// 具名数据：移动到 (x, y)</span></span><br><span class="line">    <span class="title function_ invoke__">Write</span>(<span class="type">String</span>),                 <span class="comment">// 元组数据：写入文本</span></span><br><span class="line">    <span class="title function_ invoke__">ChangeColor</span>(<span class="type">i32</span>, <span class="type">i32</span>, <span class="type">i32</span>),    <span class="comment">// 元组数据：修改颜色（RGB）</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 创建枚举实例</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">msg1</span> = Message::Quit;                    <span class="comment">// 退出消息</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">msg2</span> = Message::Move &#123; x: <span class="number">10</span>, y: <span class="number">20</span> &#125;;   <span class="comment">// 移动到 (10, 20)</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">msg3</span> = Message::<span class="title function_ invoke__">Write</span>(<span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;hello&quot;</span>));  <span class="comment">// 写入文本</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">msg4</span> = Message::<span class="title function_ invoke__">ChangeColor</span>(<span class="number">255</span>, <span class="number">128</span>, <span class="number">0</span>);     <span class="comment">// 修改颜色</span></span><br></pre></td></tr></table></figure>

<h3 id="模式匹配"><a href="#模式匹配" class="headerlink" title="模式匹配"></a>模式匹配</h3><p>枚举的核心用法是通过 <code>match</code> 表达式匹配其变体，从而处理不同情况（类似”多分支条件判断”，但更安全）。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">process_message</span>(msg: Message) &#123;</span><br><span class="line">    <span class="keyword">match</span> msg &#123;</span><br><span class="line">        Message::Quit =&gt; <span class="built_in">println!</span>(<span class="string">&quot;收到退出消息&quot;</span>),</span><br><span class="line">        Message::Move &#123; x, y &#125; =&gt; <span class="built_in">println!</span>(<span class="string">&quot;移动到坐标：(&#123;&#125;, &#123;&#125;)&quot;</span>, x, y),</span><br><span class="line">        Message::<span class="title function_ invoke__">Write</span>(text) =&gt; <span class="built_in">println!</span>(<span class="string">&quot;写入内容：&#123;&#125;&quot;</span>, text),</span><br><span class="line">        Message::<span class="title function_ invoke__">ChangeColor</span>(r, g, b) =&gt; <span class="built_in">println!</span>(<span class="string">&quot;修改颜色为 RGB(&#123;&#125;, &#123;&#125;, &#123;&#125;)&quot;</span>, r, g, b),</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">process_message</span>(msg1); <span class="comment">// 输出：收到退出消息</span></span><br><span class="line"><span class="title function_ invoke__">process_message</span>(msg2); <span class="comment">// 输出：移动到坐标：(10, 20)</span></span><br><span class="line"><span class="title function_ invoke__">process_message</span>(msg3); <span class="comment">// 输出：写入内容：hello</span></span><br></pre></td></tr></table></figure>

<h3 id="常见枚举"><a href="#常见枚举" class="headerlink" title="常见枚举"></a>常见枚举</h3><h4 id="Option-lt-T-gt-表示可选值"><a href="#Option-lt-T-gt-表示可选值" class="headerlink" title="Option&lt;T&gt; - 表示可选值"></a><code>Option&lt;T&gt;</code> - 表示可选值</h4><p><code>Option&lt;T&gt;</code> 是 Rust 中用于表示”有值”或”无值”的枚举，彻底避免了空指针（Null）问题。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Option</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="title function_ invoke__">Some</span>(T),  <span class="comment">// 有值：存储类型为 T 的值</span></span><br><span class="line">    <span class="literal">None</span>,     <span class="comment">// 无值</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="Result-lt-T-E-gt-表示操作结果"><a href="#Result-lt-T-E-gt-表示操作结果" class="headerlink" title="Result&lt;T, E&gt; - 表示操作结果"></a><code>Result&lt;T, E&gt;</code> - 表示操作结果</h4><p><code>Result&lt;T, E&gt;</code> 用于表示操作”成功”或”失败”的结果，是 Rust 错误处理的核心类型。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Result</span>&lt;T, E&gt; &#123;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(T),   <span class="comment">// 成功：存储类型为 T 的结果</span></span><br><span class="line">    <span class="title function_ invoke__">Err</span>(E),  <span class="comment">// 失败：存储类型为 E 的错误信息</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="Ordering-表示比较结果"><a href="#Ordering-表示比较结果" class="headerlink" title="Ordering - 表示比较结果"></a><code>Ordering</code> - 表示比较结果</h4><p><code>Ordering</code> 用于表示两个值的比较结果（小于、等于、大于）。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Ordering</span> &#123;</span><br><span class="line">    Less,    <span class="comment">// 小于</span></span><br><span class="line">    Equal,   <span class="comment">// 等于</span></span><br><span class="line">    Greater, <span class="comment">// 大于</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="Struct-vs-Enum-对比"><a href="#Struct-vs-Enum-对比" class="headerlink" title="Struct vs Enum 对比"></a>Struct vs Enum 对比</h2><table>
<thead>
<tr>
<th>特性</th>
<th>Struct</th>
<th>Enum</th>
</tr>
</thead>
<tbody><tr>
<td><strong>用途</strong></td>
<td>组合相关数据（”与”关系）</td>
<td>表示互斥的可能性（”或”关系）</td>
</tr>
<tr>
<td><strong>字段</strong></td>
<td>一个实例包含所有字段</td>
<td>一个实例只属于某个变体</td>
</tr>
<tr>
<td><strong>内存</strong></td>
<td>所有字段连续存储</td>
<td>只存储当前变体的数据</td>
</tr>
<tr>
<td><strong>匹配</strong></td>
<td>直接访问字段</td>
<td>必须通过模式匹配</td>
</tr>
<tr>
<td><strong>多态</strong></td>
<td>通过 trait 实现</td>
<td>通过模式匹配实现</td>
</tr>
<tr>
<td><strong>适用场景</strong></td>
<td>数据聚合、数据模型</td>
<td>有限状态、错误处理、可选值</td>
</tr>
</tbody></table>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong>Struct</strong>：用于<strong>组织相关数据</strong>，所有字段同时存在，适合表示复合对象</li>
<li><strong>Enum</strong>：用于表示<strong>多个互斥状态</strong>，任一时刻只有一个变体，适合表示有限的可能性</li>
<li><strong>Trait</strong>：Rust 的多态机制，通过静态分发（性能好）和动态分发（灵活）提供灵活的抽象</li>
<li><strong>Pattern Matching</strong>：枚举的强大特性，在编译时保证所有情况都被处理</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中Protobuf使用简介</title>
    <url>/2024/01/26/Rust%E4%B8%ADProtobuf%E4%BD%BF%E7%94%A8%E7%AE%80%E4%BB%8B/</url>
    <content><![CDATA[<p>Protobuf（Protocol Buffers）是 Google 推出的高效序列化协议。Rust中，prost 是最主流的 Protobuf 实现，提供了编译期代码生成和运行时序列化&#x2F;反序列化能力。</p>
<span id="more"></span>

<h2 id="Protobuf-简介"><a href="#Protobuf-简介" class="headerlink" title="Protobuf 简介"></a>Protobuf 简介</h2><p>Protobuf 是一种数据序列化协议，具有以下特点：</p>
<ul>
<li><strong>高效性</strong>：比 JSON、XML 更紧凑，序列化速度更快</li>
<li><strong>向后兼容性</strong>：支持新旧版本共存</li>
<li><strong>语言中立</strong>：支持多种编程语言（Java、Python、C++、Go、Rust等）</li>
<li><strong>模式演进</strong>：灵活支持字段添加和修改</li>
</ul>
<h2 id="1-工具与环境"><a href="#1-工具与环境" class="headerlink" title="1. 工具与环境"></a>1. 工具与环境</h2><p>使用 Rust + Protobuf 需要以下工具：</p>
<h3 id="必备工具"><a href="#必备工具" class="headerlink" title="必备工具"></a>必备工具</h3><ul>
<li><strong>protoc</strong>：Protobuf 官方编译器（负责解析 <code>.proto</code> 文件）</li>
<li><strong>prost</strong>：Rust 的 Protobuf 运行时库（提供序列化&#x2F;反序列化逻辑）</li>
<li><strong>prost-build</strong>：Rust 的 Protobuf 编译工具（集成 <code>protoc</code>，生成 Rust 代码）</li>
</ul>
<h3 id="Windows-下-protoc-安装"><a href="#Windows-下-protoc-安装" class="headerlink" title="Windows 下 protoc 安装"></a>Windows 下 protoc 安装</h3><p>使用 winget 安装（推荐）：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 1. 搜索 protoc</span></span><br><span class="line">winget search protoc</span><br><span class="line"></span><br><span class="line"><span class="comment"># 2. 安装</span></span><br><span class="line">winget install Google.Protobuf</span><br><span class="line"></span><br><span class="line"><span class="comment"># 3. 验证安装</span></span><br><span class="line">protoc --version</span><br><span class="line"><span class="comment"># 输出 libprotoc 33.0 或更高版本即可</span></span><br></pre></td></tr></table></figure>

<h3 id="其他系统安装方式"><a href="#其他系统安装方式" class="headerlink" title="其他系统安装方式"></a>其他系统安装方式</h3><ul>
<li><strong>macOS</strong>：<code>brew install protobuf</code></li>
<li><strong>Linux</strong>：<code>apt-get install protobuf-compiler</code>（Ubuntu&#x2F;Debian）</li>
</ul>
<h2 id="2-项目中使用"><a href="#2-项目中使用" class="headerlink" title="2. 项目中使用"></a>2. 项目中使用</h2><h3 id="配置-Cargo-toml"><a href="#配置-Cargo-toml" class="headerlink" title="配置 Cargo.toml"></a>配置 Cargo.toml</h3><p>在项目的 <code>Cargo.toml</code> 中添加依赖：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">prost</span> = <span class="string">&quot;0.14.1&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[build-dependencies]</span></span><br><span class="line"><span class="attr">prost-build</span> = <span class="string">&quot;0.14.1&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="创建-Proto-文件"><a href="#创建-Proto-文件" class="headerlink" title="创建 Proto 文件"></a>创建 Proto 文件</h3><p>在项目中创建 <code>proto/hello.proto</code> 文件：</p>
<figure class="highlight protobuf"><table><tr><td class="code"><pre><span class="line">syntax = <span class="string">&quot;proto3&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> hello;</span><br><span class="line"></span><br><span class="line"><span class="keyword">message </span><span class="title class_">MyMessage</span> &#123;</span><br><span class="line">  <span class="type">string</span> name = <span class="number">1</span>;</span><br><span class="line">  <span class="type">int32</span> id = <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Proto 文件说明：</strong></p>
<ul>
<li><code>syntax = &quot;proto3&quot;</code>：使用 proto3 语法</li>
<li><code>package hello</code>：定义包名（对应 Rust 中的 module）</li>
<li><code>message</code>：定义消息结构（对应 Rust 中的 struct）</li>
<li><code>string name = 1</code>：字段类型 + 字段名 + 字段序号</li>
</ul>
<h3 id="编写-build-rs-脚本"><a href="#编写-build-rs-脚本" class="headerlink" title="编写 build.rs 脚本"></a>编写 build.rs 脚本</h3><p>在项目根目录创建 <code>build.rs</code> 用于编译 proto 文件为 Rust 代码：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> prost_build;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;cargo:rerun-if-changed=proto/hello.proto&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建输出目录</span></span><br><span class="line">    std::fs::<span class="title function_ invoke__">create_dir_all</span>(<span class="string">&quot;src/pb&quot;</span>)</span><br><span class="line">        .<span class="title function_ invoke__">expect</span>(<span class="string">&quot;Failed to create output directory&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 编译 proto 文件</span></span><br><span class="line">    prost_build::Config::<span class="title function_ invoke__">new</span>()</span><br><span class="line">        .<span class="title function_ invoke__">out_dir</span>(<span class="string">&quot;src/pb&quot;</span>)</span><br><span class="line">        .<span class="title function_ invoke__">compile_protos</span>(&amp;[<span class="string">&quot;proto/hello.proto&quot;</span>], &amp;[<span class="string">&quot;proto/&quot;</span>])</span><br><span class="line">        .<span class="title function_ invoke__">expect</span>(<span class="string">&quot;Failed to compile proto files&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>build.rs 参数说明：</strong></p>
<ul>
<li><code>out_dir(&quot;src/pb&quot;)</code>：编译生成的 Rust 文件存放位置</li>
<li><code>compile_protos(&amp;[&quot;proto/hello.proto&quot;], &amp;[&quot;proto/&quot;])</code>：<ul>
<li>第一个参数：要编译的 proto 文件（支持多个）</li>
<li>第二个参数：proto 文件所在的目录</li>
</ul>
</li>
</ul>
<p><strong>build 成功后</strong>，会在 <code>src/pb</code> 下创建 <code>hello.rs</code> 文件（与 proto 文件中的 package 名称相同）。</p>
<h3 id="使用生成的代码"><a href="#使用生成的代码" class="headerlink" title="使用生成的代码"></a>使用生成的代码</h3><h4 id="方法1：使用-include-宏"><a href="#方法1：使用-include-宏" class="headerlink" title="方法1：使用 include! 宏"></a>方法1：使用 include! 宏</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line">include!(<span class="string">&quot;pb/hello.rs&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> prost::Message;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">msg</span> = MyMessage &#123;</span><br><span class="line">        name: <span class="string">&quot;Protobuf example&quot;</span>.<span class="title function_ invoke__">to_string</span>(),</span><br><span class="line">        id: <span class="number">123</span>,</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">let</span> <span class="variable">encoded</span> = msg.<span class="title function_ invoke__">encode_to_vec</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Encoded Msg: &#123;:?&#125;&quot;</span>, encoded);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">decoded</span> = MyMessage::<span class="title function_ invoke__">decode</span>(&amp;encoded[..]).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Decoded Msg: &#123;:?&#125;&quot;</span>, decoded);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="方法2：使用-mod-和-use（推荐）"><a href="#方法2：使用-mod-和-use（推荐）" class="headerlink" title="方法2：使用 mod 和 use（推荐）"></a>方法2：使用 mod 和 use（推荐）</h4><p><strong>1. 在 src&#x2F;pb 下创建 mod.rs：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">mod</span> hello;</span><br></pre></td></tr></table></figure>

<p><strong>2. 在 src&#x2F;main.rs 中引入：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">mod</span> pb;</span><br><span class="line"></span><br><span class="line"><span class="keyword">use</span> pb::hello::MyMessage;</span><br><span class="line"><span class="keyword">use</span> prost::Message;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">msg</span> = MyMessage &#123;</span><br><span class="line">        name: <span class="string">&quot;Protobuf example&quot;</span>.<span class="title function_ invoke__">to_string</span>(),</span><br><span class="line">        id: <span class="number">123</span>,</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 序列化</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">encoded</span> = msg.<span class="title function_ invoke__">encode_to_vec</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Encoded Msg: &#123;:?&#125;&quot;</span>, encoded);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 反序列化</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">decoded</span> = MyMessage::<span class="title function_ invoke__">decode</span>(&amp;encoded[..]).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Decoded Msg: &#123;:?&#125;&quot;</span>, decoded);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-常见-Proto-类型映射"><a href="#3-常见-Proto-类型映射" class="headerlink" title="3. 常见 Proto 类型映射"></a>3. 常见 Proto 类型映射</h2><table>
<thead>
<tr>
<th>Proto 类型</th>
<th>Rust 类型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>string</code></td>
<td><code>String</code></td>
<td>字符串</td>
</tr>
<tr>
<td><code>bytes</code></td>
<td><code>Vec&lt;u8&gt;</code></td>
<td>字节数组</td>
</tr>
<tr>
<td><code>int32</code> &#x2F; <code>int64</code></td>
<td><code>i32</code> &#x2F; <code>i64</code></td>
<td>整数</td>
</tr>
<tr>
<td><code>uint32</code> &#x2F; <code>uint64</code></td>
<td><code>u32</code> &#x2F; <code>u64</code></td>
<td>无符号整数</td>
</tr>
<tr>
<td><code>float</code> &#x2F; <code>double</code></td>
<td><code>f32</code> &#x2F; <code>f64</code></td>
<td>浮点数</td>
</tr>
<tr>
<td><code>bool</code></td>
<td><code>bool</code></td>
<td>布尔值</td>
</tr>
<tr>
<td><code>repeated</code></td>
<td><code>Vec&lt;T&gt;</code></td>
<td>数组&#x2F;列表</td>
</tr>
</tbody></table>
<h2 id="4-常用序列化方法"><a href="#4-常用序列化方法" class="headerlink" title="4. 常用序列化方法"></a>4. 常用序列化方法</h2><h3 id="序列化（Encode）"><a href="#序列化（Encode）" class="headerlink" title="序列化（Encode）"></a>序列化（Encode）</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> prost::Message;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">msg</span> = MyMessage &#123; ... &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编码为字节向量</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">bytes</span> = msg.<span class="title function_ invoke__">encode_to_vec</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编码并写入 Writer</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">buf</span> = <span class="type">Vec</span>::<span class="title function_ invoke__">new</span>();</span><br><span class="line">msg.<span class="title function_ invoke__">encode</span>(&amp;<span class="keyword">mut</span> buf).<span class="title function_ invoke__">unwrap</span>();</span><br></pre></td></tr></table></figure>

<h3 id="反序列化（Decode）"><a href="#反序列化（Decode）" class="headerlink" title="反序列化（Decode）"></a>反序列化（Decode）</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 从字节切片解码</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">decoded</span> = MyMessage::<span class="title function_ invoke__">decode</span>(&amp;bytes[..]).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 从 Reader 解码</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">decoded</span> = MyMessage::<span class="title function_ invoke__">decode</span>(&amp;<span class="keyword">mut</span> reader).<span class="title function_ invoke__">unwrap</span>();</span><br></pre></td></tr></table></figure>

<h2 id="5-更复杂的例子"><a href="#5-更复杂的例子" class="headerlink" title="5. 更复杂的例子"></a>5. 更复杂的例子</h2><p><strong>proto 文件：</strong></p>
<figure class="highlight protobuf"><table><tr><td class="code"><pre><span class="line">syntax = <span class="string">&quot;proto3&quot;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> user;</span><br><span class="line"></span><br><span class="line"><span class="keyword">message </span><span class="title class_">User</span> &#123;</span><br><span class="line">  <span class="type">int32</span> id = <span class="number">1</span>;</span><br><span class="line">  <span class="type">string</span> name = <span class="number">2</span>;</span><br><span class="line">  <span class="type">string</span> email = <span class="number">3</span>;</span><br><span class="line">  <span class="keyword">repeated</span> <span class="type">string</span> tags = <span class="number">4</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">message </span><span class="title class_">UserList</span> &#123;</span><br><span class="line">  <span class="keyword">repeated</span> User users = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Rust 使用代码：</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> prost::Message;</span><br><span class="line"><span class="keyword">use</span> pb::user::&#123;User, UserList&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">user1</span> = User &#123;</span><br><span class="line">        id: <span class="number">1</span>,</span><br><span class="line">        name: <span class="string">&quot;Alice&quot;</span>.<span class="title function_ invoke__">to_string</span>(),</span><br><span class="line">        email: <span class="string">&quot;alice@example.com&quot;</span>.<span class="title function_ invoke__">to_string</span>(),</span><br><span class="line">        tags: <span class="built_in">vec!</span>[<span class="string">&quot;admin&quot;</span>.<span class="title function_ invoke__">to_string</span>(), <span class="string">&quot;user&quot;</span>.<span class="title function_ invoke__">to_string</span>()],</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">user2</span> = User &#123;</span><br><span class="line">        id: <span class="number">2</span>,</span><br><span class="line">        name: <span class="string">&quot;Bob&quot;</span>.<span class="title function_ invoke__">to_string</span>(),</span><br><span class="line">        email: <span class="string">&quot;bob@example.com&quot;</span>.<span class="title function_ invoke__">to_string</span>(),</span><br><span class="line">        tags: <span class="built_in">vec!</span>[<span class="string">&quot;user&quot;</span>.<span class="title function_ invoke__">to_string</span>()],</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">user_list</span> = UserList &#123;</span><br><span class="line">        users: <span class="built_in">vec!</span>[user1, user2],</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 序列化</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">bytes</span> = user_list.<span class="title function_ invoke__">encode_to_vec</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Serialized: &#123;&#125; bytes&quot;</span>, bytes.<span class="title function_ invoke__">len</span>());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 反序列化</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">decoded</span> = UserList::<span class="title function_ invoke__">decode</span>(&amp;bytes[..]).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">user</span> <span class="keyword">in</span> decoded.users &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;User: &#123;&#125; (&#123;&#125;)&quot;</span>, user.name, user.email);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong>Protobuf</strong> 是高效的数据序列化协议，适合网络传输和存储</li>
<li><strong>prost</strong> 是 Rust 中最流行的 Protobuf 实现</li>
<li><strong>build.rs</strong> 脚本在编译时自动生成 Rust 代码</li>
<li><strong>序列化&#x2F;反序列化</strong> 通过 <code>Message</code> trait 的方法实现</li>
<li>Protobuf 支持复杂嵌套结构、数组、向后兼容性等高级特性</li>
<li>适用场景：微服务、RPC、消息队列、数据存储等</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中的for循环与迭代器</title>
    <url>/2024/02/10/Rust%E4%B8%AD%E7%9A%84for%E5%BE%AA%E7%8E%AF%E4%B8%8E%E8%BF%AD%E4%BB%A3%E5%99%A8/</url>
    <content><![CDATA[<p><code>for</code> 循环是迭代器的语法糖，而迭代器是 Rust 处理集合（如 <code>Vec</code>、<code>HashMap</code>）的核心抽象。</p>
<span id="more"></span>

<h2 id="1-for-循环"><a href="#1-for-循环" class="headerlink" title="1. for 循环"></a>1. for 循环</h2><h3 id="for-循环本质"><a href="#for-循环本质" class="headerlink" title="for 循环本质"></a>for 循环本质</h3><p><code>for</code> 循环本质是自动遍历一个”可迭代对象”（实现 <code>IntoIterator</code> trait 的类型），底层逻辑如下：</p>
<ol>
<li>对目标对象调用 <code>into_iter()</code> 方法，将其转换为迭代器（Iterator）</li>
<li>循环调用迭代器的 <code>next()</code> 方法，每次获取一个元素（<code>Some(T)</code>）</li>
<li>当 <code>next()</code> 返回 <code>None</code> 时，循环终止</li>
</ol>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// for 循环的原始形式</span></span><br><span class="line"><span class="keyword">for</span> <span class="variable">item</span> <span class="keyword">in</span> collection &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 会转换为以下近似代码</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">iter</span> = <span class="built_in">IntoIterator</span>::<span class="title function_ invoke__">into_iter</span>(collection);</span><br><span class="line">    <span class="keyword">while</span> <span class="keyword">let</span> <span class="variable">Some</span>(item) = iter.<span class="title function_ invoke__">next</span>() &#123;</span><br><span class="line">        <span class="comment">// ...</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="for-循环的用法"><a href="#for-循环的用法" class="headerlink" title="for 循环的用法"></a>for 循环的用法</h3><p>任何实现 <code>IntoIterator</code> 的类型，都能直接用 <code>for</code> 循环遍历。</p>
<h4 id="1-遍历集合"><a href="#1-遍历集合" class="headerlink" title="1. 遍历集合"></a>1. 遍历集合</h4><p>需注意所有权与借用模式，三种核心模式：</p>
<ul>
<li><p><strong>所有权转移遍历</strong>：<code>into_iter()</code></p>
<ul>
<li>转移所有权，<code>T</code> 类型</li>
<li><code>for x in v</code></li>
</ul>
</li>
<li><p><strong>不可变借用遍历</strong>：<code>iter()</code></p>
<ul>
<li>不可变引用 <code>&amp;T</code>，需通过解引用（<code>*</code>）访问元素值</li>
<li><code>for x in &amp;v</code></li>
</ul>
</li>
<li><p><strong>可变借用遍历</strong>：<code>iter_mut()</code></p>
<ul>
<li>可变引用 <code>&amp;mut T</code>，需通过解引用（<code>*</code>）修改元素</li>
<li><code>for x in &amp;mut v</code></li>
</ul>
</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">v</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 所有权转移（消耗集合）</span></span><br><span class="line"><span class="keyword">for</span> <span class="variable">x</span> <span class="keyword">in</span> v.<span class="title function_ invoke__">into_iter</span>() &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, x); <span class="comment">// x 是 i32</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// v 不再可用</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 不可变借用（只读）</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">v</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="keyword">for</span> <span class="variable">x</span> <span class="keyword">in</span> &amp;v &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, x); <span class="comment">// x 是 &amp;i32</span></span><br><span class="line">    <span class="comment">// 需要解引用：println!(&quot;&#123;&#125;&quot;, *x);</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// v 仍可用</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 可变借用（修改）</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">v</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="keyword">for</span> <span class="variable">x</span> <span class="keyword">in</span> &amp;<span class="keyword">mut</span> v &#123;</span><br><span class="line">    *x += <span class="number">10</span>; <span class="comment">// 修改元素</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, v); <span class="comment">// [11, 12, 13]</span></span><br></pre></td></tr></table></figure>

<h4 id="2-计数循环-Range-区间遍历"><a href="#2-计数循环-Range-区间遍历" class="headerlink" title="2. 计数循环 - Range 区间遍历"></a>2. 计数循环 - Range 区间遍历</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 左闭右开区间 (a..b)</span></span><br><span class="line"><span class="keyword">for</span> <span class="variable">i</span> <span class="keyword">in</span> <span class="number">0</span>..<span class="number">5</span> &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, i); <span class="comment">// 输出 0, 1, 2, 3, 4</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 左闭右闭区间 (a..=b)</span></span><br><span class="line"><span class="keyword">for</span> <span class="variable">i</span> <span class="keyword">in</span> <span class="number">0</span>..=<span class="number">5</span> &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, i); <span class="comment">// 输出 0, 1, 2, 3, 4, 5</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 反向计数 - rev() 反向迭代</span></span><br><span class="line"><span class="keyword">for</span> <span class="variable">i</span> <span class="keyword">in</span> (<span class="number">1</span>..=<span class="number">5</span>).<span class="title function_ invoke__">rev</span>() &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, i); <span class="comment">// 输出 5, 4, 3, 2, 1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-键值对遍历（HashMap-x2F-BTreeMap）"><a href="#3-键值对遍历（HashMap-x2F-BTreeMap）" class="headerlink" title="3. 键值对遍历（HashMap&#x2F;BTreeMap）"></a>3. 键值对遍历（HashMap&#x2F;BTreeMap）</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::HashMap;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">map</span> = HashMap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">map.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Alice&quot;</span>);</span><br><span class="line">map.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;age&quot;</span>, <span class="string">&quot;30&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// for (key, value) in &amp;map（返回 key 与 value 的引用）</span></span><br><span class="line"><span class="keyword">for</span> (key, value) <span class="keyword">in</span> &amp;map &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;: &#123;&#125;&quot;</span>, key, value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="4-遍历索引与值"><a href="#4-遍历索引与值" class="headerlink" title="4. 遍历索引与值"></a>4. 遍历索引与值</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">fruits</span> = <span class="built_in">vec!</span>[<span class="string">&quot;apple&quot;</span>, <span class="string">&quot;banana&quot;</span>, <span class="string">&quot;orange&quot;</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// enumerate() 适配器（返回 (index, value) 元组）</span></span><br><span class="line"><span class="keyword">for</span> (idx, fruit) <span class="keyword">in</span> fruits.<span class="title function_ invoke__">iter</span>().<span class="title function_ invoke__">enumerate</span>() &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;. &#123;&#125;&quot;</span>, idx, fruit);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 输出：</span></span><br><span class="line"><span class="comment">// 0. apple</span></span><br><span class="line"><span class="comment">// 1. banana</span></span><br><span class="line"><span class="comment">// 2. orange</span></span><br></pre></td></tr></table></figure>

<h3 id="三种遍历模式对比"><a href="#三种遍历模式对比" class="headerlink" title="三种遍历模式对比"></a>三种遍历模式对比</h3><table>
<thead>
<tr>
<th>for 循环形式</th>
<th>底层调用</th>
<th>元素类型</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>for x in v</code></td>
<td><code>v.into_iter()</code></td>
<td><code>T</code>（原值）</td>
<td>所有权转移（消耗集合）</td>
</tr>
<tr>
<td><code>for x in &amp;v</code></td>
<td><code>v.iter()</code></td>
<td><code>&amp;T</code>（不可变引用）</td>
<td>不可变借用（只读集合）</td>
</tr>
<tr>
<td><code>for x in &amp;mut v</code></td>
<td><code>v.iter_mut()</code></td>
<td><code>&amp;mut T</code>（可变引用）</td>
<td>可变借用（修改集合）</td>
</tr>
<tr>
<td><code>for i in 0..n</code></td>
<td>Range 迭代器</td>
<td>值拷贝</td>
<td>计数循环</td>
</tr>
</tbody></table>
<h3 id="绑定解构"><a href="#绑定解构" class="headerlink" title="绑定解构"></a>绑定解构</h3><p>迭代器遍历中元素的类型由 <code>Iterator</code> trait 中的 <code>Item</code> 确定。对于引用类型，可通过 <code>&amp;</code>（不是取地址）进行解构。以 <code>iter()</code>（<code>Item</code> 是 <code>&amp;T</code>）遍历为例：</p>
<ul>
<li>**用 <code>x</code>**：绑定 <code>&amp;T</code>（保留引用），需通过 <code>*x</code> 解引用操作值</li>
<li>**用 <code>&amp;x</code>**：解构一层引用，直接绑定 <code>T</code>（原值），无需解引用</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::HashMap;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">type_of</span>&lt;T&gt;(_: &amp;T) <span class="punctuation">-&gt;</span> &amp;<span class="symbol">&#x27;static</span> <span class="type">str</span> &#123;</span><br><span class="line">    std::any::type_name::&lt;T&gt;()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">map</span> = HashMap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    map.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;english&quot;</span>, <span class="number">80</span>);</span><br><span class="line">    map.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;math&quot;</span>, <span class="number">90</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 用 &amp;(&amp;key, &amp;value) 解构一层引用</span></span><br><span class="line">    <span class="keyword">for</span> (&amp;key, &amp;value) <span class="keyword">in</span> &amp;map &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;: &#123;&#125; (类型: &#123;&#125;: &#123;&#125;)&quot;</span>, </span><br><span class="line">            key, value,</span><br><span class="line">            <span class="title function_ invoke__">type_of</span>(&amp;key),      <span class="comment">// &amp;str</span></span><br><span class="line">            <span class="title function_ invoke__">type_of</span>(&amp;value)     <span class="comment">// i32</span></span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 不解构，保留引用</span></span><br><span class="line">    <span class="keyword">for</span> (key, value) <span class="keyword">in</span> &amp;map &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;: &#123;&#125; (类型: &#123;&#125;: &#123;&#125;)&quot;</span>, </span><br><span class="line">            key, value,</span><br><span class="line">            <span class="title function_ invoke__">type_of</span>(&amp;key),      <span class="comment">// &amp;&amp;str</span></span><br><span class="line">            <span class="title function_ invoke__">type_of</span>(&amp;value)     <span class="comment">// &amp;i32</span></span><br><span class="line">        );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-迭代器"><a href="#2-迭代器" class="headerlink" title="2. 迭代器"></a>2. 迭代器</h2><p>Rust 的迭代器是实现了 <code>Iterator</code> trait 的对象。迭代器不仅仅是一个”遍历工具”，还能惰性处理数据流，即<strong>惰性求值（lazy evaluation）</strong>。</p>
<h3 id="迭代器的特点"><a href="#迭代器的特点" class="headerlink" title="迭代器的特点"></a>迭代器的特点</h3><p>多数方法不会立即执行，只是返回一个新的迭代器。直到调用 <code>.collect()</code>、<code>.for_each()</code> 等最终消费方法时，才计算。</p>
<h3 id="Iterator-trait"><a href="#Iterator-trait" class="headerlink" title="Iterator trait"></a>Iterator trait</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">trait</span> <span class="title class_">Iterator</span> &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Item</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">next</span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;<span class="keyword">Self</span>::Item&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 其他很多默认实现的方法（map, filter, fold 等）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="适配器（Adapter）"><a href="#适配器（Adapter）" class="headerlink" title="适配器（Adapter）"></a>适配器（Adapter）</h3><p>适配器是返回新迭代器的方法，用于转换、筛选、组合原迭代器。多个适配器可组成”链式”，即通过把多个迭代器适配器组合在一起形成流水线式的数据处理流程。</p>
<h4 id="常见适配器"><a href="#常见适配器" class="headerlink" title="常见适配器"></a>常见适配器</h4><table>
<thead>
<tr>
<th>适配器</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>map(f)</code></td>
<td>对元素应用 f，转换元素类型</td>
</tr>
<tr>
<td><code>filter(f)</code></td>
<td>保留满足条件 f 的元素，返回 bool</td>
</tr>
<tr>
<td><code>flat_map(f)</code></td>
<td>对元素应用 f，扁平化结果</td>
</tr>
<tr>
<td><code>rev()</code></td>
<td>反向遍历（需迭代器实现 DoubleEndedIterator）</td>
</tr>
<tr>
<td><code>skip(n)</code></td>
<td>跳过前 n 个元素</td>
</tr>
<tr>
<td><code>take(n)</code></td>
<td>取前 n 个元素，之后终止迭代</td>
</tr>
<tr>
<td><code>chain(iter2)</code></td>
<td>拼接两个迭代器</td>
</tr>
<tr>
<td><code>enumerate()</code></td>
<td>同时获取元素的索引和值，返回一个元组 (usize, T)</td>
</tr>
</tbody></table>
<h4 id="适配器链式处理示例"><a href="#适配器链式处理示例" class="headerlink" title="适配器链式处理示例"></a>适配器链式处理示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 1. 拆分单词为字符 → 2. 筛选小写字母 → 3. 转换为大写 → 4. 收集为 Vec&lt;char&gt;</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">words</span> = <span class="built_in">vec!</span>[<span class="string">&quot;Hello&quot;</span>, <span class="string">&quot;World&quot;</span>, <span class="string">&quot;Rust&quot;</span>];</span><br><span class="line"><span class="keyword">let</span> <span class="variable">uppercase_chars</span>: <span class="type">Vec</span>&lt;<span class="type">char</span>&gt; = words</span><br><span class="line">    .<span class="title function_ invoke__">iter</span>()</span><br><span class="line">    .<span class="title function_ invoke__">flat_map</span>(|s| s.<span class="title function_ invoke__">chars</span>())              <span class="comment">// 扁平化：[&#x27;H&#x27;,&#x27;e&#x27;,&#x27;l&#x27;,&#x27;l&#x27;,&#x27;o&#x27;, ...]</span></span><br><span class="line">    .<span class="title function_ invoke__">filter</span>(|&amp;c| c.<span class="title function_ invoke__">is_ascii_lowercase</span>())  <span class="comment">// 筛选小写字母</span></span><br><span class="line">    .<span class="title function_ invoke__">map</span>(|c| c.<span class="title function_ invoke__">to_ascii_uppercase</span>())      <span class="comment">// 转换为大写</span></span><br><span class="line">    .<span class="title function_ invoke__">collect</span>();                           <span class="comment">// 消耗型方法：触发惰性求值</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, uppercase_chars);</span><br><span class="line"><span class="comment">// 输出: [&#x27;E&#x27;, &#x27;L&#x27;, &#x27;L&#x27;, &#x27;O&#x27;, &#x27;O&#x27;, &#x27;R&#x27;, &#x27;L&#x27;, &#x27;D&#x27;, &#x27;U&#x27;, &#x27;S&#x27;, &#x27;T&#x27;]</span></span><br></pre></td></tr></table></figure>

<h3 id="消耗性方法（Consuming-Adapter）"><a href="#消耗性方法（Consuming-Adapter）" class="headerlink" title="消耗性方法（Consuming Adapter）"></a>消耗性方法（Consuming Adapter）</h3><p>消耗型方法会遍历迭代器、消耗元素，并返回最终结果（触发惰性求值）。</p>
<h4 id="常见消耗性方法"><a href="#常见消耗性方法" class="headerlink" title="常见消耗性方法"></a>常见消耗性方法</h4><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>collect&lt;T&gt;()</code></td>
<td>收集元素为集合 T</td>
</tr>
<tr>
<td><code>count()</code></td>
<td>返回元素个数</td>
</tr>
<tr>
<td><code>fold(init, f)</code></td>
<td>折叠元素（聚合计算），init 为初始值</td>
</tr>
<tr>
<td><code>any(f)</code></td>
<td>存在元素满足 f，则返回 true</td>
</tr>
<tr>
<td><code>all(f)</code></td>
<td>所有元素满足 f，则返回 true</td>
</tr>
<tr>
<td><code>find(f)</code></td>
<td>查找第一个满足 f 的元素，返回 Option&lt;Item&gt;</td>
</tr>
<tr>
<td><code>for_each(f)</code></td>
<td>对每个元素执行 f，等价于 for 循环</td>
</tr>
</tbody></table>
<h4 id="消耗性方法示例"><a href="#消耗性方法示例" class="headerlink" title="消耗性方法示例"></a>消耗性方法示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">nums</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>];</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 求积（fold 折叠）</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">product</span> = nums.<span class="title function_ invoke__">iter</span>().<span class="title function_ invoke__">fold</span>(<span class="number">1</span>, |init, &amp;x| init * x);</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;阶乘：&#123;&#125;&quot;</span>, product); <span class="comment">// 输出 120</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 查找第一个偶数</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">first_even</span> = nums.<span class="title function_ invoke__">iter</span>().<span class="title function_ invoke__">find</span>(|&amp;&amp;x| x % <span class="number">2</span> == <span class="number">0</span>);</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;第一个偶数：&#123;:?&#125;&quot;</span>, first_even); <span class="comment">// 输出 Some(&amp;2)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 判断是否存在大于 5 的元素</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">has_over_five</span> = nums.<span class="title function_ invoke__">iter</span>().<span class="title function_ invoke__">any</span>(|&amp;x| x &gt; <span class="number">5</span>);</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;是否存在大于5的元素：&#123;&#125;&quot;</span>, has_over_five); <span class="comment">// 输出 false</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 4. 判断所有元素都大于 0</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">all_positive</span> = nums.<span class="title function_ invoke__">iter</span>().<span class="title function_ invoke__">all</span>(|&amp;x| x &gt; <span class="number">0</span>);</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;所有元素都为正数：&#123;&#125;&quot;</span>, all_positive); <span class="comment">// 输出 true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 5. 收集为向量</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">doubled</span>: <span class="type">Vec</span>&lt;<span class="type">i32</span>&gt; = nums.<span class="title function_ invoke__">iter</span>().<span class="title function_ invoke__">map</span>(|&amp;x| x * <span class="number">2</span>).<span class="title function_ invoke__">collect</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;翻倍后：&#123;:?&#125;&quot;</span>, doubled); <span class="comment">// 输出 [2, 4, 6, 8, 10]</span></span><br></pre></td></tr></table></figure>

<h3 id="无限迭代器"><a href="#无限迭代器" class="headerlink" title="无限迭代器"></a>无限迭代器</h3><p>无限迭代器（如 <code>iter::repeat</code>、<code>iter::cycle</code>），需通过 <code>take(n)</code> 等方法限制迭代次数，否则会陷入死循环。</p>
<h4 id="常见无限迭代器"><a href="#常见无限迭代器" class="headerlink" title="常见无限迭代器"></a>常见无限迭代器</h4><ul>
<li>**<code>iter::repeat(val)</code>**：无限重复 <code>val</code></li>
<li>**<code>iter::cycle()</code>**：循环迭代原迭代器（原迭代器需是 <code>Clone</code>）</li>
<li>**<code>(0..).step_by(n)</code>**：无限递增序列（步长 <code>n</code>）</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::iter;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 无限重复，取前 3 个</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">repeated</span>: <span class="type">Vec</span>&lt;<span class="type">i32</span>&gt; = iter::<span class="title function_ invoke__">repeat</span>(<span class="number">5</span>).<span class="title function_ invoke__">take</span>(<span class="number">3</span>).<span class="title function_ invoke__">collect</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, repeated); <span class="comment">// [5, 5, 5]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 循环迭代</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">v</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"><span class="keyword">let</span> <span class="variable">cycled</span>: <span class="type">Vec</span>&lt;<span class="type">i32</span>&gt; = v.<span class="title function_ invoke__">iter</span>()</span><br><span class="line">    .<span class="title function_ invoke__">map</span>(|&amp;x| x)</span><br><span class="line">    .<span class="title function_ invoke__">cycle</span>()</span><br><span class="line">    .<span class="title function_ invoke__">take</span>(<span class="number">7</span>)</span><br><span class="line">    .<span class="title function_ invoke__">collect</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, cycled); <span class="comment">// [1, 2, 3, 1, 2, 3, 1]</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 无限递增序列</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">increasing</span>: <span class="type">Vec</span>&lt;<span class="type">i32</span>&gt; = (<span class="number">0</span>..)</span><br><span class="line">    .<span class="title function_ invoke__">step_by</span>(<span class="number">2</span>)</span><br><span class="line">    .<span class="title function_ invoke__">take</span>(<span class="number">5</span>)</span><br><span class="line">    .<span class="title function_ invoke__">collect</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;:?&#125;&quot;</span>, increasing); <span class="comment">// [0, 2, 4, 6, 8]</span></span><br></pre></td></tr></table></figure>

<h3 id="自定义迭代器"><a href="#自定义迭代器" class="headerlink" title="自定义迭代器"></a>自定义迭代器</h3><p>要创建自定义迭代器，只需实现 <code>Iterator</code> trait 的 <code>next()</code> 方法（其他方法为默认实现）。</p>
<p><code>Iterator</code> trait 继承了 <code>IntoIterator</code> trait 的默认实现（<code>IntoIterator::into_iter()</code> 会直接返回迭代器本身（<code>Self</code>））。因此，只要一个类型实现了 <code>Iterator::next()</code>，就自动支持 <code>for</code> 循环遍历。</p>
<h4 id="计数迭代器示例"><a href="#计数迭代器示例" class="headerlink" title="计数迭代器示例"></a>计数迭代器示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">struct</span> <span class="title class_">Counter</span> &#123;</span><br><span class="line">    count: <span class="type">u32</span>,</span><br><span class="line">    upper: <span class="type">u32</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Counter</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">new</span>(upper: <span class="type">u32</span>) <span class="punctuation">-&gt;</span> Counter &#123;</span><br><span class="line">        Counter &#123; count: <span class="number">0</span>, upper &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Iterator</span> <span class="keyword">for</span> <span class="title class_">Counter</span> &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Item</span> = <span class="type">u32</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">next</span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;<span class="keyword">Self</span>::Item&gt; &#123;</span><br><span class="line">        <span class="keyword">self</span>.count += <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">self</span>.count &lt;= <span class="keyword">self</span>.upper &#123;</span><br><span class="line">            <span class="title function_ invoke__">Some</span>(<span class="keyword">self</span>.count)</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="literal">None</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="comment">// for 循环自动调用 into_iter()，返回 Counter 本身</span></span><br><span class="line">    <span class="keyword">for</span> <span class="variable">num</span> <span class="keyword">in</span> Counter::<span class="title function_ invoke__">new</span>(<span class="number">5</span>) &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, num); <span class="comment">// 输出 1, 2, 3, 4, 5</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 也可以使用迭代器方法</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">sum</span>: <span class="type">u32</span> = Counter::<span class="title function_ invoke__">new</span>(<span class="number">10</span>)</span><br><span class="line">        .<span class="title function_ invoke__">filter</span>(|x| x % <span class="number">2</span> == <span class="number">0</span>)</span><br><span class="line">        .<span class="title function_ invoke__">map</span>(|x| x * x)</span><br><span class="line">        .<span class="title function_ invoke__">sum</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Sum: &#123;&#125;&quot;</span>, sum); <span class="comment">// 输出 2+4+6+...+10 的平方和</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="自定义容器"><a href="#自定义容器" class="headerlink" title="自定义容器"></a>自定义容器</h3><p>容器要被 <code>for</code> 循环遍历，需让容器（或其引用）实现 <code>IntoIterator</code> trait：</p>
<ul>
<li><code>iter()</code> -&gt; 返回不可变引用的迭代器</li>
<li><code>iter_mut()</code> -&gt; 返回可变引用的迭代器</li>
<li><code>into_iter()</code> -&gt; 转移所有权迭代器</li>
</ul>
<h4 id="IntoIterator-实现示例"><a href="#IntoIterator-实现示例" class="headerlink" title="IntoIterator 实现示例"></a>IntoIterator 实现示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">MyContainer</span>&lt;T&gt; &#123;</span><br><span class="line">    items: <span class="type">Vec</span>&lt;T&gt;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现 1: for x in my_container（消耗所有权）</span></span><br><span class="line"><span class="keyword">impl</span>&lt;T&gt; <span class="built_in">IntoIterator</span> <span class="keyword">for</span> <span class="title class_">MyContainer</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Item</span> = T;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">IntoIter</span> = std::vec::IntoIter&lt;T&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">into_iter</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span>::IntoIter &#123;</span><br><span class="line">        <span class="keyword">self</span>.items.<span class="title function_ invoke__">into_iter</span>()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现 2: for x in &amp;my_container（不可变引用）</span></span><br><span class="line"><span class="keyword">impl</span>&lt;<span class="symbol">&#x27;a</span>, T&gt; <span class="built_in">IntoIterator</span> <span class="keyword">for</span> &amp;<span class="symbol">&#x27;a</span> MyContainer&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Item</span> = &amp;<span class="symbol">&#x27;a</span> T;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">IntoIter</span> = std::slice::Iter&lt;<span class="symbol">&#x27;a</span>, T&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">into_iter</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span>::IntoIter &#123;</span><br><span class="line">        <span class="keyword">self</span>.items.<span class="title function_ invoke__">iter</span>()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现 3: for x in &amp;mut my_container（可变引用）</span></span><br><span class="line"><span class="keyword">impl</span>&lt;<span class="symbol">&#x27;a</span>, T&gt; <span class="built_in">IntoIterator</span> <span class="keyword">for</span> &amp;<span class="symbol">&#x27;a</span> <span class="keyword">mut</span> MyContainer&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Item</span> = &amp;<span class="symbol">&#x27;a</span> <span class="keyword">mut</span> T;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">IntoIter</span> = std::slice::IterMut&lt;<span class="symbol">&#x27;a</span>, T&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">into_iter</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span>::IntoIter &#123;</span><br><span class="line">        <span class="keyword">self</span>.items.<span class="title function_ invoke__">iter_mut</span>()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">container</span> = MyContainer &#123;</span><br><span class="line">        items: <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>],</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 所有权转移</span></span><br><span class="line">    <span class="keyword">for</span> <span class="variable">x</span> <span class="keyword">in</span> container &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, x);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 下面无法执行，因为 container 已被消耗</span></span><br><span class="line">    <span class="comment">// for x in &amp;container &#123; ... &#125;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="环形缓冲区示例（高级）"><a href="#环形缓冲区示例（高级）" class="headerlink" title="环形缓冲区示例（高级）"></a>环形缓冲区示例（高级）</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::mem::MaybeUninit;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">RingBuffer</span>&lt;T, <span class="keyword">const</span> N: <span class="type">usize</span>&gt; &#123;</span><br><span class="line">    data: [MaybeUninit&lt;T&gt;; N],</span><br><span class="line">    head: <span class="type">usize</span>,</span><br><span class="line">    len: <span class="type">usize</span>,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span>&lt;T, <span class="keyword">const</span> N: <span class="type">usize</span>&gt; RingBuffer&lt;T, N&gt; &#123;</span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">new</span>() <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">        <span class="keyword">Self</span> &#123;</span><br><span class="line">            data: <span class="keyword">unsafe</span> &#123; MaybeUninit::<span class="title function_ invoke__">uninit</span>().<span class="title function_ invoke__">assume_init</span>() &#125;,</span><br><span class="line">            head: <span class="number">0</span>,</span><br><span class="line">            len: <span class="number">0</span>,</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">push</span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>, value: T) &#123;</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">self</span>.len &lt; N &#123;</span><br><span class="line">            <span class="keyword">let</span> <span class="variable">tail</span> = (<span class="keyword">self</span>.head + <span class="keyword">self</span>.len) % N;</span><br><span class="line">            <span class="keyword">self</span>.data[tail] = MaybeUninit::<span class="title function_ invoke__">new</span>(value);</span><br><span class="line">            <span class="keyword">self</span>.len += <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">pop</span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;T&gt; &#123;</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">self</span>.len == <span class="number">0</span> &#123;</span><br><span class="line">            <span class="literal">None</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">let</span> <span class="variable">val</span> = std::mem::<span class="title function_ invoke__">replace</span>(</span><br><span class="line">                &amp;<span class="keyword">mut</span> <span class="keyword">self</span>.data[<span class="keyword">self</span>.head],</span><br><span class="line">                MaybeUninit::<span class="title function_ invoke__">uninit</span>(),</span><br><span class="line">            );</span><br><span class="line">            <span class="keyword">self</span>.head = (<span class="keyword">self</span>.head + <span class="number">1</span>) % N;</span><br><span class="line">            <span class="keyword">self</span>.len -= <span class="number">1</span>;</span><br><span class="line">            <span class="title function_ invoke__">Some</span>(<span class="keyword">unsafe</span> &#123; val.<span class="title function_ invoke__">assume_init</span>() &#125;)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 为 RingBuffer 实现三种迭代方式</span></span><br><span class="line"><span class="keyword">impl</span>&lt;T, <span class="keyword">const</span> N: <span class="type">usize</span>&gt; <span class="built_in">IntoIterator</span> <span class="keyword">for</span> <span class="title class_">RingBuffer</span>&lt;T, N&gt; &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Item</span> = T;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">IntoIter</span> = RingBufferIntoIter&lt;T, N&gt;;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">into_iter</span>(<span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span>::IntoIter &#123;</span><br><span class="line">        RingBufferIntoIter &#123;</span><br><span class="line">            buffer: <span class="keyword">self</span>,</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">struct</span> <span class="title class_">RingBufferIntoIter</span>&lt;T, <span class="keyword">const</span> N: <span class="type">usize</span>&gt; &#123;</span><br><span class="line">    buffer: RingBuffer&lt;T, N&gt;,</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span>&lt;T, <span class="keyword">const</span> N: <span class="type">usize</span>&gt; <span class="built_in">Iterator</span> <span class="keyword">for</span> <span class="title class_">RingBufferIntoIter</span>&lt;T, N&gt; &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Item</span> = T;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">next</span>(&amp;<span class="keyword">mut</span> <span class="keyword">self</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;<span class="keyword">Self</span>::Item&gt; &#123;</span><br><span class="line">        <span class="keyword">self</span>.buffer.<span class="title function_ invoke__">pop</span>()</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><h3 id="for-循环"><a href="#for-循环" class="headerlink" title="for 循环"></a>for 循环</h3><ul>
<li><code>for</code> 循环是 <code>IntoIterator</code> trait 的语法糖</li>
<li>三种核心模式：所有权转移（<code>into_iter()</code>）、不可变借用（<code>iter()</code>）、可变借用（<code>iter_mut()</code>）</li>
<li>支持 Range、键值对、索引+值 等多种遍历方式</li>
</ul>
<h3 id="迭代器"><a href="#迭代器" class="headerlink" title="迭代器"></a>迭代器</h3><ul>
<li><strong>惰性求值</strong>：适配器不会立即执行，只有消耗型方法才会触发计算</li>
<li><strong>适配器链式处理</strong>：通过组合多个适配器实现复杂的数据处理流程</li>
<li><strong>自定义迭代器</strong>：实现 <code>Iterator</code> trait 的 <code>next()</code> 方法即可</li>
<li><strong>自定义容器</strong>：实现 <code>IntoIterator</code> trait 支持 <code>for</code> 循环遍历</li>
<li><strong>无限迭代器</strong>：需要通过 <code>take()</code> 等方法限制迭代次数</li>
</ul>
<p>迭代器是 Rust 处理集合和序列的核心工具，具有高效、安全、表达力强的特点。</p>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust多线程编程之Thread与Channel</title>
    <url>/2025/04/30/Rust%E5%A4%9A%E7%BA%BF%E7%A8%8B%E7%BC%96%E7%A8%8B%E4%B9%8BThread%E4%B8%8EChannel/</url>
    <content><![CDATA[<h2 id="Rust-多线程编程之-Thread-与-Channel"><a href="#Rust-多线程编程之-Thread-与-Channel" class="headerlink" title="Rust 多线程编程之 Thread 与 Channel"></a>Rust 多线程编程之 Thread 与 Channel</h2><p>Rust 利用所有权（Ownership）系统在编译时保证线程安全，从根本上杜绝数据竞争（data race）。本文整理并补充了原文关于 <code>std::thread</code> 与 <code>std::sync::mpsc</code>（Channel） 的用法、错误处理、以及多消费者 &#x2F; 广播等实战模式与示例。</p>
<span id="more"></span>

<h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul>
<li>线程模型与所有权</li>
<li><code>std::thread</code>：创建与管理线程</li>
<li>线程间通信：<code>std::sync::mpsc</code></li>
<li>错误处理（<code>send</code> &#x2F; <code>recv</code>）</li>
<li>多消费者与广播模式</li>
<li>使用 <code>crossbeam-channel</code> 的进阶示例</li>
<li>小结与参考</li>
</ul>
<hr>
<h2 id="线程模型与所有权"><a href="#线程模型与所有权" class="headerlink" title="线程模型与所有权"></a>线程模型与所有权</h2><p>Rust 标准库采用 1:1 线程模型（每个 Rust 线程对应一个操作系统线程）。通过所有权与类型系统，Rust 在编译期就能防止数据竞争，减少运行时同步错误。</p>
<p>要注意：如果在新线程中使用主线程的变量，必须将这些变量的所有权移动（<code>move</code>）到闭包中，或使用线程安全的共享类型（例如 <code>Arc&lt;Mutex&lt;T&gt;&gt;</code>）。</p>
<h2 id="std-thread：创建与管理线程"><a href="#std-thread：创建与管理线程" class="headerlink" title="std::thread：创建与管理线程"></a><code>std::thread</code>：创建与管理线程</h2><p>最常用的创建线程方式是 <code>thread::spawn</code>，它接收一个闭包并返回 <code>JoinHandle&lt;T&gt;</code>，可以用来等待线程结束并获取返回值。</p>
<p>示例：简单的 <code>spawn</code> 与 <code>join</code></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"><span class="keyword">use</span> std::time::Duration;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">	<span class="keyword">let</span> <span class="variable">v</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 必须使用 move 将 v 的所有权转移到新线程</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">handle</span> = thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;向量 v: &#123;:?&#125;&quot;</span>, v);</span><br><span class="line">        thread::<span class="title function_ invoke__">sleep</span>(Duration::<span class="title function_ invoke__">from_millis</span>(<span class="number">10</span>));</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 等待线程结束</span></span><br><span class="line">    handle.<span class="title function_ invoke__">join</span>().<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用 <code>thread::Builder</code> 可以设置线程名、栈大小等：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">builder</span> = thread::Builder::<span class="title function_ invoke__">new</span>()</span><br><span class="line">    .<span class="title function_ invoke__">name</span>(<span class="string">&quot;channel-test&quot;</span>.<span class="title function_ invoke__">to_string</span>())</span><br><span class="line">    .<span class="title function_ invoke__">stack_size</span>(<span class="number">4</span> * <span class="number">1024</span> * <span class="number">1024</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">handle</span> = builder.<span class="title function_ invoke__">spawn</span>(|| &#123;</span><br><span class="line">    <span class="comment">// 线程体</span></span><br><span class="line">&#125;).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line"></span><br><span class="line">handle.<span class="title function_ invoke__">join</span>().<span class="title function_ invoke__">unwrap</span>();</span><br></pre></td></tr></table></figure>

<p>线程可以被 <code>join</code>（等待）或“detach”（即不 join，让操作系统接管）。对需要保证执行完成的工作使用 <code>join</code>。</p>
<h2 id="线程间通信：std-sync-mpsc"><a href="#线程间通信：std-sync-mpsc" class="headerlink" title="线程间通信：std::sync::mpsc"></a>线程间通信：<code>std::sync::mpsc</code></h2><p>Rust 标准库提供基于消息传递的通道（Channel）：<code>std::sync::mpsc</code>（multiple producer, single consumer）。通道传递的是值的所有权，因此天然避免了数据竞争问题。</p>
<ul>
<li><code>Sender&lt;T&gt;</code>：发送端，可以 <code>clone()</code> 支持多生产者。</li>
<li><code>Receiver&lt;T&gt;</code>：接收端，默认不可克隆（单消费者）。</li>
</ul>
<p>创建通道：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::mpsc;</span><br><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> (tx, rx) = mpsc::<span class="title function_ invoke__">channel</span>(); <span class="comment">// 无界通道</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">tx_clone</span> = tx.<span class="title function_ invoke__">clone</span>();</span><br><span class="line"></span><br><span class="line">thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">    tx.<span class="title function_ invoke__">send</span>(<span class="string">&quot;hello&quot;</span>).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> <span class="variable">received</span> <span class="keyword">in</span> rx &#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;接收到: &#123;&#125;&quot;</span>, received);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>mpsc::sync_channel(n)</code> 可以创建带缓存容量的通道（阻塞发送者直到有空间）。当容量为 0 时，发送方会严格等待接收方（同步模式）。</p>
<h3 id="发送-x2F-接收的返回值与错误处理"><a href="#发送-x2F-接收的返回值与错误处理" class="headerlink" title="发送&#x2F;接收的返回值与错误处理"></a>发送&#x2F;接收的返回值与错误处理</h3><p><code>send</code> 与 <code>recv</code> 都会返回 <code>Result</code>：</p>
<ul>
<li><code>send</code> 返回 <code>Result&lt;(), SendError&lt;T&gt;&gt;</code>：当所有 <code>Receiver</code> 被丢弃时，发送会失败并返回错误；发送失败会把要发送的值带回 <code>SendError&lt;T&gt;</code>。</li>
<li><code>recv</code> 返回 <code>Result&lt;T, RecvError&gt;</code>：当所有 <code>Sender</code> 已被丢弃且通道中无更多消息时，<code>recv</code> 返回错误。</li>
</ul>
<p>示例：处理 <code>send</code>&#x2F;<code>recv</code> 错误</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::mpsc;</span><br><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> (tx, rx) = mpsc::channel::&lt;<span class="type">i32</span>&gt;();</span><br><span class="line"></span><br><span class="line">thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">    <span class="keyword">if</span> tx.<span class="title function_ invoke__">send</span>(<span class="number">1</span>).<span class="title function_ invoke__">is_err</span>() &#123;</span><br><span class="line">        <span class="built_in">eprintln!</span>(<span class="string">&quot;发送失败：接收者已关闭&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">match</span> rx.<span class="title function_ invoke__">recv</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(v) =&gt; <span class="built_in">println!</span>(<span class="string">&quot;收到: &#123;&#125;&quot;</span>, v),</span><br><span class="line">    <span class="title function_ invoke__">Err</span>(_) =&gt; <span class="built_in">println!</span>(<span class="string">&quot;通道已关闭，无更多数据&quot;</span>),</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="多消费者（Multiple-Consumers）与选择方案"><a href="#多消费者（Multiple-Consumers）与选择方案" class="headerlink" title="多消费者（Multiple Consumers）与选择方案"></a>多消费者（Multiple Consumers）与选择方案</h2><p>标准库的 <code>mpsc</code> 仅提供单消费者。要实现多消费者或更高性能的并发消费模型，可以选择：</p>
<ol>
<li>使用 <code>crossbeam-channel</code>（推荐，性能好、功能丰富，支持 MPMC&#x2F;广播等）。</li>
<li>用 <code>Arc&lt;Mutex&lt;Receiver&gt;&gt;</code> 包裹 <code>Receiver</code>（不推荐，锁带来开销且降低并发性能）。</li>
</ol>
<p>示例：使用 <code>crossbeam_channel::unbounded</code> 实现多个消费者抢占消息（竞争消费）：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"><span class="keyword">use</span> crossbeam_channel::unbounded;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">run_mpmc</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> (s, r) = unbounded::&lt;<span class="type">String</span>&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> <span class="variable">i</span> <span class="keyword">in</span> <span class="number">0</span>..<span class="number">3</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">r_clone</span> = r.<span class="title function_ invoke__">clone</span>();</span><br><span class="line">        thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">            <span class="keyword">for</span> <span class="variable">msg</span> <span class="keyword">in</span> r_clone.<span class="title function_ invoke__">iter</span>() &#123;</span><br><span class="line">                <span class="built_in">println!</span>(<span class="string">&quot;消费者 &#123;&#125; 收到: &#123;&#125;&quot;</span>, i, msg);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> <span class="variable">i</span> <span class="keyword">in</span> <span class="number">0</span>..<span class="number">5</span> &#123;</span><br><span class="line">        s.<span class="title function_ invoke__">send</span>(<span class="built_in">format!</span>(<span class="string">&quot;消息 &#123;&#125;&quot;</span>, i)).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 发送端 drop 后，消费者会结束迭代</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>crossbeam-channel</code> 的特点是高性能并且支持多种模式（无界&#x2F;有界、超时、选择等）。</p>
<h2 id="广播模式"><a href="#广播模式" class="headerlink" title="广播模式"></a>广播模式</h2><p>如果需要广播（同一条消息被多个消费者都收到），可以选择：</p>
<ul>
<li>异步环境下：<code>tokio::sync::broadcast</code>。</li>
<li>同步（阻塞）场景：使用 <code>bus</code>、<code>postage</code> 等 crate（<code>bus</code> 示例）：</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"><span class="keyword">use</span> bus::Bus;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">run_bus</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">bus</span> = Bus::<span class="title function_ invoke__">new</span>(<span class="number">10</span>); <span class="comment">// 容量 10</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">rx1</span> = bus.<span class="title function_ invoke__">add_rx</span>();</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">rx2</span> = bus.<span class="title function_ invoke__">add_rx</span>();</span><br><span class="line"></span><br><span class="line">    thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;消费者 1 收到: &#123;&#125;&quot;</span>, rx1.<span class="title function_ invoke__">recv</span>().<span class="title function_ invoke__">unwrap</span>());</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;消费者 2 收到: &#123;&#125;&quot;</span>, rx2.<span class="title function_ invoke__">recv</span>().<span class="title function_ invoke__">unwrap</span>());</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    bus.<span class="title function_ invoke__">broadcast</span>(<span class="string">&quot;广播消息&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意：不同广播实现的语义不同（一些库是有容量限制并可能丢弃旧消息）。选择时请阅读对应库文档。</p>
<h2 id="线程间通信的设计要点与最佳实践"><a href="#线程间通信的设计要点与最佳实践" class="headerlink" title="线程间通信的设计要点与最佳实践"></a>线程间通信的设计要点与最佳实践</h2><ul>
<li>优先使用消息传递（ownership transfer）而非共享可变状态。</li>
<li>对于需要多个消费者的高吞吐场景，优先考虑 <code>crossbeam-channel</code>。</li>
<li>避免用 <code>Arc&lt;Mutex&lt;Receiver&gt;&gt;</code> 作为首选方案，因为锁可能成为瓶颈。</li>
<li>使用 <code>sync_channel</code> 实现生产者速率限制（backpressure）。</li>
</ul>
<h2 id="常见示例汇总"><a href="#常见示例汇总" class="headerlink" title="常见示例汇总"></a>常见示例汇总</h2><p>多生产者、单消费者（标准库）：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::mpsc;</span><br><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">run_mpsc_clone</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> (tx, rx) = mpsc::<span class="title function_ invoke__">channel</span>();</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">tx1</span> = tx.<span class="title function_ invoke__">clone</span>();</span><br><span class="line"></span><br><span class="line">    thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">vals</span> = <span class="built_in">vec!</span>[<span class="string">&quot;消息1&quot;</span>, <span class="string">&quot;消息2&quot;</span>, <span class="string">&quot;消息3&quot;</span>];</span><br><span class="line">        <span class="keyword">for</span> <span class="variable">val</span> <span class="keyword">in</span> vals &#123;</span><br><span class="line">            tx.<span class="title function_ invoke__">send</span>(val).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">            thread::<span class="title function_ invoke__">sleep</span>(std::time::Duration::<span class="title function_ invoke__">from_millis</span>(<span class="number">10</span>));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="variable">vals</span> = <span class="built_in">vec!</span>[<span class="string">&quot;消息A&quot;</span>, <span class="string">&quot;消息B&quot;</span>, <span class="string">&quot;消息C&quot;</span>];</span><br><span class="line">        <span class="keyword">for</span> <span class="variable">val</span> <span class="keyword">in</span> vals &#123;</span><br><span class="line">            tx1.<span class="title function_ invoke__">send</span>(val).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> <span class="variable">received</span> <span class="keyword">in</span> rx &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;主线程收到: &#123;&#125;&quot;</span>, received);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用 <code>sync_channel</code> 的同步发送示例（实现 backpressure）：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::mpsc;</span><br><span class="line"><span class="keyword">use</span> std::thread;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">run_sync_channel</span>() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">let</span> (tx, rx) = mpsc::<span class="title function_ invoke__">sync_channel</span>(<span class="number">2</span>); <span class="comment">// 容量 2</span></span><br><span class="line"></span><br><span class="line">    thread::<span class="title function_ invoke__">spawn</span>(<span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="keyword">for</span> <span class="variable">i</span> <span class="keyword">in</span> <span class="number">0</span>..<span class="number">5</span> &#123;</span><br><span class="line">            tx.<span class="title function_ invoke__">send</span>(i).<span class="title function_ invoke__">unwrap</span>(); <span class="comment">// 当队列满时会阻塞</span></span><br><span class="line">            <span class="built_in">println!</span>(<span class="string">&quot;发送: &#123;&#125;&quot;</span>, i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> <span class="variable">v</span> <span class="keyword">in</span> rx &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;接收: &#123;&#125;&quot;</span>, v);</span><br><span class="line">        thread::<span class="title function_ invoke__">sleep</span>(std::time::Duration::<span class="title function_ invoke__">from_millis</span>(<span class="number">50</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="小结与参考"><a href="#小结与参考" class="headerlink" title="小结与参考"></a>小结与参考</h2><p>Rust 的线程与通道模型以所有权为核心，消息传递可以在不加锁的情况下安全地移动数据。对于性能和扩展性要求较高的场景，<code>crossbeam-channel</code> 是比标准库更好的选择；需要广播时选择合适的广播库。</p>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中错误处理机制</title>
    <url>/2023/08/24/Rust%E4%B8%AD%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<p>Rust 的错误处理是通过 <code>Result</code> 类型和 <code>panic!</code> 宏两个层次来完成的：</p>
<span id="more"></span>

<table>
<thead>
<tr>
<th>错误类型</th>
<th>特征</th>
<th>处理方式</th>
</tr>
</thead>
<tbody><tr>
<td>可恢复错误</td>
<td>程序逻辑上可以预期并处理，比如文件不存在、网络超时</td>
<td>使用 <code>Result&amp;lt;T, E&amp;gt;</code></td>
</tr>
<tr>
<td>不可恢复错误</td>
<td>程序出现 bug 或严重问题，无法继续运行</td>
<td>使用 <code>panic!</code> 宏</td>
</tr>
<tr>
<td>可选值缺失</td>
<td>一个值可能存在 <code>Some(T)</code> 或不存在 <code>None</code>，避免空指针问题</td>
<td>使用 <code>Option&amp;lt;T&amp;gt;</code></td>
</tr>
</tbody></table>
<h2 id="可恢复错误（Result-lt-T-E-gt-）"><a href="#可恢复错误（Result-lt-T-E-gt-）" class="headerlink" title="可恢复错误（Result&lt;T, E&gt;）"></a>可恢复错误（Result&lt;T, E&gt;）</h2><p><code>Result</code> 是一个泛型枚举，强制调用者处理成功和失败两种情况（是零开销的，由编译器静态保证必须处理）：</p>
<ul>
<li><code>T</code>：成功时返回的值类型；</li>
<li><code>E</code>：错误时返回的错误类型。</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fs::File;</span><br><span class="line"><span class="keyword">use</span> std::io::Error;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">open_file</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;File, Error&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">file</span> = File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;hello.txt&quot;</span>);</span><br><span class="line">    <span class="keyword">match</span> file &#123;</span><br><span class="line">        <span class="title function_ invoke__">Ok</span>(f) =&gt; <span class="title function_ invoke__">Ok</span>(f),</span><br><span class="line">        <span class="title function_ invoke__">Err</span>(e) =&gt; <span class="title function_ invoke__">Err</span>(e),</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="常用方法"><a href="#常用方法" class="headerlink" title="常用方法"></a>常用方法</h3><h4 id="unwrap-和-expect"><a href="#unwrap-和-expect" class="headerlink" title="unwrap 和 expect"></a><code>unwrap</code> 和 <code>expect</code></h4><p>快速处理错误，失败时会 panic：</p>
<ul>
<li><code>unwrap()</code>：若 <code>Result</code> 为 <code>Ok(v)</code> 则返回 <code>v</code>；若为 <code>Err(e)</code> 则触发 <code>panic!</code>（适合 “理论上不可能失败” 的场景）。</li>
<li><code>expect(msg)</code>：功能与 <code>unwrap</code> 类似，但可自定义 <code>panic!</code> 的错误消息（更易调试）。</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fs::read_to_string;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="comment">// 若文件不存在，unwrap会 panic</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">content</span> = <span class="title function_ invoke__">read_to_string</span>(<span class="string">&quot;test.txt&quot;</span>).<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 自定义 panic 消息</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">content</span> = <span class="title function_ invoke__">read_to_string</span>(<span class="string">&quot;test.txt&quot;</span>).<span class="title function_ invoke__">expect</span>(<span class="string">&quot;读取 test.txt 失败&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="链式方法"><a href="#链式方法" class="headerlink" title="链式方法"></a>链式方法</h4><p><code>Result</code> 提供了大量链式方法，用于灵活地处理成功或失败分支：</p>
<table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>.is_ok()</code></td>
<td>是否是 <code>Ok</code></td>
</tr>
<tr>
<td><code>.is_err()</code></td>
<td>是否是 <code>Err</code></td>
</tr>
<tr>
<td><code>.ok()</code></td>
<td>取值，返回 <code>Option&amp;lt;T&amp;gt;</code></td>
</tr>
<tr>
<td><code>.err()</code></td>
<td>取错误，返回 <code>Option&amp;lt;E&amp;gt;</code></td>
</tr>
<tr>
<td><code>.unwrap()</code></td>
<td>成功取值 T，否则 panic</td>
</tr>
<tr>
<td><code>.expect(msg)</code></td>
<td>成功取值 T，否则 panic 并打印 msg</td>
</tr>
<tr>
<td><code>.unwrap_or(default)</code></td>
<td>失败则返回默认值</td>
</tr>
<tr>
<td><code>.unwrap_or_else(f)</code></td>
<td>失败时执行函数生成默认值</td>
</tr>
<tr>
<td><code>.map(f)</code></td>
<td>若是 Ok，应用函数 <code>F(T) -&amp;gt; U</code> 到内部值；否则返回原 Err</td>
</tr>
<tr>
<td><code>.map_err(f)</code></td>
<td>对 Err 执行 map</td>
</tr>
<tr>
<td><code>.and_then(f)</code></td>
<td>若是 Ok，应用函数 <code>F(T) -&amp;gt; Result&amp;lt;U, E&amp;gt;</code> 到内部值；否则返回原 Err</td>
</tr>
<tr>
<td><code>.or_else(f)</code></td>
<td>对 Err 执行 f</td>
</tr>
</tbody></table>
<h4 id="map-与-unwrap-示例"><a href="#map-与-unwrap-示例" class="headerlink" title="map 与 unwrap 示例"></a>map 与 unwrap 示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">parse_and_double</span>(vec: <span class="type">Vec</span>&lt;&amp;<span class="type">str</span>&gt;) <span class="punctuation">-&gt;</span> <span class="type">Vec</span>&lt;<span class="type">String</span>&gt; &#123;</span><br><span class="line">    vec.<span class="title function_ invoke__">into_iter</span>()</span><br><span class="line">        .<span class="title function_ invoke__">map</span>(|s| &#123;</span><br><span class="line">            s.parse::&lt;<span class="type">i32</span>&gt;()</span><br><span class="line">                .<span class="title function_ invoke__">map</span>(|n| (n * <span class="number">2</span>).<span class="title function_ invoke__">to_string</span>())</span><br><span class="line">                .<span class="title function_ invoke__">unwrap_or_else</span>(|_| s.<span class="title function_ invoke__">to_string</span>())</span><br><span class="line">        &#125;)</span><br><span class="line">        .<span class="title function_ invoke__">collect</span>()</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">test_parse_and_double</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">vec</span> = <span class="built_in">vec!</span>[<span class="string">&quot;1&quot;</span>, <span class="string">&quot;2&quot;</span>, <span class="string">&quot;three&quot;</span>, <span class="string">&quot;4&quot;</span>];</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">doubled</span> = <span class="title function_ invoke__">parse_and_double</span>(vec);</span><br><span class="line">    <span class="built_in">assert_eq!</span>(doubled, <span class="built_in">vec!</span>[<span class="string">&quot;2&quot;</span>, <span class="string">&quot;4&quot;</span>, <span class="string">&quot;three&quot;</span>, <span class="string">&quot;8&quot;</span>]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="错误传播（-运算符）"><a href="#错误传播（-运算符）" class="headerlink" title="错误传播（? 运算符）"></a>错误传播（<code>?</code> 运算符）</h3><p><code>?</code> 是 Rust 中最常用的错误传播工具：</p>
<ul>
<li>如果是 <code>Ok(t)</code>，提取 <code>t</code>；</li>
<li>如果是 <code>Err(e)</code>，提前返回当前函数，并将错误向上传递。</li>
</ul>
<h4 id="文件读写示例"><a href="#文件读写示例" class="headerlink" title="文件读写示例"></a>文件读写示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fs::File;</span><br><span class="line"><span class="keyword">use</span> std::io::&#123;<span class="keyword">self</span>, Read&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">read_username</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;<span class="type">String</span>, io::Error&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;username.txt&quot;</span>)?.<span class="title function_ invoke__">read_to_string</span>(&amp;<span class="keyword">mut</span> s)?;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(s)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等价于</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">read_username_verbose</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;<span class="type">String</span>, io::Error&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">f</span> = <span class="keyword">match</span> File::<span class="title function_ invoke__">open</span>(<span class="string">&quot;username.txt&quot;</span>) &#123;</span><br><span class="line">        <span class="title function_ invoke__">Ok</span>(file) =&gt; file,</span><br><span class="line">        <span class="title function_ invoke__">Err</span>(e) =&gt; <span class="keyword">return</span> <span class="title function_ invoke__">Err</span>(e),</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">match</span> f.<span class="title function_ invoke__">read_to_string</span>(&amp;<span class="keyword">mut</span> s) &#123;</span><br><span class="line">        <span class="title function_ invoke__">Ok</span>(_) =&gt; <span class="title function_ invoke__">Ok</span>(s),</span><br><span class="line">        <span class="title function_ invoke__">Err</span>(e) =&gt; <span class="title function_ invoke__">Err</span>(e),</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="错误类型自动转换"><a href="#错误类型自动转换" class="headerlink" title="错误类型自动转换"></a>错误类型自动转换</h4><p>当使用 <code>?</code> 时，错误类型会自动通过 <code>From</code> trait 转换。这意味着可以组合不同来源的错误，只要能被转换到同一个错误类型。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::num::ParseIntError;</span><br><span class="line"><span class="keyword">use</span> std::io;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Debug)]</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="title function_ invoke__">Io</span>(io::Error),</span><br><span class="line">    <span class="title function_ invoke__">Parse</span>(ParseIntError),</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">From</span>&lt;io::Error&gt; <span class="keyword">for</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">from</span>(e: io::Error) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">        MyError::<span class="title function_ invoke__">Io</span>(e)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">From</span>&lt;ParseIntError&gt; <span class="keyword">for</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">from</span>(e: ParseIntError) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">        MyError::<span class="title function_ invoke__">Parse</span>(e)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">read_and_parse</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;<span class="type">i32</span>, MyError&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">content</span> = std::fs::<span class="title function_ invoke__">read_to_string</span>(<span class="string">&quot;data.txt&quot;</span>)?;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">num</span> = content.<span class="title function_ invoke__">trim</span>().<span class="title function_ invoke__">parse</span>()?;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(num)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Option-枚举：处理”空值”"><a href="#Option-枚举：处理”空值”" class="headerlink" title="Option 枚举：处理”空值”"></a><code>Option</code> 枚举：处理”空值”</h3><p><code>Option</code> 虽不直接表示”错误”，但用于处理”可能没有值”的场景（避免空指针异常）；可认为 <code>Option</code> 是 <code>Result</code> 的一个简化版本（错误类型固定为 <code>()</code>）。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">enum</span> <span class="title class_">Option</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="title function_ invoke__">Some</span>(T), <span class="comment">// 有值：包含具体值（类型为 T）</span></span><br><span class="line">    <span class="literal">None</span>,    <span class="comment">// 无值：表示&quot;空&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="Option-的常用方法"><a href="#Option-的常用方法" class="headerlink" title="Option 的常用方法"></a>Option 的常用方法</h4><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>is_some() / is_none()</code></td>
<td>检查是否有值</td>
</tr>
<tr>
<td><code>unwrap()</code></td>
<td>直接取值（若为 None 则 panic）</td>
</tr>
<tr>
<td><code>unwrap_or(default)</code></td>
<td>若为 None 返回默认值</td>
</tr>
<tr>
<td><code>unwrap_or_else(f)</code></td>
<td>若为 None 执行函数返回值</td>
</tr>
<tr>
<td><code>map(f)</code></td>
<td>映射 Some 中的值</td>
</tr>
<tr>
<td><code>and_then(f)</code></td>
<td>链式操作，f 返回 Option</td>
</tr>
<tr>
<td><code>ok_or(err)</code></td>
<td>转换为 Result</td>
</tr>
<tr>
<td><code>filter(pred)</code></td>
<td>条件筛选</td>
</tr>
</tbody></table>
<h4 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">find_user</span>(id: <span class="type">u32</span>) <span class="punctuation">-&gt;</span> <span class="type">Option</span>&lt;<span class="type">String</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">if</span> id == <span class="number">1</span> &#123;</span><br><span class="line">        <span class="title function_ invoke__">Some</span>(<span class="string">&quot;Alice&quot;</span>.<span class="title function_ invoke__">to_string</span>()) <span class="comment">// 找到用户</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="literal">None</span> <span class="comment">// 未找到用户</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="comment">// 方式 1：match 匹配</span></span><br><span class="line">    <span class="keyword">match</span> <span class="title function_ invoke__">find_user</span>(<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="title function_ invoke__">Some</span>(name) =&gt; <span class="built_in">println!</span>(<span class="string">&quot;找到用户：&#123;&#125;&quot;</span>, name),</span><br><span class="line">        <span class="literal">None</span> =&gt; <span class="built_in">println!</span>(<span class="string">&quot;用户不存在&quot;</span>),</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 方式 2：? 运算符（需函数返回 Option）</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>(user) = <span class="title function_ invoke__">find_user</span>(<span class="number">1</span>) &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;用户：&#123;&#125;&quot;</span>, user);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 方式 3：map 链式处理</span></span><br><span class="line">    <span class="title function_ invoke__">find_user</span>(<span class="number">1</span>)</span><br><span class="line">        .<span class="title function_ invoke__">map</span>(|name| <span class="built_in">println!</span>(<span class="string">&quot;找到用户：&#123;&#125;&quot;</span>, name))</span><br><span class="line">        .<span class="title function_ invoke__">unwrap_or_else</span>(|| <span class="built_in">println!</span>(<span class="string">&quot;用户不存在&quot;</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="自定义错误类型"><a href="#自定义错误类型" class="headerlink" title="自定义错误类型"></a>自定义错误类型</h3><h4 id="基本要求"><a href="#基本要求" class="headerlink" title="基本要求"></a>基本要求</h4><p>自定义错误类型需要满足：</p>
<ul>
<li>必须实现 <code>std::error::Error</code> trait：使其成为标准错误类型；</li>
<li>必须实现 <code>std::fmt::Display</code> trait：以提供用户友好的错误信息。</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::fmt;</span><br><span class="line"><span class="keyword">use</span> std::error::Error;</span><br><span class="line"><span class="keyword">use</span> std::io;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 自定义错误枚举：包含两种错误类型</span></span><br><span class="line"><span class="meta">#[derive(Debug)]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">enum</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="title function_ invoke__">Io</span>(io::Error),           <span class="comment">// 包装 IO 错误</span></span><br><span class="line">    <span class="title function_ invoke__">Parse</span>(<span class="type">String</span>),           <span class="comment">// 解析错误（携带错误信息）</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现 Display trait：定义错误的用户可见信息</span></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">fmt</span>::Display <span class="keyword">for</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">fmt</span>(&amp;<span class="keyword">self</span>, f: &amp;<span class="keyword">mut</span> fmt::Formatter&lt;<span class="symbol">&#x27;_</span>&gt;) <span class="punctuation">-&gt;</span> fmt::<span class="type">Result</span> &#123;</span><br><span class="line">        <span class="keyword">match</span> <span class="keyword">self</span> &#123;</span><br><span class="line">            MyError::<span class="title function_ invoke__">Io</span>(err) =&gt; <span class="built_in">write!</span>(f, <span class="string">&quot;IO error: &#123;&#125;&quot;</span>, err),</span><br><span class="line">            MyError::<span class="title function_ invoke__">Parse</span>(msg) =&gt; <span class="built_in">write!</span>(f, <span class="string">&quot;Parse error: &#123;&#125;&quot;</span>, msg),</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 实现 Error trait：标记为标准错误类型</span></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">Error</span> <span class="keyword">for</span> <span class="title class_">MyError</span> &#123;&#125;</span><br></pre></td></tr></table></figure>

<h4 id="实现-From"><a href="#实现-From" class="headerlink" title="实现 From"></a>实现 <code>From</code></h4><p>为错误类型实现 <code>From</code>，这样就能使用 <code>?</code> 运算符自动转换错误。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">impl</span> <span class="title class_">From</span>&lt;std::io::Error&gt; <span class="keyword">for</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">from</span>(err: std::io::Error) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">        MyError::<span class="title function_ invoke__">Io</span>(err)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">From</span>&lt;std::num::ParseIntError&gt; <span class="keyword">for</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">from</span>(err: std::num::ParseIntError) <span class="punctuation">-&gt;</span> <span class="keyword">Self</span> &#123;</span><br><span class="line">        MyError::<span class="title function_ invoke__">Parse</span>(err.<span class="title function_ invoke__">to_string</span>())</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">read_number_from_file</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;<span class="type">i32</span>, MyError&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">content</span> = std::fs::<span class="title function_ invoke__">read_to_string</span>(<span class="string">&quot;data.txt&quot;</span>)?;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">num</span> = content.<span class="title function_ invoke__">trim</span>().<span class="title function_ invoke__">parse</span>()?;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(num)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="三方库简化错误定义"><a href="#三方库简化错误定义" class="headerlink" title="三方库简化错误定义"></a>三方库简化错误定义</h4><p>手写 <code>Display</code>、<code>From</code> 很繁琐，Rust 社区提供了两个常用库。</p>
<h5 id="thiserror（-derive-Error-）-定义结构化错误"><a href="#thiserror（-derive-Error-）-定义结构化错误" class="headerlink" title="thiserror（#[derive(Error)]）- 定义结构化错误"></a><code>thiserror</code>（<code>#[derive(Error)]</code>）- 定义结构化错误</h5><ul>
<li><code>#[derive(Error, Debug)]</code>：自动生成 <code>Error</code> 和 <code>Debug</code> trait 的实现。</li>
<li><code>#[error(&quot;消息模板&quot;)]</code>：定义 <code>Display</code> trait 的输出格式。</li>
<li><code>#[from]</code>：自动实现 <code>From&amp;lt;T&amp;gt;</code> trait（<code>T</code> 是字段类型），允许通过 <code>?</code> 运算符将 <code>T</code> 类型的错误自动转换为当前自定义错误。</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> thiserror::Error;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[derive(Debug, Error)]</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">enum</span> <span class="title class_">MyError</span> &#123;</span><br><span class="line">    <span class="meta">#[error(<span class="string">&quot;IO error: &#123;0&#125;&quot;</span>)]</span></span><br><span class="line">    <span class="title function_ invoke__">Io</span>(<span class="meta">#[from]</span> std::io::Error),</span><br><span class="line">    </span><br><span class="line">    <span class="meta">#[error(<span class="string">&quot;Parse error: &#123;0&#125;&quot;</span>)]</span></span><br><span class="line">    <span class="title function_ invoke__">Parse</span>(<span class="meta">#[from]</span> std::num::ParseIntError),</span><br><span class="line">    </span><br><span class="line">    <span class="meta">#[error(<span class="string">&quot;Not found&quot;</span>)]</span></span><br><span class="line">    NotFound,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="anyhow-简化上层错误处理"><a href="#anyhow-简化上层错误处理" class="headerlink" title="anyhow - 简化上层错误处理"></a><code>anyhow</code> - 简化上层错误处理</h5><ul>
<li><code>anyhow::Result&amp;lt;T&amp;gt;</code> 实际是 <code>Result&amp;lt;T, anyhow::Error&amp;gt;</code></li>
<li>可容纳任何实现 <code>Error</code> trait 的类型，无需手动转换</li>
<li><code>context</code> 方法会将原始错误，并添加额外描述，便于追踪错误发生的场景。</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> anyhow::&#123;<span class="type">Result</span>, Context&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 函数返回 anyhow::Error，可容纳任何实现 Error trait 的类型</span></span><br><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">test_anyhow</span>() <span class="punctuation">-&gt;</span> <span class="type">Result</span>&lt;()&gt; &#123;</span><br><span class="line">    <span class="title function_ invoke__">test_my_error</span>()</span><br><span class="line">        .<span class="title function_ invoke__">context</span>(<span class="string">&quot;test failed&quot;</span>)?; <span class="comment">// 为错误添加上下文</span></span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(())</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="不可恢复错误（panic-）"><a href="#不可恢复错误（panic-）" class="headerlink" title="不可恢复错误（panic!）"></a>不可恢复错误（panic!）</h2><p>当程序遇到不可恢复的错误（如违反内存安全、逻辑断言失败）时，使用 <code>panic!</code> 宏触发程序终止。默认会导致程序终止，但在某些场景下（如隔离第三方库的崩溃、实现容错机制），我们需要”安全捕获” <code>panic</code> 以避免程序整体崩溃。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">divide</span>(a: <span class="type">i32</span>, b: <span class="type">i32</span>) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> b == <span class="number">0</span> &#123;</span><br><span class="line">        <span class="built_in">panic!</span>(<span class="string">&quot;除数不能为 0&quot;</span>); <span class="comment">// 逻辑错误：不允许除 0</span></span><br><span class="line">    &#125;</span><br><span class="line">    a / b</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Panic-的执行流程"><a href="#Panic-的执行流程" class="headerlink" title="Panic 的执行流程"></a>Panic 的执行流程</h3><p>当 <code>panic!</code> 被触发时，Rust 默认会执行”栈展开”（可在 <code>Cargo.toml</code> 中添加 <code>panic = &quot;abort&quot;</code> 直接终止，不进行展开）：</p>
<ul>
<li>从 <code>panic!</code> 发生的位置开始，沿着调用栈向上回溯。</li>
<li>依次调用每个栈帧中局部变量的析构函数（清理资源，如关闭文件、释放锁等）。</li>
<li>最终打印错误信息（包含 <code>panic!</code> 的位置和消息）并终止程序。</li>
</ul>
<p>这种行为保证了”资源安全” —— 即使程序崩溃，也不会泄露资源。</p>
<h3 id="catch-unwind"><a href="#catch-unwind" class="headerlink" title="catch_unwind"></a><code>catch_unwind</code></h3><p><code>catch_unwind</code> 接收一个闭包作为参数，返回 <code>Result&lt;(), Box&lt;dyn Any + Send&gt;&gt;</code>：</p>
<ul>
<li>若闭包执行中未发生 <code>panic</code>，返回 <code>Ok(())</code>。</li>
<li>若发生 <code>panic</code>，返回 <code>Err(cause)</code>，其中 <code>cause</code> 是 <code>panic!</code> 传递的值（包装为 <code>Box&lt;dyn Any + Send&gt;</code>）。</li>
</ul>
<h4 id="基本使用示例"><a href="#基本使用示例" class="headerlink" title="基本使用示例"></a>基本使用示例</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::panic;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="comment">// 捕获闭包中的 panic</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">result</span> = panic::<span class="title function_ invoke__">catch_unwind</span>(|| &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;执行可能 panic 的代码&quot;</span>);</span><br><span class="line">        <span class="built_in">panic!</span>(<span class="string">&quot;发生错误！&quot;</span>); <span class="comment">// 触发 panic</span></span><br><span class="line">    &#125;);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 处理捕获结果</span></span><br><span class="line">    <span class="keyword">match</span> result &#123;</span><br><span class="line">        <span class="title function_ invoke__">Ok</span>(_) =&gt; <span class="built_in">println!</span>(<span class="string">&quot;未发生 panic&quot;</span>),</span><br><span class="line">        <span class="title function_ invoke__">Err</span>(cause) =&gt; &#123;</span><br><span class="line">            <span class="comment">// 将 cause 转换为具体类型（通常是 &amp;str 或 String）</span></span><br><span class="line">            <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>(msg) = cause.downcast_ref::&lt;&amp;<span class="type">str</span>&gt;() &#123;</span><br><span class="line">                <span class="built_in">println!</span>(<span class="string">&quot;捕获到 panic：&#123;&#125;&quot;</span>, msg);</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>(msg) = cause.downcast_ref::&lt;<span class="type">String</span>&gt;() &#123;</span><br><span class="line">                <span class="built_in">println!</span>(<span class="string">&quot;捕获到 panic：&#123;&#125;&quot;</span>, msg);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="built_in">println!</span>(<span class="string">&quot;捕获到未知类型的 panic&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="catch-unwind-的限制"><a href="#catch-unwind-的限制" class="headerlink" title="catch_unwind 的限制"></a><code>catch_unwind</code> 的限制</h4><p><code>catch_unwind</code> 并非万能，存在以下关键限制：</p>
<ul>
<li><strong>仅能捕获”栈展开”的 panic</strong>：若程序配置为 <code>panic = &quot;abort&quot;</code>，<code>catch_unwind</code> 无法捕获（因为不会触发栈展开）。</li>
<li><strong>无法捕获所有终止情况</strong>：如调用 <code>std::process::abort()</code>、操作系统信号（如 <code>SIGKILL</code>）导致的终止，<code>catch_unwind</code> 无效。</li>
<li><strong>闭包需满足 <code>Send</code> 约束</strong>：<code>catch_unwind</code> 的闭包参数要求 <code>FnOnce() + Send</code>，这意味着闭包中不能包含非 <code>Send</code> 类型（如 <code>Rc</code>、<code>RefCell</code> 等线程不安全类型）。</li>
<li><strong>程序状态可能不稳定</strong>：<code>panic</code> 发生时，栈展开会清理局部资源，但全局状态、共享资源可能处于不一致状态（如部分更新的缓存）。捕获 <code>panic</code> 后需谨慎处理，避免依赖不稳定状态。</li>
</ul>
<h4 id="有效载荷-Payload"><a href="#有效载荷-Payload" class="headerlink" title="有效载荷 (Payload)"></a>有效载荷 (Payload)</h4><p><code>panic!()</code> 可以携带 <code>&amp;&#39;static str</code> 或 <code>String</code> 等任意 <code>Any + Send</code> 类型作为 payload。捕获后可以尝试 <code>downcast_ref</code> 来读取消息：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::panic;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Err</span>(payload) = panic::<span class="title function_ invoke__">catch_unwind</span>(|| <span class="built_in">panic!</span>(<span class="string">&quot;oh no&quot;</span>)) &#123;</span><br><span class="line">        <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>(s) = payload.downcast_ref::&lt;&amp;<span class="type">str</span>&gt;() &#123;</span><br><span class="line">            <span class="built_in">println!</span>(<span class="string">&quot;&amp;str payload: &#123;&#125;&quot;</span>, s);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>(s) = payload.downcast_ref::&lt;<span class="type">String</span>&gt;() &#123;</span><br><span class="line">            <span class="built_in">println!</span>(<span class="string">&quot;String payload: &#123;&#125;&quot;</span>, s);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="built_in">println!</span>(<span class="string">&quot;未知 payload 类型&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="资源与不变式"><a href="#资源与不变式" class="headerlink" title="资源与不变式"></a>资源与不变式</h4><p>当线程在持有 <code>Mutex</code> 时 panic，该 <code>Mutex</code> 会被标记为”poisoned”（污染）。随后 <code>lock()</code> 会返回 <code>PoisonError</code>，表示需要小心处理共享状态可能处于不一致状态。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::Mutex;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">m</span> = Mutex::<span class="title function_ invoke__">new</span>(<span class="number">0</span>);</span><br><span class="line"><span class="comment">// 线程 panic 造成 poison 的示例请参见文档</span></span><br><span class="line"><span class="keyword">match</span> m.<span class="title function_ invoke__">lock</span>() &#123;</span><br><span class="line">    <span class="title function_ invoke__">Ok</span>(<span class="keyword">mut</span> guard) =&gt; *guard += <span class="number">1</span>,</span><br><span class="line">    <span class="title function_ invoke__">Err</span>(poisoned) =&gt; &#123;</span><br><span class="line">        <span class="comment">// 取回锁仍然可用的数据，但知道它可能不一致</span></span><br><span class="line">        <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">guard</span> = poisoned.<span class="title function_ invoke__">into_inner</span>();</span><br><span class="line">        *guard += <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li>**<code>Result&amp;lt;T, E&amp;gt;</code>**：用于处理可恢复错误，通过 <code>match</code>、<code>?</code>、链式方法灵活处理</li>
<li>**<code>Option&amp;lt;T&amp;gt;</code>**：用于处理可能缺失的值，避免空指针问题</li>
<li><strong><code>?</code> 运算符</strong>：最简洁的错误传播方式，配合 <code>From</code> trait 实现自动错误转换</li>
<li><strong>自定义错误类型</strong>：实现 <code>Error</code> 和 <code>Display</code> trait，推荐使用 <code>thiserror</code> 库简化代码</li>
<li>**<code>panic!</code>**：处理不可恢复错误，通过栈展开保证资源安全</li>
<li>**<code>catch_unwind</code>**：在特定场景（如隔离第三方库）捕获 panic，但需注意其限制</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust学习笔记</title>
    <url>/2023/06/20/Rust%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>Rust 语言是一种高效、可靠的通用高级语言。其高效不仅限于开发效率，它的执行效率也是令人称赞的，是一种少有的兼顾开发效率和执行效率的语言。</p>
<span id="more"></span>
<p>Rust语言的特点<br>高性能 - Rust 速度惊人且内存利用率极高。由于没有运行时和垃圾回收，它能够胜任对性能要求特别高的服务，可以在嵌入式设备上运行，还能轻松和其他语言集成。</p>
<p>可靠性 - Rust 丰富的类型系统和所有权模型保证了内存安全和线程安全，让您在编译期就能够消除各种各样的错误。</p>
<p>生产力 - Rust 拥有出色的文档、友好的编译器和清晰的错误提示信息， 还集成了一流的工具 —— 包管理器和构建工具， 智能地自动补全和类型检验的多编辑器支持， 以及自动格式化代码等等。</p>
<p>学习笔记已上传至gitHub：<a href="https://github.com/thefu/RustPrroject">https://github.com/thefu/RustPrroject</a></p>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中的集合Collection</title>
    <url>/2023/09/26/Rust%E4%B8%AD%E7%9A%84%E9%9B%86%E5%90%88Collection/</url>
    <content><![CDATA[<p>集合（Collections）是存储多个值的数据结构，主要存放在标准库 <code>std::collections</code> 模块中。集合的大小可以动态变化，且数据通常存储在堆上。</p>
<span id="more"></span>

<h2 id="Rust-标准库中的主要集合类型"><a href="#Rust-标准库中的主要集合类型" class="headerlink" title="Rust 标准库中的主要集合类型"></a>Rust 标准库中的主要集合类型</h2><table>
<thead>
<tr>
<th>集合类型</th>
<th>说明</th>
<th>特点</th>
</tr>
</thead>
<tbody><tr>
<td><code>Vec&lt;T&gt;</code></td>
<td>动态数组</td>
<td>元素有序、可变长、随机访问高效</td>
</tr>
<tr>
<td><code>VecDeque&lt;T&gt;</code></td>
<td>双端队列</td>
<td>支持两端高效插入&#x2F;删除</td>
</tr>
<tr>
<td><code>LinkedList&lt;T&gt;</code></td>
<td>双向链表</td>
<td>插入删除快，随机访问慢</td>
</tr>
<tr>
<td><code>HashMap&lt;K,V&gt;</code></td>
<td>哈希表</td>
<td>基于哈希的键值存储，无序</td>
</tr>
<tr>
<td><code>BTreeMap&lt;K,V&gt;</code></td>
<td>平衡树映射</td>
<td>基于排序的键值存储，有序</td>
</tr>
<tr>
<td><code>HashSet&lt;T&gt;</code></td>
<td>哈希集合</td>
<td>不重复元素，无序</td>
</tr>
<tr>
<td><code>BTreeSet&lt;T&gt;</code></td>
<td>有序集合</td>
<td>不重复元素，有序</td>
</tr>
<tr>
<td><code>BinaryHeap&lt;T&gt;</code></td>
<td>堆结构</td>
<td>可用于优先队列（最大堆）</td>
</tr>
</tbody></table>
<h2 id="1-动态数组-Vec-lt-T-gt"><a href="#1-动态数组-Vec-lt-T-gt" class="headerlink" title="1. 动态数组 Vec&lt;T&gt;"></a>1. 动态数组 Vec&lt;T&gt;</h2><p><code>Vec&lt;T&gt;</code> 是 Rust 中最常用的集合，本质是动态数组，支持在末尾高效插入&#x2F;删除元素，且可通过索引随机访问。</p>
<h3 id="创建方式"><a href="#创建方式" class="headerlink" title="创建方式"></a>创建方式</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">v1</span> = <span class="type">Vec</span>::<span class="title function_ invoke__">new</span>();              <span class="comment">// 空 vector</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">v2</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];           <span class="comment">// 宏创建</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">v3</span> = <span class="type">Vec</span>::<span class="title function_ invoke__">with_capacity</span>(<span class="number">10</span>); <span class="comment">// 预分配容量</span></span><br></pre></td></tr></table></figure>

<h3 id="常用方法"><a href="#常用方法" class="headerlink" title="常用方法"></a>常用方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>返回类型</th>
</tr>
</thead>
<tbody><tr>
<td><code>new()</code></td>
<td>创建空向量</td>
<td><code>Vec&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>with_capacity(n)</code></td>
<td>预分配容量</td>
<td><code>Vec&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>push(value)</code></td>
<td>末尾添加元素</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>pop()</code></td>
<td>移除最后一个元素</td>
<td><code>Option&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>insert(index, value)</code></td>
<td>在指定位置插入</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>remove(index)</code></td>
<td>移除指定位置</td>
<td><code>T</code></td>
</tr>
<tr>
<td><code>clear()</code></td>
<td>清空所有元素</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>len()</code></td>
<td>返回长度</td>
<td><code>usize</code></td>
</tr>
<tr>
<td><code>capacity()</code></td>
<td>返回容量</td>
<td><code>usize</code></td>
</tr>
<tr>
<td><code>reserve(n)</code></td>
<td>扩容</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>shrink_to_fit()</code></td>
<td>收缩容量</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>get(i)</code></td>
<td>安全访问元素</td>
<td><code>Option&lt;&amp;T&gt;</code></td>
</tr>
<tr>
<td><code>get_mut(i)</code></td>
<td>可变访问元素</td>
<td><code>Option&lt;&amp;mut T&gt;</code></td>
</tr>
<tr>
<td><code>first() / last()</code></td>
<td>获取首&#x2F;尾元素</td>
<td><code>Option&lt;&amp;T&gt;</code></td>
</tr>
<tr>
<td><code>iter() / iter_mut()</code></td>
<td>遍历元素</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>sort() / sort_by()</code></td>
<td>排序</td>
<td><code>()</code></td>
</tr>
</tbody></table>
<h3 id="Vec-示例"><a href="#Vec-示例" class="headerlink" title="Vec 示例"></a>Vec 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="comment">// 1. 创建和添加元素</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">v</span> = <span class="type">Vec</span>::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    v.<span class="title function_ invoke__">push</span>(<span class="number">1</span>);</span><br><span class="line">    v.<span class="title function_ invoke__">push</span>(<span class="number">2</span>);</span><br><span class="line">    v.<span class="title function_ invoke__">push</span>(<span class="number">3</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Vec: &#123;:?&#125;&quot;</span>, v); <span class="comment">// [1, 2, 3]</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 2. 访问元素</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;First: &#123;&#125;&quot;</span>, v[<span class="number">0</span>]); <span class="comment">// 1</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Safe get: &#123;:?&#125;&quot;</span>, v.<span class="title function_ invoke__">get</span>(<span class="number">1</span>)); <span class="comment">// Some(&amp;2)</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 3. 修改元素</span></span><br><span class="line">    v[<span class="number">0</span>] = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 4. 删除元素</span></span><br><span class="line">    v.<span class="title function_ invoke__">pop</span>(); <span class="comment">// 删除末尾元素 3</span></span><br><span class="line">    v.<span class="title function_ invoke__">remove</span>(<span class="number">0</span>); <span class="comment">// 删除首元素 10</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 5. 遍历</span></span><br><span class="line">    <span class="keyword">for</span> <span class="variable">item</span> <span class="keyword">in</span> &amp;v &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, item);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 6. 排序</span></span><br><span class="line">    v.<span class="title function_ invoke__">sort</span>();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 7. 容量管理</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Len: &#123;&#125;, Capacity: &#123;&#125;&quot;</span>, v.<span class="title function_ invoke__">len</span>(), v.<span class="title function_ invoke__">capacity</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-双端队列-VecDeque-lt-T-gt"><a href="#2-双端队列-VecDeque-lt-T-gt" class="headerlink" title="2. 双端队列 VecDeque&lt;T&gt;"></a>2. 双端队列 VecDeque&lt;T&gt;</h2><p><code>VecDeque&lt;T&gt;</code>（Double-ended Queue）是双端队列，底层使用环形缓冲区实现。支持在首尾两端高效插入&#x2F;删除元素。</p>
<h3 id="创建和基本操作"><a href="#创建和基本操作" class="headerlink" title="创建和基本操作"></a>创建和基本操作</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::VecDeque;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">deque</span> = VecDeque::<span class="title function_ invoke__">new</span>();</span><br><span class="line">deque.<span class="title function_ invoke__">push_back</span>(<span class="number">2</span>);    <span class="comment">// 末尾入队</span></span><br><span class="line">deque.<span class="title function_ invoke__">push_front</span>(<span class="number">1</span>);   <span class="comment">// 头部入队</span></span><br><span class="line">deque.<span class="title function_ invoke__">pop_front</span>();     <span class="comment">// 从头部出队，返回 Some(1)</span></span><br><span class="line">deque.<span class="title function_ invoke__">pop_back</span>();      <span class="comment">// 从尾部出队，返回 Some(2)</span></span><br></pre></td></tr></table></figure>

<h3 id="常用方法-1"><a href="#常用方法-1" class="headerlink" title="常用方法"></a>常用方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>返回类型</th>
</tr>
</thead>
<tbody><tr>
<td><code>new()</code></td>
<td>创建空队列</td>
<td><code>VecDeque&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>with_capacity(n)</code></td>
<td>预分配容量</td>
<td><code>VecDeque&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>push_back(value)</code></td>
<td>末尾入队</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>push_front(value)</code></td>
<td>头部入队</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>pop_back()</code></td>
<td>从尾部出队</td>
<td><code>Option&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>pop_front()</code></td>
<td>从头部出队</td>
<td><code>Option&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>front() / back()</code></td>
<td>查看首尾元素</td>
<td><code>Option&lt;&amp;T&gt;</code></td>
</tr>
<tr>
<td><code>len() / is_empty()</code></td>
<td>长度&#x2F;是否空</td>
<td><code>usize / bool</code></td>
</tr>
<tr>
<td><code>get(i)</code></td>
<td>安全索引访问</td>
<td><code>Option&lt;&amp;T&gt;</code></td>
</tr>
<tr>
<td><code>iter() / iter_mut()</code></td>
<td>遍历</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>rotate_left(k) / rotate_right(k)</code></td>
<td>循环旋转</td>
<td><code>()</code></td>
</tr>
</tbody></table>
<h3 id="VecDeque-示例"><a href="#VecDeque-示例" class="headerlink" title="VecDeque 示例"></a>VecDeque 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::VecDeque;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">deque</span> = VecDeque::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 从两端插入</span></span><br><span class="line">    deque.<span class="title function_ invoke__">push_back</span>(<span class="number">2</span>);</span><br><span class="line">    deque.<span class="title function_ invoke__">push_back</span>(<span class="number">3</span>);</span><br><span class="line">    deque.<span class="title function_ invoke__">push_front</span>(<span class="number">1</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Deque: &#123;:?&#125;&quot;</span>, deque); <span class="comment">// [1, 2, 3]</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 从两端删除</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Front: &#123;:?&#125;&quot;</span>, deque.<span class="title function_ invoke__">pop_front</span>()); <span class="comment">// Some(1)</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Back: &#123;:?&#125;&quot;</span>, deque.<span class="title function_ invoke__">pop_back</span>());   <span class="comment">// Some(3)</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 旋转</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">deque</span> = VecDeque::<span class="title function_ invoke__">from</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>]);</span><br><span class="line">    deque.<span class="title function_ invoke__">rotate_left</span>(<span class="number">2</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;After rotate_left(2): &#123;:?&#125;&quot;</span>, deque); <span class="comment">// [3, 4, 5, 1, 2]</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-双向链表-LinkedList-lt-T-gt"><a href="#3-双向链表-LinkedList-lt-T-gt" class="headerlink" title="3. 双向链表 LinkedList&lt;T&gt;"></a>3. 双向链表 LinkedList&lt;T&gt;</h2><p><code>LinkedList&lt;T&gt;</code> 是双向链表，元素通过指针连接，不连续存储。</p>
<h3 id="性能考量"><a href="#性能考量" class="headerlink" title="性能考量"></a>性能考量</h3><ul>
<li><strong>实际性能较差</strong>：因元素不连续，缓存友好性低，随机访问效率低（O(n)）</li>
<li><strong>极少使用</strong>：除非明确需要链表的理论特性，否则优先用 <code>Vec</code> 或 <code>VecDeque</code></li>
</ul>
<h3 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::LinkedList;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">list</span> = LinkedList::<span class="title function_ invoke__">new</span>();</span><br><span class="line">list.<span class="title function_ invoke__">push_back</span>(<span class="number">1</span>);     <span class="comment">// 尾部添加：[1]</span></span><br><span class="line">list.<span class="title function_ invoke__">push_front</span>(<span class="number">0</span>);    <span class="comment">// 头部添加：[0, 1]</span></span><br><span class="line">list.<span class="title function_ invoke__">pop_back</span>();       <span class="comment">// 尾部移除：返回 Some(1)</span></span><br><span class="line">list.<span class="title function_ invoke__">pop_front</span>();      <span class="comment">// 头部移除：返回 Some(0)</span></span><br></pre></td></tr></table></figure>

<h3 id="常用方法-2"><a href="#常用方法-2" class="headerlink" title="常用方法"></a>常用方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>返回类型</th>
</tr>
</thead>
<tbody><tr>
<td><code>new()</code></td>
<td>创建空链表</td>
<td><code>LinkedList&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>push_back(value)</code></td>
<td>尾部插入</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>push_front(value)</code></td>
<td>头部插入</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>pop_back() / pop_front()</code></td>
<td>移除首&#x2F;尾</td>
<td><code>Option&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>front() / back()</code></td>
<td>获取首尾引用</td>
<td><code>Option&lt;&amp;T&gt;</code></td>
</tr>
<tr>
<td><code>len() / is_empty()</code></td>
<td>获取长度&#x2F;是否为空</td>
<td><code>usize / bool</code></td>
</tr>
<tr>
<td><code>clear()</code></td>
<td>清空链表</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>append(&amp;mut other)</code></td>
<td>拼接另一个链表</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>iter() / iter_mut()</code></td>
<td>遍历</td>
<td>迭代器</td>
</tr>
</tbody></table>
<h2 id="4-哈希表-HashMap-lt-K-V-gt"><a href="#4-哈希表-HashMap-lt-K-V-gt" class="headerlink" title="4. 哈希表 HashMap&lt;K,V&gt;"></a>4. 哈希表 HashMap&lt;K,V&gt;</h2><p><code>HashMap&lt;K, V&gt;</code> 基于哈希表实现，通过哈希函数将键映射到存储位置，查询和插入的平均时间复杂度为 O(1)。</p>
<h3 id="特点"><a href="#特点" class="headerlink" title="特点"></a>特点</h3><ul>
<li><strong>无序存储</strong>（键的顺序不固定）</li>
<li>键 <code>K</code> 需实现 <code>Hash</code> 和 <code>Eq</code> trait（用于哈希计算和相等性比较）</li>
<li>性能依赖哈希函数的质量（避免哈希冲突）</li>
</ul>
<h3 id="创建方式-1"><a href="#创建方式-1" class="headerlink" title="创建方式"></a>创建方式</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::HashMap;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 1. 基本创建</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">scores</span> = HashMap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">scores.<span class="title function_ invoke__">insert</span>(<span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Alice&quot;</span>), <span class="number">90</span>);</span><br><span class="line">scores.<span class="title function_ invoke__">insert</span>(<span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Bob&quot;</span>), <span class="number">85</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 从迭代器创建</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">teams</span> = <span class="built_in">vec!</span>[<span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Blue&quot;</span>), <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Red&quot;</span>)];</span><br><span class="line"><span class="keyword">let</span> <span class="variable">initial_scores</span> = <span class="built_in">vec!</span>[<span class="number">10</span>, <span class="number">50</span>];</span><br><span class="line"><span class="keyword">let</span> <span class="variable">scores</span>: HashMap&lt;_, _&gt; = teams</span><br><span class="line">    .<span class="title function_ invoke__">into_iter</span>()</span><br><span class="line">    .<span class="title function_ invoke__">zip</span>(initial_scores.<span class="title function_ invoke__">into_iter</span>())</span><br><span class="line">    .<span class="title function_ invoke__">collect</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 3. 使用 from_iter</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">map</span> = HashMap::<span class="title function_ invoke__">from_iter</span>(<span class="built_in">vec!</span>[(<span class="string">&quot;a&quot;</span>, <span class="number">1</span>), (<span class="string">&quot;b&quot;</span>, <span class="number">2</span>)]);</span><br></pre></td></tr></table></figure>

<h3 id="常用方法-3"><a href="#常用方法-3" class="headerlink" title="常用方法"></a>常用方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>返回类型</th>
</tr>
</thead>
<tbody><tr>
<td><code>new()</code></td>
<td>创建空映射</td>
<td><code>HashMap&lt;K,V&gt;</code></td>
</tr>
<tr>
<td><code>with_capacity(n)</code></td>
<td>预分配容量</td>
<td><code>HashMap&lt;K,V&gt;</code></td>
</tr>
<tr>
<td><code>insert(key, val)</code></td>
<td>插入或更新键值</td>
<td><code>Option&lt;V&gt;</code>（旧值）</td>
</tr>
<tr>
<td><code>remove(key)</code></td>
<td>删除键</td>
<td><code>Option&lt;V&gt;</code></td>
</tr>
<tr>
<td><code>get(key)</code></td>
<td>获取值引用</td>
<td><code>Option&lt;&amp;V&gt;</code></td>
</tr>
<tr>
<td><code>get_mut(key)</code></td>
<td>可变引用</td>
<td><code>Option&lt;&amp;mut V&gt;</code></td>
</tr>
<tr>
<td><code>contains_key(key)</code></td>
<td>是否存在</td>
<td><code>bool</code></td>
</tr>
<tr>
<td><code>entry(key)</code></td>
<td>获取或创建 entry</td>
<td><code>Entry&lt;K,V&gt;</code></td>
</tr>
<tr>
<td><code>len() / is_empty()</code></td>
<td>长度&#x2F;是否空</td>
<td><code>usize / bool</code></td>
</tr>
<tr>
<td><code>keys() / values() / iter()</code></td>
<td>迭代键、值、键值对</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>clear()</code></td>
<td>清空</td>
<td><code>()</code></td>
</tr>
</tbody></table>
<h3 id="HashMap-示例"><a href="#HashMap-示例" class="headerlink" title="HashMap 示例"></a>HashMap 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::HashMap;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">map</span> = HashMap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 插入</span></span><br><span class="line">    map.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;name&quot;</span>, <span class="string">&quot;Alice&quot;</span>);</span><br><span class="line">    map.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;age&quot;</span>, <span class="string">&quot;30&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 查询</span></span><br><span class="line">    <span class="keyword">match</span> map.<span class="title function_ invoke__">get</span>(<span class="string">&quot;name&quot;</span>) &#123;</span><br><span class="line">        <span class="title function_ invoke__">Some</span>(value) =&gt; <span class="built_in">println!</span>(<span class="string">&quot;Name: &#123;&#125;&quot;</span>, value),</span><br><span class="line">        <span class="literal">None</span> =&gt; <span class="built_in">println!</span>(<span class="string">&quot;Not found&quot;</span>),</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 更新</span></span><br><span class="line">    map.<span class="title function_ invoke__">insert</span>(<span class="string">&quot;age&quot;</span>, <span class="string">&quot;31&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 遍历</span></span><br><span class="line">    <span class="keyword">for</span> (key, value) <span class="keyword">in</span> &amp;map &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;: &#123;&#125;&quot;</span>, key, value);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 删除</span></span><br><span class="line">    map.<span class="title function_ invoke__">remove</span>(<span class="string">&quot;age&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Entry API（高效更新）</span></span><br><span class="line">    map.<span class="title function_ invoke__">entry</span>(<span class="string">&quot;city&quot;</span>).<span class="title function_ invoke__">or_insert</span>(<span class="string">&quot;Beijing&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-有序映射-BTreeMap-lt-K-V-gt"><a href="#5-有序映射-BTreeMap-lt-K-V-gt" class="headerlink" title="5. 有序映射 BTreeMap&lt;K,V&gt;"></a>5. 有序映射 BTreeMap&lt;K,V&gt;</h2><p><code>BTreeMap&lt;K, V&gt;</code> 基于 B 树实现，键值对按键的排序顺序存储，查询时间复杂度为 O(log n)。</p>
<h3 id="特点-1"><a href="#特点-1" class="headerlink" title="特点"></a>特点</h3><ul>
<li><strong>有序存储</strong>：键值对按键的自然顺序（或自定义排序）有序存储</li>
<li>键 <code>K</code> 需实现 <code>Ord</code> trait（用于排序）</li>
<li><strong>支持范围查询</strong>：如获取”大于 x 且小于 y”的键值对</li>
</ul>
<h3 id="基本操作-1"><a href="#基本操作-1" class="headerlink" title="基本操作"></a>基本操作</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::BTreeMap;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">map</span> = BTreeMap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">map.<span class="title function_ invoke__">insert</span>(<span class="number">3</span>, <span class="string">&quot;c&quot;</span>);</span><br><span class="line">map.<span class="title function_ invoke__">insert</span>(<span class="number">1</span>, <span class="string">&quot;a&quot;</span>);</span><br><span class="line">map.<span class="title function_ invoke__">insert</span>(<span class="number">2</span>, <span class="string">&quot;b&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 遍历：按键的升序输出 (1,&quot;a&quot;), (2,&quot;b&quot;), (3,&quot;c&quot;)</span></span><br><span class="line"><span class="keyword">for</span> (k, v) <span class="keyword">in</span> &amp;map &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;: &#123;&#125;&quot;</span>, k, v);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 范围查询：获取键 &gt;1 且 &lt;=3 的键值对</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">range</span> = map.<span class="title function_ invoke__">range</span>(<span class="number">1</span>..=<span class="number">3</span>); <span class="comment">// 包含1和3</span></span><br></pre></td></tr></table></figure>

<h3 id="常用方法-4"><a href="#常用方法-4" class="headerlink" title="常用方法"></a>常用方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>返回类型</th>
</tr>
</thead>
<tbody><tr>
<td><code>new()</code></td>
<td>创建空映射</td>
<td><code>BTreeMap&lt;K,V&gt;</code></td>
</tr>
<tr>
<td><code>insert(key, val)</code></td>
<td>插入或替换</td>
<td><code>Option&lt;V&gt;</code></td>
</tr>
<tr>
<td><code>remove(key)</code></td>
<td>删除键</td>
<td><code>Option&lt;V&gt;</code></td>
</tr>
<tr>
<td><code>get(key)</code></td>
<td>获取引用</td>
<td><code>Option&lt;&amp;V&gt;</code></td>
</tr>
<tr>
<td><code>contains_key(key)</code></td>
<td>检查存在</td>
<td><code>bool</code></td>
</tr>
<tr>
<td><code>len() / is_empty()</code></td>
<td>长度&#x2F;空</td>
<td><code>usize / bool</code></td>
</tr>
<tr>
<td><code>iter()</code></td>
<td>遍历有序键值</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>range(range)</code></td>
<td>范围遍历</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>first_key_value() / last_key_value()</code></td>
<td>最小&#x2F;最大键</td>
<td><code>Option&lt;(&amp;K,&amp;V)&gt;</code></td>
</tr>
<tr>
<td><code>clear()</code></td>
<td>清空</td>
<td><code>()</code></td>
</tr>
</tbody></table>
<h3 id="BTreeMap-示例"><a href="#BTreeMap-示例" class="headerlink" title="BTreeMap 示例"></a>BTreeMap 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::BTreeMap;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">map</span> = BTreeMap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    map.<span class="title function_ invoke__">insert</span>(<span class="number">5</span>, <span class="string">&quot;five&quot;</span>);</span><br><span class="line">    map.<span class="title function_ invoke__">insert</span>(<span class="number">2</span>, <span class="string">&quot;two&quot;</span>);</span><br><span class="line">    map.<span class="title function_ invoke__">insert</span>(<span class="number">8</span>, <span class="string">&quot;eight&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 有序遍历</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Ordered iteration:&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (k, v) <span class="keyword">in</span> &amp;map &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;: &#123;&#125;&quot;</span>, k, v); <span class="comment">// 输出 2, 5, 8</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 范围查询</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Range [2, 7]:&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> (k, v) <span class="keyword">in</span> map.<span class="title function_ invoke__">range</span>(<span class="number">2</span>..=<span class="number">7</span>) &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;: &#123;&#125;&quot;</span>, k, v); <span class="comment">// 输出 2, 5</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 最小最大元素</span></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>((k, v)) = map.<span class="title function_ invoke__">first_key_value</span>() &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;First: &#123;&#125;: &#123;&#125;&quot;</span>, k, v);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="6-哈希集合-HashSet-lt-T-gt"><a href="#6-哈希集合-HashSet-lt-T-gt" class="headerlink" title="6. 哈希集合 HashSet&lt;T&gt;"></a>6. 哈希集合 HashSet&lt;T&gt;</h2><p><code>HashSet&lt;T&gt;</code> 基于 <code>HashMap&lt;T, ()&gt;</code> 实现，存储唯一元素，无序。</p>
<h3 id="特点-2"><a href="#特点-2" class="headerlink" title="特点"></a>特点</h3><ul>
<li><strong>元素唯一</strong>：插入重复元素会被忽略</li>
<li>元素 <code>T</code> 需实现 <code>Hash</code> 和 <code>Eq</code> trait</li>
<li>插入、查询、删除的平均时间复杂度为 O(1)</li>
</ul>
<h3 id="基本操作-2"><a href="#基本操作-2" class="headerlink" title="基本操作"></a>基本操作</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::HashSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">set</span> = HashSet::<span class="title function_ invoke__">new</span>();</span><br><span class="line">set.<span class="title function_ invoke__">insert</span>(<span class="number">1</span>);</span><br><span class="line">set.<span class="title function_ invoke__">insert</span>(<span class="number">2</span>);</span><br><span class="line">set.<span class="title function_ invoke__">insert</span>(<span class="number">1</span>); <span class="comment">// 重复元素，插入失败</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 常用方法</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">has_one</span> = set.<span class="title function_ invoke__">contains</span>(&amp;<span class="number">1</span>);  <span class="comment">// true</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">removed</span> = set.<span class="title function_ invoke__">remove</span>(&amp;<span class="number">1</span>);    <span class="comment">// true，移除1</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">len</span> = set.<span class="title function_ invoke__">len</span>();             <span class="comment">// 1（剩余元素2）</span></span><br></pre></td></tr></table></figure>

<h3 id="常用方法-5"><a href="#常用方法-5" class="headerlink" title="常用方法"></a>常用方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>返回类型</th>
</tr>
</thead>
<tbody><tr>
<td><code>new()</code></td>
<td>创建空集合</td>
<td><code>HashSet&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>insert(value)</code></td>
<td>插入</td>
<td><code>bool</code>(是否新插入)</td>
</tr>
<tr>
<td><code>remove(value)</code></td>
<td>删除</td>
<td><code>bool</code></td>
</tr>
<tr>
<td><code>contains(value)</code></td>
<td>是否存在</td>
<td><code>bool</code></td>
</tr>
<tr>
<td><code>len() / is_empty()</code></td>
<td>长度&#x2F;空</td>
<td><code>usize / bool</code></td>
</tr>
<tr>
<td><code>clear()</code></td>
<td>清空</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>iter()</code></td>
<td>遍历</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>union(&amp;other)</code></td>
<td>并集</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>intersection(&amp;other)</code></td>
<td>交集</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>difference(&amp;other)</code></td>
<td>差集</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>symmetric_difference(&amp;other)</code></td>
<td>对称差</td>
<td>迭代器</td>
</tr>
</tbody></table>
<h3 id="HashSet-示例"><a href="#HashSet-示例" class="headerlink" title="HashSet 示例"></a>HashSet 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::HashSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">set1</span> = HashSet::<span class="title function_ invoke__">from</span>([<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>]);</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">set2</span> = HashSet::<span class="title function_ invoke__">from</span>([<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 插入和查询</span></span><br><span class="line">    set1.<span class="title function_ invoke__">insert</span>(<span class="number">5</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Contains 3: &#123;&#125;&quot;</span>, set1.<span class="title function_ invoke__">contains</span>(&amp;<span class="number">3</span>)); <span class="comment">// true</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 集合操作</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">union</span>: HashSet&lt;_&gt; = set1.<span class="title function_ invoke__">union</span>(&amp;set2).<span class="title function_ invoke__">cloned</span>().<span class="title function_ invoke__">collect</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Union: &#123;:?&#125;&quot;</span>, <span class="keyword">union</span>); <span class="comment">// &#123;1, 2, 3, 4, 5&#125;</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">let</span> <span class="variable">intersection</span>: HashSet&lt;_&gt; = set1.<span class="title function_ invoke__">intersection</span>(&amp;set2).<span class="title function_ invoke__">cloned</span>().<span class="title function_ invoke__">collect</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Intersection: &#123;:?&#125;&quot;</span>, intersection); <span class="comment">// &#123;2, 3&#125;</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">let</span> <span class="variable">difference</span>: HashSet&lt;_&gt; = set1.<span class="title function_ invoke__">difference</span>(&amp;set2).<span class="title function_ invoke__">cloned</span>().<span class="title function_ invoke__">collect</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Difference: &#123;:?&#125;&quot;</span>, difference); <span class="comment">// &#123;1, 5&#125;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="7-有序集合-BTreeSet-lt-T-gt"><a href="#7-有序集合-BTreeSet-lt-T-gt" class="headerlink" title="7. 有序集合 BTreeSet&lt;T&gt;"></a>7. 有序集合 BTreeSet&lt;T&gt;</h2><p><code>BTreeSet&lt;T&gt;</code> 基于 <code>BTreeMap&lt;T, ()&gt;</code> 实现，存储唯一元素，且按元素顺序排序。</p>
<h3 id="特点-3"><a href="#特点-3" class="headerlink" title="特点"></a>特点</h3><ul>
<li><strong>元素唯一且有序</strong>：按 <code>Ord</code> trait 排序</li>
<li>元素 <code>T</code> 需实现 <code>Ord</code> trait</li>
<li><strong>支持范围查询</strong>，查询时间复杂度为 O(log n)</li>
</ul>
<h3 id="基本操作-3"><a href="#基本操作-3" class="headerlink" title="基本操作"></a>基本操作</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::BTreeSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">set</span> = BTreeSet::<span class="title function_ invoke__">new</span>();</span><br><span class="line">set.<span class="title function_ invoke__">insert</span>(<span class="number">3</span>);</span><br><span class="line">set.<span class="title function_ invoke__">insert</span>(<span class="number">1</span>);</span><br><span class="line">set.<span class="title function_ invoke__">insert</span>(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 遍历：按顺序输出 1, 2, 3</span></span><br><span class="line"><span class="keyword">for</span> <span class="variable">num</span> <span class="keyword">in</span> &amp;set &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, num);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 范围查询：获取 &gt;1 且 &lt;=3 的元素</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">range</span> = set.<span class="title function_ invoke__">range</span>(<span class="number">1</span>..=<span class="number">3</span>); <span class="comment">// 包含1和3</span></span><br></pre></td></tr></table></figure>

<h3 id="常用方法-6"><a href="#常用方法-6" class="headerlink" title="常用方法"></a>常用方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>返回类型</th>
</tr>
</thead>
<tbody><tr>
<td><code>new()</code></td>
<td>创建空集合</td>
<td><code>BTreeSet&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>insert(value)</code></td>
<td>插入</td>
<td><code>bool</code></td>
</tr>
<tr>
<td><code>remove(value)</code></td>
<td>删除</td>
<td><code>bool</code></td>
</tr>
<tr>
<td><code>contains(value)</code></td>
<td>是否存在</td>
<td><code>bool</code></td>
</tr>
<tr>
<td><code>len() / is_empty()</code></td>
<td>长度&#x2F;空</td>
<td><code>usize / bool</code></td>
</tr>
<tr>
<td><code>iter()</code></td>
<td>遍历有序元素</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>range(range)</code></td>
<td>范围遍历</td>
<td>迭代器</td>
</tr>
<tr>
<td><code>first() / last()</code></td>
<td>最小&#x2F;最大元素</td>
<td><code>Option&lt;&amp;T&gt;</code></td>
</tr>
<tr>
<td><code>union(&amp;other) / intersection(&amp;other)</code></td>
<td>集合操作</td>
<td>迭代器</td>
</tr>
</tbody></table>
<h3 id="BTreeSet-示例"><a href="#BTreeSet-示例" class="headerlink" title="BTreeSet 示例"></a>BTreeSet 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::BTreeSet;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">set</span> = BTreeSet::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    set.<span class="title function_ invoke__">insert</span>(<span class="number">5</span>);</span><br><span class="line">    set.<span class="title function_ invoke__">insert</span>(<span class="number">2</span>);</span><br><span class="line">    set.<span class="title function_ invoke__">insert</span>(<span class="number">8</span>);</span><br><span class="line">    set.<span class="title function_ invoke__">insert</span>(<span class="number">2</span>); <span class="comment">// 重复，不会插入</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 有序遍历</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Ordered:&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">num</span> <span class="keyword">in</span> &amp;set &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, num); <span class="comment">// 输出 2, 5, 8</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 范围查询</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Range [2, 6]:&quot;</span>);</span><br><span class="line">    <span class="keyword">for</span> <span class="variable">num</span> <span class="keyword">in</span> set.<span class="title function_ invoke__">range</span>(<span class="number">2</span>..=<span class="number">6</span>) &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, num); <span class="comment">// 输出 2, 5</span></span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 首尾元素</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;First: &#123;:?&#125;&quot;</span>, set.<span class="title function_ invoke__">first</span>()); <span class="comment">// 2</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Last: &#123;:?&#125;&quot;</span>, set.<span class="title function_ invoke__">last</span>());   <span class="comment">// 8</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="8-二叉堆-BinaryHeap-lt-T-gt"><a href="#8-二叉堆-BinaryHeap-lt-T-gt" class="headerlink" title="8. 二叉堆 BinaryHeap&lt;T&gt;"></a>8. 二叉堆 BinaryHeap&lt;T&gt;</h2><p><code>BinaryHeap&lt;T&gt;</code> 实现优先队列功能。</p>
<h3 id="特点-4"><a href="#特点-4" class="headerlink" title="特点"></a>特点</h3><ul>
<li><strong>默认是最大堆</strong></li>
<li>可通过 <code>Reverse()</code> 包装改为最小堆</li>
</ul>
<h3 id="基本操作-4"><a href="#基本操作-4" class="headerlink" title="基本操作"></a>基本操作</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::BinaryHeap;</span><br><span class="line"><span class="keyword">use</span> std::cmp::Reverse;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 最大堆</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">max_heap</span> = BinaryHeap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">max_heap.<span class="title function_ invoke__">push</span>(<span class="number">10</span>);</span><br><span class="line">max_heap.<span class="title function_ invoke__">push</span>(<span class="number">5</span>);</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, max_heap.<span class="title function_ invoke__">pop</span>().<span class="title function_ invoke__">unwrap</span>()); <span class="comment">// 10</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 最小堆</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">min_heap</span> = BinaryHeap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">min_heap.<span class="title function_ invoke__">push</span>(<span class="title function_ invoke__">Reverse</span>(<span class="number">10</span>));</span><br><span class="line">min_heap.<span class="title function_ invoke__">push</span>(<span class="title function_ invoke__">Reverse</span>(<span class="number">5</span>));</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, min_heap.<span class="title function_ invoke__">pop</span>().<span class="title function_ invoke__">unwrap</span>().<span class="number">0</span>); <span class="comment">// 5</span></span><br></pre></td></tr></table></figure>

<h3 id="常用方法-7"><a href="#常用方法-7" class="headerlink" title="常用方法"></a>常用方法</h3><table>
<thead>
<tr>
<th>方法</th>
<th>说明</th>
<th>返回类型</th>
</tr>
</thead>
<tbody><tr>
<td><code>new()</code></td>
<td>创建空堆</td>
<td><code>BinaryHeap&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>from(vec)</code></td>
<td>从 Vec 构建堆</td>
<td><code>BinaryHeap&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>push(value)</code></td>
<td>插入元素</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>pop()</code></td>
<td>弹出最大元素</td>
<td><code>Option&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>peek()</code></td>
<td>查看最大元素</td>
<td><code>Option&lt;&amp;T&gt;</code></td>
</tr>
<tr>
<td><code>len() / is_empty()</code></td>
<td>长度&#x2F;空</td>
<td><code>usize / bool</code></td>
</tr>
<tr>
<td><code>clear()</code></td>
<td>清空</td>
<td><code>()</code></td>
</tr>
<tr>
<td><code>into_sorted_vec()</code></td>
<td>升序取出所有元素</td>
<td><code>Vec&lt;T&gt;</code></td>
</tr>
<tr>
<td><code>drain_sorted()</code></td>
<td>按排序迭代出元素</td>
<td>迭代器</td>
</tr>
</tbody></table>
<h3 id="BinaryHeap-示例"><a href="#BinaryHeap-示例" class="headerlink" title="BinaryHeap 示例"></a>BinaryHeap 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::collections::BinaryHeap;</span><br><span class="line"><span class="keyword">use</span> std::cmp::Reverse;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="comment">// 最大堆示例</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">heap</span> = BinaryHeap::<span class="title function_ invoke__">from</span>([<span class="number">10</span>, <span class="number">5</span>, <span class="number">20</span>, <span class="number">15</span>]);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Max: &#123;&#125;&quot;</span>, heap.<span class="title function_ invoke__">pop</span>().<span class="title function_ invoke__">unwrap</span>()); <span class="comment">// 20</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 最小堆示例（使用 Reverse）</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">min_heap</span> = BinaryHeap::<span class="title function_ invoke__">from</span>([</span><br><span class="line">        <span class="title function_ invoke__">Reverse</span>(<span class="number">10</span>),</span><br><span class="line">        <span class="title function_ invoke__">Reverse</span>(<span class="number">5</span>),</span><br><span class="line">        <span class="title function_ invoke__">Reverse</span>(<span class="number">20</span>),</span><br><span class="line">    ]);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Min: &#123;&#125;&quot;</span>, min_heap.<span class="title function_ invoke__">pop</span>().<span class="title function_ invoke__">unwrap</span>().<span class="number">0</span>); <span class="comment">// 5</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 优先队列应用</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">pq</span> = BinaryHeap::<span class="title function_ invoke__">new</span>();</span><br><span class="line">    pq.<span class="title function_ invoke__">push</span>((<span class="number">1</span>, <span class="string">&quot;Low&quot;</span>));</span><br><span class="line">    pq.<span class="title function_ invoke__">push</span>((<span class="number">10</span>, <span class="string">&quot;High&quot;</span>));</span><br><span class="line">    pq.<span class="title function_ invoke__">push</span>((<span class="number">5</span>, <span class="string">&quot;Medium&quot;</span>));</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span> <span class="keyword">let</span> <span class="variable">Some</span>((priority, task)) = pq.<span class="title function_ invoke__">pop</span>() &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;Task: &#123;&#125; (Priority: &#123;&#125;)&quot;</span>, task, priority);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="集合选择指南"><a href="#集合选择指南" class="headerlink" title="集合选择指南"></a>集合选择指南</h2><table>
<thead>
<tr>
<th>场景</th>
<th>推荐集合</th>
<th>理由</th>
</tr>
</thead>
<tbody><tr>
<td>通用有序集合</td>
<td><code>Vec&lt;T&gt;</code></td>
<td>简单高效，大多数场景首选</td>
</tr>
<tr>
<td>双端队列（FIFO&#x2F;LIFO）</td>
<td><code>VecDeque&lt;T&gt;</code></td>
<td>两端高效操作</td>
</tr>
<tr>
<td>键值存储（无序）</td>
<td><code>HashMap&lt;K,V&gt;</code></td>
<td>平均 O(1) 查询</td>
</tr>
<tr>
<td>键值存储（有序）</td>
<td><code>BTreeMap&lt;K,V&gt;</code></td>
<td>支持范围查询，O(log n) 查询</td>
</tr>
<tr>
<td>去重集合（无序）</td>
<td><code>HashSet&lt;T&gt;</code></td>
<td>快速去重和存在性检查</td>
</tr>
<tr>
<td>去重集合（有序）</td>
<td><code>BTreeSet&lt;T&gt;</code></td>
<td>支持范围查询，有序遍历</td>
</tr>
<tr>
<td>优先队列</td>
<td><code>BinaryHeap&lt;T&gt;</code></td>
<td>高效取最大&#x2F;最小值</td>
</tr>
<tr>
<td>链表操作</td>
<td><code>LinkedList&lt;T&gt;</code></td>
<td>极少使用，除非明确需要</td>
</tr>
</tbody></table>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong>Vec</strong> 是最常用的集合，适合大多数场景</li>
<li><strong>HashMap&#x2F;BTreeMap</strong> 用于键值存储，选择取决于是否需要有序性</li>
<li><strong>HashSet&#x2F;BTreeSet</strong> 用于去重和集合操作</li>
<li><strong>VecDeque</strong> 适合双端队列操作</li>
<li><strong>BinaryHeap</strong> 实现优先队列功能</li>
<li><strong>LinkedList</strong> 应避免使用，实际性能不如 Vec&#x2F;VecDeque</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust异步编程之async与.await</title>
    <url>/2023/10/23/Rust%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B%E4%B9%8Basync%E4%B8%8E-await/</url>
    <content><![CDATA[<p>Rust的异步编程主要用于高并发I&#x2F;O密集型任务；Rust的异步模型基于 Future + Polling，在单线程上即可完成数千个并发任务。<code>async</code>&#x2F;<code>.await</code> 是Rust内置语法，用于让异步函数编写得像同步代码。<code>async</code> 将代码块转化成实现了 <code>Future</code> trait 的状态机。</p>
<span id="more"></span>

<h2 id="1-async-创建-Future"><a href="#1-async-创建-Future" class="headerlink" title="1. async - 创建 Future"></a>1. async - 创建 Future</h2><p><code>async</code> 是用于创建 Future 的语法糖，用来标记一个函数为异步函数。</p>
<h3 id="异步函数（async-fn）"><a href="#异步函数（async-fn）" class="headerlink" title="异步函数（async fn）"></a>异步函数（async fn）</h3><ul>
<li>调用后不会立即执行，而是返回一个 Future</li>
<li>返回值会被自动包装为 Future</li>
<li>异步函数内部可以直接使用 <code>.await</code></li>
<li>异步函数的参数支持所有普通函数的参数类型（包括引用、所有权类型等），但要注意生命周期</li>
</ul>
<h3 id="异步块（async-…-）"><a href="#异步块（async-…-）" class="headerlink" title="异步块（async { … }）"></a>异步块（async { … }）</h3><p><code>async</code> 块可以捕获外部变量（类似闭包）</p>
<ul>
<li><code>async &#123; ... &#125;</code>：默认像闭包捕获，按借用捕获外部变量</li>
<li><code>async move &#123; ... &#125;</code>：把需要的变量按值 <code>move</code> 进 Future，Future 对它们具有所有权</li>
</ul>
<h3 id="异步闭包"><a href="#异步闭包" class="headerlink" title="异步闭包"></a>异步闭包</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 异步函数</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">fn</span> <span class="title function_">fetch_data</span>() <span class="punctuation">-&gt;</span> <span class="type">u32</span> &#123;</span><br><span class="line">    <span class="number">42</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 异步块</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">fut</span> = <span class="keyword">async</span> &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Working...&quot;</span>);</span><br><span class="line">    <span class="number">100</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 异步闭包</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">async_func</span> = <span class="keyword">async</span> |x: <span class="type">i32</span>| x + <span class="number">1</span>;</span><br></pre></td></tr></table></figure>

<h2 id="2-await-等待-Future-完成"><a href="#2-await-等待-Future-完成" class="headerlink" title="2. .await - 等待 Future 完成"></a>2. .await - 等待 Future 完成</h2><p><code>.await</code> 是用于等待 Future 执行完成的操作符；只能在 <code>async</code> 函数或 <code>async</code> 块内部使用。</p>
<p>对 Future 调用 <code>.await</code>，会暂停当前 Future 的执行（并将线程让给其他可执行的 Future），直到被等待的 Future 完成，然后返回其 <code>Output</code> 结果。</p>
<h3 id="await-工作原理"><a href="#await-工作原理" class="headerlink" title=".await 工作原理"></a>.await 工作原理</h3><p>Rust中代码编译扫描时，把每一个可能”挂起”的 <code>.await</code> 看作一个边界，把整个函数拆成几段；同时定义状态机，把后续要继续执行所需的局部变量保存下来。</p>
<h3 id="await-示例"><a href="#await-示例" class="headerlink" title=".await 示例"></a>.await 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 原始示例函数</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">fn</span> <span class="title function_">foo</span>() <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">x</span> = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">y</span> = <span class="keyword">async</span> &#123; <span class="number">2</span> &#125;.<span class="keyword">await</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">let</span> <span class="variable">z</span> = x + y;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">w</span> = <span class="keyword">async</span> &#123; z * <span class="number">10</span> &#125;.<span class="keyword">await</span>;</span><br><span class="line">    </span><br><span class="line">    w</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 翻译的伪代码</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">FooState</span> &#123;</span><br><span class="line">    <span class="comment">// 初始状态</span></span><br><span class="line">    Start,</span><br><span class="line">    <span class="comment">// 正在等待第一个 .await： async &#123; 2 &#125;</span></span><br><span class="line">    AwaitY &#123;</span><br><span class="line">        <span class="comment">// 需要保存仍然会用到的局部变量</span></span><br><span class="line">        x: <span class="type">i32</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="comment">// 正在等待第二个 .await： async &#123; z * 10 &#125;</span></span><br><span class="line">    AwaitW &#123;</span><br><span class="line">        z: <span class="type">i32</span>,</span><br><span class="line">    &#125;,</span><br><span class="line">    Done,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-Future-异步计算的抽象"><a href="#3-Future-异步计算的抽象" class="headerlink" title="3. Future - 异步计算的抽象"></a>3. Future - 异步计算的抽象</h2><p>Future（<code>std::future::Future</code>）是 Rust 异步编程的基础，<code>async</code> 语法的本质是生成 Future，<code>.await</code> 则是触发 Future 执行并等待结果。</p>
<h3 id="Future-Trait-定义"><a href="#Future-Trait-定义" class="headerlink" title="Future Trait 定义"></a>Future Trait 定义</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">trait</span> <span class="title class_">Future</span> &#123;</span><br><span class="line">    <span class="comment">// Future 最终产生的结果类型</span></span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Output</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 驱动 Future 执行：返回 Poll&lt;Output&gt;</span></span><br><span class="line">    <span class="comment">// waker：用于唤醒暂停的 Future</span></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">poll</span>(<span class="keyword">self</span>: Pin&lt;&amp;<span class="keyword">mut</span> <span class="keyword">Self</span>&gt;, cx: &amp;<span class="keyword">mut</span> Context&lt;<span class="symbol">&#x27;_</span>&gt;) <span class="punctuation">-&gt;</span> Poll&lt;<span class="keyword">Self</span>::Output&gt;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Poll 枚举：表示 Future 的执行状态</span></span><br><span class="line"><span class="keyword">enum</span> <span class="title class_">Poll</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="comment">// Future 仍在执行中（需要等待某个事件，如 IO 完成）</span></span><br><span class="line">    Pending,</span><br><span class="line">    <span class="comment">// Future 已完成，返回结果 T</span></span><br><span class="line">    <span class="title function_ invoke__">Ready</span>(T),</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Future-的特性"><a href="#Future-的特性" class="headerlink" title="Future 的特性"></a>Future 的特性</h3><p>Future 是惰性的——定义后不会自动执行，必须通过 <code>poll</code> 方法”驱动”它运行。只有当调用者（通常是异步执行器）调用 <code>poll</code> 时，Future 才会推进执行，直到遇到”需要等待的事件”（如网络请求、文件IO），此时返回 <code>Pending</code> 并暂停，等待被 <code>waker</code> 唤醒后再次执行。</p>
<h2 id="4-执行器（Executor）"><a href="#4-执行器（Executor）" class="headerlink" title="4. 执行器（Executor）"></a>4. 执行器（Executor）</h2><p>要运行一个异步函数（<code>async fn</code>），必须使用一个执行器（Runtime &#x2F; Executor）来驱动它。</p>
<h3 id="执行器使用方式"><a href="#执行器使用方式" class="headerlink" title="执行器使用方式"></a>执行器使用方式</h3><table>
<thead>
<tr>
<th>场景</th>
<th>方式</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td>应用程序入口</td>
<td>使用宏</td>
<td><code>#[tokio::main] async fn main() &#123; ... &#125;</code></td>
</tr>
<tr>
<td>普通函数中</td>
<td>手动创建 Runtime</td>
<td><code>Runtime::new().unwrap().block_on(future)</code></td>
</tr>
<tr>
<td>轻量级&#x2F;测试</td>
<td>使用简单执行器</td>
<td><code>futures::executor::block_on(future)</code></td>
</tr>
</tbody></table>
<h3 id="并发执行多个-Future"><a href="#并发执行多个-Future" class="headerlink" title="并发执行多个 Future"></a>并发执行多个 Future</h3><p><code>async</code>&#x2F;<code>.await</code> 本身是串行的，若要并发执行多个 Future，需使用并发宏：</p>
<ul>
<li><strong><code>tokio::join!</code></strong> &#x2F; **<code>async_std::future::join!</code>**：等待所有 Future 完成（类似 Promise.all）</li>
<li><strong><code>tokio::select!</code></strong> &#x2F; **<code>async_std::future::select!</code>**：等待第一个完成的 Future（类似 Promise.race）</li>
<li>**<code>tokio::try_join!</code>**：并发执行，且所有 Future 均返回 <code>Result</code>，一旦有一个失败则立即返回错误</li>
</ul>
<h2 id="5-运行时（Runtime）"><a href="#5-运行时（Runtime）" class="headerlink" title="5. 运行时（Runtime）"></a>5. 运行时（Runtime）</h2><p>运行时是驱动异步任务（Future）执行的”引擎”，负责线程池管理、任务调度、IO事件复用（如 epoll&#x2F;kqueue）等底层工作。</p>
<h3 id="运行时的三个核心部分"><a href="#运行时的三个核心部分" class="headerlink" title="运行时的三个核心部分"></a>运行时的三个核心部分</h3><ol>
<li><p><strong>Executor（执行器）</strong></p>
<ul>
<li>负责不断调用 <code>Future::poll()</code> 推进任务执行</li>
<li>支持任务调度、任务队列、线程池等</li>
</ul>
</li>
<li><p><strong>Reactor（反应器）</strong></p>
<ul>
<li>基于操作系统的 IO 多路复用机制（epoll&#x2F;Linux、kqueue&#x2F;macOS、IOCP&#x2F;Windows）</li>
<li>当 socket、文件或计时器可用时，唤醒对应任务</li>
</ul>
</li>
<li><p><strong>Concurrency Primitives（并发原语）</strong></p>
<ul>
<li>提供异步锁（如 <code>Mutex</code>）、通道（<code>Channel</code>）、计时器（<code>Timer</code>）等工具，支持多任务安全通信</li>
</ul>
</li>
</ol>
<h3 id="常见运行时库对比"><a href="#常见运行时库对比" class="headerlink" title="常见运行时库对比"></a>常见运行时库对比</h3><table>
<thead>
<tr>
<th>特性</th>
<th>Tokio</th>
<th>async-std</th>
<th>futures</th>
</tr>
</thead>
<tbody><tr>
<td>性能</td>
<td>最高（多线程调度）</td>
<td>中等</td>
<td>轻量，适中</td>
</tr>
<tr>
<td>使用体验</td>
<td>偏工程化、配置丰富</td>
<td>最接近标准库</td>
<td>极简、底层</td>
</tr>
<tr>
<td>生态支持</td>
<td>最强（hyper, tonic, reqwest）</td>
<td>中等</td>
<td>少量核心库</td>
</tr>
<tr>
<td>内部实现</td>
<td>work-stealing + reactor</td>
<td>smol 内核</td>
<td>模块化组件</td>
</tr>
<tr>
<td>适合场景</td>
<td>高并发服务端</td>
<td>快速开发、教学</td>
<td>小型嵌入式、自定义运行时</td>
</tr>
</tbody></table>
<h3 id="Tokio-示例"><a href="#Tokio-示例" class="headerlink" title="Tokio 示例"></a>Tokio 示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> tokio::time::sleep;</span><br><span class="line"><span class="keyword">use</span> std::time::Duration;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[tokio::main]</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="comment">// 并发执行两个任务</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">task1</span> = <span class="keyword">async</span> &#123;</span><br><span class="line">        <span class="title function_ invoke__">sleep</span>(Duration::<span class="title function_ invoke__">from_secs</span>(<span class="number">1</span>)).<span class="keyword">await</span>;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;Task 1 done&quot;</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">let</span> <span class="variable">task2</span> = <span class="keyword">async</span> &#123;</span><br><span class="line">        <span class="title function_ invoke__">sleep</span>(Duration::<span class="title function_ invoke__">from_millis</span>(<span class="number">500</span>)).<span class="keyword">await</span>;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;Task 2 done&quot;</span>);</span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// join! 等待两个任务都完成</span></span><br><span class="line">    tokio::join!(task1, task2);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong><code>async</code></strong> 将函数或代码块转化为 Future</li>
<li><strong><code>.await</code></strong> 用于暂停执行并等待 Future 完成</li>
<li><strong>Future</strong> 是 Rust 异步编程的基础，需要通过执行器来驱动</li>
<li><strong>执行器</strong> 负责调度和运行 Future，支持单线程或多线程并发</li>
<li><strong>运行时</strong> 提供了完整的异步生态，Tokio 是最流行的选择</li>
<li>异步编程适合 I&#x2F;O 密集型任务，可在单线程上实现高并发</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust中的闭包</title>
    <url>/2023/12/10/Rust%E4%B8%AD%E7%9A%84%E9%97%AD%E5%8C%85/</url>
    <content><![CDATA[<p>Rust 的闭包在功能和用途上与其他语言的 lambda 相同，都是在本地定义的小型匿名函数，可以捕获外部变量。</p>
<span id="more"></span>

<h2 id="基本语法"><a href="#基本语法" class="headerlink" title="基本语法"></a>基本语法</h2><p>Rust 闭包的基本定义形式如下：</p>
<ul>
<li><code>|参数列表|</code>：用竖线包裹参数，多个参数用逗号分隔（如 <code>|x, y|</code>）。</li>
<li>函数体：可以是单个表达式（省略 <code>&#123;&#125;</code>），也可以是多个语句（需用 <code>&#123;&#125;</code> 包裹）。</li>
</ul>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">closure_name</span> = |参数列表| <span class="punctuation">-&gt;</span> 返回类型 &#123;</span><br><span class="line">    表达式或语句块</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="基本语法示例"><a href="#基本语法示例" class="headerlink" title="基本语法示例"></a>基本语法示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 无参数，单个表达式（省略花括号）</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">greet</span> = || <span class="built_in">println!</span>(<span class="string">&quot;Hello!&quot;</span>);</span><br><span class="line"><span class="title function_ invoke__">greet</span>();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 有参数，单个表达式</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">add</span> = |x, y| x + y;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">result</span> = <span class="title function_ invoke__">add</span>(<span class="number">2</span>, <span class="number">3</span>); <span class="comment">// result = 5</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 有参数，多个语句（需要花括号）</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">multiply</span> = |x, y| &#123;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Calculating: &#123;&#125; * &#123;&#125;&quot;</span>, x, y);</span><br><span class="line">    x * y</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 指定返回类型</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">divide</span> = |x: <span class="type">i32</span>, y: <span class="type">i32</span>| <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> y == <span class="number">0</span> &#123;</span><br><span class="line">        <span class="number">0</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        x / y</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="类型与特征"><a href="#类型与特征" class="headerlink" title="类型与特征"></a>类型与特征</h2><p>闭包在 Rust 中有独立的匿名类型，并自动实现以下特征之一：</p>
<table>
<thead>
<tr>
<th>特征</th>
<th>捕获方式</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><code>Fn</code></td>
<td>不可变捕获环境</td>
<td>只读访问。可多次调用，捕获的变量仍可在外部使用</td>
</tr>
<tr>
<td><code>FnMut</code></td>
<td>可变捕获环境</td>
<td>可修改捕获的变量。可多次调用，但捕获的变量在外部需为 <code>mut</code></td>
</tr>
<tr>
<td><code>FnOnce</code></td>
<td>转移捕获变量的所有权</td>
<td>使用 <code>move</code> 关键字。只能调用一次（所有权转移后无法再次使用）</td>
</tr>
</tbody></table>
<h3 id="类型与特征示例"><a href="#类型与特征示例" class="headerlink" title="类型与特征示例"></a>类型与特征示例</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">pub</span> <span class="keyword">fn</span> <span class="title function_">closure_test</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">greeting</span> = <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Hi&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// Fn（只读）</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">say_hi</span> = || <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, greeting);</span><br><span class="line">    <span class="title function_ invoke__">say_hi</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;greeting: &#123;&#125;&quot;</span>, greeting);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// FnMut（可变）</span></span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">num</span> = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">add_one</span> = || num += <span class="number">1</span>;</span><br><span class="line">    <span class="title function_ invoke__">add_one</span>();</span><br><span class="line">    <span class="title function_ invoke__">add_one</span>();</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;num: &#123;&#125;&quot;</span>, num);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// FnOnce（移动）</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Rust&quot;</span>);</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">consume</span> = <span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, s);</span><br><span class="line">        <span class="comment">// s 已被消耗</span></span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="title function_ invoke__">consume</span>(); <span class="comment">// 正常</span></span><br><span class="line">    <span class="comment">// consume(); // 编译错误：只能调用一次</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="move-语义"><a href="#move-语义" class="headerlink" title="move 语义"></a>move 语义</h2><p>默认情况下，闭包会根据对变量的操作<strong>自动选择捕获方式</strong>（不可变借用、可变借用），尽量避免所有权转移。而 <code>move</code> 关键字会强制闭包”夺取”捕获变量的所有权（对于非 <code>Copy</code> 类型），或复制变量（对于 <code>Copy</code> 类型）。</p>
<ul>
<li><strong>非 <code>Copy</code> 类型</strong>（如 <code>String</code>、<code>Vec</code>）的所有权具有唯一性，<code>move</code> 会让闭包直接获取其所有权，原变量在闭包外<strong>不可再使用</strong>（所有权已转移）。</li>
<li><strong><code>Copy</code> 类型</strong>在赋值时会自动复制，<code>move</code> 会让闭包获取变量的副本，原变量仍可正常使用。</li>
</ul>
<h3 id="move-与闭包-Trait"><a href="#move-与闭包-Trait" class="headerlink" title="move 与闭包 Trait"></a>move 与闭包 Trait</h3><p><code>move</code> 仅决定闭包如何捕获变量（所有权转移），但闭包最终实现 <code>Fn</code>&#x2F;<code>FnMut</code>&#x2F;<code>FnOnce</code> 中的哪个 trait，取决于闭包如何使用捕获的变量（而非 <code>move</code> 本身）。</p>
<h4 id="move-只读使用-→-实现-Fn"><a href="#move-只读使用-→-实现-Fn" class="headerlink" title="move + 只读使用 → 实现 Fn"></a><code>move</code> + 只读使用 → 实现 <code>Fn</code></h4><p>若 <code>move</code> 闭包仅只读访问捕获的变量（不修改、不消耗），则实现 <code>Fn</code> trait，可多次调用。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line"><span class="comment">// move 捕获 s 的所有权，闭包仅读取 s</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">closure</span> = <span class="keyword">move</span> || <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, s);</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">closure</span>(); <span class="comment">// 正常</span></span><br><span class="line"><span class="title function_ invoke__">closure</span>(); <span class="comment">// 正常（可多次调用，因未消耗 s）</span></span><br><span class="line"><span class="comment">// println!(&quot;&#123;&#125;&quot;, s); // 编译错误：s 的所有权已被转移到闭包中</span></span><br></pre></td></tr></table></figure>

<h4 id="move-修改使用-→-实现-FnMut"><a href="#move-修改使用-→-实现-FnMut" class="headerlink" title="move + 修改使用 → 实现 FnMut"></a><code>move</code> + 修改使用 → 实现 <code>FnMut</code></h4><p>若 <code>move</code> 闭包修改捕获的变量（但不消耗），则实现 <code>FnMut</code> trait，闭包自身需为 <code>mut</code>。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 非 Copy 语义类型，所有权被转移</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">v</span> = <span class="built_in">vec!</span>[<span class="number">1</span>, <span class="number">2</span>];</span><br><span class="line"><span class="comment">// move 捕获 v 的所有权，闭包修改 v</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">closure</span> = <span class="keyword">move</span> || v.<span class="title function_ invoke__">push</span>(<span class="number">3</span>);</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">closure</span>(); <span class="comment">// v 变为 [1, 2, 3]</span></span><br><span class="line"><span class="title function_ invoke__">closure</span>(); <span class="comment">// v 变为 [1, 2, 3, 3]（可多次调用）</span></span><br><span class="line"><span class="comment">// println!(&quot;&#123;&#125;&quot;, v); // 编译错误：v 的所有权已被转移到闭包中</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// ==========================================</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Copy 语义类型，复制一个副本，修改不影响外部</span></span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">num</span> = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">add_one</span> = <span class="keyword">move</span> || num += <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">add_one</span>();</span><br><span class="line"><span class="title function_ invoke__">add_one</span>();</span><br><span class="line"><span class="built_in">println!</span>(<span class="string">&quot;num: &#123;&#125;&quot;</span>, num); <span class="comment">// 输出 0（num 未被修改）</span></span><br></pre></td></tr></table></figure>

<h4 id="move-消耗使用-→-实现-FnOnce"><a href="#move-消耗使用-→-实现-FnOnce" class="headerlink" title="move + 消耗使用 → 实现 FnOnce"></a><code>move</code> + 消耗使用 → 实现 <code>FnOnce</code></h4><p>若 <code>move</code> 闭包消耗捕获的变量（如调用 <code>drop</code> 或转移所有权给其他对象），则实现 <code>FnOnce</code> trait，只能调用一次。</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">test_fnonce</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">s</span> = <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line">    <span class="comment">// move 捕获 s 的所有权，闭包消耗 s（转移给 println!）</span></span><br><span class="line">    <span class="keyword">let</span> <span class="variable">closure</span> = <span class="keyword">move</span> || &#123;</span><br><span class="line">        <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, s);</span><br><span class="line">        s <span class="comment">// 这里 s 被消耗</span></span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    <span class="title function_ invoke__">closure</span>(); <span class="comment">// 正常</span></span><br><span class="line">    <span class="comment">// closure(); // 编译错误：s 已被消耗，闭包只能调用一次</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="典型使用场景"><a href="#典型使用场景" class="headerlink" title="典型使用场景"></a>典型使用场景</h3><p><code>move</code> 主要用于解决生命周期问题：</p>
<ul>
<li><strong>线程间传递闭包</strong>：线程的生命周期是独立的，无法保证外部变量的生命周期能覆盖线程的。此时 <code>move</code> 可将变量所有权转移到线程内部，<strong>避免悬垂引用</strong>。</li>
<li><strong>延长变量生命周期</strong>：当闭包需要”带走”变量并在更晚的时候使用时，<code>move</code> 可确保变量的所有权随闭包一起存在，避免提前销毁。</li>
</ul>
<h2 id="实现原理"><a href="#实现原理" class="headerlink" title="实现原理"></a>实现原理</h2><p>闭包在编译时会被转换为一个匿名结构体类型（即使两个闭包代码完全相同，也会生成不同的类型）。结构体的字段为捕获的变量，实际字段类型取决于捕获方式（借用、所有权）。</p>
<ul>
<li>若<strong>不捕获任何变量</strong>：结构体为零大小（ZST，无字段），不占用内存空间，调用成本极低（编译器可完全优化掉）。</li>
<li>若<strong>捕获多个变量</strong>：结构体字段按声明顺序连续存储（类似普通结构体）。<ul>
<li>捕获引用时：结构体中仅存储变量的引用；</li>
<li>捕获所有权时：结构体存储被捕获变量的实际值（拥有所有权）。</li>
</ul>
</li>
</ul>
<h3 id="闭包与对应的结构体"><a href="#闭包与对应的结构体" class="headerlink" title="闭包与对应的结构体"></a>闭包与对应的结构体</h3><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">x</span> = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">y</span> = <span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;hello&quot;</span>);</span><br><span class="line"><span class="keyword">let</span> <span class="variable">closure</span> = |z| x + z; <span class="comment">// 捕获 x（不可变借用），不捕获 y</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 以上闭包会生成类似下面的</span></span><br><span class="line"><span class="comment">// 匿名结构体，命名为 ClosureType 仅作示例</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">ClosureType</span>&lt;<span class="symbol">&#x27;a</span>&gt; &#123;</span><br><span class="line">    x: &amp;<span class="symbol">&#x27;a</span> <span class="type">i32</span>, <span class="comment">// 捕获 x 的不可变引用（因仅读取 x）</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span>&lt;<span class="symbol">&#x27;a</span>&gt; <span class="built_in">Fn</span>&lt;(<span class="type">i32</span>,)&gt; <span class="keyword">for</span> <span class="title class_">ClosureType</span>&lt;<span class="symbol">&#x27;a</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Output</span> = <span class="type">i32</span>;</span><br><span class="line">    <span class="comment">// call 方法的逻辑即闭包体：x + z</span></span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">call</span>(&amp;<span class="keyword">self</span>, args: (<span class="type">i32</span>,)) <span class="punctuation">-&gt;</span> <span class="type">i32</span> &#123;</span><br><span class="line">        <span class="keyword">self</span>.x + args.<span class="number">0</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 如果闭包捕获了 y 的所有权并转移给其他对象，则实现 FnOnce</span></span><br><span class="line"><span class="keyword">let</span> <span class="variable">closure2</span> = <span class="keyword">move</span> || y; <span class="comment">// 消耗 y</span></span><br><span class="line"><span class="comment">// 对应的结构体</span></span><br><span class="line"><span class="keyword">struct</span> <span class="title class_">ClosureType2</span> &#123;</span><br><span class="line">    y: <span class="type">String</span>, <span class="comment">// 直接存储 String 值</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">impl</span> <span class="title class_">FnOnce</span>&lt;()&gt; <span class="keyword">for</span> <span class="title class_">ClosureType2</span> &#123;</span><br><span class="line">    <span class="keyword">type</span> <span class="title class_">Output</span> = <span class="type">String</span>;</span><br><span class="line">    <span class="keyword">fn</span> <span class="title function_">call_once</span>(<span class="keyword">self</span>, _args: ()) <span class="punctuation">-&gt;</span> <span class="type">String</span> &#123;</span><br><span class="line">        <span class="keyword">self</span>.y <span class="comment">// 所有权转移给调用者</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong>闭包的三个 trait</strong>：<code>Fn</code>（只读）、<code>FnMut</code>（可变）、<code>FnOnce</code>（消耗）由闭包的实际使用方式自动确定</li>
<li><strong><code>move</code> 关键字</strong>：强制转移捕获变量的所有权（非 <code>Copy</code> 类型）或复制（<code>Copy</code> 类型）</li>
<li><strong>编译时优化</strong>：闭包在编译时转换为匿名结构体，零参数闭包可被完全优化</li>
<li><strong>典型应用</strong>：解决生命周期问题，特别是线程间传递数据时</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust智能指针</title>
    <url>/2024/03/12/Rust%E6%99%BA%E8%83%BD%E6%8C%87%E9%92%88/</url>
    <content><![CDATA[<p>Rust的智能指针：内存管理的利器</p>
<p>Rust以其内存安全和无垃圾回收的设计而闻名，而智能指针则是Rust内存管理的重要组成部分。通过智能指针，Rust能在保证内存安全的同时，提供灵活且高效的内存管理机制。</p>
<p>本文将介绍Rust中的几种常见智能指针，帮助你更好地理解Rust如何管理内存以及智能指针如何提升代码的安全性和可读性。</p>
<span id="more"></span>

<h2 id="什么是智能指针？"><a href="#什么是智能指针？" class="headerlink" title="什么是智能指针？"></a>什么是智能指针？</h2><p>智能指针是一个包装类型，它模拟了传统指针的行为，并且提供了一些额外的功能，尤其是与内存管理相关的功能。在Rust中，智能指针通常实现了Drop trait，这意味着它们能够在生命周期结束时自动清理资源。</p>
<p>Rust中的智能指针常见的有以下几种：</p>
<ul>
<li><strong>Box<T></T></strong></li>
<li><strong>Rc<T></T></strong> 和 <strong>Arc<T></T></strong></li>
<li><strong>RefCell<T></T></strong> 和 <strong>Mutex<T></T></strong></li>
</ul>
<ol>
<li>Box<T> — 在堆上分配内存</T></li>
</ol>
<p>Box<T>是Rust最基本的智能指针之一，它用于在堆上分配内存并存储数据。Box通过所有权系统管理堆上的数据，确保在Box离开作用域时自动清理内存。</T></p>
<p><strong>示例</strong></p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">b</span> = <span class="type">Box</span>::<span class="title function_ invoke__">new</span>(<span class="number">5</span>); <span class="comment">// 在堆上分配整数 5</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;b = &#123;&#125;&quot;</span>, b);</span><br><span class="line">&#125; <span class="comment">// 当 b 离开作用域时，堆上的内存会自动被释放</span></span><br></pre></td></tr></table></figure>


<p>在这个例子中，Box::new(5)会在堆上为整数5分配内存，并返回一个指向该内存的Box。当b离开作用域时，Box会自动释放堆上的内存，避免内存泄漏。</p>
<p>使用场景</p>
<ul>
<li>当你想要在堆上存储大对象（例如大型数组或结构体）时，<code>Box&lt;T&gt;</code> 是非常有用的。</li>
<li>当你需要拥有数据的唯一所有权时，Box 是一个简洁的解决方案。</li>
</ul>
<h3 id="2-Rc-lt-T-gt-和-Arc-lt-T-gt-—-引用计数"><a href="#2-Rc-lt-T-gt-和-Arc-lt-T-gt-—-引用计数" class="headerlink" title="2. Rc&lt;T&gt; 和 Arc&lt;T&gt; — 引用计数"></a>2. Rc&lt;T&gt; 和 Arc&lt;T&gt; — 引用计数</h3><p>Rc<T>（Reference Counted）和Arc<T>（Atomic Reference Counted）都实现了引用计数智能指针，用于共享内存。与Box<T>不同，Rc<T>和Arc<T>允许多个所有者共享对数据的引用。</T></T></T></T></T></p>
<ul>
<li><code>Rc&lt;T&gt;</code>：在单线程中使用，性能较高。</li>
<li><code>Arc&lt;T&gt;</code>：线程安全的版本，适用于多线程环境。</li>
</ul>
<h4 id="示例：使用Rc-lt-T-gt"><a href="#示例：使用Rc-lt-T-gt" class="headerlink" title="示例：使用Rc&lt;T&gt;"></a>示例：使用Rc&lt;T&gt;</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::rc::Rc;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">s</span> = Rc::<span class="title function_ invoke__">new</span>(<span class="type">String</span>::<span class="title function_ invoke__">from</span>(<span class="string">&quot;Hello, world!&quot;</span>));</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">s2</span> = Rc::<span class="title function_ invoke__">clone</span>(&amp;s); <span class="comment">// 克隆 Rc，增加引用计数</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;s = &#123;&#125;, s2 = &#123;&#125;&quot;</span>, s, s2);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Reference count: &#123;&#125;&quot;</span>, Rc::<span class="title function_ invoke__">strong_count</span>(&amp;s)); <span class="comment">// 打印引用计数</span></span><br><span class="line">&#125; <span class="comment">// 当所有 Rc 指针离开作用域时，内存会被释放</span></span><br></pre></td></tr></table></figure>

<p>使用场景</p>
<ul>
<li><code>Rc&lt;T&gt;</code> 适合用于需要多个所有者共享数据的场景，且只在单线程环境中使用。</li>
<li><code>Arc&lt;T&gt;</code> 适用于多线程环境，允许多个线程共享数据，确保线程安全。</li>
</ul>
<h3 id="3-RefCell-lt-T-gt-和-Mutex-lt-T-gt-—-可变性与内部可变性"><a href="#3-RefCell-lt-T-gt-和-Mutex-lt-T-gt-—-可变性与内部可变性" class="headerlink" title="3. RefCell&lt;T&gt; 和 Mutex&lt;T&gt; — 可变性与内部可变性"></a>3. RefCell&lt;T&gt; 和 Mutex&lt;T&gt; — 可变性与内部可变性</h3><p>虽然Rust的所有权和借用系统通常不允许同时存在多个可变引用，但通过智能指针，我们可以绕过这一限制，提供内部可变性。</p>
<ul>
<li><code>RefCell&lt;T&gt;</code>：允许在单线程中进行可变借用，提供运行时的借用检查。</li>
<li><code>Mutex&lt;T&gt;</code>：提供多线程中的内部可变性，确保数据在多个线程之间的安全共享。</li>
</ul>
<h4 id="示例：使用RefCell-lt-T-gt"><a href="#示例：使用RefCell-lt-T-gt" class="headerlink" title="示例：使用RefCell&lt;T&gt;"></a>示例：使用RefCell&lt;T&gt;</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::cell::RefCell;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">x</span> = RefCell::<span class="title function_ invoke__">new</span>(<span class="number">5</span>);</span><br><span class="line">    *x.<span class="title function_ invoke__">borrow_mut</span>() = <span class="number">10</span>; <span class="comment">// 可变借用</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;x = &#123;&#125;&quot;</span>, x.<span class="title function_ invoke__">borrow</span>()); <span class="comment">// 不可变借用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<p>在这个例子中，RefCell允许我们通过borrow_mut()方法进行可变借用，并在运行时检查借用规则。</p>
<h4 id="示例：使用Mutex-lt-T-gt"><a href="#示例：使用Mutex-lt-T-gt" class="headerlink" title="示例：使用Mutex&lt;T&gt;"></a>示例：使用Mutex&lt;T&gt;</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::sync::Mutex;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">m</span> = Mutex::<span class="title function_ invoke__">new</span>(<span class="number">5</span>);</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">num</span> = m.<span class="title function_ invoke__">lock</span>().<span class="title function_ invoke__">unwrap</span>(); <span class="comment">// 获取锁</span></span><br><span class="line">        *num = <span class="number">10</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;m = &#123;&#125;&quot;</span>, m.<span class="title function_ invoke__">lock</span>().<span class="title function_ invoke__">unwrap</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Mutex<T>确保在多线程环境中，只有一个线程能访问数据，避免数据竞争。</T></p>
<p><strong>使用场景</strong></p>
<ul>
<li><code>RefCell&lt;T&gt;</code> 适合用于需要动态检查和管理可变借用的单线程应用。</li>
<li><code>Mutex&lt;T&gt;</code> 用于多线程环境下，确保数据的线程安全访问。</li>
</ul>
<h3 id="4-Cell-lt-T-gt-—-简单的内部可变性"><a href="#4-Cell-lt-T-gt-—-简单的内部可变性" class="headerlink" title="4. Cell&lt;T&gt; — 简单的内部可变性"></a>4. Cell&lt;T&gt; — 简单的内部可变性</h3><p>Cell<T>提供了一种非常轻量级的方式来实现内部可变性，适用于无需引用的情况下存储数据。它通常用于存储基本类型数据，如整数、布尔值等。</T></p>
<h4 id="示例：使用Cell-lt-T-gt"><a href="#示例：使用Cell-lt-T-gt" class="headerlink" title="示例：使用Cell&lt;T&gt;"></a>示例：使用Cell&lt;T&gt;</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> std::cell::Cell;</span><br><span class="line"></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">x</span> = Cell::<span class="title function_ invoke__">new</span>(<span class="number">5</span>);</span><br><span class="line">    x.<span class="title function_ invoke__">set</span>(<span class="number">10</span>);</span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;x = &#123;&#125;&quot;</span>, x.<span class="title function_ invoke__">get</span>());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用场景</p>
<p><code>Cell&lt;T&gt;</code> 适合在单线程中提供简单的可变数据存储，尤其是对于基础数据类型。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>Rust的智能指针提供了强大的内存管理能力，它们在确保内存安全的同时，也使得我们能够更灵活地操作数据。在Rust中，智能指针不仅仅是内存管理的工具，更是借用和所有权系统的核心组成部分，它们帮助开发者避免内存泄漏和数据竞争的问题。</p>
<ul>
<li>**Box<T>**：适用于堆分配数据，拥有唯一所有权。</T></li>
<li><strong>Rc<T></T></strong> 和 **Arc<T>**：适用于共享数据，提供引用计数。</T></li>
<li><strong>RefCell<T></T></strong> 和 **Mutex<T>**：提供内部可变性，支持单线程和多线程场景。</T></li>
<li>**Cell<T>**：适用于简单类型的数据存储，支持内部可变性。</T></li>
</ul>
<p>通过合理选择合适的智能指针，我们可以写出既高效又安全的Rust程序。</p>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>redis 相关知识</title>
    <url>/2023/05/24/redis-%E7%9B%B8%E5%85%B3%E7%9F%A5%E8%AF%86/</url>
    <content><![CDATA[<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p>Redis是一个高性能的key-value数据库。</p>
<h3 id="Redis优势"><a href="#Redis优势" class="headerlink" title="Redis优势"></a>Redis优势</h3><ul>
<li>性能极高 – Redis能读的速度是110000次&#x2F;s,写的速度是81000次&#x2F;s 。</li>
<li>丰富的数据类型 – Redis支持二进制案例的 Strings, Lists, Hashes, Sets 及 Ordered Sets 数据类型操作。</li>
<li>原子 – Redis的所有操作都是原子性的，意思就是要么成功执行要么失败完全不执行。单个操作是原子性的。多个操作也支持事务，即原子性，通过MULTI和EXEC指令包起来。</li>
<li>丰富的特性 – Redis还支持 publish&#x2F;subscribe, 通知, key 过期等等特性。</li>
</ul>
<span id="more"></span>
<h2 id="redis的数据结构"><a href="#redis的数据结构" class="headerlink" title="redis的数据结构"></a>redis的数据结构</h2><p>Redis支持五中数据类型：String、hash、list、set和zset（有序集合）</p>
<h3 id="String-类型"><a href="#String-类型" class="headerlink" title="String 类型"></a>String 类型</h3><p>String 类型是一个（key&#x3D;&#x3D;&gt;value）的键值对</p>
<blockquote>
<p>set test “test”<br>OK<br>get test<br>“test”</p>
</blockquote>
<p>*注意：一个键最大能存储512MB</p>
<h3 id="Hash（哈希）"><a href="#Hash（哈希）" class="headerlink" title="Hash（哈希）"></a>Hash（哈希）</h3><p>String 类型是一个（key&#x3D;&#x3D;&gt;value）的键值对集合，每个最外层key对应多个（key&#x3D;&#x3D;&gt;value）形式的键值对<br>使用时需要用命令HSET 和HGET命令。</p>
<blockquote>
<p>hset htest h1 “t1” h2 “t2”<br>2<br>hget htest h1<br>“t1”<br>hget htest h2<br>“t2”</p>
</blockquote>
<h3 id="List（列表）"><a href="#List（列表）" class="headerlink" title="List（列表）"></a>List（列表）</h3><p>是一个简单的字符串列表，排序默认按照插入顺序排序<br>需要用命令LPUSH 和LRANGE start end(给出索引起始和结束位置左闭又闭区间)命令。<br> lpush ltest qq<br>(integer) 1</p>
<blockquote>
<p>lpush ltest ll<br>(integer) 2<br>lrange ltest 0 10</p>
</blockquote>
<ol>
<li>“ll”</li>
<li>“qq”</li>
</ol>
<h3 id="set（集合）"><a href="#set（集合）" class="headerlink" title="set（集合）"></a>set（集合）</h3><p>添加一个string对象到对应的set集合中，成功返回1，如果元素已在集合中返回0<br>添加元素使用：sadd，显示所有元素使用 smembers</p>
<blockquote>
<p>sadd stest 1<br>(integer) 1<br>sadd stest 2<br>(integer) 1<br>sadd stest 1<br>(integer) 0<br>smembers stest</p>
</blockquote>
<ol>
<li>“1”</li>
<li>“2”</li>
</ol>
<h3 id="zset（有序集合）"><a href="#zset（有序集合）" class="headerlink" title="zset（有序集合）"></a>zset（有序集合）</h3><p>Redis zset 和 set 一样也是string类型元素的集合,且不允许重复的成员。<br>不同的是每个元素都会关联一个double类型的分数。redis正是通过分数来为集合中的成员进行从小到大的排序。<br>zset的成员是唯一的,但分数(score)却可以重复。<br>需要使用命令</p>
<blockquote>
<p>zadd ztest 0 t1<br>(integer) 1<br>zadd ztest 1 t2<br>(integer) 1<br>zadd ztest 0 t3<br>(integer) 1<br>zrangebyscore ztest 0 3</p>
</blockquote>
<ol>
<li>“t1”</li>
<li>“t3”</li>
<li>“t2”</li>
</ol>
<h2 id="redis持久化"><a href="#redis持久化" class="headerlink" title="redis持久化"></a>redis持久化</h2><p>为什么需要持久化？<br>Redis的数据都存储在内存中，若没有配置持久化，redis重启后数据就丢失了，于是需要开启redis的持久化功能，将数据保存到磁盘上，重启后从磁盘恢复数据。redis提供了两种持久化方式：1.RDB持久化（定时将redis在内存中的数据生成快照，全量同步到磁盘）。2.AOF（append only file）持久化（将redis的操作日志以追加的方式写入文件）。</p>
<ol>
<li><p>RDB持久化<br>过程：在指定时间间隔内将内存中的数据集快照写入磁盘，实际操作过程就是fork一个子进程，先将数据集写入临时文件，写入成功后，再替换之前的文件，用于二进制压缩存储。<br><a href="https://imgse.com/i/pCtU9i9"><img src="https://s1.ax1x.com/2023/06/24/pCtU9i9.png" alt="pCtU9i9.png"></a><br>优点：<br>1.redis只会包含一个文件，便于文件备份。可以配置每小时归档最近24小时的数据，同时每天归档最近30天的数据，通过这样的策略，一旦系统出现灾难性故障，可以很容易的进行恢复。<br>2.RDB适合于灾难恢复，可以很轻松的将一个单独的文件压缩后再转移到其他存储介质上。<br>3.性能最大化。fork出一个子进程完成持久化操作，避免了单个线程的IO问题。<br>4.相比于AOF机制，如果数据集很大，RDB的启动效率更高。<br>缺点：<br>1.无法保证数据的高可用性，即最大限度的避免数据丢失。如果在持久化之前出现宕机现象，此前未来得及写入磁盘的数据都将丢失。<br>2.fork子进程完成持久化操作时，如果数据集较大时，可能会导致服务器停止服务几百毫秒到1秒。</p>
</li>
<li><p>AOF持久化<br>过程：AOF以日志的形式来记录每个写操作，将redis执行过的所有写指令记录下来，只许追加文件但不允许改写文件，redis启动之后就会读取该文件重构数据集。默认是不开启的，需要将appendonly改为yes就开启了aof，重启后生效。<br><a href="https://imgse.com/i/pCtUCGR"><img src="https://s1.ax1x.com/2023/06/24/pCtUCGR.png" alt="pCtUCGR.png"></a><br>优点：<br>1.更高的数据安全性，保证数据在redis重启前后是一致的。<br>2.日志写操作采用的是append方式，写入过程中出现宕机也不会破坏日志文件中已存在的内容。<br>3.如果日志过大，redis会自动启用rewrite机制。即Redis以append模式不断的将修改数据写入到老的磁盘文件中，同时Redis还会创建一个新的文件用于记录此期间有哪些修改命令被执行。因此在进行rewrite切换时可以更好的保证数据安全性。<br>4.AOF包含一个格式清晰、易于理解的日志文件用于记录所有的修改操作。事实上，我们也可以通过该文件完成数据的重建。<br>缺点：<br>1.对于相同数量的数据集而言，AOF文件通常要大于RDB文件。RDB 在恢复大数据集时的速度比 AOF 的恢复速度要快。<br>2.根据同步策略的不同，AOF在运行效率上往往会慢于RDB。总之，每秒同步策略的效率是比较高的，同步禁用策略的效率和RDB一样高效。</p>
</li>
</ol>
<h2 id="Redis集群"><a href="#Redis集群" class="headerlink" title="Redis集群"></a>Redis集群</h2><p>1.主从模式<br>至少需要两台服务器，一台主节点（master），一台从节点（slave）。master主要负责写，slave主要负责读，主从模式实现了读写分离。<br><a href="https://imgse.com/i/pCtUkM6"><img src="https://s1.ax1x.com/2023/06/24/pCtUkM6.png" alt="pCtUkM6.png"></a><br>主从复制的作用：<br>数据冗余：实现了数据的热备份，是持久化之外的另一种数据冗余方式<br>故障恢复：master故障时，slave可以提供服务，实现故障快速恢复<br>负载均衡：master负责写，slave负责读。在写少读多的场景下可以极大提高redis吞吐量<br>高可用基石：主从复制是redis哨兵模式和集群模式的基础。<br>主从复制实现原理：<br>主从复制过程主要可以分为3个阶段：连接建立阶段、数据同步阶段、命令传播阶段。<br>连接建立阶段：在主从节点之间建立连接，为数据同步做准备。<br>数据同步阶段：执行数据的全量（或增量）复制（复制RDB文件）<br>命令传播阶段：主节点将已执行的命令发送给从节点，从节点接收命令并执行，从而实现主从节点的数据一致性<br>主从模式中，一个主节点可以有多个从节点。为了减少主从复制对主节点的性能影响，一个从节点可以作为另外一个从节点的主节点进行主从复制。<br>不足之处：主节点宕机之后，需要手动拉起从节点来提供业务，不能达到高可用。</p>
<p>2.哨兵模式<br><a href="https://imgse.com/i/pCtUZZD"><img src="https://s1.ax1x.com/2023/06/24/pCtUZZD.png" alt="pCtUZZD.png"></a><br>哨兵模式的原理<br>Sentinel是一种特殊的redis节点，每个sentinel节点会维护与其他redis节点（包括master&#x2F;slave&#x2F;sentinel）的心跳。<br>当一个sentinel节点与master节点的心跳丢失时，这个sentinel节点就会认为master节点出现了故障，处于不可用的状态，这种判定叫作主观下线（即sentinel节点自己主观认为master下线了），之后，这个sentinel节点会与其他sentinel节点交换信息，如果发现认为主节点发生故障的sentinel节点的个数超过了某个阈值（通常为sentinel节点总数的1&#x2F;2+1，即超过半数），则sentinel会认为master节点已经处于客观下线的状态，即大家都认为master故障不可用了。<br>之后，sentinel节点中会选举处一个sentinel leader来执行redis主节点的故障转移。<br>被选举出的 Sentinel 领导者进行故障转移的具体步骤如下：<br>（1）在从节点列表中选出一个节点作为新的主节点</p>
<ul>
<li>过滤不健康或者不满足要求的节点；</li>
<li>选择 slave-priority（优先级）最高的从节点， 如果存在则返回， 不存在则继续；</li>
<li>选择复制偏移量最大的从节点 ， 如果存在则返回， 不存在则继续；</li>
<li>选择 runid 最小的从节点。<br>（2）Sentinel 领导者节点会对选出来的从节点执行 slaveof no one 命令让其成为主节点。<br>（3）Sentinel 领导者节点会向剩余的从节点发送命令，让他们从新的主节点上复制数据。<br>（4）Sentinel 领导者会将原来的主节点更新为从节点， 并对其进行监控， 当其恢复后命令它去复制新的主节点。</li>
</ul>
<p>3.集群模式<br>如图所示集群模式其实就是多个主从复制的结构组合起来的，每一个主从复制结构可以看成一个节点，那么上面的Cluster集群中就有三个节点。<br><a href="https://imgse.com/i/pCtUmIH"><img src="https://s1.ax1x.com/2023/06/24/pCtUmIH.png" alt="pCtUmIH.png"></a></p>
]]></content>
      <tags>
        <tag>Redis</tag>
      </tags>
  </entry>
  <entry>
    <title>rust中的Cargo.toml文件</title>
    <url>/2023/08/28/rust%E4%B8%AD%E7%9A%84Cargo-toml%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<p><code>Cargo.toml</code>又称清单（manifest），是 Rust 项目的核心配置文件，采用TOML格式（Tom’s Obvious Minimal Language）。它定义了一个包（crate）的：</p>
<ul>
<li>名称、版本、作者、许可证</li>
<li>依赖项</li>
<li>编译特性</li>
<li>目标配置</li>
<li>构建脚本</li>
<li>工作区设置等</li>
</ul>
<span id="more"></span>

<h2 id="1-Cargo-toml-vs-Cargo-lock"><a href="#1-Cargo-toml-vs-Cargo-lock" class="headerlink" title="1. Cargo.toml vs Cargo.lock"></a>1. Cargo.toml vs Cargo.lock</h2><table>
<thead>
<tr>
<th>对比项</th>
<th>Cargo.toml</th>
<th>Cargo.lock</th>
</tr>
</thead>
<tbody><tr>
<td>作用</td>
<td>源配置（声明意图）</td>
<td>锁定文件（记录实际依赖版本）</td>
</tr>
<tr>
<td>手动编辑</td>
<td>是（开发者编写&#x2F;修改）</td>
<td>否（Cargo自动生成&#x2F;更新）</td>
</tr>
<tr>
<td>提交 Git</td>
<td>需提交</td>
<td>库的不需提交；二进制的需提交</td>
</tr>
<tr>
<td>核心内容</td>
<td>项目元信息、依赖、构建规则</td>
<td>精确的依赖版本树、校验和</td>
</tr>
</tbody></table>
<h2 id="2-基础结构"><a href="#2-基础结构" class="headerlink" title="2. 基础结构"></a>2. 基础结构</h2><p>Cargo.toml 按「区段（Section）」组织，每个区段用 <code>[区段名]</code> 标识，核心区段包括：</p>
<ul>
<li>**<code>[package]</code>**：定义项目元信息（必选）</li>
<li>**<code>[dependencies]</code>**：生产依赖（必选&#x2F;可选，视项目类型）</li>
<li>**<code>[dev-dependencies]</code>**：开发依赖（测试、文档测试用）</li>
<li>**<code>[build-dependencies]</code>**：构建脚本依赖</li>
<li>**<code>[lib]</code>**：库目标配置</li>
<li>**<code>[[bin]]</code>**：二进制目标配置（支持多个）</li>
<li>**<code>[profile.*]</code>**：构建配置文件（编译优化、调试信息等）</li>
<li>**<code>[features]</code>**：特性开关（条件编译、可选依赖）</li>
<li>**<code>[workspace]</code>**：工作空间配置（多包项目）</li>
</ul>
<h3 id="2-1-package-元信息"><a href="#2-1-package-元信息" class="headerlink" title="2.1 package 元信息"></a>2.1 package 元信息</h3><p>定义项目的基本信息。</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
<th>值类型</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>name</code></td>
<td>项目名称（crates.io唯一）</td>
<td>字符串：小写字母+连字符</td>
<td><code>name = &quot;my-rust-project&quot;</code></td>
</tr>
<tr>
<td><code>version</code></td>
<td>版本号（语义化版本）</td>
<td>遵循 MAJOR.MINOR.PATCH</td>
<td><code>version = &quot;0.1.0&quot;</code></td>
</tr>
<tr>
<td><code>edition</code></td>
<td>Rust版本 edition</td>
<td>支持 2018&#x2F;2021&#x2F;2024</td>
<td><code>edition = &quot;2024&quot;</code></td>
</tr>
<tr>
<td><code>maintainers</code></td>
<td>维护者信息（替代 authors，推荐）</td>
<td>数组，格式 [“姓名 &lt;邮箱&gt;”]</td>
<td><code>maintainers = [&quot;Bob &lt;bob@example.com&gt;&quot;]</code></td>
</tr>
<tr>
<td><code>description</code></td>
<td>项目描述（crates.io展示）</td>
<td>字符串，简洁明了（≤100字符）</td>
<td><code>description = &quot;A simple Rust utility&quot;</code></td>
</tr>
<tr>
<td><code>license</code></td>
<td>开源许可证（SPDX标识符）</td>
<td>字符串，如 MIT、Apache-2.0、GPL-3.0</td>
<td><code>license = &quot;MIT&quot;</code></td>
</tr>
<tr>
<td><code>license-file</code></td>
<td>自定义许可证文件路径</td>
<td>字符串，当 license 无法用 SPDX 标识时使用</td>
<td><code>license-file = &quot;LICENSE.txt&quot;</code></td>
</tr>
<tr>
<td><code>repository</code></td>
<td>代码仓库地址</td>
<td>字符串（Git地址或网页链接）</td>
<td><code>repository = &quot;https://github.com/foo/bar&quot;</code></td>
</tr>
<tr>
<td><code>homepage</code></td>
<td>项目主页</td>
<td>字符串（HTTP&#x2F;HTTPS链接）</td>
<td><code>homepage = &quot;https://foo.example.com&quot;</code></td>
</tr>
<tr>
<td><code>readme</code></td>
<td>README文件路径</td>
<td>字符串，默认 README.md&#x2F;README.rst，可省略路径</td>
<td><code>readme = &quot;README.md&quot;</code></td>
</tr>
<tr>
<td><code>keywords</code></td>
<td>搜索关键词（crates.io用）</td>
<td>字符串数组，最多5个</td>
<td><code>keywords = [&quot;rust&quot;, &quot;utility&quot;, &quot;cli&quot;]</code></td>
</tr>
<tr>
<td><code>categories</code></td>
<td>分类（crates.io用）</td>
<td>字符串数组，需从官方分类选择</td>
<td><code>categories = [&quot;command-line-utilities&quot;]</code></td>
</tr>
<tr>
<td><code>exclude</code></td>
<td>构建&#x2F;发布时排除的文件</td>
<td>字符串数组（支持glob模式）</td>
<td><code>exclude = [&quot;docs/*&quot;, &quot;tests/*.txt&quot;]</code></td>
</tr>
<tr>
<td><code>include</code></td>
<td>构建&#x2F;发布时强制包含的文件</td>
<td>字符串数组（优先级高于exclude）</td>
<td><code>include = [&quot;src/**/*&quot;, &quot;Cargo.toml&quot;]</code></td>
</tr>
<tr>
<td><code>publish</code></td>
<td>是否允许发布到 crates.io</td>
<td>布尔值（true&#x2F;false）</td>
<td><code>publish = false</code>（私有项目）</td>
</tr>
<tr>
<td><code>rust-version</code></td>
<td>最低支持的 Rust 版本</td>
<td>字符串（如 1.70.0）</td>
<td><code>rust-version = &quot;1.70.0&quot;</code></td>
</tr>
</tbody></table>
<h3 id="2-2-dependencies-普通依赖"><a href="#2-2-dependencies-普通依赖" class="headerlink" title="2.2 dependencies 普通依赖"></a>2.2 dependencies 普通依赖</h3><p>定义项目的运行时依赖，支持多种版本约束语法。</p>
<h4 id="版本约束语法"><a href="#版本约束语法" class="headerlink" title="版本约束语法"></a>版本约束语法</h4><table>
<thead>
<tr>
<th>语法</th>
<th>说明</th>
<th>示例</th>
<th>范围</th>
</tr>
</thead>
<tbody><tr>
<td><code>^x.y.z</code></td>
<td>兼容更新（默认，推荐）</td>
<td><code>serde = &quot;^1.0.188&quot;</code></td>
<td>1.0.188 ≤ 版本 &lt; 2.0.0</td>
</tr>
<tr>
<td><code>~x.y.z</code></td>
<td>补丁更新（仅允许PATCH版本升级）</td>
<td><code>rand = &quot;~0.8.5&quot;</code></td>
<td>0.8.5 ≤ 版本 &lt; 0.9.0</td>
</tr>
<tr>
<td><code>&gt;=x.y.z</code></td>
<td>最低版本（允许所有更高版本）</td>
<td><code>tokio = &quot;&gt;=1.20.0&quot;</code></td>
<td>1.20.0及以上</td>
</tr>
<tr>
<td><code>=x.y.z</code></td>
<td>精确版本（禁止任何升级）</td>
<td><code>log = &quot;=0.4.20&quot;</code></td>
<td>仅0.4.20</td>
</tr>
<tr>
<td><code>*</code></td>
<td>任意版本（不推荐，风险高）</td>
<td><code>foo = &quot;*&quot;</code></td>
<td>所有版本</td>
</tr>
</tbody></table>
<h4 id="依赖声明方式"><a href="#依赖声明方式" class="headerlink" title="依赖声明方式"></a>依赖声明方式</h4><figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="comment"># 简洁版本约束（等价于 ^1.0）</span></span><br><span class="line"><span class="attr">serde</span> = <span class="string">&quot;1.0&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 明确指定版本约束</span></span><br><span class="line"><span class="attr">rand</span> = <span class="string">&quot;=0.8.5&quot;</span>              <span class="comment"># 精确版本</span></span><br><span class="line"><span class="attr">tokio</span> = <span class="string">&quot;~1.0&quot;</span>               <span class="comment"># 允许patch版本更新（1.0 &lt;= version &lt; 1.1）</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用特性（features）</span></span><br><span class="line"><span class="attr">serde</span> = &#123; version = <span class="string">&quot;1.0&quot;</span>, features = [<span class="string">&quot;derive&quot;</span>] &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 依赖来自Git仓库</span></span><br><span class="line"><span class="attr">custom-fork</span> = &#123; git = <span class="string">&quot;https://github.com/user/repo&quot;</span>, branch = <span class="string">&quot;fix&quot;</span> &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># 本地路径依赖</span></span><br><span class="line"><span class="attr">my-lib</span> = &#123; path = <span class="string">&quot;../my-lib&quot;</span> &#125;</span><br></pre></td></tr></table></figure>

<h4 id="其他依赖类型"><a href="#其他依赖类型" class="headerlink" title="其他依赖类型"></a>其他依赖类型</h4><ul>
<li>**<code>[dev-dependencies]</code>**：仅在 <code>cargo test</code>、<code>cargo bench</code> 等开发模式下使用</li>
<li>**<code>[build-dependencies]</code>**：在 <code>build.rs</code> 文件中使用的依赖</li>
<li>**<code>[target.&#39;cfg(windows)&#39;.dependencies]</code> &#x2F; <code>[target.&#39;cfg(unix)&#39;.dependencies]</code>**：指定特定平台依赖</li>
</ul>
<h3 id="2-3-Features-特性"><a href="#2-3-Features-特性" class="headerlink" title="2.3 Features 特性"></a>2.3 Features 特性</h3><p>Features 是 Cargo 提供的一种条件编译机制。</p>
<h4 id="特性配置"><a href="#特性配置" class="headerlink" title="特性配置"></a>特性配置</h4><figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[features]</span></span><br><span class="line"><span class="attr">default</span> = [<span class="string">&quot;tls-rustls&quot;</span>]     <span class="comment"># 默认启用的特性</span></span><br><span class="line"><span class="attr">tls-rustls</span> = [<span class="string">&quot;rustls&quot;</span>, <span class="string">&quot;reqwest-rustls&quot;</span>]  <span class="comment"># 启用Rustls TLS实现</span></span><br><span class="line"><span class="attr">tls-native</span> = [<span class="string">&quot;native-tls&quot;</span>, <span class="string">&quot;reqwest-native&quot;</span>]  <span class="comment"># 启用系统TLS实现</span></span><br><span class="line"><span class="attr">compression</span> = [<span class="string">&quot;dep:flate2&quot;</span>] <span class="comment"># 依赖其他包的特性</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 空特性（仅用于条件编译，不关联依赖）</span></span><br><span class="line"><span class="attr">feat1</span> = []</span><br><span class="line"></span><br><span class="line"><span class="comment"># 特性继承（feat2 启用时，自动启用 feat1 和 &quot;advanced-dep&quot; 依赖）</span></span><br><span class="line"><span class="attr">feat2</span> = [<span class="string">&quot;feat1&quot;</span>, <span class="string">&quot;dep:advanced-dep&quot;</span>]</span><br></pre></td></tr></table></figure>

<h4 id="使用特性"><a href="#使用特性" class="headerlink" title="使用特性"></a>使用特性</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 启用指定特性</span></span><br><span class="line">cargo build --features <span class="string">&quot;async,json&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 关闭默认特性</span></span><br><span class="line">cargo build --no-default-features</span><br><span class="line"></span><br><span class="line"><span class="comment"># 启用所有特性</span></span><br><span class="line">cargo build --all-features</span><br></pre></td></tr></table></figure>

<h4 id="代码中使用特性"><a href="#代码中使用特性" class="headerlink" title="代码中使用特性"></a>代码中使用特性</h4><figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 仅当 &quot;feat1&quot; 特性启用时编译此模块</span></span><br><span class="line"><span class="meta">#[cfg(feature = <span class="string">&quot;feat1&quot;</span>)]</span></span><br><span class="line"><span class="keyword">mod</span> feat1_module &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 特性启用时执行不同逻辑</span></span><br><span class="line"><span class="keyword">fn</span> <span class="title function_">do_something</span>() &#123;</span><br><span class="line">    <span class="meta">#[cfg(feature = <span class="string">&quot;feat2&quot;</span>)]</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Feat2 is enabled!&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="meta">#[cfg(not(feature = <span class="string">&quot;feat2&quot;</span>))]</span></span><br><span class="line">    <span class="built_in">println!</span>(<span class="string">&quot;Feat2 is disabled!&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-目标配置"><a href="#3-目标配置" class="headerlink" title="3. 目标配置"></a>3. 目标配置</h2><p>Rust项目支持两种目标类型：库（lib）和二进制可执行文件（bin）。</p>
<h3 id="3-1-lib-库配置"><a href="#3-1-lib-库配置" class="headerlink" title="3.1 lib 库配置"></a>3.1 lib 库配置</h3><p>仅当项目是库（或同时是库+二进制）时需要，定义库的构建规则。</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>name</code></td>
<td>库名称（默认与 package.name一致）</td>
<td><code>name = &quot;my-lib&quot;</code></td>
</tr>
<tr>
<td><code>path</code></td>
<td>库源文件路径（默认 src&#x2F;lib.rs）</td>
<td><code>path = &quot;src/my_lib.rs&quot;</code></td>
</tr>
<tr>
<td><code>crate_type</code></td>
<td>生成的库类型（支持多个）</td>
<td><code>crate_type = [&quot;rlib&quot;, &quot;cdylib&quot;]</code></td>
</tr>
<tr>
<td><code>crate_name</code></td>
<td>编译后的 crate 名称（链接时使用）</td>
<td><code>crate_name = &quot;my_custom_lib&quot;</code></td>
</tr>
</tbody></table>
<h4 id="库类型说明"><a href="#库类型说明" class="headerlink" title="库类型说明"></a>库类型说明</h4><ul>
<li>**<code>rlib</code>**：Rust静态库（默认，供Rust项目依赖）</li>
<li>**<code>dylib</code>**：Rust动态库（供Rust项目动态链接）</li>
<li>**<code>cdylib</code>**：C兼容动态库（供C&#x2F;C++等其他语言调用）</li>
<li>**<code>staticlib</code>**：C兼容静态库（供其他语言静态链接）</li>
<li>**<code>proc-macro</code>**：过程宏库（仅当项目是过程宏时使用）</li>
</ul>
<h3 id="3-2-bin-二进制配置"><a href="#3-2-bin-二进制配置" class="headerlink" title="3.2 bin 二进制配置"></a>3.2 bin 二进制配置</h3><p>定义可执行文件的构建规则，<code>[[bin]]</code> 是数组语法（<code>[[ ]]</code>），支持多个二进制目标（一个项目可生成多个可执行文件）。</p>
<table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>name</code></td>
<td>可执行文件名称（默认与源文件名称一致）</td>
<td><code>name = &quot;my-cli&quot;</code></td>
</tr>
<tr>
<td><code>path</code></td>
<td>源文件路径（默认 src&#x2F;bin&#x2F;*.rs）</td>
<td><code>path = &quot;src/cli/main.rs&quot;</code></td>
</tr>
<tr>
<td><code>required-features</code></td>
<td>启用该二进制目标所需的特性</td>
<td><code>required-features = [&quot;cli&quot;]</code></td>
</tr>
</tbody></table>
<h4 id="默认约定"><a href="#默认约定" class="headerlink" title="默认约定"></a>默认约定</h4><ul>
<li><strong>单个二进制目标</strong>：源文件放在 <code>src/main.rs</code>，无需配置 <code>[[bin]]</code></li>
<li><strong>多个二进制目标</strong>：源文件放在 <code>src/bin/</code> 目录下（如 <code>src/bin/cli.rs</code>、<code>src/bin/utils.rs</code>），Cargo自动识别，无需配置 <code>[[bin]]</code>（除非需要自定义名称&#x2F;路径）</li>
</ul>
<h4 id="二进制配置示例"><a href="#二进制配置示例" class="headerlink" title="二进制配置示例"></a>二进制配置示例</h4><figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 自定义单个二进制目标（源文件不是 src/main.rs）</span></span><br><span class="line"><span class="section">[[bin]]</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;my-cli&quot;</span></span><br><span class="line"><span class="attr">path</span> = <span class="string">&quot;src/custom_main.rs&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 多个二进制目标（自定义名称和路径）</span></span><br><span class="line"><span class="section">[[bin]]</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;cli&quot;</span></span><br><span class="line"><span class="attr">path</span> = <span class="string">&quot;src/cli.rs&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[[bin]]</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;daemon&quot;</span></span><br><span class="line"><span class="attr">path</span> = <span class="string">&quot;src/daemon.rs&quot;</span></span><br><span class="line"><span class="attr">required-features</span> = [<span class="string">&quot;daemon&quot;</span>] <span class="comment"># 仅当启用 &quot;daemon&quot; 特性时才构建</span></span><br></pre></td></tr></table></figure>

<h2 id="4-profile-构建配置"><a href="#4-profile-构建配置" class="headerlink" title="4. profile.* 构建配置"></a>4. profile.* 构建配置</h2><p>控制编译优化、调试信息、代码生成等规则。Cargo提供5个默认配置文件。</p>
<h3 id="预设配置"><a href="#预设配置" class="headerlink" title="预设配置"></a>预设配置</h3><table>
<thead>
<tr>
<th>Profile</th>
<th>默认设置</th>
<th>opt-level</th>
<th>使用场景</th>
</tr>
</thead>
<tbody><tr>
<td><code>dev</code></td>
<td>开发模式（cargo build默认）</td>
<td>0（无优化）</td>
<td>本地开发、调试（编译快）</td>
</tr>
<tr>
<td><code>release</code></td>
<td>发布模式（cargo build –release）</td>
<td>3（全优化）</td>
<td>生产环境（运行快，编译慢）</td>
</tr>
<tr>
<td><code>test</code></td>
<td>测试模式（cargo test默认）</td>
<td>0</td>
<td>运行测试（兼顾编译速度）</td>
</tr>
<tr>
<td><code>bench</code></td>
<td>基准测试模式（cargo bench）</td>
<td>3</td>
<td>性能测试（需要最优性能）</td>
</tr>
<tr>
<td><code>doc</code></td>
<td>文档模式（cargo doc）</td>
<td>0</td>
<td>生成文档（编译依赖快）</td>
</tr>
</tbody></table>
<h3 id="可配置字段（常用）"><a href="#可配置字段（常用）" class="headerlink" title="可配置字段（常用）"></a>可配置字段（常用）</h3><table>
<thead>
<tr>
<th>字段</th>
<th>说明</th>
<th>可用值</th>
</tr>
</thead>
<tbody><tr>
<td><code>opt-level</code></td>
<td>优化级别</td>
<td>0&#x2F;1&#x2F;2&#x2F;3（优化程度）、s&#x2F;z（体积优化）</td>
</tr>
<tr>
<td><code>debug</code></td>
<td>是否生成调试信息</td>
<td>true&#x2F;false（dev默认为true，release默认为false）</td>
</tr>
<tr>
<td><code>debug-assertions</code></td>
<td>是否启用调试断言</td>
<td>true&#x2F;false（dev默认为true，release默认为false）</td>
</tr>
<tr>
<td><code>strip</code></td>
<td>是否剥离符号信息（减小二进制体积）</td>
<td>“none”、”debuginfo”、”all”</td>
</tr>
<tr>
<td><code>lto</code></td>
<td>链接时优化（LTO，提升性能&#x2F;减小体积）</td>
<td>false&#x2F;true&#x2F;“thin”</td>
</tr>
<tr>
<td><code>codegen-units</code></td>
<td>代码生成单元数量（影响编译速度&#x2F;优化）</td>
<td>整数（默认 dev&#x3D;256，release&#x3D;16）</td>
</tr>
<tr>
<td><code>panic</code></td>
<td>panic处理方式</td>
<td>“unwind”（默认）、”abort”</td>
</tr>
<tr>
<td><code>incremental</code></td>
<td>是否启用增量编译</td>
<td>true&#x2F;false（dev默认为true，release默认为false）</td>
</tr>
</tbody></table>
<h3 id="构建配置示例"><a href="#构建配置示例" class="headerlink" title="构建配置示例"></a>构建配置示例</h3><figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 开发配置：快速编译，便于调试</span></span><br><span class="line"><span class="section">[profile.dev]</span></span><br><span class="line"><span class="attr">opt-level</span> = <span class="number">0</span></span><br><span class="line"><span class="attr">debug</span> = <span class="literal">true</span></span><br><span class="line"><span class="attr">debug-assertions</span> = <span class="literal">true</span></span><br><span class="line"><span class="attr">incremental</span> = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 发布配置：全优化</span></span><br><span class="line"><span class="section">[profile.release]</span></span><br><span class="line"><span class="attr">opt-level</span> = <span class="number">3</span></span><br><span class="line"><span class="attr">debug</span> = <span class="literal">false</span></span><br><span class="line"><span class="attr">debug-assertions</span> = <span class="literal">false</span></span><br><span class="line"><span class="attr">lto</span> = <span class="string">&quot;thin&quot;</span>          <span class="comment"># 增量LTO，比全LTO编译更快</span></span><br><span class="line"><span class="attr">codegen-units</span> = <span class="number">16</span></span><br><span class="line"><span class="attr">strip</span> = <span class="string">&quot;debuginfo&quot;</span>   <span class="comment"># 剥离调试信息，减小体积</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 自定义配置：兼顾性能和编译速度</span></span><br><span class="line"><span class="section">[profile.release-fast]</span></span><br><span class="line"><span class="attr">inherits</span> = <span class="string">&quot;release&quot;</span></span><br><span class="line"><span class="attr">codegen-units</span> = <span class="number">128</span>   <span class="comment"># 降低codegen-units，加快编译</span></span><br><span class="line"><span class="attr">lto</span> = <span class="literal">false</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 体积优化</span></span><br><span class="line"><span class="section">[profile.release-small]</span></span><br><span class="line"><span class="attr">inherits</span> = <span class="string">&quot;release&quot;</span></span><br><span class="line"><span class="attr">opt-level</span> = <span class="string">&quot;z&quot;</span>       <span class="comment"># 极致体积优化</span></span><br><span class="line"><span class="attr">strip</span> = <span class="string">&quot;all&quot;</span>         <span class="comment"># 剥离所有符号</span></span><br></pre></td></tr></table></figure>

<h2 id="5-完整的-Cargo-toml-示例"><a href="#5-完整的-Cargo-toml-示例" class="headerlink" title="5. 完整的 Cargo.toml 示例"></a>5. 完整的 Cargo.toml 示例</h2><figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[package]</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;my-rust-app&quot;</span></span><br><span class="line"><span class="attr">version</span> = <span class="string">&quot;0.1.0&quot;</span></span><br><span class="line"><span class="attr">edition</span> = <span class="string">&quot;2024&quot;</span></span><br><span class="line"><span class="attr">description</span> = <span class="string">&quot;A full-featured Rust application&quot;</span></span><br><span class="line"><span class="attr">license</span> = <span class="string">&quot;MIT&quot;</span></span><br><span class="line"><span class="attr">repository</span> = <span class="string">&quot;https://github.com/foo/my-rust-app&quot;</span></span><br><span class="line"><span class="attr">readme</span> = <span class="string">&quot;README.md&quot;</span></span><br><span class="line"><span class="attr">keywords</span> = [<span class="string">&quot;rust&quot;</span>, <span class="string">&quot;app&quot;</span>, <span class="string">&quot;cli&quot;</span>]</span><br><span class="line"><span class="attr">categories</span> = [<span class="string">&quot;command-line-utilities&quot;</span>]</span><br><span class="line"><span class="attr">rust-version</span> = <span class="string">&quot;1.75.0&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[workspace]</span></span><br><span class="line"><span class="attr">members</span> = [<span class="string">&quot;crates/*&quot;</span>, <span class="string">&quot;cli&quot;</span>]</span><br><span class="line"><span class="attr">default-members</span> = [<span class="string">&quot;cli&quot;</span>]</span><br><span class="line"></span><br><span class="line"><span class="section">[workspace.dependencies]</span></span><br><span class="line"><span class="attr">serde</span> = &#123; version = <span class="string">&quot;1.0&quot;</span>, features = [<span class="string">&quot;derive&quot;</span>] &#125;</span><br><span class="line"><span class="attr">rand</span> = <span class="string">&quot;0.8&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">serde</span> = &#123; workspace = <span class="literal">true</span> &#125;</span><br><span class="line"><span class="attr">tokio</span> = &#123; version = <span class="string">&quot;1.0&quot;</span>, features = [<span class="string">&quot;full&quot;</span>] &#125;</span><br><span class="line"><span class="attr">anyhow</span> = <span class="string">&quot;1.0&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[dev-dependencies]</span></span><br><span class="line"><span class="attr">criterion</span> = <span class="string">&quot;0.5&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[build-dependencies]</span></span><br><span class="line"><span class="attr">prost-build</span> = <span class="string">&quot;0.11&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[features]</span></span><br><span class="line"><span class="attr">default</span> = [<span class="string">&quot;async&quot;</span>]</span><br><span class="line"><span class="attr">async</span> = []</span><br><span class="line"><span class="attr">cli</span> = []</span><br><span class="line"></span><br><span class="line"><span class="section">[[bin]]</span></span><br><span class="line"><span class="attr">name</span> = <span class="string">&quot;my-app&quot;</span></span><br><span class="line"><span class="attr">path</span> = <span class="string">&quot;src/main.rs&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="section">[profile.dev]</span></span><br><span class="line"><span class="attr">opt-level</span> = <span class="number">0</span></span><br><span class="line"><span class="attr">debug</span> = <span class="literal">true</span></span><br><span class="line"><span class="attr">incremental</span> = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="section">[profile.release]</span></span><br><span class="line"><span class="attr">opt-level</span> = <span class="number">3</span></span><br><span class="line"><span class="attr">debug</span> = <span class="literal">false</span></span><br><span class="line"><span class="attr">lto</span> = <span class="string">&quot;thin&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="6-Workspace-工作区"><a href="#6-Workspace-工作区" class="headerlink" title="6. Workspace 工作区"></a>6. Workspace 工作区</h2><p>当项目包含多个crate（如核心库、CLI工具、测试工具）时，用工作空间统一管理依赖和构建。</p>
<h3 id="工作区配置"><a href="#工作区配置" class="headerlink" title="工作区配置"></a>工作区配置</h3><figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 工作空间根目录的 Cargo.toml</span></span><br><span class="line"><span class="section">[workspace]</span></span><br><span class="line"><span class="comment"># 包含的子包（支持 glob 模式）</span></span><br><span class="line"><span class="attr">members</span> = [</span><br><span class="line">    <span class="string">&quot;crates/*&quot;</span>,      <span class="comment"># 所有 crates 目录下的子包</span></span><br><span class="line">    <span class="string">&quot;cli&quot;</span>,           <span class="comment"># 单个子包</span></span><br><span class="line">]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 默认构建的子包（cargo build 时默认构建这些）</span></span><br><span class="line"><span class="attr">default-members</span> = [<span class="string">&quot;crates/core&quot;</span>, <span class="string">&quot;cli&quot;</span>]</span><br><span class="line"></span><br><span class="line"><span class="comment"># 工作空间共享依赖版本（子包可直接引用，避免版本重复）</span></span><br><span class="line"><span class="section">[workspace.dependencies]</span></span><br><span class="line"><span class="attr">serde</span> = &#123; version = <span class="string">&quot;1.0&quot;</span>, features = [<span class="string">&quot;derive&quot;</span>] &#125;</span><br><span class="line"><span class="attr">rand</span> = <span class="string">&quot;0.8&quot;</span></span><br><span class="line"><span class="attr">tokio</span> = <span class="string">&quot;1.0&quot;</span></span><br></pre></td></tr></table></figure>

<h3 id="子包中引用工作区依赖"><a href="#子包中引用工作区依赖" class="headerlink" title="子包中引用工作区依赖"></a>子包中引用工作区依赖</h3><figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 子包（crates/core/Cargo.toml）</span></span><br><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="comment"># 直接引用工作空间定义的依赖版本</span></span><br><span class="line"><span class="attr">serde</span> = &#123; workspace = <span class="literal">true</span> &#125;</span><br><span class="line"><span class="attr">rand</span> = &#123; workspace = <span class="literal">true</span>, features = [<span class="string">&quot;small_rng&quot;</span>] &#125; <span class="comment"># 追加特性</span></span><br><span class="line"><span class="attr">tokio</span> = &#123; workspace = <span class="literal">true</span>, features = [<span class="string">&quot;rt&quot;</span>] &#125;</span><br></pre></td></tr></table></figure>

<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong>package</strong>：定义项目元信息、版本、许可证等</li>
<li><strong>dependencies</strong>：管理项目依赖，支持crates.io、Git、本地路径多种来源</li>
<li><strong>features</strong>：实现条件编译和可选依赖</li>
<li>**[lib] 和 [[bin]]**：定义库和二进制目标</li>
<li>**profile.***：控制编译优化策略</li>
<li><strong>workspace</strong>：多包项目的统一管理</li>
<li><strong>Cargo.lock</strong>：记录精确的依赖版本，二进制项目应提交到Git</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>什么是以太坊</title>
    <url>/2025/08/02/%E4%BB%80%E4%B9%88%E6%98%AF%E4%BB%A5%E5%A4%AA%E5%9D%8A/</url>
    <content><![CDATA[<h2 id="什么是以太坊"><a href="#什么是以太坊" class="headerlink" title="什么是以太坊"></a>什么是以太坊</h2><p>以太坊是一个 Layer 1 区块链，它作为一个开源的、分布式计算平台，用于开发和部署智能合约和去中心化应用（dApps）。它扩展了比特币提出的区块链技术的属性，强调安全性和去中心化，以扩展其用例。</p>
<span id="more"></span>

<p>以太坊最初使用工作量证明（PoW）共识机制，但在 2022 年通过一项名为”The Merge”的升级过渡到权益证明（PoS）系统。这使其能源消耗降低了 99.95%，使以太坊成为一个低碳网络。</p>
<p>以太币（ETH）是市值第二大的加密货币，是用以太坊区块链上交易的原生代币。</p>
<h2 id="面向开发者的以太坊核心特性"><a href="#面向开发者的以太坊核心特性" class="headerlink" title="面向开发者的以太坊核心特性"></a>面向开发者的以太坊核心特性</h2><p>以太坊为开发者提供了强大的工具，例如以太坊虚拟机（EVM）和智能合约。这些特性支持创建和部署可互操作的去中心化资产、服务和应用（dApps）。</p>
<p><strong>智能合约</strong>是自动执行的代码，记录在区块链上，并作为 dApps 的支柱。</p>
<p>作为一个去中心化网络，以太坊允许用户保持对其资产和身份的控制。它的点对点（P2P）架构促进了协调、协议和数字资产的转移。</p>
<h2 id="以太坊如何工作"><a href="#以太坊如何工作" class="headerlink" title="以太坊如何工作"></a>以太坊如何工作</h2><h3 id="以太坊中的权益证明"><a href="#以太坊中的权益证明" class="headerlink" title="以太坊中的权益证明"></a>以太坊中的权益证明</h3><p>以太坊区块链作为网络的主干，记录和存储所有交易和智能合约数据。它采用权益证明（PoS）共识机制，该机制要求节点存入并质押 32 个 ETH 才能成为验证者。</p>
<p>从 PoW 的迁移使以太坊能够实施分片链，从而提高数据处理能力和网络吞吐量。</p>
<p>区块链的透明、可靠和抗审查性质确保它没有单点故障，从而提供安全性和弹性。</p>
<h3 id="以太坊虚拟机（EVM）"><a href="#以太坊虚拟机（EVM）" class="headerlink" title="以太坊虚拟机（EVM）"></a>以太坊虚拟机（EVM）</h3><p>EVM 是以太坊的原生处理系统。它规定了创建智能合约的预定义规则，并促进它们与节点之间的无缝交互。</p>
<p>EVM 作为状态机运行，逐块执行脚本，以基于预定规则创建新的有效状态。</p>
<p>当用户执行智能合约时，EVM 会解释合约的固有代码，并使用这些信息来执行指定的任务。EVM 确保智能合约的代码按照指定的方式精确执行。</p>
<h2 id="以太坊的核心概念"><a href="#以太坊的核心概念" class="headerlink" title="以太坊的核心概念"></a>以太坊的核心概念</h2><p>比特币介绍了区块链技术。作为第一个去中心化网络，它解决了双重支付的挑战，这是一个允许系统复制货币的关键流程。此外，比特币使用户能够在没有权威的情况下建立价值。尽管这项创新非常强大，但它将区块链的使用案例限制在少数几个。</p>
<h3 id="智能合约如何使以太坊具有灵活性"><a href="#智能合约如何使以太坊具有灵活性" class="headerlink" title="智能合约如何使以太坊具有灵活性"></a>智能合约如何使以太坊具有灵活性</h3><p>比特币的 PoW 网络难以扩展，限制了其原生代币 BTC 主要用作价值存储。另一方面，以太坊已经升级了当前的互联网基础设施，利用智能合约来自动化日常流程。</p>
<p><strong>以太坊智能合约</strong>是计算机代码，可以促进、执行和验证协议的谈判和交付。换句话说，它们是自动执行的、在以太坊区块链上记录的程序化协议，旨在按照”如果-那么”逻辑运行。</p>
<p>这意味着它们定义了一个动作 Y，如果一个动作 X 的所有子集都满足，那么该动作就会发生。</p>
<p>以太坊的特性允许用户执行更复杂的过程，例如借贷和赚取利息，以及更复杂的金融交易，而无需依赖银行和经纪代理等中介机构。</p>
<h2 id="什么是以太坊-2-0"><a href="#什么是以太坊-2-0" class="headerlink" title="什么是以太坊 2.0"></a>什么是以太坊 2.0</h2><p>以太坊 2.0（或 ETH2）代表以太坊网络的一项重大升级，旨在显著提高可扩展性、安全性、速度和整体效率。引入此升级是为了让以太坊在发展的同时解决其现有的局限性。</p>
<p>被称为”The Merge”的过渡已于 2022 年 9 月成功实施，将原始以太坊区块链与 ETH2 区块链相结合。</p>
<h3 id="为什么以太坊-2-0-是必要的"><a href="#为什么以太坊-2-0-是必要的" class="headerlink" title="为什么以太坊 2.0 是必要的"></a>为什么以太坊 2.0 是必要的</h3><p>将流行的加密资产迁移到新平台并非易事，但对于以太坊来说，适应和发展是必要的。</p>
<p>ETH 1.0 区块链用于交易验证的工作量证明（PoW）方法导致了瓶颈、费用增加和大量资源消耗（尤其是电力）。</p>
<h2 id="以太坊用例"><a href="#以太坊用例" class="headerlink" title="以太坊用例"></a>以太坊用例</h2><p>以太坊的多功能性导致了各个行业中各种实际应用。下面，我们将探讨以太坊区块链的一些最具影响力的用例。</p>
<h3 id="去中心化金融应用（dApp）"><a href="#去中心化金融应用（dApp）" class="headerlink" title="去中心化金融应用（dApp）"></a>去中心化金融应用（dApp）</h3><p>这些协议通过锚定在可编程、无需许可的区块链网络上的智能合约提供金融服务和工具。在提供服务时，它们消除了银行、经纪公司和交易所等中间人。</p>
<h3 id="非同质化代币（NFT）"><a href="#非同质化代币（NFT）" class="headerlink" title="非同质化代币（NFT）"></a>非同质化代币（NFT）</h3><p>这些数字资产无法被替换或复制，并且对于它们所代表的物品来说是独一无二的。它们象征着各种资产的所有权，包括艺术品、房地产、收藏品和游戏内物品。</p>
<h3 id="自主身份（SSI）"><a href="#自主身份（SSI）" class="headerlink" title="自主身份（SSI）"></a>自主身份（SSI）</h3><p>保护在线身份和私人信息对于防止未经授权的访问或身份盗窃至关重要。SSI 提供了一种更安全的方式来存储此类数据，并提供安全共享的条款。</p>
<h3 id="可追溯性和供应链管理"><a href="#可追溯性和供应链管理" class="headerlink" title="可追溯性和供应链管理"></a>可追溯性和供应链管理</h3><p>供应链描述了商品从来源地到预期客户的路径。可追溯性跟踪这些商品在两个关键点之间的流动。以太坊区块链的透明账本可以通过锁定假冒产品来保护供应链。</p>
<h3 id="DAO-的开发"><a href="#DAO-的开发" class="headerlink" title="DAO 的开发"></a>DAO 的开发</h3><p>去中心化自治组织（DAO）支持 Web 3、游戏和风险投资等领域的去中心化决策。DAO 使用智能合约来自动化投票、投资和收益分配，确保透明度，而无需第三方。</p>
<h2 id="了解以太坊的安全模型"><a href="#了解以太坊的安全模型" class="headerlink" title="了解以太坊的安全模型"></a>了解以太坊的安全模型</h2><p>以太坊的安全性由去中心化的节点网络维护，这使其具有很强的抗攻击能力。质押 ETH 的验证者在保护交易和维护区块链的完整性方面发挥着关键作用。</p>
<p>质押的 ETH 越多，网络就变得越强大和更具弹性，因为它会阻止潜在的威胁并增加任何攻击的成本。</p>
<p>通过要求验证者质押 32 个 ETH，以太坊为诚实行为创造了强大的激励措施，确保验证者在经济上有动力按照网络的最佳利益行事。</p>
<h2 id="ETH-质押和再质押"><a href="#ETH-质押和再质押" class="headerlink" title="ETH 质押和再质押"></a>ETH 质押和再质押</h2><h3 id="以太坊质押"><a href="#以太坊质押" class="headerlink" title="以太坊质押"></a>以太坊质押</h3><p>要成为验证者并保护以太坊网络，用户需要质押 32 个 ETH。这种质押过程不仅有助于网络安全，而且还会奖励参与者。</p>
<p>用户还可以加入质押池或使用中心化交易所，这些交易所提供少量质押服务，从而使那些没有全部 32 个 ETH 的人更容易参与。</p>
<h3 id="以太坊再质押"><a href="#以太坊再质押" class="headerlink" title="以太坊再质押"></a>以太坊再质押</h3><p>以太坊再质押允许用户重用其质押的 ETH 来保护核心网络之外的其他服务和协议，从而有效地提高其资产的效用。</p>
<p>EigenLayer、Symbiotic 和 Karak 等平台提供再质押解决方案，为质押者提供最大化回报的机会，同时为更广泛的以太坊生态系统做出贡献。</p>
<h2 id="谁创建了以太坊区块链"><a href="#谁创建了以太坊区块链" class="headerlink" title="谁创建了以太坊区块链"></a>谁创建了以太坊区块链</h2><p>以太坊由俄罗斯出生的加拿大计算机程序员 Vitalik Buterin 与 Gavin Wood、Jeff Wilke、Joseph Lubin、Charles Hoskinson、Mihai Alisie、Amir Chetrit 和 Anthony Di Iorio 共同创建。除了 Vitalik 之外，所有人都离开了以太坊去追求其他兴趣。</p>
<p>Gavin 后来创建了 Polkadot 和 Kusama，并编写了 Solidity 编程语言。Joseph 是以太坊投资孵化器 Consensys 的创始人，而 Charles 是 Input Output Global（IOG）的联合创始人兼首席执行官。</p>
<p>Amir 为 Colored Coin 项目做出了贡献，而 Anthony 创立了 Jaxx 加密钱包和 Decentral。Mihai 创建了 Akasha 并共同创立了 Bitcoin Magazine。</p>
<p>通过以太坊基金会，该平台从 2014 年进行的在线众筹中获得了约 1800 万美元。</p>
<h2 id="关于以太坊的常见问题"><a href="#关于以太坊的常见问题" class="headerlink" title="关于以太坊的常见问题"></a>关于以太坊的常见问题</h2><h3 id="什么是以太坊（ETH）"><a href="#什么是以太坊（ETH）" class="headerlink" title="什么是以太坊（ETH）?"></a>什么是以太坊（ETH）?</h3><p>以太坊是一个 Layer 1 区块链，构建为开源的、分布式计算平台，用于创建和部署去中心化应用（dApp）。</p>
<h3 id="什么是以太坊-2-0（ETH2）"><a href="#什么是以太坊-2-0（ETH2）" class="headerlink" title="什么是以太坊 2.0（ETH2）?"></a>什么是以太坊 2.0（ETH2）?</h3><p>以太坊 2.0 是以太坊网络的一项重大升级，旨在提高可扩展性、安全性和效率。它涉及从工作量证明（PoW）过渡到权益证明（PoS），以解决以前的网络限制。</p>
<h3 id="以太坊-2-0-的好处是什么"><a href="#以太坊-2-0-的好处是什么" class="headerlink" title="以太坊 2.0 的好处是什么?"></a>以太坊 2.0 的好处是什么?</h3><p>以太坊 2.0 提高了可扩展性，通过过渡到权益证明降低了能源消耗，并允许通过分片链更有效地处理交易。</p>
<h3 id="以太坊区块链的核心概念是什么"><a href="#以太坊区块链的核心概念是什么" class="headerlink" title="以太坊区块链的核心概念是什么?"></a>以太坊区块链的核心概念是什么?</h3><p>以太坊已经升级了互联网基础设施，利用智能合约来自动化日常流程。以太坊的特性允许用户执行更复杂的过程，例如借贷和赚取利息，以及更复杂的金融交易，而无需依赖银行和经纪代理等中介机构。</p>
<h3 id="什么是以太坊再质押"><a href="#什么是以太坊再质押" class="headerlink" title="什么是以太坊再质押?"></a>什么是以太坊再质押?</h3><p>以太坊再质押允许用户重用其质押的 ETH 来保护核心网络之外的其他服务和协议，从而提供更高的回报并支持更广泛的以太坊生态系统。EigenLayer、Symbiotic 和 Karak 等平台促进了再质押。</p>
<h3 id="谁创建了以太坊"><a href="#谁创建了以太坊" class="headerlink" title="谁创建了以太坊?"></a>谁创建了以太坊?</h3><p>以太坊由俄罗斯出生的加拿大计算机程序员 Vitalik Buterin 与 Gavin Wood、Jeff Wilke、Joseph Lubin、Charles Hoskinson、Mihai Alisie、Amir Chetrit 和 Anthony Di Iorio 共同创建。</p>
<h3 id="以太坊区块链的用例是什么"><a href="#以太坊区块链的用例是什么" class="headerlink" title="以太坊区块链的用例是什么?"></a>以太坊区块链的用例是什么?</h3><p>以太坊区块链非常适合托管 DeFi 应用、NFT 和数字收藏品，运行智能合约和 dApp 来自动化金融服务，以及促进可追溯性和供应链管理。</p>
<hr>
]]></content>
      <tags>
        <tag>web3</tag>
      </tags>
  </entry>
  <entry>
    <title>Rust编译参数与优化控制</title>
    <url>/2023/11/26/Rust%E7%BC%96%E8%AF%91%E5%8F%82%E6%95%B0%E4%B8%8E%E4%BC%98%E5%8C%96%E6%8E%A7%E5%88%B6/</url>
    <content><![CDATA[<p>Rust 的核心工具包括：</p>
<ul>
<li>**<code>rustc</code>**：Rust 的底层编译器。<code>cargo</code> 最终会调用 <code>rustc</code> 来编译代码。<code>rustc</code> 接受大量的命令行参数来精确控制编译过程。</li>
<li>**<code>cargo</code>**：Rust 的官方构建工具和包管理器。根据 <code>Cargo.toml</code> 的配置，自动调用 <code>rustc</code> 并传递合适的参数。</li>
</ul>
<span id="more"></span>

<h2 id="1-Cargo-工具"><a href="#1-Cargo-工具" class="headerlink" title="1. Cargo 工具"></a>1. Cargo 工具</h2><p>Cargo 是 Rust 开发的核心工具，统一了「项目创建、编译、运行、测试、依赖管理、发布」等全流程。</p>
<h3 id="Cargo-的核心职责"><a href="#Cargo-的核心职责" class="headerlink" title="Cargo 的核心职责"></a>Cargo 的核心职责</h3><ul>
<li><strong>项目脚手架</strong>：创建标准 Rust 项目（二进制&#x2F;库）</li>
<li><strong>依赖管理</strong>：下载、更新、编译依赖（从 crates.io 或自定义源）</li>
<li><strong>构建与运行</strong>：调用 <code>rustc</code> 编译项目，自动处理链接、优化参数</li>
<li><strong>测试与基准</strong>：运行单元测试、集成测试、性能基准测试</li>
<li><strong>发布与分发</strong>：将库发布到 crates.io，或生成可分发的二进制文件</li>
</ul>
<h3 id="项目结构"><a href="#项目结构" class="headerlink" title="项目结构"></a>项目结构</h3><p>通过 cargo 创建的项目，会包含：</p>
<ul>
<li><strong>Cargo.toml</strong>：「清单文件」，记录项目名称、版本、依赖、编译配置等；可手动编辑</li>
<li><strong>Cargo.lock</strong>：「版本锁文件」，精确记录所有依赖的具体版本（确保多人协作&#x2F;跨环境编译一致性）；自动生成</li>
</ul>
<h3 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h3><table>
<thead>
<tr>
<th>类别</th>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>项目创建</strong></td>
<td><code>cargo new &lt;name&gt;</code></td>
<td>创建一个新的二进制项目（<code>--no-git</code> 不初始化git）</td>
</tr>
<tr>
<td></td>
<td><code>cargo new --lib &lt;name&gt;</code></td>
<td>创建一个新的库项目</td>
</tr>
<tr>
<td></td>
<td><code>cargo init</code></td>
<td>在当前目录初始化一个 Cargo 项目</td>
</tr>
<tr>
<td><strong>编译构建</strong></td>
<td><code>cargo build</code></td>
<td>编译项目（开发模式），生成到 <code>target/debug/</code> 下</td>
</tr>
<tr>
<td></td>
<td><code>cargo build --release</code></td>
<td>编译项目（发布模式），生成到 <code>target/release/</code> 下</td>
</tr>
<tr>
<td></td>
<td><code>cargo check</code></td>
<td>快速检查代码是否有编译错误，不生成可执行文件，速度最快</td>
</tr>
<tr>
<td></td>
<td><code>cargo check --release</code></td>
<td>在发布模式下快速检查代码</td>
</tr>
<tr>
<td><strong>运行程序</strong></td>
<td><code>cargo run</code></td>
<td>以开发模式编译并运行项目</td>
</tr>
<tr>
<td></td>
<td><code>cargo run --release</code></td>
<td>以发布模式编译并运行项目</td>
</tr>
<tr>
<td></td>
<td><code>cargo run -- &lt;args&gt;</code></td>
<td>带命令行参数（<code>--</code> 后的内容）方式运行</td>
</tr>
<tr>
<td><strong>测试</strong></td>
<td><code>cargo test</code></td>
<td>编译并运行项目中的所有测试</td>
</tr>
<tr>
<td></td>
<td><code>cargo test &lt;name&gt;</code></td>
<td>运行名称中包含 <code>&lt;name&gt;</code> 的特定测试</td>
</tr>
<tr>
<td></td>
<td><code>cargo test -- --nocapture</code></td>
<td>运行测试时，显示 <code>println!</code> 等输出信息</td>
</tr>
<tr>
<td></td>
<td><code>cargo test -- --ignored</code></td>
<td>运行被 <code>#[ignore]</code> 标记的（通常被忽略的）测试</td>
</tr>
<tr>
<td><strong>文档</strong></td>
<td><code>cargo doc</code></td>
<td>为项目生成 HTML 文档</td>
</tr>
<tr>
<td></td>
<td><code>cargo doc --open</code></td>
<td>生成文档并在浏览器中打开</td>
</tr>
<tr>
<td><strong>依赖管理</strong></td>
<td><code>cargo add &lt;crate&gt;</code></td>
<td>向 Cargo.toml 添加一个依赖；<code>--features</code> 添加包属性</td>
</tr>
<tr>
<td></td>
<td><code>cargo update</code></td>
<td>更新 Cargo.lock，将依赖升级到允许的最新版本</td>
</tr>
<tr>
<td></td>
<td><code>cargo search &lt;keyword&gt;</code></td>
<td>在 crates.io 上搜索包</td>
</tr>
<tr>
<td><strong>发布</strong></td>
<td><code>cargo publish</code></td>
<td>将库发布到 crates.io</td>
</tr>
<tr>
<td><strong>清理</strong></td>
<td><code>cargo clean</code></td>
<td>删除 <code>target/</code> 目录，清除所有编译产物和缓存</td>
</tr>
<tr>
<td><strong>代码质量</strong></td>
<td><code>cargo fmt</code></td>
<td>(需安装 rustfmt) 格式化代码，使其符合 Rust 风格指南</td>
</tr>
<tr>
<td></td>
<td><code>cargo clippy</code></td>
<td>(需安装 clippy) 运行 lint 工具，发现代码中的 bug、错误和不良模式</td>
</tr>
</tbody></table>
<h4 id="添加依赖示例"><a href="#添加依赖示例" class="headerlink" title="添加依赖示例"></a>添加依赖示例</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cargo add reqwest --features blocking</span><br></pre></td></tr></table></figure>

<p>会在 toml 中添加：</p>
<figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="section">[dependencies]</span></span><br><span class="line"><span class="attr">reqwest</span> = &#123; version = <span class="string">&quot;0.12.24&quot;</span>, features = [<span class="string">&quot;blocking&quot;</span>] &#125;</span><br></pre></td></tr></table></figure>

<h3 id="Profile（编译规则集合）"><a href="#Profile（编译规则集合）" class="headerlink" title="Profile（编译规则集合）"></a>Profile（编译规则集合）</h3><p>Cargo 的 <code>profile</code> 是编译规则的集合，核心作用是将「优化等级、调试信息、编译速度、链接策略」等配置按「场景」（开发、测试、发布等）归类，避免每次编译都手动传递大量参数。</p>
<h4 id="Profile-的核心特点"><a href="#Profile-的核心特点" class="headerlink" title="Profile 的核心特点"></a>Profile 的核心特点</h4><ul>
<li><strong>统一管理编译配置</strong>：将分散的 <code>rustc</code> 参数（如优化等级、LTO）整合为场景化配置</li>
<li><strong>优先级明确</strong>：命令行参数（如 <code>--release</code>）&gt; <code>Cargo.toml</code> 配置 &gt; 默认值</li>
<li><strong>继承机制</strong>：部分默认 profile 会继承其他 profile 的配置<ul>
<li><code>test</code> ← <code>dev</code>：修改 <code>dev</code> 的配置会影响 <code>test</code>（除非单独覆盖 <code>test</code> 的字段）</li>
<li><code>bench</code> ← <code>release</code>：修改 <code>release</code> 的配置会影响 <code>bench</code>（除非单独覆盖 <code>bench</code> 的字段）</li>
</ul>
</li>
</ul>
<h4 id="五个默认-Profile"><a href="#五个默认-Profile" class="headerlink" title="五个默认 Profile"></a>五个默认 Profile</h4><table>
<thead>
<tr>
<th>Profile</th>
<th>场景</th>
<th>默认 opt-level</th>
<th>默认 debug</th>
</tr>
</thead>
<tbody><tr>
<td><code>dev</code></td>
<td>开发调试</td>
<td>0（无优化）</td>
<td>true（基础调试信息）</td>
</tr>
<tr>
<td><code>test</code></td>
<td>单元测试</td>
<td>0</td>
<td>true</td>
</tr>
<tr>
<td><code>bench</code></td>
<td>性能基准测试</td>
<td>3（最高优化）</td>
<td>false</td>
</tr>
<tr>
<td><code>release</code></td>
<td>发布生产</td>
<td>3（最高优化）</td>
<td>false</td>
</tr>
<tr>
<td><code>custom</code></td>
<td>自定义场景</td>
<td>灵活配置</td>
<td>灵活配置</td>
</tr>
</tbody></table>
<h4 id="配置项详解"><a href="#配置项详解" class="headerlink" title="配置项详解"></a>配置项详解</h4><h5 id="opt-level（优化级别）"><a href="#opt-level（优化级别）" class="headerlink" title="opt-level（优化级别）"></a><code>opt-level</code>（优化级别）</h5><p>影响程序性能最关键的选项，直接控制 <code>rustc</code> 的优化级别。</p>
<ul>
<li><p><strong>值类型</strong>：字符串 (<code>&quot;0&quot;</code>, <code>&quot;1&quot;</code>, <code>&quot;2&quot;</code>, <code>&quot;3&quot;</code>, <code>&quot;s&quot;</code>, <code>&quot;z&quot;</code>) 或整数 (0, 1, 2, 3)</p>
</li>
<li><p><strong>含义</strong>：</p>
<ul>
<li><code>&quot;0&quot;</code>：无优化。编译最快，但性能最差；<code>dev</code> 的默认值</li>
<li><code>&quot;1&quot;</code>：启用基本的优化</li>
<li><code>&quot;2&quot;</code>：启用更多优化，是性能和编译时间的良好平衡</li>
<li><code>&quot;3&quot;</code>：最高级别的优化。启用所有可用的优化，显著提升性能，但编译时间很长；<code>release</code> 的默认值</li>
<li><code>&quot;s&quot;</code>：在 <code>&quot;2&quot;</code> 的基础上，优先减小代码大小 (size)。适用于对二进制体积敏感的场景</li>
<li><code>&quot;z&quot;</code>：在 <code>&quot;s&quot;</code> 的基础上，进行更激进的大小优化，追求最小的代码体积。通常比 <code>&quot;s&quot;</code> 编译更慢</li>
</ul>
</li>
</ul>
<h5 id="debug（调试信息）"><a href="#debug（调试信息）" class="headerlink" title="debug（调试信息）"></a><code>debug</code>（调试信息）</h5><p>控制是否为目标文件生成调试信息。</p>
<ul>
<li><p><strong>值类型</strong>：布尔值 (<code>true</code>&#x2F;<code>false</code>) 或整数 (0, 1, 2)</p>
</li>
<li><p><strong>含义</strong>：</p>
<ul>
<li><code>true</code> &#x2F; <code>1</code>：生成基础的调试信息（如行号），足以支持 <code>gdb</code>&#x2F;<code>lldb</code> 等调试器的基本操作</li>
<li><code>2</code>：生成完整的调试信息，支持更复杂的调试功能</li>
<li><code>false</code> &#x2F; <code>0</code>：不生成任何调试信息；会使二进制文件更小，但将无法使用调试器或从崩溃中获取有意义的回溯（backtrace）</li>
</ul>
</li>
</ul>
<h5 id="lto（Link-Time-Optimization，链接时优化）"><a href="#lto（Link-Time-Optimization，链接时优化）" class="headerlink" title="lto（Link-Time Optimization，链接时优化）"></a><code>lto</code>（Link-Time Optimization，链接时优化）</h5><p>一个强大的全局优化技术。允许编译器在链接阶段进行跨 crate 的深度优化（如函数内联、死代码消除）。</p>
<ul>
<li><p><strong>值类型</strong>：字符串 (<code>&quot;off&quot;</code>, <code>&quot;on&quot;</code>, <code>&quot;fat&quot;</code>, <code>&quot;thin&quot;</code>, <code>&quot;thin-local&quot;</code>) 或布尔值</p>
</li>
<li><p><strong>含义</strong>：</p>
<ul>
<li><code>&quot;off&quot;</code> &#x2F; <code>false</code>：禁用 LTO；默认值</li>
<li><code>&quot;on&quot;</code> &#x2F; <code>&quot;fat&quot;</code> &#x2F; <code>true</code>：全量 LTO (Fat LTO)。效果最好，能产生最高效的代码，但链接时间非常长，内存消耗巨大</li>
<li><code>&quot;thin&quot;</code>：薄 LTO (Thin LTO)。旨在提供接近 Fat LTO 的性能收益，同时显著减少链接时间和内存占用。是追求高性能时的推荐选择</li>
<li><code>&quot;thin-local&quot;</code>：Thin LTO 的一个变种，只在当前 crate 内部进行优化</li>
</ul>
</li>
</ul>
<h5 id="codegen-units（代码生成单元）"><a href="#codegen-units（代码生成单元）" class="headerlink" title="codegen-units（代码生成单元）"></a><code>codegen-units</code>（代码生成单元）</h5><p>控制并行代码生成的单元数量；会影响编译速度和最终的优化程度。</p>
<ul>
<li><p><strong>值类型</strong>：正整数</p>
</li>
<li><p><strong>含义</strong>：</p>
<ul>
<li>数值越大（如 <code>16</code>, <code>256</code>）：编译可以更好地并行化，编译速度更快，但限制了跨单元的优化</li>
<li>数值越小（理想是 <code>1</code>）：编译器能看到更多的上下文，有利于 LTO 发挥最大效果，生成更优的代码，但编译速度变慢</li>
</ul>
</li>
<li><p><strong>默认</strong>：<code>16</code> (对于 <code>dev</code>), <code>16</code> (对于 <code>release</code>，但在启用 LTO 时建议设为 <code>1</code>)</p>
</li>
</ul>
<h5 id="panic（Panic-处理策略）"><a href="#panic（Panic-处理策略）" class="headerlink" title="panic（Panic 处理策略）"></a><code>panic</code>（Panic 处理策略）</h5><p>指定当 <code>panic!</code> 宏被触发时，程序的行为。</p>
<ul>
<li><p><strong>值类型</strong>：字符串 (<code>&quot;unwind&quot;</code>, <code>&quot;abort&quot;</code>)</p>
</li>
<li><p><strong>含义</strong>：</p>
<ul>
<li><code>&quot;unwind&quot;</code>：栈展开 (Stack Unwinding)。程序会沿着调用栈向上清理（执行析构函数），然后终止。默认值</li>
<li><code>&quot;abort&quot;</code>：立即终止 (Abort)。程序直接终止，不执行任何析构函数。生成的代码更小、性能略好，但资源（如文件句柄、内存）可能不会被正确释放</li>
</ul>
</li>
</ul>
<h5 id="strip（信息剥离）"><a href="#strip（信息剥离）" class="headerlink" title="strip（信息剥离）"></a><code>strip</code>（信息剥离）</h5><p>控制移除哪些调试和符号信息。</p>
<ul>
<li><code>&quot;none&quot;</code>：保留所有信息；默认</li>
<li><code>&quot;debuginfo&quot;</code>：移除调试信息</li>
<li><code>&quot;symbols&quot;</code>：移除符号表但保留必要的调试信息</li>
<li><code>&quot;all&quot;</code>：移除所有可选信息，包括调试和符号数据</li>
</ul>
<h5 id="incremental（增量编译）"><a href="#incremental（增量编译）" class="headerlink" title="incremental（增量编译）"></a><code>incremental</code>（增量编译）</h5><p>控制是否启用增量编译。</p>
<ul>
<li><p><strong>值类型</strong>：布尔值 (<code>true</code>&#x2F;<code>false</code>)</p>
</li>
<li><p><strong>含义</strong>：</p>
<ul>
<li><code>true</code>：启用增量编译。<code>cargo</code> 会缓存部分编译结果，当下次编译只有少量文件改变时，可以极大加快编译速度</li>
<li><code>false</code>：禁用增量编译。每次都是从头开始编译，速度慢，但结果更确定</li>
</ul>
</li>
</ul>
<h5 id="overflow-checks（整数溢出检查）"><a href="#overflow-checks（整数溢出检查）" class="headerlink" title="overflow-checks（整数溢出检查）"></a><code>overflow-checks</code>（整数溢出检查）</h5><p>控制是否在运行时检查整数运算的溢出。</p>
<ul>
<li><p><strong>值类型</strong>：布尔值 (<code>true</code>&#x2F;<code>false</code>)</p>
</li>
<li><p><strong>含义</strong>：</p>
<ul>
<li><code>true</code>：启用溢出检查。如果发生溢出（如 <code>u32::MAX + 1</code>），程序会 panic。这增加了安全性，但有轻微的性能开销</li>
<li><code>false</code>：禁用溢出检查。溢出时会进行”环绕”（wrap around），例如 <code>u8::MAX + 1</code> 会变成 <code>0</code>。性能更好，但可能引入安全漏洞</li>
</ul>
</li>
</ul>
<h4 id="自定义-Profile"><a href="#自定义-Profile" class="headerlink" title="自定义 Profile"></a>自定义 Profile</h4><p>除默认的 5 个 profile，还可以创建自定义 profile（如 <code>staging</code> 预发布环境、<code>min-size</code> 极致体积环境），满足特殊需求。</p>
<p>通过 <code>--profile</code> 在命令指定要使用的 profile：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cargo build --profile staging</span><br></pre></td></tr></table></figure>

<h5 id="自定义-Profile-示例"><a href="#自定义-Profile-示例" class="headerlink" title="自定义 Profile 示例"></a>自定义 Profile 示例</h5><figure class="highlight toml"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 自定义预发布环境（staging）：介于 dev 和 release 之间</span></span><br><span class="line"><span class="section">[profile.staging]</span></span><br><span class="line"><span class="comment"># 继承 release 的基础配置（可选，减少重复）</span></span><br><span class="line"><span class="attr">inherits</span> = <span class="string">&quot;release&quot;</span></span><br><span class="line"><span class="comment"># 覆盖继承的配置：中级优化，保留基础调试信息</span></span><br><span class="line"><span class="attr">opt-level</span> = <span class="number">2</span></span><br><span class="line"><span class="attr">debug</span> = <span class="number">1</span></span><br><span class="line"><span class="comment"># 启用瘦 LTO，平衡性能与编译速度</span></span><br><span class="line"><span class="attr">lto</span> = <span class="string">&quot;thin&quot;</span></span><br><span class="line"><span class="comment"># 启用增量编译（预发布环境可能频繁编译）</span></span><br><span class="line"><span class="attr">incremental</span> = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="section">[profile.tiny]</span></span><br><span class="line"><span class="attr">inherits</span> = <span class="string">&quot;release&quot;</span></span><br><span class="line"><span class="attr">opt-level</span> = <span class="string">&quot;z&quot;</span></span><br><span class="line"><span class="attr">lto</span> = <span class="literal">true</span></span><br><span class="line"><span class="attr">strip</span> = <span class="string">&quot;all&quot;</span></span><br></pre></td></tr></table></figure>

<h2 id="2-rustc-编译器"><a href="#2-rustc-编译器" class="headerlink" title="2. rustc 编译器"></a>2. rustc 编译器</h2><p><code>rustc</code> 是 Rust 的底层编译器，Cargo 的编译逻辑最终都会转化为 <code>rustc</code> 命令与参数（可通过 <code>cargo build -v</code> 查看实际调用的 <code>rustc</code> 命令）。</p>
<h3 id="核心编译参数"><a href="#核心编译参数" class="headerlink" title="核心编译参数"></a>核心编译参数</h3><table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>-o &lt;out-name&gt;</code></td>
<td>指定输出文件名（默认生成与 crate 名相同）</td>
<td><code>rustc src/main.rs -o target/my_app.exe</code></td>
</tr>
<tr>
<td><code>--crate-type &lt;type&gt;</code></td>
<td>指定输出类型（默认bin）：<code>lib</code>&#x2F;<code>rlib</code>（Rust 静态库）、<code>staticlib</code>（原生静态库）、<code>dylib</code>（Rust 动态库）、<code>cdylib</code>（原生动态库）、<code>bin</code>（可执行程序）、<code>proc-macro</code>（过程宏库）</td>
<td><code>rustc src/lib.rs --crate-type lib</code></td>
</tr>
<tr>
<td><code>--edition &lt;2018/2021/2024&gt;</code></td>
<td>指定 Rust 版本（默认跟随 Cargo.toml 的 edition）</td>
<td><code>rustc src/main.rs --edition 2021</code></td>
</tr>
<tr>
<td><code>--extern &lt;name&gt;=&lt;path&gt;</code></td>
<td>关联外部依赖</td>
<td><code>rustc src/main.rs --extern serde=./target/debug/deps/libserde.rlib</code></td>
</tr>
<tr>
<td><code>-L &lt;dir&gt;</code></td>
<td>添加库搜索路径（让编译器找到依赖库或系统库）</td>
<td><code>rustc src/main.rs -L ./target/debug/deps</code></td>
</tr>
<tr>
<td><code>--print &lt;item&gt;</code></td>
<td>打印编译器信息（如目标平台、配置宏）</td>
<td><code>rustc --print target-list</code></td>
</tr>
</tbody></table>
<h3 id="优化参数（核心重点）"><a href="#优化参数（核心重点）" class="headerlink" title="优化参数（核心重点）"></a>优化参数（核心重点）</h3><p>与 Cargo Profile 的 <code>opt-level</code>、<code>lto</code> 等字段一一对应，是控制运行性能、二进制体积的底层参数。</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
<th>对应 Profile 字段</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>-O0</code> &#x2F; <code>-O1</code> &#x2F; <code>-O2</code> &#x2F; <code>-O3</code> &#x2F; <code>-Os</code> &#x2F; <code>-Oz</code> 或 <code>-C opt-level=&lt;0-3/s/z&gt;</code></td>
<td>优化等级</td>
<td><code>opt-level</code></td>
<td><code>rustc src/main.rs -O3</code></td>
</tr>
<tr>
<td><code>-C inline-threshold=&lt;n&gt;</code></td>
<td>调整函数内联阈值（值越大越多函数被内联，默认 225）</td>
<td>-</td>
<td><code>rustc src/main.rs -C inline-threshold=300</code></td>
</tr>
<tr>
<td><code>-C lto</code> &#x2F; <code>-C lto=thin</code></td>
<td>启用链接时优化</td>
<td><code>lto</code></td>
<td><code>rustc src/main.rs -O3 -C lto=thin</code></td>
</tr>
<tr>
<td><code>-C codegen-units=&lt;n&gt;</code></td>
<td>指定代码生成单元（平衡编译速度与优化效果，默认 dev&#x3D;16、release&#x3D;1）</td>
<td><code>codegen-units</code></td>
<td><code>rustc src/main.rs -C codegen-units=4</code></td>
</tr>
<tr>
<td><code>-C target-cpu=&lt;cpu&gt;</code></td>
<td>针对特定 CPU 架构优化（<code>native</code>&#x3D; 自动适配本地 CPU）</td>
<td><code>rustflags = [&quot;-C target-cpu=native&quot;]</code></td>
<td><code>rustc src/main.rs -C target-cpu=aarch64</code></td>
</tr>
</tbody></table>
<h3 id="调试信息与符号参数"><a href="#调试信息与符号参数" class="headerlink" title="调试信息与符号参数"></a>调试信息与符号参数</h3><p>控制二进制文件的调试信息（供 <code>gdb</code>&#x2F;<code>lldb</code> 调试）和符号剥离，影响文件体积。</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
<th>对应 Profile 字段</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>-g / -g1 / -g2 / -g3</code></td>
<td>启用调试信息（<code>-g</code> 等价于 <code>-g2</code>）</td>
<td><code>debug</code></td>
<td><code>rustc src/main.rs -g</code></td>
</tr>
<tr>
<td><code>-C debuginfo=&lt;0-3&gt;</code></td>
<td>与 <code>-g</code> 等价，显式指定调试级别（0 &#x3D; 无，1 &#x3D; 基础，2 &#x3D; 完整，3 &#x3D; 额外）</td>
<td><code>debug</code></td>
<td><code>rustc src/main.rs -C debuginfo=1</code></td>
</tr>
<tr>
<td><code>-C debuginfo-format=&lt;fmt&gt;</code></td>
<td>指定调试信息格式（<code>rustc</code>&#x3D;Rust 专用，<code>llvm</code>&#x3D;LLVM 格式，<code>gdb</code>&#x3D; 兼容 GDB）</td>
<td>-</td>
<td><code>rustc src/main.rs -g -C debuginfo-format=gdb</code></td>
</tr>
<tr>
<td><code>-C strip=&lt;none/debuginfo/symbols&gt;</code></td>
<td>剥离符号信息（<code>debuginfo</code>&#x3D; 仅剥离调试信息，<code>symbols</code>&#x3D; 剥离所有符号）</td>
<td><code>strip</code></td>
<td><code>rustc src/main.rs -O3 -C strip=debuginfo</code></td>
</tr>
</tbody></table>
<h3 id="目标平台与交叉编译参数"><a href="#目标平台与交叉编译参数" class="headerlink" title="目标平台与交叉编译参数"></a>目标平台与交叉编译参数</h3><p>控制编译的目标平台（架构&#x2F;操作系统），用于交叉编译场景，与 Cargo 的 <code>--target</code> 对应。</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>--target &lt;triple&gt;</code></td>
<td>指定目标平台三元组（如 <code>aarch64-unknown-linux-musl</code>）</td>
<td><code>rustc src/main.rs --target aarch64-unknown-linux-musl</code>（交叉编译到 ARM Linux）</td>
</tr>
<tr>
<td><code>--print target-triple</code></td>
<td>打印当前平台的目标三元组（确认编译环境）</td>
<td><code>rustc --print target-triple</code> # 输出 <code>x86_64-unknown-linux-gnu</code></td>
</tr>
<tr>
<td><code>--print target-spec-json</code></td>
<td>打印目标平台的详细配置（JSON 格式，用于自定义目标）</td>
<td><code>rustc --target aarch64-unknown-linux-musl --print target-spec-json</code></td>
</tr>
<tr>
<td><code>-C linker=&lt;linker&gt;</code></td>
<td>指定链接器（交叉编译时需配置目标平台的链接器）</td>
<td><code>rustc src/main.rs --target aarch64-unknown-linux-musl -C linker=aarch64-linux-musl-gcc</code></td>
</tr>
</tbody></table>
<h3 id="链接参数（关联系统库-x2F-自定义链接逻辑）"><a href="#链接参数（关联系统库-x2F-自定义链接逻辑）" class="headerlink" title="链接参数（关联系统库&#x2F;自定义链接逻辑）"></a>链接参数（关联系统库&#x2F;自定义链接逻辑）</h3><p>用于链接系统库（如 <code>libc</code>、<code>openssl</code>）或传递参数给链接器（如 <code>ld</code>、<code>clang</code>）。</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>-l &lt;lib&gt;</code></td>
<td>链接系统库（省略前缀 lib 和后缀，如 libm 写作 m）</td>
<td><code>rustc src/main.rs -l m -l ssl</code></td>
</tr>
<tr>
<td><code>-L &lt;dir&gt;</code></td>
<td>添加库搜索路径（让链接器找到系统库或自定义库）</td>
<td><code>rustc src/main.rs -L /usr/local/lib -l redis</code></td>
</tr>
<tr>
<td><code>-C link-arg=&lt;arg&gt;</code></td>
<td>传递参数给链接器（如 ld 的 <code>-s</code> 剥离符号、<code>-static</code> 静态链接）</td>
<td><code>rustc src/main.rs -C link-arg=-s -C link-arg=-static</code></td>
</tr>
<tr>
<td><code>-C link-mode=&lt;static/dynamic&gt;</code></td>
<td>指定链接模式（静态&#x2F;动态，默认自动）</td>
<td><code>rustc src/main.rs -C link-mode=static</code></td>
</tr>
</tbody></table>
<h3 id="代码生成与安全参数"><a href="#代码生成与安全参数" class="headerlink" title="代码生成与安全参数"></a>代码生成与安全参数</h3><p>更精细的代码生成控制，包括 panic 策略、栈大小、安全检查等。</p>
<table>
<thead>
<tr>
<th>参数</th>
<th>说明</th>
<th>对应 Profile 字段</th>
<th>示例</th>
</tr>
</thead>
<tbody><tr>
<td><code>-C panic=&lt;unwind/abort&gt;</code></td>
<td>指定 panic 策略（<code>unwind</code>&#x3D; 展开栈，<code>abort</code>&#x3D; 直接终止）</td>
<td><code>panic</code></td>
<td><code>rustc src/main.rs -C panic=abort</code></td>
</tr>
<tr>
<td><code>-C incremental=&lt;true/false&gt;</code></td>
<td>启用&#x2F;禁用增量编译（默认 dev&#x3D;true、release&#x3D;false）</td>
<td><code>incremental</code></td>
<td><code>rustc src/main.rs -C incremental=false</code></td>
</tr>
<tr>
<td><code>-C stack-size=&lt;size&gt;</code></td>
<td>指定主线程栈大小（如 8MB、16384 字节）</td>
<td>-</td>
<td><code>rustc src/main.rs -C stack-size=8388608</code></td>
</tr>
<tr>
<td><code>-C fstack-protector=&lt;all/strong/none&gt;</code></td>
<td>启用栈保护（防止栈溢出攻击，默认 none）</td>
<td>-</td>
<td><code>rustc src/main.rs -C fstack-protector=strong</code></td>
</tr>
<tr>
<td><code>-C debug-assertions=&lt;true/false&gt;</code></td>
<td>启用&#x2F;禁用调试断言（<code>debug_assert!</code>）</td>
<td><code>debug-assertions</code></td>
<td><code>rustc src/main.rs -C debug-assertions=false</code></td>
</tr>
</tbody></table>
<h2 id="实践建议"><a href="#实践建议" class="headerlink" title="实践建议"></a>实践建议</h2><h3 id="开发阶段"><a href="#开发阶段" class="headerlink" title="开发阶段"></a>开发阶段</h3><ul>
<li>使用默认 <code>dev</code> profile（opt-level&#x3D;0）</li>
<li>启用增量编译加快构建速度</li>
<li>保留调试信息便于调试</li>
</ul>
<h3 id="性能测试"><a href="#性能测试" class="headerlink" title="性能测试"></a>性能测试</h3><ul>
<li>使用 <code>release</code> profile 或创建自定义 profile</li>
<li>启用 <code>lto = &quot;thin&quot;</code> 获得更好的性能</li>
<li>设置 <code>codegen-units = 1</code> 以充分利用 LTO</li>
</ul>
<h3 id="生产发布"><a href="#生产发布" class="headerlink" title="生产发布"></a>生产发布</h3><ul>
<li>使用 <code>release</code> profile（opt-level&#x3D;3）</li>
<li>启用 LTO（推荐 <code>&quot;thin&quot;</code>）</li>
<li>根据需要剥离调试信息以减小体积</li>
</ul>
<h3 id="体积优化"><a href="#体积优化" class="headerlink" title="体积优化"></a>体积优化</h3><ul>
<li>设置 <code>opt-level = &quot;z&quot;</code> 实现最小体积</li>
<li>使用 <code>strip = &quot;all&quot;</code> 剥离所有符号</li>
<li>启用 LTO 进行全局优化</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ul>
<li><strong>Cargo</strong> 是高层抽象，提供统一的构建界面和 Profile 管理</li>
<li><strong>rustc</strong> 是底层编译器，接受精细化的编译参数</li>
<li><strong>Profile</strong> 是最实用的编译配置管理方案，建议优先使用</li>
<li>理解 <code>opt-level</code>、<code>lto</code>、<code>codegen-units</code> 等参数可以精细调优性能和体积</li>
<li>使用 <code>cargo build -v</code> 可以看到实际的 rustc 命令，便于学习和调试</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>web3-智能合约</title>
    <url>/2025/11/27/web3-%E6%99%BA%E8%83%BD%E5%90%88%E7%BA%A6/</url>
    <content><![CDATA[<h2 id="什么是智能合约"><a href="#什么是智能合约" class="headerlink" title="什么是智能合约"></a>什么是智能合约</h2><p>智能合约（Smart Contract）是部署在区块链上的自执行程序，用代码形式编写合约条款和规则。当预定条件满足时，智能合约会自动执行对应的逻辑，无需中介参与，具有透明性、不可篡改性和自动化执行的特点。</p>
<span id="more"></span>

<h3 id="核心特征"><a href="#核心特征" class="headerlink" title="核心特征"></a>核心特征</h3><ul>
<li><strong>自动执行</strong>：当条件满足时，无需人工干预即可自动执行</li>
<li><strong>透明性</strong>：所有参与者都能查看合约代码和执行过程</li>
<li><strong>不可篡改</strong>：部署到区块链后，代码和数据无法更改</li>
<li><strong>去中心化</strong>：由网络中的节点共同执行和验证，无中心化机构</li>
<li><strong>确定性</strong>：相同的输入在相同的执行环境下总能产生相同的结果</li>
</ul>
<h2 id="智能合约的工作原理"><a href="#智能合约的工作原理" class="headerlink" title="智能合约的工作原理"></a>智能合约的工作原理</h2><h3 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">编写代码 → 编译 → 部署 → 交互 → 执行 → 记录</span><br></pre></td></tr></table></figure>

<ol>
<li><strong>编写</strong>：用 Solidity、Rust、Go 等语言编写合约代码</li>
<li><strong>编译</strong>：编译为字节码（bytecode）</li>
<li><strong>部署</strong>：将字节码发送到区块链网络，创建合约账户</li>
<li><strong>交互</strong>：用户通过交易与合约交互，触发合约函数</li>
<li><strong>执行</strong>：节点执行合约代码，修改状态</li>
<li><strong>记录</strong>：执行结果被记录在区块链上</li>
</ol>
<h3 id="智能合约的创建和运行"><a href="#智能合约的创建和运行" class="headerlink" title="智能合约的创建和运行"></a>智能合约的创建和运行</h3><ol>
<li>智能合约的代码写完后，要编译成bytecode;</li>
<li>创建合约：外部账户发起一个转账交易到0x0的地址；转账的金额是0，但是要支付汽油费；合约的代码放在data域中；</li>
<li>智能合约运行在EVM（Ethereum Virtual Machine）上</li>
<li>以太坊是一个交易驱动的状态机，调用智能合约的交易发布到区块链上之后，每个矿工都会执行这个交易，从当前状态确定性的转移到下一个状态；</li>
</ol>
<h3 id="执行模式"><a href="#执行模式" class="headerlink" title="执行模式"></a>执行模式</h3><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">用户发起交易 → 节点接收交易 → EVM 执行合约 → 状态更新 → 写入区块 → 共识确认</span><br></pre></td></tr></table></figure>

<h2 id="主流智能合约平台"><a href="#主流智能合约平台" class="headerlink" title="主流智能合约平台"></a>主流智能合约平台</h2><h3 id="Ethereum（以太坊）"><a href="#Ethereum（以太坊）" class="headerlink" title="Ethereum（以太坊）"></a>Ethereum（以太坊）</h3><ul>
<li><strong>语言</strong>：Solidity（首选）、Vyper</li>
<li><strong>虚拟机</strong>：EVM（以太坊虚拟机）</li>
<li><strong>特点</strong>：最成熟、生态最活跃、Gas 费用相对较高</li>
<li><strong>用途</strong>：DeFi、NFT、DAO、Token 标准（ERC-20、ERC-721 等）</li>
</ul>
<h3 id="Solana"><a href="#Solana" class="headerlink" title="Solana"></a>Solana</h3><ul>
<li><strong>语言</strong>：Rust、C</li>
<li><strong>特点</strong>：高吞吐量、低交易费用、并行处理能力强</li>
<li><strong>用途</strong>：高频交易、实时应用、大规模 NFT 生成</li>
</ul>
<h3 id="Polygon（以太坊侧链）"><a href="#Polygon（以太坊侧链）" class="headerlink" title="Polygon（以太坊侧链）"></a>Polygon（以太坊侧链）</h3><ul>
<li><strong>语言</strong>：Solidity</li>
<li><strong>特点</strong>：EVM 兼容、更低费用、更快交易确认</li>
<li><strong>用途</strong>：以太坊应用的扩展方案</li>
</ul>
<h3 id="Cosmos-x2F-Tendermint"><a href="#Cosmos-x2F-Tendermint" class="headerlink" title="Cosmos &#x2F; Tendermint"></a>Cosmos &#x2F; Tendermint</h3><ul>
<li><strong>语言</strong>：Go、Rust</li>
<li><strong>特点</strong>：模块化设计、跨链通信、灵活的共识机制</li>
<li><strong>用途</strong>：专业级应用、跨链协议</li>
</ul>
<h3 id="NEAR-Protocol"><a href="#NEAR-Protocol" class="headerlink" title="NEAR Protocol"></a>NEAR Protocol</h3><ul>
<li><strong>语言</strong>：Rust、AssemblyScript</li>
<li><strong>特点</strong>：分片技术、易于扩展、开发者友好</li>
<li><strong>用途</strong>：GameFi、社交应用、企业应用</li>
</ul>
<h2 id="Solidity-智能合约基础"><a href="#Solidity-智能合约基础" class="headerlink" title="Solidity 智能合约基础"></a>Solidity 智能合约基础</h2><h3 id="基本结构"><a href="#基本结构" class="headerlink" title="基本结构"></a>基本结构</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// SPDX-License-Identifier: MIT</span><br><span class="line">pragma solidity ^0.8.0;</span><br><span class="line"></span><br><span class="line">contract MyContract &#123;</span><br><span class="line">    // 状态变量</span><br><span class="line">    uint256 public count;</span><br><span class="line">    address public owner;</span><br><span class="line">    </span><br><span class="line">    // 事件</span><br><span class="line">    event CountChanged(uint256 newCount);</span><br><span class="line">    </span><br><span class="line">    // 修饰符</span><br><span class="line">    modifier onlyOwner() &#123;</span><br><span class="line">        require(msg.sender == owner, &quot;Only owner can call this&quot;);</span><br><span class="line">        _;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 构造函数</span><br><span class="line">    constructor() &#123;</span><br><span class="line">        owner = msg.sender;</span><br><span class="line">        count = 0;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 公开函数</span><br><span class="line">    function increment() public &#123;</span><br><span class="line">        count += 1;</span><br><span class="line">        emit CountChanged(count);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 仅所有者可调用</span><br><span class="line">    function reset() public onlyOwner &#123;</span><br><span class="line">        count = 0;</span><br><span class="line">        emit CountChanged(count);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 只读函数（不修改状态）</span><br><span class="line">    function getCount() public view returns (uint256) &#123;</span><br><span class="line">        return count;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 纯函数（不读取状态）</span><br><span class="line">    function add(uint256 a, uint256 b) public pure returns (uint256) &#123;</span><br><span class="line">        return a + b;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="关键概念"><a href="#关键概念" class="headerlink" title="关键概念"></a>关键概念</h3><h4 id="状态变量"><a href="#状态变量" class="headerlink" title="状态变量"></a>状态变量</h4><p>持久化存储在区块链上的数据，每次修改都会消耗 Gas 费用。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">uint256 public balance;           // 无符号整数</span><br><span class="line">address public owner;              // 账户地址</span><br><span class="line">bool public isActive;              // 布尔值</span><br><span class="line">string public name;                // 字符串</span><br><span class="line">bytes32 public hash;               // 字节数据</span><br></pre></td></tr></table></figure>

<h4 id="函数可见性"><a href="#函数可见性" class="headerlink" title="函数可见性"></a>函数可见性</h4><table>
<thead>
<tr>
<th>修饰符</th>
<th>说明</th>
<th>调用方式</th>
</tr>
</thead>
<tbody><tr>
<td><code>public</code></td>
<td>公开可见，任何人可调用</td>
<td>外部、内部</td>
</tr>
<tr>
<td><code>internal</code></td>
<td>仅合约内部和继承合约可调用</td>
<td>内部</td>
</tr>
<tr>
<td><code>external</code></td>
<td>仅从合约外部调用</td>
<td>外部</td>
</tr>
<tr>
<td><code>private</code></td>
<td>仅当前合约内部可调用</td>
<td>内部</td>
</tr>
</tbody></table>
<h4 id="函数修饰符"><a href="#函数修饰符" class="headerlink" title="函数修饰符"></a>函数修饰符</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// view：读取状态，不消耗 Gas</span><br><span class="line">function getBalance() public view returns (uint256) &#123;</span><br><span class="line">    return balance;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// pure：不读取状态，不消耗 Gas</span><br><span class="line">function multiply(uint256 a, uint256 b) public pure returns (uint256) &#123;</span><br><span class="line">    return a * b;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// payable：允许接收以太币（ETH）</span><br><span class="line">function deposit() public payable &#123;</span><br><span class="line">    require(msg.value &gt; 0, &quot;Must send ETH&quot;);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 自定义修饰符</span><br><span class="line">modifier onlyOwner() &#123;</span><br><span class="line">    require(msg.sender == owner);</span><br><span class="line">    _;  // 执行函数体</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">function withdraw() public onlyOwner &#123;</span><br><span class="line">    // 只有 owner 能调用</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="事件（Events）"><a href="#事件（Events）" class="headerlink" title="事件（Events）"></a>事件（Events）</h4><p>允许离链系统监听和响应合约执行情况。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 事件声明</span><br><span class="line">event Transfer(address indexed from, address indexed to, uint256 value);</span><br><span class="line">event Approval(address indexed owner, address indexed spender, uint256 value);</span><br><span class="line"></span><br><span class="line">// 触发事件</span><br><span class="line">function transfer(address to, uint256 amount) public &#123;</span><br><span class="line">    balance -= amount;</span><br><span class="line">    emit Transfer(msg.sender, to, amount);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="常见模式"><a href="#常见模式" class="headerlink" title="常见模式"></a>常见模式</h3><h4 id="ERC-20-Token-标准"><a href="#ERC-20-Token-标准" class="headerlink" title="ERC-20 Token 标准"></a>ERC-20 Token 标准</h4><p>最常见的代币标准，定义了代币的基本接口。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">interface IERC20 &#123;</span><br><span class="line">    function transfer(address to, uint256 amount) external returns (bool);</span><br><span class="line">    function approve(address spender, uint256 amount) external returns (bool);</span><br><span class="line">    function transferFrom(address from, address to, uint256 amount) external returns (bool);</span><br><span class="line">    function balanceOf(address account) external view returns (uint256);</span><br><span class="line">    function allowance(address owner, address spender) external view returns (uint256);</span><br><span class="line">    </span><br><span class="line">    event Transfer(address indexed from, address indexed to, uint256 value);</span><br><span class="line">    event Approval(address indexed owner, address indexed spender, uint256 value);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="ERC-721-NFT-标准"><a href="#ERC-721-NFT-标准" class="headerlink" title="ERC-721 NFT 标准"></a>ERC-721 NFT 标准</h4><p>非同质化代币（NFT）标准，每个代币都是唯一的。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">interface IERC721 &#123;</span><br><span class="line">    function safeTransferFrom(address from, address to, uint256 tokenId) external;</span><br><span class="line">    function transferFrom(address from, address to, uint256 tokenId) external;</span><br><span class="line">    function approve(address to, uint256 tokenId) external;</span><br><span class="line">    function setApprovalForAll(address operator, bool approved) external;</span><br><span class="line">    function balanceOf(address owner) external view returns (uint256);</span><br><span class="line">    function ownerOf(uint256 tokenId) external view returns (address);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="委托模式（Delegation）"><a href="#委托模式（Delegation）" class="headerlink" title="委托模式（Delegation）"></a>委托模式（Delegation）</h4><p>合约通过委托调用执行其他合约的逻辑，保持数据存储在当前合约。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 代理合约</span><br><span class="line">contract Proxy &#123;</span><br><span class="line">    address implementation;</span><br><span class="line">    </span><br><span class="line">    fallback() external payable &#123;</span><br><span class="line">        (bool success, ) = implementation.delegatecall(msg.data);</span><br><span class="line">        require(success);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="智能合约的安全考虑"><a href="#智能合约的安全考虑" class="headerlink" title="智能合约的安全考虑"></a>智能合约的安全考虑</h2><h3 id="常见漏洞"><a href="#常见漏洞" class="headerlink" title="常见漏洞"></a>常见漏洞</h3><h4 id="重入攻击（Reentrancy）"><a href="#重入攻击（Reentrancy）" class="headerlink" title="重入攻击（Reentrancy）"></a>重入攻击（Reentrancy）</h4><p>攻击者在函数执行完成前再次调用该函数，导致状态不一致。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 易受攻击的代码</span><br><span class="line">function withdraw() public &#123;</span><br><span class="line">    uint amount = balances[msg.sender];</span><br><span class="line">    (bool success, ) = msg.sender.call&#123;value: amount&#125;(&quot;&quot;);</span><br><span class="line">    require(success);</span><br><span class="line">    balances[msg.sender] = 0;  // 状态更新延迟，可被重入</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 安全的代码（检查-修改-交互模式）</span><br><span class="line">function withdraw() public &#123;</span><br><span class="line">    uint amount = balances[msg.sender];</span><br><span class="line">    balances[msg.sender] = 0;  // 先更新状态</span><br><span class="line">    (bool success, ) = msg.sender.call&#123;value: amount&#125;(&quot;&quot;);</span><br><span class="line">    require(success);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="整数溢出-x2F-下溢"><a href="#整数溢出-x2F-下溢" class="headerlink" title="整数溢出&#x2F;下溢"></a>整数溢出&#x2F;下溢</h4><p>在 Solidity 0.8.0 之前，需要手动检查或使用 SafeMath 库。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// SafeMath 库使用</span><br><span class="line">import &quot;@openzeppelin/contracts/utils/math/SafeMath.sol&quot;;</span><br><span class="line"></span><br><span class="line">contract Safe &#123;</span><br><span class="line">    using SafeMath for uint256;</span><br><span class="line">    </span><br><span class="line">    function add(uint256 a, uint256 b) public pure returns (uint256) &#123;</span><br><span class="line">        return a.add(b);  // 自动检查溢出</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="访问控制漏洞"><a href="#访问控制漏洞" class="headerlink" title="访问控制漏洞"></a>访问控制漏洞</h4><p>函数缺少权限检查，导致任何人都能调用。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 不安全：任何人都能转账</span><br><span class="line">function transfer(address to, uint256 amount) public &#123;</span><br><span class="line">    balances[msg.sender] -= amount;</span><br><span class="line">    balances[to] += amount;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 安全：检查权限</span><br><span class="line">modifier onlyOwner() &#123;</span><br><span class="line">    require(msg.sender == owner, &quot;Only owner&quot;);</span><br><span class="line">    _;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">function transfer(address to, uint256 amount) public onlyOwner &#123;</span><br><span class="line">    balances[msg.sender] -= amount;</span><br><span class="line">    balances[to] += amount;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a>最佳实践</h3><ul>
<li><strong>审计代码</strong>：在部署前进行专业安全审计</li>
<li><strong>使用成熟库</strong>：优先使用 OpenZeppelin 等经过审计的库</li>
<li><strong>遵循设计模式</strong>：使用检查-修改-交互（CEI）模式</li>
<li><strong>限制权限</strong>：最小权限原则，定义明确的访问控制</li>
<li><strong>测试充分</strong>：编写单元测试和集成测试</li>
<li><strong>版本锁定</strong>：明确指定 Solidity 版本，避免意外升级</li>
<li><strong>监控 Gas 使用</strong>：优化合约以减少 Gas 消耗</li>
<li><strong>应急方案</strong>：设计暂停机制和升级机制</li>
</ul>
<h2 id="智能合约开发工具"><a href="#智能合约开发工具" class="headerlink" title="智能合约开发工具"></a>智能合约开发工具</h2><h3 id="开发框架"><a href="#开发框架" class="headerlink" title="开发框架"></a>开发框架</h3><table>
<thead>
<tr>
<th>框架</th>
<th>特点</th>
<th>主要功能</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Hardhat</strong></td>
<td>Node.js 环境、灵活、插件丰富</td>
<td>编译、测试、调试、部署</td>
</tr>
<tr>
<td><strong>Truffle</strong></td>
<td>完整工具套件、生态成熟</td>
<td>编译、迁移、测试、部署</td>
</tr>
<tr>
<td><strong>Foundry</strong></td>
<td>Rust 实现、性能高、实验特性</td>
<td>快速开发、模糊测试、Gas 分析</td>
</tr>
<tr>
<td><strong>Brownie</strong></td>
<td>Python 环境、易于学习</td>
<td>编译、测试、部署、交互</td>
</tr>
</tbody></table>
<h3 id="开发流程示例（Hardhat）"><a href="#开发流程示例（Hardhat）" class="headerlink" title="开发流程示例（Hardhat）"></a>开发流程示例（Hardhat）</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 初始化项目</span></span><br><span class="line">npm install -g hardhat</span><br><span class="line">npx hardhat</span><br><span class="line"></span><br><span class="line"><span class="comment"># 编写合约</span></span><br><span class="line"><span class="comment"># contracts/Token.sol</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 编写测试</span></span><br><span class="line"><span class="comment"># test/Token.js</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 编译</span></span><br><span class="line">npx hardhat compile</span><br><span class="line"></span><br><span class="line"><span class="comment"># 测试</span></span><br><span class="line">npx hardhat <span class="built_in">test</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 部署</span></span><br><span class="line">npx hardhat run scripts/deploy.js --network mainnet</span><br></pre></td></tr></table></figure>

<h3 id="验证和监控"><a href="#验证和监控" class="headerlink" title="验证和监控"></a>验证和监控</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Etherscan 上验证合约源码</span></span><br><span class="line">npx hardhat verify --network mainnet CONTRACT_ADDRESS <span class="string">&quot;constructor args&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用 Tenderly 进行交易模拟和调试</span></span><br><span class="line">npx tenderly <span class="built_in">export</span> CONTRACT_ADDRESS</span><br></pre></td></tr></table></figure>

<h2 id="Gas-优化"><a href="#Gas-优化" class="headerlink" title="Gas 优化"></a>Gas 优化</h2><h3 id="什么是-Gas"><a href="#什么是-Gas" class="headerlink" title="什么是 Gas"></a>什么是 Gas</h3><p>Gas 是在以太坊上执行操作所需的计算费用单位，用户需要支付 Gas 费用来执行交易。</p>
<h3 id="Gas-成本示例"><a href="#Gas-成本示例" class="headerlink" title="Gas 成本示例"></a>Gas 成本示例</h3><table>
<thead>
<tr>
<th>操作</th>
<th>Gas 消耗</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>读取存储</td>
<td>2,100</td>
<td>免费读取</td>
</tr>
<tr>
<td>写入存储</td>
<td>20,000</td>
<td>首次写入</td>
</tr>
<tr>
<td>更新存储</td>
<td>5,000</td>
<td>已存在的值更新</td>
</tr>
<tr>
<td>算术运算</td>
<td>3-5</td>
<td>加、减、乘</td>
</tr>
<tr>
<td>事件日志</td>
<td>375+</td>
<td>每个日志条目</td>
</tr>
<tr>
<td>创建合约</td>
<td>53,000</td>
<td>创建新合约</td>
</tr>
</tbody></table>
<h3 id="优化技巧"><a href="#优化技巧" class="headerlink" title="优化技巧"></a>优化技巧</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 1. 减少存储写入</span><br><span class="line">function batchUpdate(uint256[] memory values) public &#123;</span><br><span class="line">    uint256 length = values.length;</span><br><span class="line">    for (uint256 i = 0; i &lt; length; i++) &#123;</span><br><span class="line">        // 在内存中计算，最后一次写入存储</span><br><span class="line">    &#125;</span><br><span class="line">    result = calculateOnce(values);  // 一次写入</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 2. 使用紧凑的数据类型</span><br><span class="line">struct Packed &#123;</span><br><span class="line">    uint8 flag;      // 1 字节</span><br><span class="line">    uint8 status;    // 1 字节</span><br><span class="line">    uint128 value;   // 16 字节</span><br><span class="line">    // 总共 18 字节，共享一个 32 字节的存储槽</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 3. 避免不必要的计算</span><br><span class="line">function getDiscount(uint256 amount) public view returns (uint256) &#123;</span><br><span class="line">    // 不好：每次都计算</span><br><span class="line">    // return amount * 10 / 100;</span><br><span class="line">    </span><br><span class="line">    // 更好：预先计算常数</span><br><span class="line">    return amount * DISCOUNT_PERCENT / 100;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 4. 使用 calldata 代替 memory</span><br><span class="line">function process(bytes calldata data) public pure &#123;</span><br><span class="line">    // calldata 是只读的，不需要复制到内存</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="智能合约的真实应用"><a href="#智能合约的真实应用" class="headerlink" title="智能合约的真实应用"></a>智能合约的真实应用</h2><h3 id="DeFi（去中心化金融）"><a href="#DeFi（去中心化金融）" class="headerlink" title="DeFi（去中心化金融）"></a>DeFi（去中心化金融）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// Uniswap 风格的交换合约简化版</span><br><span class="line">contract SimpleSwap &#123;</span><br><span class="line">    mapping(address =&gt; uint256) public tokenABalance;</span><br><span class="line">    mapping(address =&gt; uint256) public tokenBBalance;</span><br><span class="line">    </span><br><span class="line">    event Swap(address indexed user, uint256 amountA, uint256 amountB);</span><br><span class="line">    </span><br><span class="line">    // x * y = k 恒定乘积公式</span><br><span class="line">    function swap(address tokenFrom, uint256 amountIn) public returns (uint256) &#123;</span><br><span class="line">        uint256 balanceIn = (tokenFrom == tokenA) ? tokenABalance[address(this)] : tokenBBalance[address(this)];</span><br><span class="line">        uint256 balanceOut = (tokenFrom == tokenA) ? tokenBBalance[address(this)] : tokenABalance[address(this)];</span><br><span class="line">        </span><br><span class="line">        uint256 amountOut = (amountIn * balanceOut) / (balanceIn + amountIn);</span><br><span class="line">        </span><br><span class="line">        emit Swap(msg.sender, amountIn, amountOut);</span><br><span class="line">        return amountOut;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="NFT-市场"><a href="#NFT-市场" class="headerlink" title="NFT 市场"></a>NFT 市场</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 简化的 NFT 市场合约</span><br><span class="line">contract NFTMarketplace &#123;</span><br><span class="line">    IERC721 public nftContract;</span><br><span class="line">    </span><br><span class="line">    struct Listing &#123;</span><br><span class="line">        address seller;</span><br><span class="line">        uint256 price;</span><br><span class="line">        bool active;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    mapping(uint256 =&gt; Listing) public listings;</span><br><span class="line">    </span><br><span class="line">    event Listed(uint256 indexed tokenId, address indexed seller, uint256 price);</span><br><span class="line">    event Purchased(uint256 indexed tokenId, address indexed buyer, uint256 price);</span><br><span class="line">    </span><br><span class="line">    function list(uint256 tokenId, uint256 price) public &#123;</span><br><span class="line">        require(nftContract.ownerOf(tokenId) == msg.sender);</span><br><span class="line">        listings[tokenId] = Listing(msg.sender, price, true);</span><br><span class="line">        emit Listed(tokenId, msg.sender, price);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function purchase(uint256 tokenId) public payable &#123;</span><br><span class="line">        Listing memory listing = listings[tokenId];</span><br><span class="line">        require(listing.active);</span><br><span class="line">        require(msg.value &gt;= listing.price);</span><br><span class="line">        </span><br><span class="line">        listings[tokenId].active = false;</span><br><span class="line">        nftContract.transferFrom(listing.seller, msg.sender, tokenId);</span><br><span class="line">        </span><br><span class="line">        (bool success, ) = listing.seller.call&#123;value: msg.value&#125;(&quot;&quot;);</span><br><span class="line">        require(success);</span><br><span class="line">        </span><br><span class="line">        emit Purchased(tokenId, msg.sender, msg.value);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="DAO（去中心化自治组织）"><a href="#DAO（去中心化自治组织）" class="headerlink" title="DAO（去中心化自治组织）"></a>DAO（去中心化自治组织）</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 简化的 DAO 投票合约</span><br><span class="line">contract SimpleDAO &#123;</span><br><span class="line">    IERC20 public governanceToken;</span><br><span class="line">    </span><br><span class="line">    struct Proposal &#123;</span><br><span class="line">        string description;</span><br><span class="line">        uint256 voteCount;</span><br><span class="line">        bool executed;</span><br><span class="line">        mapping(address =&gt; bool) voted;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    Proposal[] public proposals;</span><br><span class="line">    </span><br><span class="line">    function createProposal(string memory description) public &#123;</span><br><span class="line">        proposals.push();</span><br><span class="line">        proposals[proposals.length - 1].description = description;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function vote(uint256 proposalId) public &#123;</span><br><span class="line">        require(governanceToken.balanceOf(msg.sender) &gt; 0, &quot;No voting power&quot;);</span><br><span class="line">        require(!proposals[proposalId].voted[msg.sender], &quot;Already voted&quot;);</span><br><span class="line">        </span><br><span class="line">        proposals[proposalId].voted[msg.sender] = true;</span><br><span class="line">        proposals[proposalId].voteCount += 1;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function executeProposal(uint256 proposalId) public &#123;</span><br><span class="line">        require(proposals[proposalId].voteCount &gt; THRESHOLD);</span><br><span class="line">        require(!proposals[proposalId].executed);</span><br><span class="line">        </span><br><span class="line">        proposals[proposalId].executed = true;</span><br><span class="line">        // 执行提案逻辑</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="智能合约的发展趋势"><a href="#智能合约的发展趋势" class="headerlink" title="智能合约的发展趋势"></a>智能合约的发展趋势</h2><h3 id="1-跨链互操作性"><a href="#1-跨链互操作性" class="headerlink" title="1. 跨链互操作性"></a>1. 跨链互操作性</h3><ul>
<li><strong>跨链桥接</strong>：允许合约跨越不同区块链交互</li>
<li><strong>原子交换</strong>：不同区块链间的原子交易</li>
<li><strong>通用资产</strong>：统一的跨链资产表示</li>
</ul>
<h3 id="2-零知识证明（ZK）"><a href="#2-零知识证明（ZK）" class="headerlink" title="2. 零知识证明（ZK）"></a>2. 零知识证明（ZK）</h3><ul>
<li><strong>隐私保护</strong>：在不暴露数据的情况下证明正确性</li>
<li><strong>可扩展性</strong>：通过 ZK-rollup 扩展交易吞吐量</li>
<li><strong>合规性</strong>：满足监管要求的隐私交易</li>
</ul>
<h3 id="3-形式化验证"><a href="#3-形式化验证" class="headerlink" title="3. 形式化验证"></a>3. 形式化验证</h3><ul>
<li><strong>数学证明</strong>：证明合约代码的正确性</li>
<li><strong>减少漏洞</strong>：早期发现逻辑错误</li>
<li><strong>提高安全性</strong>：关键合约的强制验证</li>
</ul>
<h3 id="4-高级语言和框架"><a href="#4-高级语言和框架" class="headerlink" title="4. 高级语言和框架"></a>4. 高级语言和框架</h3><ul>
<li><strong>更好的开发者体验</strong>：DSL 和现代语言支持</li>
<li><strong>更高的抽象度</strong>：减少低级错误</li>
<li><strong>自动化审计</strong>：内置的安全检查</li>
</ul>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>智能合约是 Web3 的基石，通过代码形式的自动化协议，实现了去中心化应用的核心功能。开发智能合约需要掌握：</p>
<ul>
<li><strong>基础知识</strong>：区块链原理、EVM、Gas 机制</li>
<li><strong>编程技能</strong>：Solidity 或其他合约语言</li>
<li><strong>安全意识</strong>：常见漏洞、最佳实践、审计流程</li>
<li><strong>开发工具</strong>：编译、测试、部署、验证工具</li>
<li><strong>性能优化</strong>：Gas 优化、算法优化、架构设计</li>
<li><strong>实战应用</strong>：DeFi、NFT、DAO 等实际场景</li>
</ul>
<p>只有充分理解这些要素，才能开发安全、高效、有价值的智能合约应用。</p>
<h2 id="常见问题解答"><a href="#常见问题解答" class="headerlink" title="常见问题解答"></a>常见问题解答</h2><h3 id="1-什么是智能合约？"><a href="#1-什么是智能合约？" class="headerlink" title="1. 什么是智能合约？"></a>1. 什么是智能合约？</h3><p>智能合约是运行在区块链上的一段代码，具有以下特点：</p>
<ul>
<li><strong>代码定义逻辑</strong>：代码逻辑定义了合约的执行规则和条款</li>
<li><strong>账户状态</strong>：智能合约的账户保存了合约当前的状态：<ul>
<li><code>balance</code>：当前余额</li>
<li><code>nonce</code>：交易次数</li>
<li><code>code</code>：合约代码</li>
<li><code>storage</code>：存储数据，数据结构是一颗 MPT（Merkle Patricia Tree）</li>
</ul>
</li>
<li><strong>编程语言</strong>：Solidity 是智能合约最常用的语言，语法上和 JavaScript 很接近</li>
</ul>
<h3 id="2-外部账户如何调用智能合约？"><a href="#2-外部账户如何调用智能合约？" class="headerlink" title="2. 外部账户如何调用智能合约？"></a>2. 外部账户如何调用智能合约？</h3><p>外部账户（EOA）通过创建一个交易来调用智能合约：</p>
<ul>
<li><strong>接收地址</strong>：交易的接收地址为要调用的智能合约的地址</li>
<li><strong>data 域</strong>：填写要调用的函数及其参数的编码值（ABI 编码）</li>
<li><strong>执行过程</strong>：节点接收交易后，解析 data 域，调用对应的合约函数</li>
</ul>
<h4 id="代码示例"><a href="#代码示例" class="headerlink" title="代码示例"></a>代码示例</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 合约中的函数</span><br><span class="line">contract Counter &#123;</span><br><span class="line">    uint256 public count;</span><br><span class="line">    </span><br><span class="line">    function increment(uint256 amount) public &#123;</span><br><span class="line">        count += amount;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 外部调用（通过交易）</span><br><span class="line">// 交易格式：</span><br><span class="line">// to: 0x1234...（合约地址）</span><br><span class="line">// data: 0x3ad0f3f6 + 参数编码（increment(10) 的编码）</span><br><span class="line">// value: 0（不发送 ETH）</span><br></pre></td></tr></table></figure>

<h3 id="3-一个合约如何调用另一个合约中的函数？"><a href="#3-一个合约如何调用另一个合约中的函数？" class="headerlink" title="3. 一个合约如何调用另一个合约中的函数？"></a>3. 一个合约如何调用另一个合约中的函数？</h3><p>注：以太坊中规定，一个交易只有外部账户才能够发起，合约账户不能够自己主动发起一个交易。</p>
<p>合约间调用分为以下几种方式：</p>
<h4 id="方式-1：直接调用（Call）"><a href="#方式-1：直接调用（Call）" class="headerlink" title="方式 1：直接调用（Call）"></a>方式 1：直接调用（Call）</h4><p>最常用的方式，执行目标合约的函数，状态变化在目标合约中。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 目标合约</span><br><span class="line">contract TargetContract &#123;</span><br><span class="line">    uint256 public value = 0;</span><br><span class="line">    </span><br><span class="line">    function setValue(uint256 newValue) public &#123;</span><br><span class="line">        value = newValue;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function getValue() public view returns (uint256) &#123;</span><br><span class="line">        return value;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 调用合约</span><br><span class="line">contract CallerContract &#123;</span><br><span class="line">    TargetContract target;</span><br><span class="line">    </span><br><span class="line">    constructor(address _target) &#123;</span><br><span class="line">        target = TargetContract(_target);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 方式 1：直接调用</span><br><span class="line">    function callTarget(uint256 newValue) public &#123;</span><br><span class="line">        target.setValue(newValue);  // 状态变化在 target 中</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 查询目标合约的状态</span><br><span class="line">    function queryTarget() public view returns (uint256) &#123;</span><br><span class="line">        return target.getValue();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="方式-2：委托调用（Delegatecall）"><a href="#方式-2：委托调用（Delegatecall）" class="headerlink" title="方式 2：委托调用（Delegatecall）"></a>方式 2：委托调用（Delegatecall）</h4><p>执行目标合约的代码，但状态变化在<strong>调用合约</strong>中。常用于代理模式。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 逻辑合约</span><br><span class="line">contract Logic &#123;</span><br><span class="line">    uint256 public counter;</span><br><span class="line">    </span><br><span class="line">    function increment() public &#123;</span><br><span class="line">        counter += 1;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 代理合约</span><br><span class="line">contract Proxy &#123;</span><br><span class="line">    uint256 public counter;  // 与 Logic 合约的状态变量相同</span><br><span class="line">    address public implementation;</span><br><span class="line">    </span><br><span class="line">    constructor(address _logic) &#123;</span><br><span class="line">        implementation = _logic;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 使用 delegatecall 调用逻辑合约</span><br><span class="line">    function increment() public &#123;</span><br><span class="line">        (bool success, ) = implementation.delegatecall(</span><br><span class="line">            abi.encodeWithSignature(&quot;increment()&quot;)</span><br><span class="line">        );</span><br><span class="line">        require(success, &quot;Delegatecall failed&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 状态变化在 Proxy 中，不在 Logic 中</span><br><span class="line">    function getCounter() public view returns (uint256) &#123;</span><br><span class="line">        return counter;  // 返回 Proxy 的 counter</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="方式-3：低级调用（Low-level-Call）"><a href="#方式-3：低级调用（Low-level-Call）" class="headerlink" title="方式 3：低级调用（Low-level Call）"></a>方式 3：低级调用（Low-level Call）</h4><p>使用 <code>call</code> 方法进行低级调用，适合不知道目标合约 ABI 的场景。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract Caller &#123;</span><br><span class="line">    // 低级调用：发送 ETH 并调用函数</span><br><span class="line">    function lowLevelCall(address target, bytes memory data) public payable returns (bool) &#123;</span><br><span class="line">        (bool success, bytes memory result) = target.call&#123;value: msg.value&#125;(data);</span><br><span class="line">        return success;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 使用示例</span><br><span class="line">    function callWithData(address target) public &#123;</span><br><span class="line">        // 准备函数调用数据：setValue(42)</span><br><span class="line">        bytes memory payload = abi.encodeWithSignature(&quot;setValue(uint256)&quot;, 42);</span><br><span class="line">        </span><br><span class="line">        (bool success, ) = target.call(payload);</span><br><span class="line">        require(success, &quot;Call failed&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="方式-4：接口调用（推荐）"><a href="#方式-4：接口调用（推荐）" class="headerlink" title="方式 4：接口调用（推荐）"></a>方式 4：接口调用（推荐）</h4><p>定义接口，通过接口调用目标合约，最安全和清晰的方式。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 目标合约接口</span><br><span class="line">interface ITarget &#123;</span><br><span class="line">    function setValue(uint256 newValue) external;</span><br><span class="line">    function getValue() external view returns (uint256);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 调用合约</span><br><span class="line">contract SafeCaller &#123;</span><br><span class="line">    ITarget target;</span><br><span class="line">    </span><br><span class="line">    constructor(address _target) &#123;</span><br><span class="line">        target = ITarget(_target);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 通过接口调用（最推荐）</span><br><span class="line">    function updateTarget(uint256 newValue) public &#123;</span><br><span class="line">        target.setValue(newValue);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function readTarget() public view returns (uint256) &#123;</span><br><span class="line">        return target.getValue();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-合约调用的成本和限制"><a href="#4-合约调用的成本和限制" class="headerlink" title="4. 合约调用的成本和限制"></a>4. 合约调用的成本和限制</h3><h4 id="Gas-成本"><a href="#Gas-成本" class="headerlink" title="Gas 成本"></a>Gas 成本</h4><table>
<thead>
<tr>
<th>调用方式</th>
<th>Gas 消耗</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td><strong>Call</strong></td>
<td>~600-700</td>
<td>普通外部调用</td>
</tr>
<tr>
<td><strong>Delegatecall</strong></td>
<td>~600-700</td>
<td>委托调用，代理模式</td>
</tr>
<tr>
<td><strong>Static Call</strong></td>
<td>~600-700</td>
<td>只读调用（view&#x2F;pure）</td>
</tr>
<tr>
<td><strong>Create</strong></td>
<td>~53,000</td>
<td>创建新合约</td>
</tr>
</tbody></table>
<h4 id="限制条件"><a href="#限制条件" class="headerlink" title="限制条件"></a>限制条件</h4><ol>
<li><strong>调用深度限制</strong>：最多 1024 层嵌套调用</li>
<li><strong>Gas 限制</strong>：调用方必须提供足够的 Gas</li>
<li><strong>异常处理</strong>：调用失败需要妥善处理</li>
<li><strong>状态一致性</strong>：Delegatecall 需要确保存储布局一致</li>
</ol>
<h4 id="错误处理示例"><a href="#错误处理示例" class="headerlink" title="错误处理示例"></a>错误处理示例</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract SafeCallExample &#123;</span><br><span class="line">    function safeCall(address target, bytes memory data) public returns (bool) &#123;</span><br><span class="line">        // 方式 1：使用 try-catch（Solidity 0.6.0+）</span><br><span class="line">        try &#123;</span><br><span class="line">            (bool success, ) = target.call(data);</span><br><span class="line">            return success;</span><br><span class="line">        &#125; catch &#123;</span><br><span class="line">            // 处理调用失败</span><br><span class="line">            return false;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 方式 2：检查返回值</span><br><span class="line">    function checkedCall(address target, bytes memory data) public returns (bool) &#123;</span><br><span class="line">        (bool success, bytes memory result) = target.call(data);</span><br><span class="line">        if (!success) &#123;</span><br><span class="line">            // 解析错误信息</span><br><span class="line">            if (result.length &gt; 0) &#123;</span><br><span class="line">                // 目标合约返回了错误</span><br><span class="line">                assembly &#123;</span><br><span class="line">                    revert(add(32, result), mload(result))</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; else &#123;</span><br><span class="line">                revert(&quot;Call failed&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-调用方式对比表"><a href="#5-调用方式对比表" class="headerlink" title="5. 调用方式对比表"></a>5. 调用方式对比表</h3><table>
<thead>
<tr>
<th>特性</th>
<th>Call</th>
<th>Delegatecall</th>
<th>Interface</th>
<th>Low-level</th>
</tr>
</thead>
<tbody><tr>
<td><strong>安全性</strong></td>
<td>★★★★★</td>
<td>★★★</td>
<td>★★★★★</td>
<td>★★</td>
</tr>
<tr>
<td><strong>可读性</strong></td>
<td>★★★★★</td>
<td>★★★★</td>
<td>★★★★★</td>
<td>★★</td>
</tr>
<tr>
<td><strong>灵活性</strong></td>
<td>★★★</td>
<td>★★★★★</td>
<td>★★★</td>
<td>★★★★★</td>
</tr>
<tr>
<td><strong>需要 ABI</strong></td>
<td>是</td>
<td>是</td>
<td>是</td>
<td>否</td>
</tr>
<tr>
<td><strong>典型用途</strong></td>
<td>普通调用</td>
<td>代理模式</td>
<td>标准调用</td>
<td>动态调用</td>
</tr>
<tr>
<td><strong>推荐度</strong></td>
<td>★★★★★</td>
<td>★★★★</td>
<td>★★★★★</td>
<td>★★</td>
</tr>
</tbody></table>
<h3 id="6-最佳实践"><a href="#6-最佳实践" class="headerlink" title="6. 最佳实践"></a>6. 最佳实践</h3><ul>
<li><strong>优先使用接口调用</strong>：提供类型安全和清晰的 API</li>
<li><strong>验证调用返回值</strong>：始终检查调用是否成功</li>
<li><strong>避免过深的调用</strong>：注意堆栈深度限制</li>
<li><strong>文档化合约依赖</strong>：说明与其他合约的交互方式</li>
<li><strong>谨慎使用 Delegatecall</strong>：确保存储布局兼容</li>
<li><strong>设置合适的 Gas 限制</strong>：提供足够但不过多的 Gas</li>
<li><strong>实现重入保护</strong>：在必要时使用 ReentrancyGuard</li>
</ul>
<h3 id="7-智能合约是否支持多线程？"><a href="#7-智能合约是否支持多线程？" class="headerlink" title="7. 智能合约是否支持多线程？"></a>7. 智能合约是否支持多线程？</h3><p><strong>答案：智能合约不支持多线程。</strong></p>
<h4 id="原因分析"><a href="#原因分析" class="headerlink" title="原因分析"></a>原因分析</h4><ol>
<li><p><strong>确定性要求</strong></p>
<ul>
<li>区块链需要确保每个节点都能按照相同的方式执行合约</li>
<li>多线程会引入不确定性（线程调度、竞态条件等）</li>
<li>无法保证所有节点的执行结果一致</li>
</ul>
</li>
<li><p><strong>单线程执行模型</strong></p>
<ul>
<li>智能合约在 EVM 上以单线程方式顺序执行</li>
<li>每个交易的执行是原子的，不会被中断</li>
<li>一个交易完全执行完毕后，才会执行下一个交易</li>
</ul>
</li>
<li><p><strong>状态一致性</strong></p>
<ul>
<li>多线程会导致共享状态的竞争问题</li>
<li>区块链无法通过锁或信号量等机制来同步</li>
<li>单线程执行保证了状态的一致性</li>
</ul>
</li>
</ol>
<h4 id="代码示例对比"><a href="#代码示例对比" class="headerlink" title="代码示例对比"></a>代码示例对比</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 单线程执行示例</span><br><span class="line">contract Counter &#123;</span><br><span class="line">    uint256 public count = 0;</span><br><span class="line">    </span><br><span class="line">    // 这个函数总是原子地执行，不会被中断</span><br><span class="line">    function increment() public &#123;</span><br><span class="line">        uint256 temp = count;      // 步骤 1</span><br><span class="line">        temp = temp + 1;           // 步骤 2</span><br><span class="line">        count = temp;              // 步骤 3</span><br><span class="line">        // 执行过程不会被其他交易打断</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 如果支持多线程（理论）- 会导致问题</span><br><span class="line">// 假设有两个线程同时执行 increment()</span><br><span class="line">// 线程 1: 读取 count = 0 → 加 1 → 写入 count = 1</span><br><span class="line">// 线程 2: 读取 count = 0 → 加 1 → 写入 count = 1</span><br><span class="line">// 结果：count = 1（而不是 2）- 这是竞态条件</span><br></pre></td></tr></table></figure>

<h4 id="并发性的解决方案"><a href="#并发性的解决方案" class="headerlink" title="并发性的解决方案"></a>并发性的解决方案</h4><p>虽然智能合约本身不支持多线程，但可以通过以下方式实现并发：</p>
<ol>
<li><p><strong>多个交易并发提交</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 用户 A 提交交易 1</span><br><span class="line">await contract.increment();</span><br><span class="line"></span><br><span class="line">// 用户 B 提交交易 2（与交易 1 并发提交）</span><br><span class="line">await contract.increment();</span><br><span class="line"></span><br><span class="line">// 矿工会依次执行这两个交易，最终 count = 2</span><br></pre></td></tr></table></figure>
</li>
<li><p><strong>状态通道（State Channels）</strong></p>
<ul>
<li>参与者在链外执行多个交易</li>
<li>最后一次性提交到区块链</li>
<li>实现高并发和低费用</li>
</ul>
</li>
<li><p><strong>侧链&#x2F;Rollup</strong></p>
<ul>
<li>使用 Layer 2 解决方案处理大量交易</li>
<li>定期将状态同步回主链</li>
</ul>
</li>
</ol>
<h4 id="多线程执行的限制与解决方案总结"><a href="#多线程执行的限制与解决方案总结" class="headerlink" title="多线程执行的限制与解决方案总结"></a>多线程执行的限制与解决方案总结</h4><ul>
<li><strong>智能合约采用单线程执行模型</strong></li>
<li><strong>保证了区块链的一致性和确定性</strong></li>
<li><strong>交易按顺序执行，每个交易是原子的</strong></li>
<li><strong>并发通过多个交易的顺序执行来实现</strong></li>
</ul>
<h2 id="错误处理机制"><a href="#错误处理机制" class="headerlink" title="错误处理机制"></a>错误处理机制</h2><p>智能合约中的错误处理机制确保了状态的一致性和安全性。</p>
<h3 id="基本原理"><a href="#基本原理" class="headerlink" title="基本原理"></a>基本原理</h3><ol>
<li><strong>原子性保证</strong>：一旦遇到异常，除特殊情况外，本次执行操作全部回滚</li>
<li><strong>状态恢复</strong>：所有状态变更都会被撤销，就像交易从未发生</li>
<li><strong>Gas 消耗</strong>：已消耗的 Gas 不会退回（作为执行成本的一部分）</li>
</ol>
<h3 id="抛出错误的语句"><a href="#抛出错误的语句" class="headerlink" title="抛出错误的语句"></a>抛出错误的语句</h3><h4 id="1-require-条件检查"><a href="#1-require-条件检查" class="headerlink" title="1. require() - 条件检查"></a>1. <code>require()</code> - 条件检查</h4><p>用于检查输入或外部组件引起的错误，<strong>会退回剩余 Gas</strong>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract SafeTransfer &#123;</span><br><span class="line">    mapping(address =&gt; uint256) public balances;</span><br><span class="line">    </span><br><span class="line">    function withdraw(uint256 amount) public &#123;</span><br><span class="line">        // 检查用户是否有足够的余额</span><br><span class="line">        require(balances[msg.sender] &gt;= amount, &quot;Insufficient balance&quot;);</span><br><span class="line">        </span><br><span class="line">        // 检查转账是否成功</span><br><span class="line">        require(msg.sender.send(amount), &quot;Transfer failed&quot;);</span><br><span class="line">        </span><br><span class="line">        balances[msg.sender] -= amount;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Gas 成本</strong>：约 3 Gas（如果条件满足）或 3 + 内存成本（如果失败）</p>
<h4 id="2-assert-内部检查"><a href="#2-assert-内部检查" class="headerlink" title="2. assert() - 内部检查"></a>2. <code>assert()</code> - 内部检查</h4><p>用于检查内部错误和不变量，<strong>不会退回剩余 Gas</strong>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract MathSafe &#123;</span><br><span class="line">    function add(uint256 a, uint256 b) public pure returns (uint256) &#123;</span><br><span class="line">        uint256 c = a + b;</span><br><span class="line">        </span><br><span class="line">        // 检查是否发生溢出（内部逻辑错误）</span><br><span class="line">        assert(c &gt;= a);  // 这个条件必须总是真的</span><br><span class="line">        </span><br><span class="line">        return c;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function updateState(uint256 newValue) public &#123;</span><br><span class="line">        uint256 oldValue = state;</span><br><span class="line">        state = newValue;</span><br><span class="line">        </span><br><span class="line">        // 验证内部不变量</span><br><span class="line">        assert(state != oldValue || newValue == oldValue);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Gas 成本</strong>：约 3 Gas（如果条件满足）或 Gas 全部消耗（如果失败）</p>
<h4 id="3-revert-手动终止"><a href="#3-revert-手动终止" class="headerlink" title="3. revert() - 手动终止"></a>3. <code>revert()</code> - 手动终止</h4><p>无条件地终止运行并回滚状态变动，<strong>会退回剩余 Gas</strong>。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract RoleBasedAccess &#123;</span><br><span class="line">    address public owner;</span><br><span class="line">    </span><br><span class="line">    modifier onlyOwner() &#123;</span><br><span class="line">        if (msg.sender != owner) &#123;</span><br><span class="line">            revert(&quot;Only owner can call this function&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">        _;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function criticalAction() public onlyOwner &#123;</span><br><span class="line">        // 手动检查并回滚</span><br><span class="line">        if (msg.value &lt; 1 ether) &#123;</span><br><span class="line">            revert(&quot;Minimum 1 ETH required&quot;);</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        // 执行操作</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>Gas 成本</strong>：约 0 Gas（无条件回滚）</p>
<h4 id="4-try-catch-异常捕获（Solidity-0-6-0-）"><a href="#4-try-catch-异常捕获（Solidity-0-6-0-）" class="headerlink" title="4. try-catch - 异常捕获（Solidity 0.6.0+）"></a>4. <code>try-catch</code> - 异常捕获（Solidity 0.6.0+）</h4><p>捕获外部调用的异常，允许合约继续执行。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract TryCatchExample &#123;</span><br><span class="line">    OtherContract other;</span><br><span class="line">    </span><br><span class="line">    function callOther(uint256 value) public returns (bool) &#123;</span><br><span class="line">        try other.riskyFunction(value) returns (uint256 result) &#123;</span><br><span class="line">            // 调用成功</span><br><span class="line">            emit Success(result);</span><br><span class="line">            return true;</span><br><span class="line">        &#125; catch Error(string memory reason) &#123;</span><br><span class="line">            // 捕获 require/revert 的错误信息</span><br><span class="line">            emit Error(&quot;Require failed&quot;, reason);</span><br><span class="line">            return false;</span><br><span class="line">        &#125; catch Panic(uint errorCode) &#123;</span><br><span class="line">            // 捕获 assert 失败</span><br><span class="line">            emit Error(&quot;Assertion failed&quot;, errorCode);</span><br><span class="line">            return false;</span><br><span class="line">        &#125; catch &#123;</span><br><span class="line">            // 捕获其他错误（如低级调用失败）</span><br><span class="line">            emit Error(&quot;Unknown error&quot;, &quot;&quot;);</span><br><span class="line">            return false;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="错误处理对比表"><a href="#错误处理对比表" class="headerlink" title="错误处理对比表"></a>错误处理对比表</h3><table>
<thead>
<tr>
<th>错误类型</th>
<th>说明</th>
<th>Gas 退回</th>
<th>典型用途</th>
<th>错误信息</th>
</tr>
</thead>
<tbody><tr>
<td><strong>require</strong></td>
<td>条件检查</td>
<td>✓ 是</td>
<td>输入验证、权限检查</td>
<td>支持自定义消息</td>
</tr>
<tr>
<td><strong>assert</strong></td>
<td>内部验证</td>
<td>✗ 否</td>
<td>检查不变量、内部逻辑</td>
<td>无消息</td>
</tr>
<tr>
<td><strong>revert</strong></td>
<td>主动回滚</td>
<td>✓ 是</td>
<td>复杂条件检查、自定义错误</td>
<td>支持自定义消息</td>
</tr>
<tr>
<td><strong>try-catch</strong></td>
<td>异常捕获</td>
<td>✓ 部分</td>
<td>外部调用保护</td>
<td>可捕获详细错误</td>
</tr>
</tbody></table>
<h3 id="实现最佳实践"><a href="#实现最佳实践" class="headerlink" title="实现最佳实践"></a>实现最佳实践</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract ErrorHandlingBestPractices &#123;</span><br><span class="line">    // 1. 使用自定义错误（Solidity 0.8.4+）- 最省 Gas</span><br><span class="line">    error InsufficientBalance(uint256 available, uint256 required);</span><br><span class="line">    error Unauthorized(address caller);</span><br><span class="line">    </span><br><span class="line">    uint256 public balance;</span><br><span class="line">    address public owner;</span><br><span class="line">    </span><br><span class="line">    // 2. 在函数开头检查条件（fail-fast 原则）</span><br><span class="line">    function withdraw(uint256 amount) public &#123;</span><br><span class="line">        require(msg.sender == owner, &quot;Only owner&quot;);</span><br><span class="line">        require(balance &gt;= amount, &quot;Insufficient balance&quot;);</span><br><span class="line">        </span><br><span class="line">        // 修改状态</span><br><span class="line">        balance -= amount;</span><br><span class="line">        </span><br><span class="line">        // 最后才进行外部调用</span><br><span class="line">        (bool success, ) = msg.sender.call&#123;value: amount&#125;(&quot;&quot;);</span><br><span class="line">        require(success, &quot;Transfer failed&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 3. 使用自定义错误类型</span><br><span class="line">    function transfer(address to, uint256 amount) public &#123;</span><br><span class="line">        if (balance &lt; amount) &#123;</span><br><span class="line">            revert InsufficientBalance(balance, amount);</span><br><span class="line">        &#125;</span><br><span class="line">        if (msg.sender != owner) &#123;</span><br><span class="line">            revert Unauthorized(msg.sender);</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        balance -= amount;</span><br><span class="line">        // ... transfer logic</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 4. 保护外部调用</span><br><span class="line">    function safeLowLevelCall(address target) public returns (bool) &#123;</span><br><span class="line">        (bool success, bytes memory data) = target.call(&quot;&quot;);</span><br><span class="line">        </span><br><span class="line">        if (!success) &#123;</span><br><span class="line">            // 解析错误信息</span><br><span class="line">            if (data.length &gt; 0) &#123;</span><br><span class="line">                assembly &#123;</span><br><span class="line">                    revert(add(32, data), mload(data))</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="嵌套调用与原子性"><a href="#嵌套调用与原子性" class="headerlink" title="嵌套调用与原子性"></a>嵌套调用与原子性</h2><h3 id="什么是嵌套调用"><a href="#什么是嵌套调用" class="headerlink" title="什么是嵌套调用"></a>什么是嵌套调用</h3><p>嵌套调用是指一个合约调用另一个合约中的函数。在区块链中，嵌套调用的处理方式直接影响到整个交易的原子性。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 合约 A 调用合约 B 的函数</span><br><span class="line">contract A &#123;</span><br><span class="line">    B contractB;</span><br><span class="line">    </span><br><span class="line">    function callB() public &#123;</span><br><span class="line">        contractB.doSomething();  // 这就是嵌套调用</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">contract B &#123;</span><br><span class="line">    function doSomething() public &#123;</span><br><span class="line">        // 执行逻辑</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="原子性保证"><a href="#原子性保证" class="headerlink" title="原子性保证"></a>原子性保证</h3><p><strong>智能合约的执行具有原子性</strong>：执行过程中出现错误，会导致整个交易回滚。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract AtomicExample &#123;</span><br><span class="line">    uint256 public value1 = 0;</span><br><span class="line">    uint256 public value2 = 0;</span><br><span class="line">    </span><br><span class="line">    function transferAssets(address other) public &#123;</span><br><span class="line">        value1 -= 100;</span><br><span class="line">        other.transfer(msg.value);  // 如果这里失败...</span><br><span class="line">        value2 -= 50;               // ...这里不会执行</span><br><span class="line">        </span><br><span class="line">        // 整个交易会回滚，value1 和 value2 都恢复到原始状态</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="嵌套调用的连锁回滚"><a href="#嵌套调用的连锁回滚" class="headerlink" title="嵌套调用的连锁回滚"></a>嵌套调用的连锁回滚</h3><h4 id="会触发连锁式回滚的调用方式"><a href="#会触发连锁式回滚的调用方式" class="headerlink" title="会触发连锁式回滚的调用方式"></a>会触发连锁式回滚的调用方式</h4><h5 id="直接调用（最常见）"><a href="#直接调用（最常见）" class="headerlink" title="直接调用（最常见）"></a>直接调用（最常见）</h5><p>被调用合约的异常会导致调用合约也一起回滚。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract CallerA &#123;</span><br><span class="line">    BContractB b;</span><br><span class="line">    uint256 public state = 0;</span><br><span class="line">    </span><br><span class="line">    function callB() public &#123;</span><br><span class="line">        state = 1;</span><br><span class="line">        b.riskyFunction();  // 如果这个函数 revert，整个交易回滚</span><br><span class="line">        state = 2;          // 这行不会执行</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">contract B &#123;</span><br><span class="line">    function riskyFunction() public &#123;</span><br><span class="line">        require(false, &quot;Error in B&quot;);  // B 中出错</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 结果：state 恢复为 0（未发生任何改变）</span><br></pre></td></tr></table></figure>

<h5 id="委托调用（Delegatecall）"><a href="#委托调用（Delegatecall）" class="headerlink" title="委托调用（Delegatecall）"></a>委托调用（Delegatecall）</h5><p>被调用函数在调用合约的上下文中执行，异常同样会导致全部回滚。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract ProxyA &#123;</span><br><span class="line">    address implementation;</span><br><span class="line">    uint256 public count = 0;</span><br><span class="line">    </span><br><span class="line">    function executeLogic() public &#123;</span><br><span class="line">        count = 10;</span><br><span class="line">        (bool success, ) = implementation.delegatecall(</span><br><span class="line">            abi.encodeWithSignature(&quot;increment()&quot;)</span><br><span class="line">        );</span><br><span class="line">        </span><br><span class="line">        if (!success) &#123;</span><br><span class="line">            // 即使检查了 success，count 已经被改变了</span><br><span class="line">            // delegatecall 在同一交易上下文中</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="不会触发连锁式回滚的调用方式"><a href="#不会触发连锁式回滚的调用方式" class="headerlink" title="不会触发连锁式回滚的调用方式"></a>不会触发连锁式回滚的调用方式</h4><h5 id="低级-call-调用"><a href="#低级-call-调用" class="headerlink" title="低级 call() 调用"></a>低级 <code>call()</code> 调用</h5><p>被调用合约的异常不会自动导致调用合约回滚，需要手动处理。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract CallerA &#123;</span><br><span class="line">    uint256 public state = 0;</span><br><span class="line">    </span><br><span class="line">    function safeCall(address target) public &#123;</span><br><span class="line">        state = 1;</span><br><span class="line">        </span><br><span class="line">        // 使用低级 call，返回成功/失败标志</span><br><span class="line">        (bool success, ) = target.call(</span><br><span class="line">            abi.encodeWithSignature(&quot;riskyFunction()&quot;)</span><br><span class="line">        );</span><br><span class="line">        </span><br><span class="line">        if (!success) &#123;</span><br><span class="line">            // 可以选择继续执行或回滚</span><br><span class="line">            state = 999;  // 这行会执行</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        state = 2;  // 这行也会执行</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">contract B &#123;</span><br><span class="line">    function riskyFunction() public &#123;</span><br><span class="line">        require(false, &quot;Error&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 结果：state 最终为 999 或 2（没有连锁回滚）</span><br></pre></td></tr></table></figure>

<h5 id="transfer-x2F-send-方法"><a href="#transfer-x2F-send-方法" class="headerlink" title="transfer() &#x2F; send() 方法"></a><code>transfer()</code> &#x2F; <code>send()</code> 方法</h5><p>这些方法有 Gas 限制（通常 2300 Gas），失败时不会导致调用合约回滚。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract Wallet &#123;</span><br><span class="line">    mapping(address =&gt; uint256) public balances;</span><br><span class="line">    </span><br><span class="line">    function withdraw(uint256 amount) public &#123;</span><br><span class="line">        balances[msg.sender] -= amount;</span><br><span class="line">        </span><br><span class="line">        // 使用 transfer，即使失败也不会回滚前面的状态改变</span><br><span class="line">        // （现在更推荐使用 call）</span><br><span class="line">        msg.sender.transfer(amount);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="转账引起的嵌套调用"><a href="#转账引起的嵌套调用" class="headerlink" title="转账引起的嵌套调用"></a>转账引起的嵌套调用</h3><p><strong>一个合约直接向另一个合约账户转账，没有指明调用哪个函数，仍然会引起嵌套调用。</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract Sender &#123;</span><br><span class="line">    function sendETH(address receiver) public payable &#123;</span><br><span class="line">        // 发送 ETH 到 receiver</span><br><span class="line">        (bool success, ) = receiver.call&#123;value: msg.value&#125;(&quot;&quot;);</span><br><span class="line">        require(success, &quot;Transfer failed&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">contract Receiver &#123;</span><br><span class="line">    // 如果 receiver 是合约，接收 ETH 会自动调用这个函数</span><br><span class="line">    receive() external payable &#123;</span><br><span class="line">        // 这是一个嵌套调用！</span><br><span class="line">        // 如果这里 revert，整个转账交易会回滚</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 或者这个函数（如果 receive 不存在）</span><br><span class="line">    fallback() external payable &#123;</span><br><span class="line">        // 同样是嵌套调用</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="嵌套调用的最佳实践"><a href="#嵌套调用的最佳实践" class="headerlink" title="嵌套调用的最佳实践"></a>嵌套调用的最佳实践</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">contract SafeNestedCalls &#123;</span><br><span class="line">    // 1. 使用 Checks-Effects-Interactions（CEI）模式</span><br><span class="line">    function withdraw(uint256 amount) public &#123;</span><br><span class="line">        // Checks（检查）</span><br><span class="line">        require(balances[msg.sender] &gt;= amount, &quot;Insufficient balance&quot;);</span><br><span class="line">        </span><br><span class="line">        // Effects（修改状态）</span><br><span class="line">        balances[msg.sender] -= amount;</span><br><span class="line">        </span><br><span class="line">        // Interactions（外部调用）- 放在最后</span><br><span class="line">        (bool success, ) = msg.sender.call&#123;value: amount&#125;(&quot;&quot;);</span><br><span class="line">        require(success, &quot;Transfer failed&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 2. 处理低级调用的失败</span><br><span class="line">    function safeExternalCall(address target) public &#123;</span><br><span class="line">        (bool success, bytes memory result) = target.call(&quot;&quot;);</span><br><span class="line">        </span><br><span class="line">        if (!success) &#123;</span><br><span class="line">            // 手动处理错误，而不是依赖自动回滚</span><br><span class="line">            if (result.length &gt; 0) &#123;</span><br><span class="line">                assembly &#123;</span><br><span class="line">                    revert(add(32, result), mload(result))</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 3. 使用 try-catch 捕获异常</span><br><span class="line">    function robustCall(address target) public returns (bool) &#123;</span><br><span class="line">        try this.callHelper(target) &#123;</span><br><span class="line">            return true;</span><br><span class="line">        &#125; catch &#123;</span><br><span class="line">            return false;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function callHelper(address target) external &#123;</span><br><span class="line">        // 可能抛出异常的调用</span><br><span class="line">        (bool success, ) = target.call(&quot;&quot;);</span><br><span class="line">        require(success, &quot;Call failed&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    // 4. 实现重入保护</span><br><span class="line">    bool private locked;</span><br><span class="line">    </span><br><span class="line">    modifier noReentrancy() &#123;</span><br><span class="line">        require(!locked, &quot;No reentrancy&quot;);</span><br><span class="line">        locked = true;</span><br><span class="line">        _;</span><br><span class="line">        locked = false;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    function protectedWithdraw(uint256 amount) public noReentrancy &#123;</span><br><span class="line">        // 防止重入攻击的嵌套调用</span><br><span class="line">        require(balances[msg.sender] &gt;= amount);</span><br><span class="line">        balances[msg.sender] -= amount;</span><br><span class="line">        </span><br><span class="line">        (bool success, ) = msg.sender.call&#123;value: amount&#125;(&quot;&quot;);</span><br><span class="line">        require(success, &quot;Transfer failed&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="嵌套调用的调用深度限制"><a href="#嵌套调用的调用深度限制" class="headerlink" title="嵌套调用的调用深度限制"></a>嵌套调用的调用深度限制</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">// 以太坊规定最多 1024 层嵌套调用</span><br><span class="line">// 如果超过这个限制，会返回 &quot;out of stack&quot; 错误</span><br><span class="line"></span><br><span class="line">contract DepthExample &#123;</span><br><span class="line">    address public next;</span><br><span class="line">    </span><br><span class="line">    function callNext() public &#123;</span><br><span class="line">        // 如果这是第 1024 次调用，下一次调用会失败</span><br><span class="line">        next.call(abi.encodeWithSignature(&quot;callNext()&quot;));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="关键总结"><a href="#关键总结" class="headerlink" title="关键总结"></a>关键总结</h2><p>智能合约的执行模型设计确保了区块链的一致性和安全性：</p>
<ul>
<li><strong>单线程执行</strong>：保证确定性，所有节点执行结果一致</li>
<li><strong>原子性</strong>：要么全部执行，要么全部回滚</li>
<li><strong>错误处理</strong>：通过 require&#x2F;assert&#x2F;revert 机制确保状态正确</li>
<li><strong>嵌套调用</strong>：需要了解不同调用方式的回滚行为</li>
<li><strong>CEI 模式</strong>：按”检查-修改-交互”的顺序编写代码，提高安全性</li>
</ul>
]]></content>
      <tags>
        <tag>web3</tag>
      </tags>
  </entry>
  <entry>
    <title>View的事件分发机制</title>
    <url>/2023/04/30/View%E7%9A%84%E4%BA%8B%E4%BB%B6%E5%88%86%E5%8F%91%E6%9C%BA%E5%88%B6/</url>
    <content><![CDATA[<p>在讲到View的事件分发机制之前要首先了解一下Activity的组成，然后从源码的角度来分析View的事件分发机制。</p>
<span id="more"></span>
<h2 id="一、源码解析Activity的构成"><a href="#一、源码解析Activity的构成" class="headerlink" title="一、源码解析Activity的构成"></a>一、源码解析Activity的构成</h2><p>点击事件用MotionEvent来表示，当一个点击事件产生后，事件最先传递给Activity，所以我们首先了解一下Activity的构成。当我们写Activity时，会调用setContentView()<br>方法来加载布局。现在来看看setContentView()方法是怎么实现的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public void setContentView(@LayoutRes int layoutResID) &#123;</span><br><span class="line">	getWindow().setContentView(layoutResID);</span><br><span class="line">	initWindowDecorActionBar();</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>这里调用了getWindow().setContentView(layoutResID)，getWindow()指的PhoneWindow，在PhoneWindow的setContentView()方法里，先是有installDecor()方法<br>该方法里面调用了generateDecor()方法，在这里创建了一个DecorView，这个DecorView就是Activity的根View.<br>在PhoneWindow中的installDecor()方法中，还有一个generateLayout(mDecor)方法，其主要内容就是根据不同的情况加载不同的布局给layoutResource。</p>
<p>一个Acticity包含一个Window对象，这个对象是由PhoneWindow来实现的。PhoneWindow将DecorView作为整个应用窗口的根，而这个DecorView又将屏幕<br>划分为两个区域，一个是TitleView，另一个是ContentView，而我们平常做应用所写的布局正是展示在ContentView中的。</p>
<h2 id="二、源码解析View的事件分发机制"><a href="#二、源码解析View的事件分发机制" class="headerlink" title="二、源码解析View的事件分发机制"></a>二、源码解析View的事件分发机制</h2><p>当我们点击屏幕时，就产生了点击事件，这个事件被封装成一个类：MotionEvent。而当这个MotionEvent产生后，那么系统就会将这个MotionEvent传递给View的层级，<br>MotionEvent在View中的层级传递就是点击事件分发。事件分发的三个重要方法：<br>dispatchTouchEvent(MotionEvent ev)——用来进行事件的分发。<br>onInterceptTouchEvent(MotionEvent ev)——用来进行事件的拦截，在dispatchTouchEvent()方法中调用，需要注意的是View没有提供该方法。<br>onTouchEvent(MotionEvent ev)——用来处理点击事件，在dispatchTouchEvent()方法中调用。</p>
<h3 id="1-View的事件分发机制"><a href="#1-View的事件分发机制" class="headerlink" title="1.View的事件分发机制"></a>1.View的事件分发机制</h3><p>当点击事件产生后，事件首先会传递给当前的Activity，这会调用Activity的dispatchTouchEvent()方法，当然具体的事件处理工作都是交由Activity中的PhoneWindow来完成的，然后PhoneWindow再把事件处理工作交给DecorView，之后再由<br>DecorView将事件处理工作交给根ViewGroup。所以，我们从ViewGroup的dispatchTouchEvent()方法开始分析，代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">@Override</span><br><span class="line">public boolean dispatchTouchEvent (MotionEvent ev) &#123;</span><br><span class="line">	...</span><br><span class="line">	if (actionMasked == MotionEvent.ACTION_ DOWN) &#123;</span><br><span class="line">		cancelAndClearTouchTargets (ev);</span><br><span class="line">		resetTouchState () ;</span><br><span class="line">	&#125;</span><br><span class="line">	if (actionMasked == MotionEvent.ACTION_DOWN</span><br><span class="line">|		|| mFirstTouchTarget != null) (//1</span><br><span class="line">		final boolean disallowIntercept = (GroupFlags &amp; FLAG_DISALLOW_</span><br><span class="line">		INTERCEPT) != 0;//2 if (!disallowIntercept) I</span><br><span class="line">		intercepted = onInterceptTouchEvent (ev);</span><br><span class="line">		ev. setAction (action);</span><br><span class="line">	&#125; else &#123;</span><br><span class="line">		intercepted = false;</span><br><span class="line">	&#125;</span><br><span class="line">	&#125; else &#123;</span><br><span class="line">		intercepted = true;</span><br><span class="line">	&#125;</span><br><span class="line">	...</span><br><span class="line">	return handled;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里首先判断事件是否Down事件，如果是，则进行初始化，resetTouchState方法中会把mFirstTouchTarget的值置为null<br>这里为啥要初始化呢？<br>因为一个完整的事件序列是从DOWN开始，以UP结束的。所以如果是DOWN事件，那么说明这是一个新的事件序列，故而需要初始化之前的状态。<br>当ViewGroup要拦截事件的时候，那么后续的事件序列都将交给它处理，而不用再调用onInterceptTouchEvent()方法了，所以，onInterceptTouchEvent()<br>方法并不是每次事件都会调用的。<br>onInterceptTouchEvent()方法默认返回false，不进行拦截。如果想要让ViewGroup拦截事件，那么应该在自定义的ViewGroup中重写这个方法。<br>在dispatchTouchEvent()方法剩余的部分：<br>首先遍历ViewGroup的子元素，判断子元素是否能够接受到点击事件，如果子元素能够接收到点击事件，则交由子元素来处理。需要注意这个for循环<br>是倒序遍历的，即从最上层的子View开始往内层遍历。。同时它也会判断触摸点位置是否在子view的范围内或者子View是否正在播放动画，如果均不符合则<br>执行continue语句，表示这个子View不符合条件，开始遍历下一个子View。<br>如果有子View，则调用子View的dispatchTouchEvent(event)方法，如果ViewGroup没有子View，则调用super.dispatchTouchEvent(event)方法。viewgroup没有子View<br>则调用super.dispatchTouchEvent(event)方法。ViewGroup是集成View的。在View的dispatchTouchEvent(event)中，如果OnTouchListener不为null并且onTouch方法返回true，<br>则表示事件被消费，就不会执行onTouchEvent(event)，否则就会执行，可以看出OnTouchListener中的onTouch方法优先级要高于onTouchEvent(event)。<br>在onTouchEvent()方法中，只要View的Clickable和Long_clickable有一个为true，那么onTouchEvent()方法就会返回true消耗这个事件。前面两个变量分别代表<br>view可以被点击和长按点击，可以通过View的setClickable和setLongClickable方法来设置，也可以通过View的setOnclickListener和setOnLongClickListener来设置，它们<br>会自动将View设置为Lickable和Long_clickable<br>接着在Action_UP事件会调用performClick()方法，如果View设置了OnClickListener，那么它的onClick()方法就会被执行。</p>
<h3 id="点击事件分发的传递规则"><a href="#点击事件分发的传递规则" class="headerlink" title="点击事件分发的传递规则"></a>点击事件分发的传递规则</h3><p>当点击事件产生后会由Activity来处理，传递给PhoneWindow，再传递给DecorView，最后传递给顶层的ViewGroup，一般在事件传递中只考虑ViewGroup的<br>onInterceptTouchEvent方法，因为一般情况下我们不会重写dispatchTouchEvent()方法，对呀根ViewGroup，点击事件首先传递给它dispatchTouchEvent()方法，如果该ViewGroup的onInterceptTouchEvent()方法返回true，则表示它<br>要拦截这个事件，这个事件就会交给它的onTouchEvent()方法处理，如果onInterceptTouchEvent（）方法返回false，则表示它不拦截这个事件，则这个事件会交给它的子元素的dispatchTouchEvent（）来处理，如此反复下去。如果传递给底层的View，<br>View是没有子View的，就会调用View的dispatchTouchEvent（）方法，一般情况下最终会调用View的onTouchEvent（）方法。</p>
]]></content>
      <tags>
        <tag>Android</tag>
      </tags>
  </entry>
  <entry>
    <title>函数响应式编程——RxJava</title>
    <url>/2023/04/30/%E5%87%BD%E6%95%B0%E5%93%8D%E5%BA%94%E5%BC%8F%E7%BC%96%E7%A8%8B%E2%80%94%E2%80%94RxJava/</url>
    <content><![CDATA[<p>函数响应式编程是一种编程范式。我们常见的编程范式由命令式编程、函数式编程和逻辑式编程。我们常见的面向对象编程是一种命令式编程。命令式编程是面向计算机硬件的抽象，<br>有变量、赋值语句、表达式和控制语句。而函数式编程是面向数学的抽象，将计算描述为一种表达式求职，函数可以在任何地方定义，并且可以对函数进行组合。<br>响应式编程是一种面向数据流和变化传播的编程范式，数据更新是相关联的。把函数式编程里的一套思路和响应式编程合起来就是函数响应式编程。<br>在Android开发中使用函数响应式编程的主要有两大框架：一个是RxJava，另一个是Google推出的Agera。</p>
<span id="more"></span>
<p>RxJava是ReactiveX的一种Java实现，ReactiveX是Reactive Extensions的缩写，一般简写为Rx。<br>微软的定义是：Rx是一个函数库，让开发者可以利用可观察序列和LINQ风格查询操作符来编写异步和基于事件的程序。开发者可以用Observables表示异步数据流，用<br>LINQ操作符查询一部数据流，用Schedulers参数化一部数据流的并发处理，Rx可以这样定义：Rx &#x3D; Observables+LINQ+Schedulers。</p>
<p>1.为何要用RxJava<br>说到异步操作，我们会想到Android的AsyncTask和Handler。但是随着请求的数量越来越多，代码逻辑将会变得越来越复杂而RxJava却仍旧能保持清晰的逻辑。<br>RxJava的原理就是创建一个Observable对象来干活，然后使用各种操作符建立起来的链式操作，就如同流水线一样，把你想要处理的数据一步一步地加工成你<br>想要的成品，然后发射给Subscriber处理</p>
<p>2.RxJava与观察者模式<br>RxJava的异步操作是通过扩展的观察者模式来实现的。RxJava有4个角色Observable、Observer、Subscriber和Suject，这4个角色后面会具体讲解。<br>Observable和Observer 通过subscribe方法实现订阅关系，Observable就可以在需要的时候通知Observer</p>
<p>3.RxJava的基本实现<br>1）创建Observer(观察者)<br>它决定事件触发的时候将有怎样的行为，其中onCompleted、onError和onNext是必须要实现的方法。<br>2）创建Observable(被观察者)<br>它决定什么时候触发事件以及触发怎样的事件。RxJava使用create方法，just方法，或者from方来创建一个Observable<br>2）Subscribe(订阅)<br>订阅只需要一行代码就可以了<br>observable.subscribe(subscriber)</p>
<p>3.Subject<br>subject既可以是一个Observer，也可以是一个Observerable,它是连接Observer和Observerable的桥梁，因此，Subject可以被理解为Subject&#x3D;Observable+Observer。<br>RxJava提供了以下4种Subject:</p>
<ol>
<li>PublishSubject<br>publishsubject只会把在订阅发生的事件点之后来自原始Observable的数据发生给观察者。如果要确保来自原始Observable的所有数据都被分发，<br>则可以当所有观察者都已经订阅时才开始发射数据，或者改用ReplaySubject。<br>2）BehaviorSubject<br>当Observer订阅BehaviorSubject时，它开始发射原始Observable最近发射的数据<br>3）ReplaySubject<br>不管Observer何时订阅ReplaySubject，ReplaySubject均会发射所有来自原始Observable的数据给Observer。如果使用ReplaySubject作为Observer，注意不要在多个线程中调用onNext、onComplete和onError方法。这可能会导致顺序错乱，并且违反了Observer规则。<br>4）AsyncSubject<br>当Observable完成时，AsyncSubject只会发射来自原始Observable的最后一个数据</li>
</ol>
<p>4.RxJava操作符<br>RxJava操作符的类型分为创建操作符、变换操作符、过滤操作符、组合操作符、错误处理操作符、辅助操作符、条件和布尔操作符、算术和聚合操作符及连接操作符等，<br>而这些操作符类型下又有很多操作符，每个操作符可能还有很多变体。</p>
<p>创建操作符：create,just,from,defer,range,interval,start,repeat,timer<br>变换操作符：map,flatMap,concatMap,switchMap等<br>过滤操作符：filter,elementAt,distince,skip,take<br>组合操作符：startWith,merge,concat,zip</p>
]]></content>
      <tags>
        <tag>RxJava</tag>
      </tags>
  </entry>
  <entry>
    <title>如何解决hexo首页显示博客的全部文章</title>
    <url>/2022/09/25/%E5%A6%82%E4%BD%95%E8%A7%A3%E5%86%B3hexo%E9%A6%96%E9%A1%B5%E6%98%BE%E7%A4%BA%E5%8D%9A%E5%AE%A2%E7%9A%84%E5%85%A8%E9%83%A8%E6%96%87%E7%AB%A0/</url>
    <content><![CDATA[<p>在第一次书写博客后，应该会发现自己整篇博客的文章都在这个首页中显示，可能一个两个还好，但是越往后，自己写的博客越多，全都放在首页堆积，就会显得十分臃肿</p>
<span id="more"></span>

<h3 id="修改配置"><a href="#修改配置" class="headerlink" title="修改配置"></a>修改配置</h3><p><code>首页在主题的_config.yml中把设置打开：（安装时默认就打开了） excerpt_description: true</code></p>
<h3 id="方法一：写概述"><a href="#方法一：写概述" class="headerlink" title="方法一：写概述"></a>方法一：写概述</h3><p>在文章的front-matter中（即new一个博客时生成的内容里）添加description，其中description中的内容就会被显示到首页上，其余一律不显示。<br><code>title: 让首页显示部分内容 date: 2020-02-23 22:55:10 description: 这是显示在首页的概述，正文内容均会被隐藏。</code><br>比较不方便的是还需要写一个概述，很多时候懒得写概述，于是就需要第二种方法了。</p>
<h3 id="方法二：文章截断"><a href="#方法二：文章截断" class="headerlink" title="方法二：文章截断"></a>方法二：文章截断</h3><p>在需要截断的地方加入：<br><code>&lt;!--more--&gt;</code><br>首页就会显示这条以上的所有内容，隐藏接下来的内容。比如本文就是使用了这种方法<br>这个明显就方便许多，当然有利有弊，比如开头都是废话的首页开着就不是很爽，因此我一般会先选择方法二，如果感觉文章前面写的不好就用方法一。</p>
]]></content>
  </entry>
  <entry>
    <title>理解 Tokio：Rust 异步编程入门指南</title>
    <url>/2025/04/02/%E7%90%86%E8%A7%A3-Tokio%EF%BC%9ARust-%E5%BC%82%E6%AD%A5%E7%BC%96%E7%A8%8B%E5%85%A5%E9%97%A8%E6%8C%87%E5%8D%97/</url>
    <content><![CDATA[<h2 id="理解-Tokio：Rust-异步编程入门指南（摘要与实用示例）"><a href="#理解-Tokio：Rust-异步编程入门指南（摘要与实用示例）" class="headerlink" title="理解 Tokio：Rust 异步编程入门指南（摘要与实用示例）"></a>理解 Tokio：Rust 异步编程入门指南（摘要与实用示例）</h2><p>Tokio 是 Rust 生态中最流行的异步运行时（async runtime）之一，为基于 <code>async</code>&#x2F;<code>.await</code> 的并发程序提供执行器、IO 驱动、定时器、任务调度和实用工具。本文总结了 Tokio 的核心概念、常用 API、实战示例及最佳实践。</p>
<span id="more"></span>

<h3 id="核心概念"><a href="#核心概念" class="headerlink" title="核心概念"></a>核心概念</h3><ul>
<li>Runtime：Tokio 的执行环境，包含一个或多个线程（multi-threaded runtime）或单线程（current-thread runtime）。负责调度 Future 的执行。</li>
<li>Reactor &#x2F; I&#x2F;O 驱动：基于操作系统的事件通知（如 epoll&#x2F;kqueue），非阻塞地处理套接字&#x2F;文件等 I&#x2F;O 事件。</li>
<li>Executor：将就绪的任务排入执行队列并在 runtime 线程上运行。</li>
<li>Task：由 Future 封装、由 runtime 调度的执行单元（类似轻量线程）。</li>
<li>Waker：当 Future 可继续执行时唤醒对应 task。</li>
<li>spawn vs spawn_blocking：非阻塞任务（async）用 spawn，阻塞&#x2F;CPU 密集任务用 spawn_blocking。</li>
</ul>
<h3 id="启动-runtime"><a href="#启动-runtime" class="headerlink" title="启动 runtime"></a>启动 runtime</h3><ul>
<li>多线程运行时（默认，适合并发场景）：<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> tokio::runtime::Runtime;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[tokio::main]</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">fn</span> <span class="title function_">main</span>() &#123;</span><br><span class="line">    <span class="comment">// 在多线程 runtime 上运行</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
<li>单线程运行时：<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> tokio::runtime::Builder;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> <span class="variable">rt</span> = Builder::<span class="title function_ invoke__">new_current_thread</span>().<span class="title function_ invoke__">enable_all</span>().<span class="title function_ invoke__">build</span>().<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">rt.<span class="title function_ invoke__">block_on</span>(<span class="keyword">async</span> &#123;</span><br><span class="line">    <span class="comment">// 单线程执行任务</span></span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="任务调度与-spawn"><a href="#任务调度与-spawn" class="headerlink" title="任务调度与 spawn"></a>任务调度与 spawn</h3><ul>
<li><code>tokio::spawn</code> 在 runtime 上启动一个后台异步任务，返回 <code>JoinHandle</code>：<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">handle</span> = tokio::<span class="title function_ invoke__">spawn</span>(<span class="keyword">async</span> &#123;</span><br><span class="line">    <span class="comment">// 异步任务</span></span><br><span class="line">    <span class="number">42</span></span><br><span class="line">&#125;);</span><br><span class="line"><span class="keyword">let</span> <span class="variable">res</span> = handle.<span class="keyword">await</span>.<span class="title function_ invoke__">unwrap</span>();</span><br></pre></td></tr></table></figure></li>
<li>使用 <code>spawn_blocking</code> 将阻塞操作移出 async runtime 线程池，避免阻塞 reactor：<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> <span class="variable">res</span> = tokio::task::<span class="title function_ invoke__">spawn_blocking</span>(|| &#123;</span><br><span class="line">    <span class="comment">// 阻塞的计算或同步 IO</span></span><br><span class="line">    <span class="title function_ invoke__">heavy_computation</span>()</span><br><span class="line">&#125;).<span class="keyword">await</span>.<span class="title function_ invoke__">unwrap</span>();</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="异步-I-x2F-O-示例（TCP-Echo-Server）"><a href="#异步-I-x2F-O-示例（TCP-Echo-Server）" class="headerlink" title="异步 I&#x2F;O 示例（TCP Echo Server）"></a>异步 I&#x2F;O 示例（TCP Echo Server）</h3><p>简要示例展示如何使用 <code>TcpListener</code>&#x2F;<code>TcpStream</code> 实现并发 echo server：</p>
<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> tokio::net::&#123;TcpListener, TcpStream&#125;;</span><br><span class="line"><span class="keyword">use</span> tokio::io::&#123;AsyncReadExt, AsyncWriteExt&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#[tokio::main]</span></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">fn</span> <span class="title function_">main</span>() <span class="punctuation">-&gt;</span> anyhow::<span class="type">Result</span>&lt;()&gt; &#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="variable">listener</span> = TcpListener::<span class="title function_ invoke__">bind</span>(<span class="string">&quot;127.0.0.1:8080&quot;</span>).<span class="keyword">await</span>?;</span><br><span class="line">    <span class="keyword">loop</span> &#123;</span><br><span class="line">        <span class="keyword">let</span> (<span class="keyword">mut</span> socket, _) = listener.<span class="title function_ invoke__">accept</span>().<span class="keyword">await</span>?;</span><br><span class="line">        tokio::<span class="title function_ invoke__">spawn</span>(<span class="keyword">async</span> <span class="keyword">move</span> &#123;</span><br><span class="line">            <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">buf</span> = <span class="built_in">vec!</span>[<span class="number">0u8</span>; <span class="number">1024</span>];</span><br><span class="line">            <span class="keyword">loop</span> &#123;</span><br><span class="line">                <span class="keyword">let</span> <span class="variable">n</span> = <span class="keyword">match</span> socket.<span class="title function_ invoke__">read</span>(&amp;<span class="keyword">mut</span> buf).<span class="keyword">await</span> &#123;</span><br><span class="line">                    <span class="title function_ invoke__">Ok</span>(<span class="number">0</span>) =&gt; <span class="keyword">return</span>,</span><br><span class="line">                    <span class="title function_ invoke__">Ok</span>(n) =&gt; n,</span><br><span class="line">                    <span class="title function_ invoke__">Err</span>(_) =&gt; <span class="keyword">return</span>,</span><br><span class="line">                &#125;;</span><br><span class="line">                <span class="keyword">let</span> <span class="variable">_</span> = socket.<span class="title function_ invoke__">write_all</span>(&amp;buf[..n]).<span class="keyword">await</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="定时器与延时"><a href="#定时器与延时" class="headerlink" title="定时器与延时"></a>定时器与延时</h3><ul>
<li><code>tokio::time::sleep</code>、<code>interval</code> 等：<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> tokio::time::&#123;sleep, Duration&#125;;</span><br><span class="line"></span><br><span class="line"><span class="title function_ invoke__">sleep</span>(Duration::<span class="title function_ invoke__">from_secs</span>(<span class="number">1</span>)).<span class="keyword">await</span>;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="通信：Channel"><a href="#通信：Channel" class="headerlink" title="通信：Channel"></a>通信：Channel</h3><ul>
<li><code>tokio::sync::mpsc</code> 用于异步任务间消息传递；<code>oneshot</code> 用于一次性通知：<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> tokio::sync::mpsc;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> (tx, <span class="keyword">mut</span> rx) = mpsc::<span class="title function_ invoke__">channel</span>(<span class="number">32</span>);</span><br><span class="line">tokio::<span class="title function_ invoke__">spawn</span>(<span class="keyword">async</span> <span class="keyword">move</span> &#123;</span><br><span class="line">    tx.<span class="title function_ invoke__">send</span>(<span class="number">1</span>).<span class="keyword">await</span>.<span class="title function_ invoke__">unwrap</span>();</span><br><span class="line">&#125;);</span><br><span class="line"><span class="keyword">if</span> <span class="keyword">let</span> <span class="variable">Some</span>(v) = rx.<span class="title function_ invoke__">recv</span>().<span class="keyword">await</span> &#123; <span class="built_in">println!</span>(<span class="string">&quot;&#123;&#125;&quot;</span>, v); &#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="同步原语（在-async-中使用）"><a href="#同步原语（在-async-中使用）" class="headerlink" title="同步原语（在 async 中使用）"></a>同步原语（在 async 中使用）</h3><ul>
<li><code>tokio::sync::Mutex</code>&#x2F;<code>RwLock</code>：异步友好（.await 时不会阻塞线程）。</li>
<li><code>std::sync::Mutex</code> 在 async 上慎用（会阻塞线程池）。<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line"><span class="keyword">use</span> tokio::sync::Mutex;</span><br><span class="line"><span class="keyword">let</span> <span class="variable">m</span> = Mutex::<span class="title function_ invoke__">new</span>(<span class="number">0</span>);</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">let</span> <span class="keyword">mut </span><span class="variable">guard</span> = m.<span class="title function_ invoke__">lock</span>().<span class="keyword">await</span>;</span><br><span class="line">    *guard += <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="select-与并发控制"><a href="#select-与并发控制" class="headerlink" title="select! 与并发控制"></a>select! 与并发控制</h3><ul>
<li><code>tokio::select!</code> 支持在多个异步分支之间竞态，第一完成的分支执行：<figure class="highlight rust"><table><tr><td class="code"><pre><span class="line">tokio::<span class="built_in">select!</span> &#123;</span><br><span class="line">    _ = <span class="title function_ invoke__">async_task1</span>() =&gt; <span class="built_in">println!</span>(<span class="string">&quot;task1 done&quot;</span>),</span><br><span class="line">    _ = <span class="title function_ invoke__">async_task2</span>() =&gt; <span class="built_in">println!</span>(<span class="string">&quot;task2 done&quot;</span>),</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li>
</ul>
<h3 id="错误处理与取消"><a href="#错误处理与取消" class="headerlink" title="错误处理与取消"></a>错误处理与取消</h3><ul>
<li><code>JoinHandle::abort()</code> 可取消任务，但需注意资源清理。</li>
<li>任务取消会在 <code>.await</code> 点抛出 <code>Cancelled</code>，注意捕获与清理（<code>drop</code> 语义）。</li>
</ul>
<h3 id="与阻塞-x2F-CPU-密集型代码的整合"><a href="#与阻塞-x2F-CPU-密集型代码的整合" class="headerlink" title="与阻塞&#x2F;CPU 密集型代码的整合"></a>与阻塞&#x2F;CPU 密集型代码的整合</h3><ul>
<li>对于 CPU 密集任务，使用 <code>spawn_blocking</code> 或移动到专门的线程池。</li>
<li>尽量将同步 I&#x2F;O 放到独立线程或使用 async-compatible 库。</li>
</ul>
<h3 id="网络性能与-Tunables"><a href="#网络性能与-Tunables" class="headerlink" title="网络性能与 Tunables"></a>网络性能与 Tunables</h3><ul>
<li>使用多线程 runtime 提升并发吞吐。</li>
<li>减少频繁分配、避免过多 await 点导致上下文切换。</li>
<li>通过 <code>tokio::sync::Semaphore</code> 控制并发连接数或并发任务数。</li>
</ul>
<h3 id="与第三方生态"><a href="#与第三方生态" class="headerlink" title="与第三方生态"></a>与第三方生态</h3><ul>
<li>与 <code>hyper</code>（HTTP）、<code>reqwest</code>（HTTP 客户端）、<code>tokio-postgres</code>、<code>sqlx</code> 等集成良好。</li>
<li><code>tracing</code> 与 <code>tokio-console</code> 可帮助调试运行时性能和任务行为。</li>
</ul>
<h3 id="常见陷阱"><a href="#常见陷阱" class="headerlink" title="常见陷阱"></a>常见陷阱</h3><ul>
<li>在 async 中使用同步锁（如 std::sync::Mutex）会阻塞线程池，导致死锁或性能下降。</li>
<li>忽略 <code>spawn_blocking</code> 导致 runtime 线程被阻塞。</li>
<li>在多线程 runtime 中共享非线程安全数据（未加 Arc&#x2F;锁）会出错。</li>
<li>忽视任务数量与系统资源（文件描述符、连接数）限制。</li>
</ul>
<h3 id="实战建议"><a href="#实战建议" class="headerlink" title="实战建议"></a>实战建议</h3><ul>
<li>从 <code>tokio::main</code> 快速上手，逐步使用 <code>spawn</code> 将并发工作分离。</li>
<li>把短小的异步任务放到 async executor，长时间或阻塞工作放到 <code>spawn_blocking</code>。</li>
<li>使用 <code>tokio::sync</code> 系列原语做异步内部同步。</li>
<li>用 <code>tokio-console</code> 或 <code>tracing</code> 分析热点与阻塞点。</li>
<li>书写可取消、健壮的任务并注意资源清理（Drop 与 finally 模式）。</li>
</ul>
<h3 id="参考资料与深入学习"><a href="#参考资料与深入学习" class="headerlink" title="参考资料与深入学习"></a>参考资料与深入学习</h3><ul>
<li>官方文档：<a href="https://tokio.rs/">https://tokio.rs/</a></li>
<li>Tokio Tutorial 与 Examples（官方 GitHub）</li>
<li>学习方向：任务模型原理（Waker&#x2F;Context）、异步 I&#x2F;O 的底层实现、性能调优与调试工具</li>
</ul>
]]></content>
      <tags>
        <tag>Rust</tag>
      </tags>
  </entry>
  <entry>
    <title>比特币的输入输出详解</title>
    <url>/2025/06/01/%E6%AF%94%E7%89%B9%E5%B8%81%E7%9A%84%E8%BE%93%E5%85%A5%E8%BE%93%E5%87%BA%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="比特币简介"><a href="#比特币简介" class="headerlink" title="比特币简介"></a>比特币简介</h2><p>比特币（Bitcoin）是由中本聪（Satoshi Nakamoto）于2008年提出、2009年正式上线的去中心化数字货币。它基于区块链技术，通过点对点网络实现无需信任的价值转移。</p>
<span id="more"></span>
<p>比特币的核心特性包括：</p>
<ul>
<li><strong>去中心化</strong>：没有中央机构或第三方，所有节点平等参与。</li>
<li><strong>总量有限</strong>：总发行量为2100万枚，通胀可控。</li>
<li><strong>公开透明</strong>：所有交易都记录在区块链上，任何人可查验。</li>
<li><strong>抗审查性</strong>：交易难以被单一实体阻断或篡改。</li>
<li><strong>匿名性</strong>：用户通过地址进行交易，无需实名。</li>
</ul>
<p>比特币的出现不仅推动了数字货币和区块链技术的发展，也为全球金融体系带来了新的思考和变革方向。</p>
<h2 id="比特币的输入输出（UTXO）机制详解"><a href="#比特币的输入输出（UTXO）机制详解" class="headerlink" title="比特币的输入输出（UTXO）机制详解"></a>比特币的输入输出（UTXO）机制详解</h2><p>比特币采用<strong>UTXO（Unspent Transaction Output，未花费交易输出）模型</strong>，这是理解比特币交易和账本结构的核心。</p>
<h3 id="1-UTXO-模型概述"><a href="#1-UTXO-模型概述" class="headerlink" title="1. UTXO 模型概述"></a>1. UTXO 模型概述</h3><ul>
<li><strong>UTXO</strong>：每一笔比特币交易的输出（output）都可以作为后续交易的输入（input）。未被花费的输出即为 UTXO。</li>
<li><strong>账户体系 vs. UTXO</strong>：以太坊等采用账户余额模型，而比特币采用 UTXO 模型，每个 UTXO 都是独立的“零钱”。</li>
</ul>
<h3 id="2-比特币交易结构"><a href="#2-比特币交易结构" class="headerlink" title="2. 比特币交易结构"></a>2. 比特币交易结构</h3><p>一笔比特币交易（Transaction）由<strong>输入（Inputs）</strong>和<strong>输出（Outputs）</strong>两部分组成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">+-------------------+</span><br><span class="line">|   Transaction     |</span><br><span class="line">+-------------------+</span><br><span class="line">| Inputs  | Outputs |</span><br><span class="line">+-------------------+</span><br></pre></td></tr></table></figure>

<ul>
<li><strong>输入（Input）</strong>：引用之前某笔交易的输出（即 UTXO），并提供解锁脚本证明所有权。</li>
<li><strong>输出（Output）</strong>：指定比特币的接收方和金额，形成新的 UTXO。</li>
</ul>
<h4 id="交易结构示意"><a href="#交易结构示意" class="headerlink" title="交易结构示意"></a>交易结构示意</h4><figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Inputs: [</span><br><span class="line">  &#123; prev_txid, output_index, unlocking_script &#125;,</span><br><span class="line">  ...</span><br><span class="line">]</span><br><span class="line">Outputs: [</span><br><span class="line">  &#123; value, locking_script &#125;,</span><br><span class="line">  ...</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<h3 id="3-输入（Input）详解"><a href="#3-输入（Input）详解" class="headerlink" title="3. 输入（Input）详解"></a>3. 输入（Input）详解</h3><p>每个输入包含：</p>
<ul>
<li><strong>引用的 UTXO</strong>：前一笔交易的 txid 和输出序号（output_index）。</li>
<li><strong>解锁脚本（scriptSig）</strong>：证明你有权花费该 UTXO（通常是签名+公钥）。</li>
</ul>
<h3 id="4-输出（Output）详解"><a href="#4-输出（Output）详解" class="headerlink" title="4. 输出（Output）详解"></a>4. 输出（Output）详解</h3><p>每个输出包含：</p>
<ul>
<li><strong>金额（value）</strong>：该输出包含的比特币数量（单位：聪，1 BTC &#x3D; 1亿聪）。</li>
<li><strong>锁定脚本（scriptPubKey）</strong>：规定谁能花费该输出（通常是接收方地址的哈希）。</li>
</ul>
<h3 id="5-UTXO-的生命周期"><a href="#5-UTXO-的生命周期" class="headerlink" title="5. UTXO 的生命周期"></a>5. UTXO 的生命周期</h3><ol>
<li>交易 A 产生一个输出 O1，O1 成为 UTXO。</li>
<li>交易 B 的输入引用 O1，O1 被花费，不再是 UTXO。</li>
<li>交易 B 产生新的输出 O2、O3，成为新的 UTXO。</li>
</ol>
<h3 id="6-交易举例"><a href="#6-交易举例" class="headerlink" title="6. 交易举例"></a>6. 交易举例</h3><p>假设 Alice 有两个 UTXO（0.3 BTC 和 0.7 BTC），想转账 0.5 BTC 给 Bob：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">Inputs:</span><br><span class="line">  - 0.3 BTC (UTXO1)</span><br><span class="line">  - 0.7 BTC (UTXO2)</span><br><span class="line">Outputs:</span><br><span class="line">  - 0.5 BTC (Bob)</span><br><span class="line">  - 0.4999 BTC (找零给 Alice)</span><br><span class="line">  - 0.0001 BTC (矿工手续费)</span><br></pre></td></tr></table></figure>

<ul>
<li>Alice 需要把两个 UTXO 作为输入，输出一部分给 Bob，一部分找零给自己，剩下的作为手续费。</li>
</ul>
<h3 id="7-脚本系统（Script）"><a href="#7-脚本系统（Script）" class="headerlink" title="7. 脚本系统（Script）"></a>7. 脚本系统（Script）</h3><p>比特币的输入输出通过一种简单的脚本语言（Script）实现灵活的支付控制。</p>
<ul>
<li><strong>锁定脚本（scriptPubKey）</strong>：如 <code>OP_DUP OP_HASH160 &lt;公钥哈希&gt; OP_EQUALVERIFY OP_CHECKSIG</code>，规定谁能花费。</li>
<li><strong>解锁脚本（scriptSig）</strong>：如 <code>&lt;签名&gt; &lt;公钥&gt;</code>，用于解锁对应的 UTXO。</li>
</ul>
<h4 id="常见脚本类型"><a href="#常见脚本类型" class="headerlink" title="常见脚本类型"></a>常见脚本类型</h4><ul>
<li><strong>P2PKH</strong>（Pay to Public Key Hash）：最常见的“比特币地址”收款方式。</li>
<li><strong>P2SH</strong>（Pay to Script Hash）：多签名、复杂条件等。</li>
<li><strong>P2WPKH&#x2F;P2WSH</strong>：隔离见证（SegWit）脚本。</li>
</ul>
<h3 id="8-UTXO-模型的优缺点"><a href="#8-UTXO-模型的优缺点" class="headerlink" title="8. UTXO 模型的优缺点"></a>8. UTXO 模型的优缺点</h3><p><strong>优点：</strong></p>
<ul>
<li>并发性强，易于并行验证。</li>
<li>隐私性较好，难以追踪账户余额。</li>
<li>防止双花，天然抗部分攻击。</li>
</ul>
<p><strong>缺点：</strong></p>
<ul>
<li>需要管理大量 UTXO，存储和同步压力大。</li>
<li>找零机制导致 UTXO 碎片化。</li>
<li>交易确认时间长，矿工手续费高。</li>
</ul>
<h3 id="9-总结"><a href="#9-总结" class="headerlink" title="9. 总结"></a>9. 总结</h3><ul>
<li>UTXO 是比特币的核心概念，每个输出都是未花费的交易输出（UTXO）。</li>
<li>输入和输出通过解锁脚本和锁定脚本实现支付控制。</li>
<li>UTXO 模型具有高并发性、隐私性和抗双花能力，但存在存储和碎片化问题。</li>
</ul>
]]></content>
      <tags>
        <tag>web3</tag>
      </tags>
  </entry>
  <entry>
    <title>深入理解进程和线程</title>
    <url>/2022/09/21/%E6%B7%B1%E5%85%A5%E7%90%86%E8%A7%A3%E8%BF%9B%E7%A8%8B%E5%92%8C%E7%BA%BF%E7%A8%8B/</url>
    <content><![CDATA[<span id="more"></span>

<h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><p>平时helloworld这样的程序在现代系统上运行时，操作系统会提供一种假象，就好像系统上只有这个程序在运行。程序看上去是独占地使用处理器、主存和IO设备。处理器看上去就像在不间断地一条接一条执行程序中的指令，即该程序的代码和数据是系统内存中唯一的对象，这些假象是通过进程的概念来实现的，进程是计算机科学中最重要和最成功的概念之一。<br>进程是操作系统对一个正在运行的程序的一种抽象。在一个系统上可以同时运行多个进程，而每个进程都好像在独占地使用硬件。而并发运行，则是说一个进程的指令和另一个进程的指令是交错执行的。在大多数系统中。需要运行的进程数是多于可以运行它们的CPU个数的。传统系统在一个时刻只能执行一个程序，而先进的多核处理器同时能够执行多个程序。无论是在单核还是在多核系统中，一个CPU看上去都像是在并发执行多个进程，这是通过处理器在进程间切换来实现的。操作系统实现这种交错执行的机制称为上下文切换。（为了简化，下面的讨论我们只考虑包含一个CPU的单处理器系统的情况）<br>操作系统保持跟踪进程运行所需的所有状态信息。这种状态，也就是上下文，包括许多信息，比如PC和寄存器文件的当前值，以及主存的内容。在任何一个时刻，单处理器系统都只能执行一个进程的的代码，当操作系统决定要把控制权从当前进程转移到某个新进程时，就会进行上下文切换，及保存当前进程的上下文，恢复新进程的上下文，然后将控制权传递到新进程。新进程就会从它上次停止的地方开始。下图展示了程序运行场景的基本理念。<br><img src="https://s3.bmp.ovh/imgs/2022/09/25/9a7e5808598c7948.jpg"><br>示例场景中有两个并发的进程：shell进程和hello进程。最开始，只有shell进程在运行，即等待命令行上的输入。当我们让它运行hello程序时，shell通过调用一个专门的函数，即系统调用，来执行我们的请求，系统调用会将控制权传递给操作系统。操作系统保存shell进程的上下文，创建一个新的hello进程以及其上下文，然后将控制权传给新的hell咯进程。hello进程终止后，操作系统恢复shell进程的上下文，并将控制权传回给它，shell进程会继续等待下一个命令行输入。<br>如上图所示，从一个进程到另一个进程的转换是由操作系统内核管理的，内核时操作系统代码常驻主存的部分。当应用程序需要操作系统的某些操作时，比如读写文件，它就执行一条特殊的系统调用（system call）指令，将控制权传递给内核。然后内核执行被请求的操作并返回应用程序。注意，内核不是一个独立的进程。相反，它是系统管理全部进程所用代码和数据结构的集合。<br>实现进程这个抽象概念需要低级硬件和操作系统软件之间紧密合作。</p>
<h2 id="线程"><a href="#线程" class="headerlink" title="线程"></a>线程</h2><p>尽管通常我们认为一个进程只有单一的控制流，但是在现代系统中，一个进程实际上可以由多个称为线程的执行单元组成，每个线程都运行在进程的上下文中，并共享同样的代码和全局数据。由于网络服务器中对并行处理的需求，线程称为越来越重要的编程模型，因为多线程之间比多进程之间更容易共享数据，也因为线程一般来说都比进程更高效。当有多处理器可用的时候，多线程也是一种使得程序可以运行得更快得方法。</p>
<h2 id="虚拟内存"><a href="#虚拟内存" class="headerlink" title="虚拟内存"></a>虚拟内存</h2><p>虚拟内存是一个抽象概念，它为每个进程提供一个假象，即每个进程都在独占地使用主存。每个进程看到内存都是一致得，称为虚拟地址空间。下图所示是Linux进程的虚拟地址空间（其他Unix系统的设计也与此类似）。在Linux中，地址空间最上面的区域是保留给操作系统中的代码和数据的，这对所有进程来说都是一样的。地址空间的底部区域存放用户进程定义的代码和数据。请注意，途中的地址是从下往上增大的。<br><img src="https://s3.bmp.ovh/imgs/2022/09/25/39151a55fce31299.png"><br>每个进程看到的虚拟地址空间由大量准确定义的区构成，每个区都有专门的功能，以下从最低的地址开始，逐步向上介绍：</p>
<h3 id="程序代码和数据"><a href="#程序代码和数据" class="headerlink" title="程序代码和数据"></a>程序代码和数据</h3><p>对所有的进程来说，代码是从同一固定地址开始，紧接着的是和PC全局变量相对应的数据位置。代码和数据区是直接按照可执行目标文件的内容初始化的，在示例中就是可执行文件hello，</p>
<h3 id="堆"><a href="#堆" class="headerlink" title="堆"></a>堆</h3><p>代码和数据区后紧随着的是运行时堆。代码和数据区在进程一开始运行时就被制定了大小，与此不同，当调用像malloc和free这样的C标准库函数时，堆可以在运行时动态地扩展和收缩。</p>
<h3 id="共享库"><a href="#共享库" class="headerlink" title="共享库"></a>共享库</h3><p>大约在地址空间中的中间部分是一块用来存放像C标准库和数学库这样的共享库的代码和数据的区域，共享库的概念非常强大，</p>
<h3 id="栈"><a href="#栈" class="headerlink" title="栈"></a>栈</h3><p>位于用户虚拟地址空间顶部的是用户栈，编译器用它来实现函数调用，和堆一样，用户栈在程序执行期间可以动态地扩展和收缩。特别地，每次我们调用一个函数时，栈就会增长；从一个函数返回时，栈就会收缩。</p>
<h3 id="内存虚拟内存"><a href="#内存虚拟内存" class="headerlink" title="内存虚拟内存"></a>内存虚拟内存</h3><p>地址空间顶部的区域是为内核保留的，不允许应用程序读写这个区域的内容或者直接调用呢耦合代码定义的函数。相反，它们必须调用内核来执行这些操作</p>
<p>虚拟内存的运作需要硬件和操作系统软件之间精密复杂的交互，包括对处理器生成的每个地址的硬件编译。其基本思想是把一个进程虚拟内存的内容存储在磁盘上，然后主存作为磁盘的高速缓存，</p>
<h3 id="文件"><a href="#文件" class="headerlink" title="文件"></a>文件</h3><p>文件就是字节序列，仅此而已。每个IO设备，包括磁盘，键盘，显示器，甚至网络，都可以看成是文件，系统中所有输入输出都是通过使用一小组称为Unix I&#x2F;O的系统函数调用读写文件按来实现的<br>文件这个简单而精致的概念是非常强大的，因为它向应用程序中提供了一个统一的试图，来看待系统中可能含有的所有各式各样的I&#x2F;O设备。例如，处理磁盘文件内容的应用程序员可以非常幸福，因为他们无需了解具体的磁盘技术，进一步说，同一个程序可以在使用不同磁盘技术的不同系统上运行。</p>
]]></content>
  </entry>
  <entry>
    <title>设计模式系列——流程引擎</title>
    <url>/2023/01/01/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E7%B3%BB%E5%88%97%E2%80%94%E2%80%94%E6%B5%81%E7%A8%8B%E5%BC%95%E6%93%8E/</url>
    <content><![CDATA[<p>在电商里面处理复杂的业务逻辑场景有很多，我们还是以创建商品为例子。很多人可能会问创建商品很复杂吗？</p>
<span id="more"></span>

<p>创建商品的流程：<br>参数必填性校验<br>参数数据转换<br>商品基础信息校验<br>商品与商家之间的校验<br>类目信息校验<br>商品交易信息校验（这个看公司业务决定）<br>SKU相关信息校验<br>商品是否需要有特定标签校验（看公司业务决定）<br>商品类型校验（普通，卡片，视频…….）<br>商品风控校验<br>保存商品信息<br>保存SKU信息<br>保存商品详情信息<br>重量模版<br>运费<br>配送区域<br>。。。。。。。<br>这里商品类型里面还涉及到各个业务场景校验，我们就先不谈了。针对这样的情况我们应该怎么去写这个代码呢？我翻看了一下以前大学写的一些代码整体的代码格式大概也就是这个样子<br><a href="https://imgse.com/i/pSCrA7n"><img src="https://s1.ax1x.com/2023/01/01/pSCrA7n.jpg" alt="pSCrA7n.jpg"></a><br>这么写其实也没有什么问题，功能也能实现。但是这么写其实有很多弊端：<br>1.代码可读性不高<br>2.代码扩展性不高<br>3.耦合性太强，有些东西不好公用<br>4.整体创建执行流程事件太长，串行调用下游服务</p>
<p> 看到这样代码我们首先都是吐槽一番，然后还是老老实实去改，有动手能力强的会想着去优化一下。再看看这个流程，其实在创建商品的时候我们很多校验和保存数据是没有依赖且互不影响的，我们完全可以去并行执行。</p>
<p>节省创建商品的流程时间，提高用户体验。（PS：就好比两条告诉一条堵车，一条畅通无阻，选哪条？）<br>所以针对这么并行问题我还是给大家画了一个流程图：<br><a href="https://imgse.com/i/pSCsXJP"><img src="https://s1.ax1x.com/2023/01/01/pSCsXJP.jpg" alt="pSCsXJP.jpg"></a><br>通过这张图我们看到创建一个商品流程，我们调用的下游服务可能是有十几二十几或者更多，假设我们一次RPC调用的平均返回时间是50毫秒，串行执行时间可能就1-2秒了，那么我们并行执行的话也就200到300毫秒了。</p>
<p>所以本着这种思想可能会有人问，为什么我不能异步，不能用消息？创建商品假设用异步消息的话，如果消费失败那用户创建商品成功保存其他信息失败了，那对用户来说不是体验更加不好了</p>
<p>说了这么多开始撸代码：<br><a href="https://imgse.com/i/pSCykiq"><img src="https://s1.ax1x.com/2023/01/01/pSCykiq.jpg" alt="pSCykiq.jpg"></a><br>先创建一个Context上下文，作为我们调用下游服务的返回结果<br><a href="https://imgse.com/i/pSCyAJ0"><img src="https://s1.ax1x.com/2023/01/01/pSCyAJ0.jpg" alt="pSCyAJ0.jpg"></a><br>第二步创建我们的流程节点，这相当于就是保存我们整个流程中需要执行下游服务的节点，以Map作为保存数据，NodeConf节点设置参数，自定义请求服务器超时时间（因为并行我们用的线程池或者通过get设置时间get返回值结果）<br><a href="https://imgse.com/i/pSPnMDJ"><img src="https://s1.ax1x.com/2023/01/02/pSPnMDJ.jpg" alt="pSPnMDJ.jpg"></a><br>第三步引擎类，这个也是我们的核心类。通过我们添加的node节点判断我们哪些流程是需要串行的哪些是需要并行的，通过线程池创建线程放入Feature中，来1达到同步执行的效果</p>
<p>在使用线程池的时候我们需要考虑不要设置的参数过大，开启另外的线程也是会占用机器内存的，一个线程按照1兆来算的话，你开启几百上千个，也会占用很大一部分内存。尽可能的去采用池化的思想，这里就按大家实际场景去做测试了。</p>
<p><a href="https://imgse.com/i/pSPnJC6"><img src="https://s1.ax1x.com/2023/01/02/pSPnJC6.jpg" alt="pSPnJC6.jpg"></a><br>第四步执行call方法，也就是执行我们的node节点</p>
<p><a href="https://imgse.com/i/pSPnY8K"><img src="https://s1.ax1x.com/2023/01/02/pSPnY8K.jpg" alt="pSPnY8K.jpg"></a><br>第五步创建节点接口，这里我们定义一个ResultKey，这个Key也就是跟我流程中的这个节点绑定，在获取数据的时候也就是通过这个key来标识</p>
<p><a href="https://imgse.com/i/pSPnNvD"><img src="https://s1.ax1x.com/2023/01/02/pSPnNvD.jpg" alt="pSPnNvD.jpg"></a><br>第六步因为我们在节点里面存的Class类，所以我们得通过实现ApplicationContextAware类来获取Spring容器中得bean实例</p>
<p><a href="https://imgse.com/i/pSPn68f"><img src="https://s1.ax1x.com/2023/01/02/pSPn68f.jpg" alt="pSPn68f.jpg"></a><br>第七步那就是来创建两个测试node节点</p>
<p><a href="https://imgse.com/i/pSPRVmT"><img src="https://s1.ax1x.com/2023/01/02/pSPRVmT.jpg" alt="pSPRVmT.jpg"></a><br><a href="https://imgse.com/i/pSPRZ0U"><img src="https://s1.ax1x.com/2023/01/02/pSPRZ0U.jpg" alt="pSPRZ0U.jpg"></a><br>最后当然就是我们得测试结果了，这里我们创建两个节点NodeOne和NodeTwo作为模拟真实业务场景得节点，通过一个后面得three作为一个group需要并执行的节点。<br>看完代码最后再给大家来一个总体的流程图吧<br><a href="https://imgse.com/i/pSPRutJ"><img src="https://s1.ax1x.com/2023/01/02/pSPRutJ.jpg" alt="pSPRutJ.jpg"></a><br>看完是不是觉得感觉自己顿悟，以后面对再复杂的流程业务也能拿下了。</p>
<p>思考：<br>其实这里还有很多优化点，每个人遇到的复杂的场景可能也不一样，只能说给大家提供一个思想吧，针对不同的场景大家再去做改造吧</p>
<p>扩展：<br>细心的同学可能都会发现这都是强依赖性，能不能有弱依赖在里面呢？<br>答案：<br>当然可以有弱依赖了，在FlowNode.NodeConf中我们既然可以设置超时时间，我们也可以再添加一个参数来确定是都是弱依赖。在我们的future.get获取结果的时候当出现异常可以catch住，强依赖则终止流程返回错误信息，否则记录错误日志，流程continue</p>
<p>我们流程保存现在是用的静态代码块，可不可以换其他的方式保存节点呢？</p>
<p>答案：这个当然也可以，我们保存在数据库，ACM，Apollo等等都是可以的。这个取决于你们自己的业务和成本问题。因为流程我们一般是不会经常换的，所以我还是建议代码写死就可以了</p>
<p>采用线程池去调用下游服务，会不会造成服务链路追踪失败呢？</p>
<p>答案：这个不能说绝对，但是如果是保存再ThreadLocal中那肯定会失效的，ThreadLocal中的KEY也就是跟当前线程的ID有关，都开启新的线程了，那肯定也就是丢失了</p>
<p>如果采用ThreadLocal在节点中是不是就失效了</p>
<p>答案：第三点已经给出答案了</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式系列 — 单例模式</title>
    <url>/2022/12/12/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E7%B3%BB%E5%88%97-%E2%80%94-%E5%8D%95%E4%BE%8B%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p>开篇给大家一个思考题：为什么不用静态方法而用单例模式？</p>
<span id="more"></span>

<p>单例的定义：在当前进程中，通过单例模式创建的类有且只有一个实例。</p>
<p>单例有如下几个特点：<br>1.在Java应用中，单例模式能保证在一个JVM中，该对象只有一个实例存在<br>2.构造器必须是私有的，外部类无法通过调用构造器创建该实例<br>3.没有公开的set方法，外部类无法调用set方法创建该实例<br>4.提供一个公开的get方法获取唯一的这个实例</p>
<p>那单例模式有什么好处呢？<br>1.某些类创建比较频繁，对于一些大型的对象，这是一笔很大的系统开销<br>2.省去了new操作符，降低了系统内存的使用频率，减轻GC压力<br>3.系统中某些类，如Spring里的controller，控制着处理流程，如果该类可以创建多个的话，系统就全乱了<br>4.避免了对资源的重复占用</p>
<p>好了，单例模式的定义也清楚了，好处也了解了，先看一个饿汉式的写法：</p>
<h2 id="饿汉式"><a href="#饿汉式" class="headerlink" title="饿汉式"></a>饿汉式</h2><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton &#123;</span><br><span class="line">  // 创建一个实例对象</span><br><span class="line">    private static Singleton instance = new Singleton();</span><br><span class="line">    /**</span><br><span class="line">     * 私有构造方法，防止被实例化</span><br><span class="line">     */</span><br><span class="line">    private Singleton()&#123;&#125;</span><br><span class="line">    /**</span><br><span class="line">     * 静态get方法</span><br><span class="line">     */</span><br><span class="line">    public static Singleton getInstance()&#123;</span><br><span class="line">        return instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>之所以叫饿汉式大家可以理解为它饿，他想提前把对象new出来，这样别人哪怕是第一次获取这个类对象的时候直接就存在这个类了，省去了创建类这一步开销。</p>
<p>下面介绍懒汉式</p>
<h2 id="懒汉式"><a href="#懒汉式" class="headerlink" title="懒汉式"></a>懒汉式</h2><p>线程不安全的模式</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton &#123;  </span><br><span class="line">    private static Singleton instance;  </span><br><span class="line">    private Singleton ()&#123;&#125;  </span><br><span class="line">  </span><br><span class="line">    public static Singleton getInstance() &#123;  </span><br><span class="line">    if (instance == null) &#123;  </span><br><span class="line">        instance = new Singleton();  </span><br><span class="line">    &#125;  </span><br><span class="line">    return instance;  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>懒汉式大家可以理解为它懒，别人第一次调用的时候他发现自己的实例是空的，然后去初始化了，再赋值，后面的调用就和饿汉没区别了。</p>
<p>懒汉和饿汉的对比：大家可以发现两者的区别基本上就是第一次创作时候的开销问题，以及线程安全问题（线程不安全模式的懒汉）。<br>那有了这个对比，那他们的场景就好理解了，在很多电商场景，如果这个数据是经常访问的热点数据，那我就可以再系统启动的时候使用饿汉模式提前加载（类似缓存的预热）这样哪怕是第一个用户调用都不会存在创建开销，而且调用频繁也不存在内存浪费了。</p>
<p>而懒汉式呢我们可以用在不怎么热的地方，比如那个数据你不确定很长一段时间是不是有人调用，那就用懒汉，如果你使用了饿汉，但是过了几个月还没有人调用，提前加载的类再内存中是由资源浪费的。</p>
<h2 id="线程安全问题"><a href="#线程安全问题" class="headerlink" title="线程安全问题"></a>线程安全问题</h2><p>上面的懒汉我是故意没有加锁的，大家肯定都知道懒汉的线程安全问题<br><a href="https://imgse.com/i/z4IcQK"><img src="https://s1.ax1x.com/2022/12/13/z4IcQK.jpg" alt="z4IcQK.jpg"></a><br>在运行过程中可能存在这么一种情况：多个线程去调用getInstance方法来获取Singleton的实例，那么就有可能发生一种情况，当第一个线程在执行if(instance &#x3D;&#x3D; null)时，此时instance是为null的进入语句。</p>
<p>在还没有执行instance &#x3D; new Singleton()时（此时instance是为null的）第二个线程也进入了if(instance &#x3D;&#x3D; null)这个语句，因为之前进入这个语句的线程中还没有执行instance &#x3D; new Singleton()，所以它会执行instance &#x3D; new Singleton()来实例化Singleton对象，因为第二个线程也进入了if语句所以它会实例化Singleton对象。</p>
<p>这样就导致了实例化两个Singleton对象，那怎么解决呢？</p>
<p>简单粗暴，加锁就好了，这是加锁之后的代码。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton &#123;</span><br><span class="line">    private static Singleton instance = null;</span><br><span class="line">    /**</span><br><span class="line">     * 私有构造方法，防止被实例化</span><br><span class="line">     */</span><br><span class="line">    private Singleton()&#123;&#125;</span><br><span class="line">    /**</span><br><span class="line">     * 静态get方法</span><br><span class="line">     */</span><br><span class="line">    public static synchronized Singleton getInstance()&#123;</span><br><span class="line">        if(instance == null)&#123;</span><br><span class="line">            instance = new Singleton();</span><br><span class="line">        &#125;</span><br><span class="line">        return instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这是一种典型的时间换空间的写法，不管三七二十一，每次创建实例时先锁起来，在进行判断，严重降低了系统的处理速度。</p>
<p>有没有更好的处理方式呢？<br>有，通过双检锁做两次判断，代码如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton &#123;</span><br><span class="line">    private static Singleton instance = null;</span><br><span class="line">    private Singleton()&#123;&#125;</span><br><span class="line">    public static Singleton getInstance()&#123;</span><br><span class="line">        //先检查实例是否存在，如果不存在才进入下面的同步块</span><br><span class="line">        if(instance == null)&#123;</span><br><span class="line">            //同步块，线程安全的创建实例</span><br><span class="line">            synchronized (Singleton.class) &#123;</span><br><span class="line">                //再次检查实例是否存在，如果不存在才真正的创建实例</span><br><span class="line">                if(instance == null)&#123;</span><br><span class="line">                    instance = new Singleton();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>将synchronized关键字加在了内部，也就是说当调用的时候是不需要加锁的，只有在instance为null，并创建对象的时候才需要加锁，性能有一定的提升。</p>
<p>但是，这样就没有问题了吗？</p>
<p>看下面的情况：在Java指令中创建对象和赋值操作是分开进行的，也就是说instance &#x3D; new Singleton();语句是分两部执行的。</p>
<p>但是JVM并不保证这两个操作的先后顺序，也就是说有可能JVM会为新的Singleton实例分配空间，然后直接赋值给instance成员，然后再去初始化这个Singleton实例。</p>
<p>这样就可能出错了，我们以A、B两个线程为例：<br>1.A、B线程同时进入了第一个if判断<br>2.A首先进入synchronized块，由于instance为null，所以它执行instance &#x3D; new Singleton();<br>3.由于JVM内部的优化机制，JVM先画了一些分配给Singleton实例的空白内存，并赋值给instance成员（注意此时JVM没有开始初始化这个实例），然后A离开了synchronized块。<br><a href="https://imgse.com/i/z4oQOO"><img src="https://s1.ax1x.com/2022/12/13/z4oQOO.jpg" alt="z4oQOO.jpg"></a><br>4.B进入synchronized块，由于instance此时不是null，因此它马上离开了synchronized块并将结果返回给调用该方法的程序。<br>5.此时B线程打算使用singleton实例，却发现它并没有被初始化，于是出现了错误。<br>加上volatile修饰Singleton，再做一次优化：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton &#123;</span><br><span class="line">    private volatile static Singleton instance = null;</span><br><span class="line">    private Singleton()&#123;&#125;</span><br><span class="line">    public static Singleton getInstance()&#123;</span><br><span class="line">        //先检查实例是否存在，如果不存在才进入下面的同步块</span><br><span class="line">        if(instance == null)&#123;</span><br><span class="line">            //同步块，线程安全的创建实例</span><br><span class="line">            synchronized (Singleton.class) &#123;</span><br><span class="line">                //再次检查实例是否存在，如果不存在才真正的创建实例</span><br><span class="line">                if(instance == null)&#123;</span><br><span class="line">                    instance = new Singleton();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return instance;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>通过volatile修饰的变量，不会被线程本地缓存，所有线程对该对象的读写都会第一时间同步到主内存，从而保证多个线程间对该对象的准确性</p>
<h2 id="volatile的作用"><a href="#volatile的作用" class="headerlink" title="volatile的作用"></a>volatile的作用</h2><p>防止指令重排序，因为instance &#x3D; new Singleton()不是原子操作<br>保证内存可见</p>
<p>这个是比较完美的写法了，这种方式能够安全的创建唯一的一个实例，又不会对性能有太大的影响。</p>
<p>但是由于volatile关键字可能会屏蔽掉虚拟机中一些必要的代码优化，所以运行效率并不是很高，还有更优的写法吗？</p>
<p>通过静态内部类</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class Singleton &#123;  </span><br><span class="line">  </span><br><span class="line">    /* 私有构造方法，防止被实例化 */  </span><br><span class="line">    private Singleton() &#123;  </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">    /* 此处使用一个内部类来维护单例 */  </span><br><span class="line">    private static class SingletonFactory &#123;  </span><br><span class="line">        private static Singleton instance = new Singleton();  </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">    /* 获取实例 */  </span><br><span class="line">    public static Singleton getInstance() &#123;  </span><br><span class="line">        return SingletonFactory.instance;  </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">    /* 如果该对象被用于序列化，可以保证对象在序列化前后保持一致 */  </span><br><span class="line">    public Object readResolve() &#123;  </span><br><span class="line">        return getInstance();  </span><br><span class="line">    &#125;  </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure>
<p>使用内部类来维护单例的实现，JVM内部的机制能够保证当一个类加载的时候，这个类的加载过程是线程互斥的。</p>
<p>这样当我们第一次调用getInstance的时候，JVM能够帮我们保证instance只被创建一次，并且会保证把赋值给instance的内存初始化完毕，这样我们就不用担心上面的问题。</p>
<p>同时该方法也只会在第一次调用的时候使用互斥机制，这样就解决了低性能问题。这样我们暂时总结一个完美的单例模式。</p>
<p>还有更完美的写法吗？通过枚举</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public enum Singleton &#123;</span><br><span class="line">    /**</span><br><span class="line">     * 定义一个枚举的元素，它就代表了Singleton的一个实例。</span><br><span class="line">     */</span><br><span class="line">    Instance;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用枚举来实现单实例控制会更加简洁，而且JVM从根本上提供保障，绝对防止多次实例化，是更简洁，高效，安全的实现单例的方式。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>最后大家应该都知道单例模式的写法了，也知道优劣势和使用场景了，那开头的那个问题大家心里有答案了么？</p>
<p>什么？连问题都忘了？问题：为什么不用静态方法而用单例模式？</p>
<p>两者其实都能实现我们加载的最终目的，但是他们一个是基于对象，一个是面向对象的，就像我们不面向对象也能解决问题一样，面向对象的代码提供一个更好的编程思想。</p>
<p>如果一个方法和他所在类的实例对象无关，那么它就应该是静态的，反之他就应该是非静态的。如果我们确实应该使用非静态的方法，但是在创建类时又确实只需要维护一份实例时，就需要用单例模式了。</p>
<p>我们的电商系统中就有很多类，有很多配置和属性，这些配置和属性是一定存在了，又是公共的，同时需要在整个生命周期中都存在，所以只需要一份就行，这个时候如果需要我再需要的时候new一个，再给他分配值，显然是浪费内存并且再赋值没什么意义。</p>
<p>所以我们用单例模式或静态方法去维持一份这些值有且只有这一份值，但此时这些配置和属性又是通过面向对象的编码方式得到的，我们就应该使用单例模式，或者不是面向对象的，但他本身的属性应该是面对对象的，我们使用静态方法虽然能同样解决问题，但是最好的解决方案也应该是使用单例模式。</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式系列——工厂模式</title>
    <url>/2022/12/21/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F%E7%B3%BB%E5%88%97%E2%80%94%E2%80%94%E5%B7%A5%E5%8E%82%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<p>学习技术深度的时候（看一些框架的源码）总感觉只理解了表面，而没有理解里面的思想。<br>这里大家应该都知道我想说什么，就是设计模式。</p>
<span id="more"></span>

<p>今天聊一下工厂模式，工厂模式在我们的电商领域的应用是非常广泛的，写之前我看了自己电脑项目的代码，确实随处可见。<br>工厂模式主要可以分为三大类：<br>简单工厂模式<br>工厂方法模式<br>抽象工厂模式</p>
<p>今天我也主要围绕这三种模式来举例了解一下</p>
<h2 id="简单工厂模式"><a href="#简单工厂模式" class="headerlink" title="简单工厂模式"></a>简单工厂模式</h2><p>工厂模式主要是用于对实现逻辑的封装，并且通过对公共的接口提供对象的实例化的服务，在我添加新的类时不需要大动干戈，只要修改一点就好。</p>
<p>举个电商业务是怎么创建商品的：<br><a href="https://imgse.com/i/zXVPc8"><img src="https://s1.ax1x.com/2022/12/21/zXVPc8.jpg" alt="zXVPc8.jpg"></a><br>在这个简单工厂里面，如果要创建活动商品1以及活动商品2，我们要创建商品的时候只要调用简单工厂里面的创建商品方法，根据类型创建出不同的商品然后实例化返回就可以了。</p>
<p>简单工厂几种实现方式：</p>
<h3 id="静态工厂模式"><a href="#静态工厂模式" class="headerlink" title="静态工厂模式"></a>静态工厂模式</h3><p>我们还是以创建商品为例子</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class SimpleFacttory &#123;</span><br><span class="line">	public static Share createProduct(EnumProdutType type) &#123;</span><br><span class="line">		if (EnumProductType.activityone.equals(type)) &#123;</span><br><span class="line">			return new ProductOne();</span><br><span class="line">		&#125; else if (EnumProductType.acctivityTwo.equals(type)) &#123;</span><br><span class="line">			return new ProductTwo();</span><br><span class="line">		&#125;</span><br><span class="line">		return null;</span><br><span class="line">	&#125;</span><br><span class="line">	public enum EnumProductType &#123;</span><br><span class="line">		activituOne, activityTwo;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这种方式创建看起来其实也没有什么问题，根据类型创建不同的商品，但是有一个问题不知道大家发现没有？</p>
<p>是不是我没增加一种类型还要去createProduct方法上修改if else？这不是违背我们的开闭原则吗？</p>
<p>所以这种方式不好，我们还有接下来的两种：<br>使用反射机制<br>直接注册商品对象，添加一个Type类型方法，根据type类型返回自身相同类型的方法。同样的我们还是以创建商品为例：反射实现<br>同样的我们还是以创建商品为例：反射实现</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class SimpleFactoryReflection &#123;</span><br><span class="line"></span><br><span class="line">    private static final Map&lt;EnumProductType, Class&gt; activityIdMap = new HashMap&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    public  static void addProductKey(EnumProductType EnumProduct, Class product) &#123;</span><br><span class="line">        activityIdMap.put(EnumProduct, product);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static activityOne product(EnumProductType type) throws IllegalAccessException, InstantiationException &#123;</span><br><span class="line">        Class productClass = activityIdMap.get(type);</span><br><span class="line">        return (activityOne) productClass.newInstance();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) throws InstantiationException, IllegalAccessException &#123;</span><br><span class="line">        addProductKey(EnumProductType.activityOne, activityOne.class);</span><br><span class="line">        activityOne product = product(EnumProductType.activityOne);</span><br><span class="line">        System.out.println(product.toString());</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    private static class Producet&#123;</span><br><span class="line">        private String name;</span><br><span class="line">        public String getName() &#123;</span><br><span class="line">            return name;</span><br><span class="line">        &#125;</span><br><span class="line">        public void setName(String name) &#123;</span><br><span class="line">            this.name = name;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class activityOne extends Producet &#123;</span><br><span class="line">        private String stock;</span><br><span class="line">        @Override</span><br><span class="line">        public String toString() &#123;</span><br><span class="line">            return &quot;activityOne&#123;&quot; +</span><br><span class="line">                    &quot;stock=&#x27;&quot; + stock + &#x27;\&#x27;&#x27; +</span><br><span class="line">                    &#x27;&#125;&#x27;;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public class activityTwo extends Producet &#123;</span><br><span class="line">        private String stock;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public enum EnumProductType &#123;</span><br><span class="line">        activityOne, activityTwo;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure>
<p>看上面的代码，我发现反射其实也很容易就实现了，但是在一些特定的情况下，并不适用，而且在某些特定的情况下是无法实现的，而且反射机制也会降低程序的运行效果，在对想能要求很高的场景下应该避免这种实现。</p>
<p>这里还有一个问题是使用反射不当是很容易导致线上机器出问题的，因为我们反射创建的对象属性是被SoftReference软引用的，所以当**-XX:SoftRefLRUPolicyMSPerMB**没有设置好的话，会让机器的CPU一直很高。当然它的默认值是1000，也就根据大家的情况而定吧。</p>
<p>剩下的一种其实跟反射实现很像，就是为了避免使用反射，在Map的对象中不是存的要添加的类，而是将要添加的每种类对象实例，这个就不写demo了。</p>
<h3 id="工厂方法模式"><a href="#工厂方法模式" class="headerlink" title="工厂方法模式"></a>工厂方法模式</h3><p>工厂方法模式是对静态工厂模式上的一种改进，我们工厂类直接被抽象化，需要具体特定化的逻辑代码转移到实现抽象方法的子类中，这样我们就不要再去修改工厂类（即：不用再去做什么if else修改）这也是我们当前比较常用的一种方式。<br>还是以我们创建商品为例：<br><a href="https://imgse.com/i/zxHvsP"><img src="https://s1.ax1x.com/2022/12/26/zxHvsP.jpg" alt="zxHvsP.jpg"></a><br>看这张图，其实说白了就是再创建一个工厂，用来创建工厂类对象<br>接下来看代码实现：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public abstract class FactoryMethod &#123;</span><br><span class="line"></span><br><span class="line">    protected abstract Product createProduct(String name);</span><br><span class="line"></span><br><span class="line">    public Product product(String activity, String name) &#123;</span><br><span class="line">        Product product = createProduct(activity);</span><br><span class="line">        product.setProductName(name);</span><br><span class="line">        return product;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class Product &#123;</span><br><span class="line">        public String productName;</span><br><span class="line"></span><br><span class="line">        public String getProductName() &#123;</span><br><span class="line">            return productName;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public void setProductName(String productName) &#123;</span><br><span class="line">            this.productName = productName;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public enum EnumProductType &#123;</span><br><span class="line">        activityOne(&quot;one&quot;),</span><br><span class="line">        activityTwo(&quot;two&quot;);</span><br><span class="line"></span><br><span class="line">        private String name;</span><br><span class="line"></span><br><span class="line">        EnumProductType(String name) &#123;</span><br><span class="line">            this.name = name;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public String getName() &#123;</span><br><span class="line">            return name;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public void setName(String name) &#123;</span><br><span class="line">            this.name = name;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里先创建一个抽象工厂方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class ProductFactory extends FactoryMethod&#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    protected Product createProduct(String activityId) &#123;</span><br><span class="line">        if (EnumProductType.activityOne.getName().equals(activityId)) &#123;</span><br><span class="line">            //这里可以处理我们自己的想要的业务逻辑代码</span><br><span class="line">            return new OneProduct();</span><br><span class="line">        &#125; else if (EnumProductType.activityTwo.getName().equals(activityId)) &#123;</span><br><span class="line">            return new TwoProduct();</span><br><span class="line">        &#125;</span><br><span class="line">        return null;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class OneProduct extends Product &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static class TwoProduct extends Product &#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        FactoryMethod factoryMethod = new ProductFactory();</span><br><span class="line">        Product product = factoryMethod.product(&quot;one&quot;, &quot;one&quot;);</span><br><span class="line">        System.out.println(product.getProductName());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>再创建一个商品工厂去继承抽象工厂方法。</p>
<p>当还有其他类型的时候我们只需要去继承我们创建的工厂方法就可以了。<br>这个代码大家肯定都能实现出来，但是我们真正需要学习的是前辈们这种工厂模式的思想。把这种思想运用到我们真实的业务场景中，学以致用才能对我们有真正的提升。</p>
<p>举个例子</p>
<p>假设现在需要做一个分享商品图片，我们知道商品的类型有很多，比如无SKU商品，有SKU商品，下单分享，邀请分享…等一下列的场景，那我们怎么去设计这个代码做到更加的易懂，易读，今后扩展性好呢？<br>注：sku和spu是电商里面的名词，spu差不多跟item也就是商品是一个维度，一个商品item有很多sku，比如：iphone是一个商品是item也是spu，白色的iphone 12 64G就是一个具体的sku</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public interface Share &#123;</span><br><span class="line">    //获取分享类型</span><br><span class="line"></span><br><span class="line">    String getShareFunctionType();</span><br><span class="line"></span><br><span class="line">    String mainProcess(String shareName);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>第一步我们应该都是定义一个创建分享模板</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">public class ShareFactory &#123;</span><br><span class="line">    private List&lt;Share&gt; shareFunctionList;</span><br><span class="line"></span><br><span class="line">    //根据分享类型获取对应的分享处理方式</span><br><span class="line">    public Share getShareFunction(String type) &#123;</span><br><span class="line">        for(Share shareFunction : shareFunctionList) &#123;</span><br><span class="line">            if (shareFunction.getShareFunctionType().equals(type)) &#123;</span><br><span class="line">                return shareFunction;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        return null;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public enum EnumShareType &#123;</span><br><span class="line">        SUCCESS_ORDER(&quot;successOrder&quot;);</span><br><span class="line">        private String name;</span><br><span class="line"></span><br><span class="line">        EnumShareType(String name) &#123;</span><br><span class="line">            this.name = name;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public String getName() &#123;</span><br><span class="line">            return name;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        public void setName(String name) &#123;</span><br><span class="line">            this.name = name;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>第二步我们再创建一个分享工厂根据我们的类型获取我们预先加载在Spring容器中的bean实例<br>public class SuccessOrderShare implements Share {<br>    public String getShareFunctionType() {<br>        return ShareFactory.EnumShareType.SUCCESS_ORDER.getName();<br>    }</p>
<pre><code>public String mainProcess(String shareName) &#123;
    return shareNmae;
&#125;
</code></pre>
<p>}<br><a href="https://imgse.com/i/pSCBWPf"><img src="https://s1.ax1x.com/2023/01/01/pSCBWPf.jpg" alt="pSCBWPf.jpg"></a></p>
<p>最后就是定义我们不同的类型来实现分享图片。<br>其实我们学习设计模式就是这种思想，有了这种思想上面提到的三点又是才能体现出来对我们今后的成长，面对复杂业务设计以及思考能力才能提升。</p>
<p>那么问题来了，什么时候改用工厂方法模式，而非简单工厂模式呢？<br>这里引用设计模式之美里面的一句话：当对象的创建逻辑比较复杂，不只是简单的new一下就可以，而是要组合其他类对象，做各种初始化操作的时候，我们推荐使用工厂方法模式，将复杂的创建逻辑拆分到多个工厂类中，让每个工厂类都不至于过于复杂。<br>而使用简单工厂模式，将所有的创建逻辑都放到一个工厂类中，会导致这个工厂类变得很复杂。</p>
<h2 id="抽象工厂模式"><a href="#抽象工厂模式" class="headerlink" title="抽象工厂模式"></a>抽象工厂模式</h2><p>看完工厂方法模式，在理解抽象工厂模式就更加简单，因为它其实就是工厂方法的一个延伸。<br>工厂方法类中只有一个唱响方法，要想实现多种不同的类对象，只能去创建不同的具体工厂方法的子类来实例化，而抽象工厂则是让一个工厂负责创建多个不同类型的对象。<br><a href="https://imgse.com/i/pSCDpL9"><img src="https://s1.ax1x.com/2023/01/01/pSCDpL9.jpg" alt="pSCDpL9.jpg"></a><br>样子可能有点丑，但是能说明问题，其实看上去可以分为以上几个部分组成：<br>抽象工厂类<br>具体工厂类<br>抽象类<br>抽象工厂类我个人可以理解为一个刚出厂的手机，具体抽象工厂则是认为是我们每个人对这个手机壁纸自定义设置，最后抽象类我理解就是手机壁纸。<br>我们每个人可以自定义不同的壁纸：例如动图，静图风景等等。</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>使用Android sharesheet在Android应用程序中分享内容和链接</title>
    <url>/2023/03/22/%E4%BD%BF%E7%94%A8Android-sharesheet%E5%9C%A8Android%E5%BA%94%E7%94%A8%E7%A8%8B%E5%BA%8F%E4%B8%AD%E5%88%86%E4%BA%AB%E5%86%85%E5%AE%B9%E5%92%8C%E9%93%BE%E6%8E%A5/</url>
    <content><![CDATA[<p>为何要使用Android sharesheet<br>主要有三点原因：</p>
<ol>
<li>谷歌官方推荐使用</li>
<li>Target SDK 30 以上，包名可见性发生了变更，默认情况下，应用与应用是不可见的，换句话说，如果使用queryIntentActivities()方法，不一定能够获取到所有intent对应的activity，这样就给分享intent造成了麻烦。</li>
<li>Android sharesheet提供了包括界面、结果收集、固定前置和屏蔽指定app在内的全套功能，可以节省设计和开发资源<span id="more"></span></li>
</ol>
<h2 id="使用Android-sharesheet的基础步骤"><a href="#使用Android-sharesheet的基础步骤" class="headerlink" title="使用Android sharesheet的基础步骤"></a>使用Android sharesheet的基础步骤</h2><p>Android sharesheet的使用十分简单，大致分为两个步骤：准备intent，使用intent调起分享弹窗。<br>将会使用3个例子来展现sharesheet的使用，本别是：分享一段文字(可以包含某个链接)，分享一个视频，分享多个视频</p>
<h3 id="准备intent："><a href="#准备intent：" class="headerlink" title="准备intent："></a>准备intent：</h3><h3 id="分享一段文字："><a href="#分享一段文字：" class="headerlink" title="分享一段文字："></a>分享一段文字：</h3><p>如果只是想分享一段文字，并且文字中包含了一个跳转链接，是比较简单的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//linkWtihText 就是想要分享的文字，实际使用时常常包含一段跳转链接</span><br><span class="line">val sendIntent: Intent = Intent().apply &#123;</span><br><span class="line">    action = Intent.ACTION_SEND</span><br><span class="line">    putExtra(Intent.EXTRA_TEXT, linkWtihText)</span><br><span class="line">    type = &quot;text/plain&quot;</span><br><span class="line">&#125;</span><br><span class="line">//使用sendIntent来制作分享跳转使用的intent</span><br><span class="line">val shareIntent = Intent.createChooser(sendIntent, &quot;null&quot;, &quot;null&quot;)</span><br><span class="line">//添加flag</span><br><span class="line">shareIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)</span><br></pre></td></tr></table></figure>

<h3 id="分享一个视频："><a href="#分享一个视频：" class="headerlink" title="分享一个视频："></a>分享一个视频：</h3><p>其实质是分享一个视频媒体文件，所以分享一个音乐、一张图片等也是类似的步骤。首先，我们需要制作一个待分享的文件的uri，此处使用FileProvider：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//把路径转换成可以用来分享的Uri，path：待分享的文件路径</span><br><span class="line">private fun createFileUriWithPath(context: Context, path: String): Uri &#123;</span><br><span class="line">    val shareFile = File(path)</span><br><span class="line">    return FileProvider.getUriForFile(</span><br><span class="line">        context,</span><br><span class="line">        &quot;com.miui.localvideoplayer.shareprovider&quot;,</span><br><span class="line">        shareFile</span><br><span class="line">    )</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后，基于以上方法制作的uri，制作分享intent：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">val shareUri = createFileUriWithPath(appContext, path)</span><br><span class="line">val sendIntent = Intent()</span><br><span class="line">sendIntent.action = Intent.ACTION_SEND</span><br><span class="line">sendIntent.putExtra(Intent.EXTRA_STREAM, shareUri)</span><br><span class="line">sendIntent.data = shareUri</span><br><span class="line">//此处需要对uri赋予可读取的权限</span><br><span class="line">sendIntent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)</span><br><span class="line">sendIntent.type = &quot;video/*&quot;</span><br><span class="line">//使用sendIntent来制作分享跳转使用的intent</span><br><span class="line">val shareIntent = Intent.createChooser(sendIntent, &quot;null&quot;, &quot;null&quot;)</span><br><span class="line">//添加flag</span><br><span class="line">shareIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)</span><br></pre></td></tr></table></figure>

<h3 id="分享多个视频："><a href="#分享多个视频：" class="headerlink" title="分享多个视频："></a>分享多个视频：</h3><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//shareList: ArrayList&lt;String&gt; 待分享的文件路径列表</span><br><span class="line"></span><br><span class="line">if (shareList.size == 1) &#123;</span><br><span class="line">    //如果列表中只有一个文件，那么请将其当作单个文件分享，以获取更多的接受方</span><br><span class="line">    shareSingleVideoFile(context, shareList[0])</span><br><span class="line">    return</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">val shareUriList = java.util.ArrayList&lt;Uri&gt;()</span><br><span class="line">for (path in shareList) &#123;</span><br><span class="line">    //仍然需要用到上文提到的制作uri的方法，将uri们保存在一个list里</span><br><span class="line">    val shareUri = createFileUriWithPath(appContext, path)</span><br><span class="line">    shareUriList.add(shareUri)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">val sendIntent = Intent()</span><br><span class="line">//此处的action为ACTION_SEND_MULTIPLE</span><br><span class="line">sendIntent.action = Intent.ACTION_SEND_MULTIPLE</span><br><span class="line">sendIntent.putParcelableArrayListExtra(Intent.EXTRA_STREAM, shareUriList)</span><br><span class="line">sendIntent.type = &quot;video/*&quot;</span><br><span class="line">sendIntent.addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)</span><br><span class="line">val shareIntent = Intent.createChooser(sendIntent, &quot;null&quot;, &quot;null&quot;)</span><br><span class="line">shareIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)</span><br></pre></td></tr></table></figure>

<h3 id="使用intent调起分享弹窗："><a href="#使用intent调起分享弹窗：" class="headerlink" title="使用intent调起分享弹窗："></a>使用intent调起分享弹窗：</h3><p>使用context.startActivity方法：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">mContext.startActivity(shareIntent)</span><br></pre></td></tr></table></figure>

<h2 id="将某些接收端固定前置-x2F-将某些接收端屏蔽"><a href="#将某些接收端固定前置-x2F-将某些接收端屏蔽" class="headerlink" title="将某些接收端固定前置&#x2F;将某些接收端屏蔽"></a>将某些接收端固定前置&#x2F;将某些接收端屏蔽</h2><p>在实际的使用中，我们经常想要将某些应用前置在我们的分享弹窗中，例如，我希望用户把我们的链接分享到facebook,whatsapp,messenger,telegram中，这样可以方便我们进行裂变转播，sharesheet也可以做到。<br>实现方法也十分简单，只要给shareInent增加一个key为Intent.EXTRA_INITIAL_INTENTS的extra，并且往里传入一个sendIntent的typedArray，就可以了<br>首先，制作需要前置分享的app的intent list</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fun getExtraInitialIntent(text: String): ArrayList&lt;Intent&gt; &#123;</span><br><span class="line">    val intentShareList = ArrayList&lt;Intent&gt;()</span><br><span class="line">    //需要前置的receiver app的包名</span><br><span class="line">    var shareItems = </span><br><span class="line">        arrayOf(</span><br><span class="line">            &quot;com.facebook.katana&quot;,</span><br><span class="line">            &quot;com.whatsapp&quot;,</span><br><span class="line">            &quot;com.facebook.orca&quot;,</span><br><span class="line">            &quot;org.telegram.messenger&quot;</span><br><span class="line">        )</span><br><span class="line">        </span><br><span class="line">    for (packageName in shareItems) &#123;</span><br><span class="line">        val intent = Intent()</span><br><span class="line">        intent.action = Intent.ACTION_SEND</span><br><span class="line">        intent.type = &quot;text/plain&quot;</span><br><span class="line">        intent.setPackage(packageName)</span><br><span class="line">        intent.putExtra(Intent.EXTRA_TEXT, text)</span><br><span class="line">        intentShareList.add(intent)</span><br><span class="line">    &#125;</span><br><span class="line">    return intentShareList</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后，将其转换为TypedArray并且携带给shareIntent</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">shareIntent.putExtra(</span><br><span class="line">    Intent.EXTRA_INITIAL_INTENTS,</span><br><span class="line">    getExtraInitialIntent(arrayListOf(path)).toTypedArray()</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<p>同样的，我们也可以在sharesheet中排除某个receiver，比如，如果选择小米文档查看器作为receiver，那么它将把分享的文字内容当作一段地址来尝试读取某个文件，显然，这不符合我们的业务需求，并且会造成bug。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">private fun getExtraExcludeComponents(intent: Intent): ArrayList&lt;ComponentName&gt; &#123;</span><br><span class="line">    val excludeList = ArrayList&lt;ComponentName&gt;()</span><br><span class="line">    val extraExcludedItems = arrayOf(&quot;cn.wps.xiaomi.abroad.lite&quot;)</span><br><span class="line">    val pm = FrameworkApplication.getAppContext().packageManager</span><br><span class="line">    val targets = pm.queryIntentActivities(intent, 0)</span><br><span class="line">    for (resolveInfo in targets) &#123;</span><br><span class="line">        val targetPackage = resolveInfo.activityInfo.packageName</span><br><span class="line">        val appName = resolveInfo.activityInfo.name</span><br><span class="line">        for (packageName in extraExcludedItems) &#123;</span><br><span class="line">            if (packageName.equals(targetPackage, true)) &#123;</span><br><span class="line">                excludeList.add(ComponentName(targetPackage, appName))</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    return excludeList</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在制作shareIntent时，加入extra，使用EXTRA_EXCLUDE_COMPONENTS关键字，将其排除。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">shareIntent.putExtra(</span><br><span class="line">                    Intent.EXTRA_EXCLUDE_COMPONENTS,</span><br><span class="line">                    getExtraExcludeComponents(sendIntent).toTypedArray()</span><br><span class="line">                )</span><br></pre></td></tr></table></figure>

<h2 id="收集分享的结果"><a href="#收集分享的结果" class="headerlink" title="收集分享的结果"></a>收集分享的结果</h2><p>用户在点击分享后，需要在分享弹窗中选择一个渠道进行分享，例如，选择Facebook进行分享，而我们如果想要收集用户最后点击的选项，那么可以在Intent.createChooser(sendIntent, “null”, “null”)方法中传入一个广播接收器，从而可以接受用户点击后发出的广播。<br>首先，我们需要一个广播接收器类：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">class LocalShareBroadCastReceiver : BroadcastReceiver() &#123;</span><br><span class="line">    override fun onReceive(context: Context?, intent: Intent?) &#123;</span><br><span class="line">        val clickedComponentName = intent?.getParcelableExtra&lt;ComponentName&gt;(Intent.EXTRA_CHOSEN_COMPONENT)</span><br><span class="line">        val packageName = clickedComponentName?.packageName</span><br><span class="line">        //packageName就是用户最终点选的包名，此处可以进行打点等统计操作</span><br><span class="line">        context?.unregisterReceiver(this)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其次，我们需要创建一个接收器实例，并且动态注册，之后再返回一个intentSender用于构造shareIntent</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">//建议将localShareBroadCastReceiver 保存成为static final常量，在每次分享操作时，检查是否被注册。多次注册广播接收器会使得用户在第二次分享的时候执行两次onReceive中的操作</span><br><span class="line">val localShareBroadCastReceiver = LocalShareBroadCastReceiver()</span><br><span class="line"></span><br><span class="line">val sender = registerShareReceiver(</span><br><span class="line">    appContext, &quot;com.videoplayer.SHARE_ACTION_LOCAL&quot;,</span><br><span class="line">    localShareBroadCastReceiver</span><br><span class="line">)</span><br><span class="line">val shareIntent = Intent.createChooser(sendIntent, &quot;null&quot;, sender)</span><br></pre></td></tr></table></figure>

<p>其中，registerShareReceiver方法如下：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">fun registerShareReceiver(</span><br><span class="line">    context: Context,</span><br><span class="line">    action: String,</span><br><span class="line">    receiver: BroadcastReceiver</span><br><span class="line">): IntentSender &#123;</span><br><span class="line">    val receiverIntent = Intent(action)</span><br><span class="line">    receiverIntent.putExtra(&quot;test&quot;, &quot;test&quot;)</span><br><span class="line">    val pi = PendingIntent.getBroadcast(</span><br><span class="line">        FrameworkApplication.getAppContext(),</span><br><span class="line">        10000,</span><br><span class="line">        receiverIntent,</span><br><span class="line">        //Android 12 以上，一定要加PendingIntent.FLAG_MUTABLE，不然将不会收到回调</span><br><span class="line">        PendingIntent.FLAG_UPDATE_CURRENT or PendingIntent.FLAG_MUTABLE</span><br><span class="line">    )</span><br><span class="line">    context.registerReceiver(</span><br><span class="line">        receiver,</span><br><span class="line">        IntentFilter(action)</span><br><span class="line">    )</span><br><span class="line">    return pi.intentSender;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如前文注释中提到的，多次注册广播接收器会使得用户在第二次分享的时候执行两次onReceive中的操作，因此建议将localShareBroadCastReceiver 保存成为static final常量，在每次分享操作时，尝试反注册，保证每次分享操作仅有1个receiver进行监听。<br>Android开发者：应用数据和文件<br><a href="https://developer.android.google.cn/training/sharing">https://developer.android.google.cn/training/sharing</a> </p>
]]></content>
      <categories>
        <category>android</category>
      </categories>
      <tags>
        <tag>android</tag>
      </tags>
  </entry>
</search>
